# facet contract ↔ facet_appkit gap audit — 2026-08-05

What the MAUI-bootstrapped contract **declares**, what `facet_appkit`
**implements**, and where the answers are incomplete even when the
coverage gate is green.

**Headline.** Below the kind level, the debt number is zero:

```
324 declared prop/command bits
  284 live · 23 host-rendered · 5 derived · 4 modifier · 8 create-only
    0 decided · 0 no-carrier · 0 absent

 81 declared handlers
  81 wired · 0 never-fire
```

`python3 tools/verb_coverage.py --check` passes. Every declared verb has
an answer. That is not the same as every verb doing what a portable app
author would expect on a Mac — the rest of this report is that second
reading.

Sources: `vendor/facet/docs/contract.md`, `vendor/facet/src/*`,
`vendor/facet_appkit/src/*` + `MANIFEST.md`, Stage 1–4 notes, the MAUI
map in `tools/maui_map.py`.

---

## 1. What the contract is

facet's portable surface is bootstrapped from MAUI's PublicAPI (Controls
+ Core + Graphics), curated by Stage 1 into **ADOPT / DROP only** (no
FUTURE parking), then generated by Stage 2 into one module per control.

| Layer | Count | Notes |
|---|---|---|
| Generated controls | 38 | `props::K_*` 1–38 — button, label, text_field, … |
| Hand-written (facet-origin) | 4 | `symbol` 1000, `tree` 1001, `split` 1002, `window_buttons` 1003 |
| Bare container | 1 | `K_NODE` 0 — flex column/row/etc. |
| Declared prop/command bits | 324 | per-control dirty-bit space |
| Declared handlers | 81 | ride shared `C_HANDLERS` |
| Shared band on every cursor | 75 verbs | opacity, paint, transform, geometry, lifecycle |
| MAUI map | 873 rows | 496 ADOPT; 377 DROP (ENGINE 235, MODEL 86, LAYOUT 56) |

Manifest of every word: `vendor/facet/docs/contract.md`.

### Control inventory (contract → AppKit body)

| Contract kind | MAUI seed | AppKit body | Shape |
|---|---|---|---|
| `label` | Label | live | NSTextField (static) |
| `span` | Span | host-rendered | run inside label's attributed string |
| `button` | Button | live | NSButton |
| `icon_button` | ImageButton | live | NSButton |
| `checkbox` | CheckBox | live | NSButton (switch-type checkbox) |
| `radio` | RadioButton | live | NSButton radio; **group by name**, not superview |
| `toggle` | Switch | live | NSSwitch (+ drawn path when off/thumb coloured) |
| `text_field` | Entry | live | NSTextField / NSSecureTextField |
| `text_area` | Editor | live | NSTextView |
| `search_field` | SearchBar | live | NSSearchField |
| `popup` | Picker | live | NSPopUpButton |
| `slider` | Slider | live | NSSlider |
| `stepper` | Stepper | live | NSStepper |
| `progress` | ProgressBar | live | NSProgressIndicator (determinate) |
| `spinner` | ActivityIndicator | live | NSProgressIndicator (spinning) |
| `date_picker` | DatePicker | live | NSDatePicker (+ popover for `is_open`) |
| `time_picker` | TimePicker | live | NSDatePicker time elements |
| `image` | Image | live | NSImageView |
| `symbol` | *(facet)* | live | glyph view (Material Symbols / system) |
| `box` | BoxView | live | filled view |
| `bordered` | Border | live | layer border, or CAShapeLayer when stroke needs path |
| `scroll` | ScrollView | live | NSScrollView |
| `list` | ListView | live | **recycling** NSTableView (`recycler.cplus`) |
| `collection` | CollectionView | live | **materialises every row** as children |
| `table` | TableView | live | sequence host + style/row-height — **not multi-column** |
| `carousel` | CarouselView | live | paged scroll; position derived from offset |
| `tree` | *(facet)* | live | recycling outline |
| `tabs` | TabbedPage | live | **facet-drawn strip** (not NSTabView) |
| `page_dots` | IndicatorView | live | custom dots |
| `split` | *(facet)* | live | NSSplitView |
| `canvas` | GraphicsView | live | FacetCanvasView + command replay |
| `web` | WebView | live | WKWebView |
| `hybrid_web` | HybridWebView | live | WKWebView + message bridge |
| `menu` / `menu_item` | MenuBarItem / MenuItem | host-rendered | NSMenu rows (no NSView) |
| `context_menu` / `context_menu_item` | MenuFlyout* | host-rendered | hung on parent via `setMenu:` |
| `toolbar_item` | ToolbarItem | host-rendered | NSToolbarItem in window chrome |
| `refreshable` | RefreshView | live (substituted) | **refresh button overlay**, not pull-to-refresh |
| `swipeable` / `swipe_item` | SwipeView / SwipeItem | live (substituted) | **context menu + drag strip**, not touch swipe |
| `window_buttons` | *(facet)* | live | traffic-light chrome control |

Nothing in the contract is missing a kind-level body. `decided_absent`
returns `false` for every kind — even the mobile idioms got real
substitutes rather than silent pass-throughs.

---

## 2. Mechanical coverage — closed

| Check | Result |
|---|---|
| Kind has create/apply (or host/menu builder) | **42/42** |
| Prop/command bit gated or ledgered | **324/324** (0 absent) |
| Handler wired | **81/81** (0 never-fire) |
| `cannot-ledger` / `no-carrier` | **both empty** |
| Gate | `python3 tools/verb_coverage.py --check` green |

Earlier in Stage 4 the same tool (with bugs) reported large absences and
dead handlers (`text_field.on_text_changed`, `list.on_item_selected`,
etc.). Those were fixed: text write-back, handler arming, host-rendered
sync for viewless nodes, radio text carrier, group/selection carriers on
list/collection. The gate now enforces the old MANIFEST claim that an
unrecorded verb is a bug.

**Read the 0 carefully.** Zero absent means every verb is *accounted for*.
Eight are create-only by design; twenty-three only update when their
host re-applies; several mobile verbs are answered with a Mac-shaped
substitute. None of those appear as "missing" in the tool.

---

## 3. What is still missing or weak

### 3a. Create-only (8) — live flip needs a different object

Listed in `MANIFEST.md`'s `create-only` ledger. A later write does not
reach the screen:

| Verb | Why |
|---|---|
| `text_field.is_secure` | `NSSecureTextField` is a **different class**, not a property |
| `list.row_height` / `tree.row_height` | read when the data source / outline is built |
| `toolbar_item.placement` / `priority` / `is_destructive` | NSToolbarItem built once when the window opens; toolbar draws the label (no attributed destructive style) |
| `menu.priority` | NSMenu built once with the window |
| `window_chrome.spacing` | traffic lights laid out once by the window |

**Gap for apps:** flipping `is_secure` after mount does nothing; rebuild
the field (or remount) to get a secure field. Same class of limit for
toolbar item placement.

### 3b. Platform substitutes — implemented, but not the MAUI gesture

These verbs are **live** and handlers **fire**, under a different
interaction model:

| Contract | What MAUI means | What AppKit does |
|---|---|---|
| `refreshable` | pull-to-refresh on scroll | overlaid refresh **button**; sets `is_refreshing`, fires `on_refreshing` |
| `swipeable` + `swipe_item` | swipe-to-reveal actions | right-click / control-click **menu** of swipe items + optional trackpad drag strip |
| `tabs` | native tab chrome | facet row of buttons in the node's padding; panes via `display: none` (NSTabView fought flex layout) |
| `date_picker` / `time_picker` `format:` | format **string** (order + separators) | element **mask** from which components the string names; order/separators stay locale |
| `date_picker.is_open` | open the picker UI | graphical picker in an NSPopover |
| `corner_radius` (4 corners) | per-corner radii | Core Animation: **max of the four** |
| `keyboard` / `return_key` / `predicts_text` | soft-keyboard layout and Return label | hardware keyboard world: field-editor flags / next-field behaviour where they map; soft-keyboard layout has no home |
| `font_scales` | Dynamic Type per control | base size × system preference multiplier where available |

MANIFEST prose in places still reads as if some of these are pure
"AppKit cannot"; the code and the coverage tool treat them as live.
Trust the tool + the apply bodies over the older narrative sections.

### 3c. Sequence controls — shape limits (performance / columns)

| Kind | Implementation | Real limit |
|---|---|---|
| `list` | recycling NSTableView | single column; good for large N |
| `tree` | recycling outline | good for large N |
| `collection` | **materialises every row** as ordinary children | fine for dozens/hundreds; not 10k rows |
| `table` | sequence host + `TableStyle` / row height / uneven rows | **not** multi-column sortable spreadsheet; MAUI `TableView` here is settings-style sections, and the contract only declares 4 verbs |
| `carousel` | paged scroll | `wraps` is a modifier (out-of-range is error vs page); infinite wrap is not a half-built paging model |

Dead-ish path: `controls.cplus` still has `create_list` / `materialise_list`
/ `apply_list`, but `views.cplus` routes `K_LIST` to the recycler. The
materialising path is the collection/table family, not the live list.

### 3d. Contract-side holes (not facet_appkit's to invent)

Stage 4 closed the tier ledger at **57 implemented / 20 decided / 5
deferred**. The five that remain share one cause: **facet never declared
a carrier**.

| Deferred | Why backend cannot finish it |
|---|---|
| `TitleBar.LeadingContent` / `TrailingContent` / `Window.TitleBar` | AppKit has `NSTitlebarAccessoryViewController`; `Chrome` is a value struct and cannot hold a subtree |
| `ModalPushing` / `ModalPopping` / `Page.NavigatingFrom` | lifecycle only has after-hooks (`on_attach` / `on_detach`); no before-hook |
| `Page.BackgroundImageSource` | shared band has Color/Brush, not image |
| `Page.ContainerArea` / `IgnoresContainerArea` / `SafeAreaEdges` | `vocab::SafeArea` exists; only `scroll` takes one |
| `Page.IconImageSource` | no carrier; macOS window icon is document-proxy, not arbitrary image |

Also from the contract manifest itself:

| Item | Note |
|---|---|
| `observe_size` | not a cursor method; lives as `services::observe_size(n, cb, ctx:) -> Cancellable` (backend-hook shape) |

**Mobile-only decided** (toolbar back button, soft-input hide, page
busy, drawer toggle): recorded as platform-cannot at the tier level;
not control-verb debt.

### 3e. Drawing vocabulary — closed for canvas, one deliberate omission

`Drawable` is `fn(*u8, *Canvas, Rect)` with a full recorded-command
canvas. Backend replays into `drawRect:`.

| Intentionally not MAUI-complete | Reason |
|---|---|
| `ICanvas.GetStringSize` | needs a live platform context at record time; none exists while recording |
| `draw_text(at:)` y meaning | top-left (facet), not baseline (MAUI) — deliberate contract consistency |

### 3f. Stage 4 open item (docs / examples)

Item 7: revive `hello_facet` / `hello_appkit` as visual proof under the
new API. Not a contract gap; still a consumer-facing hole.

---

## 4. Per-family quick matrix

Legend: **full** = live dirty-bit path matches the portable meaning;
**sub** = implemented under a Mac-shaped substitute; **host** =
host-rendered; **create** = create-only; **weak** = works but shape/perf
limit worth knowing.

| Family | Controls | Verdict |
|---|---|---|
| Text display | label, span | full / host |
| Buttons | button, icon_button | full |
| Toggles | checkbox, radio, toggle | full (radio group-by-name is stronger than AppKit) |
| Text input | text_field, text_area, search_field | full (write-back + handlers); `is_secure` create-only |
| Value controls | slider, stepper, progress, spinner, popup | full |
| Pickers | date_picker, time_picker | full with format/is_open deviations |
| Media | image, symbol, web, hybrid_web | full |
| Layout shells | box, bordered, scroll, split, tabs, page_dots | full; tabs self-drawn |
| Sequences | list, tree | full + recycling |
| Sequences | collection, table, carousel | full verbs; **materialise / single-column / paging** weak spots |
| Menus / chrome | menu*, context_menu*, toolbar_item, window_buttons | host / create-only bits |
| Touch idioms | refreshable, swipeable | **sub** (button / menu+drag) |
| Drawing | canvas | full (recorded replay) |
| Shared band | opacity, paint, transform, layout, focus, lifecycle | full (layout via `C_LAYOUT`) |

---

## 5. What is *not* a gap (intentionally dropped from MAUI)

Stage 1 DROPped 377 rows. Absence from the contract is deliberate:

| DROP class | Examples | Why |
|---|---|---|
| MODEL (MVVM) | Command, Behaviors, Triggers, DataTemplate, SelectedItem as object, ItemsSource binding | facet is keyed-direct + fn+ctx, not bindings |
| LAYOUT | padding/margin/align on many types, SafeArea on Border | flex_layout owns layout |
| ENGINE | MapText, SendClicked, ArrangeOverride, VisibleViews, handler plumbing | framework internals |

So "MAUI has X and facet doesn't" is often correct and intended. Check
`plans/facet/maui-spec-report.md` and the map before treating absence as
debt.

MAUI types **not** in the facet control set (by design of this seed):
Shell, NavigationPage, FlyoutPage, ContentView-as-template, BoxView's
peers that are pure layout, etc. Promotion is a Stage 1 map decision,
not a backend task.

---

## 6. Recommendations (priority)

### P0 — none for "verb missing"

No undeclared-or-unanswered control verb blocks a backend install. The
gate is green.

### P1 — contract carriers (unlocks Stage 4's last five)

1. Title-bar leading/trailing **subtree slots** (or grow `Chrome` into a
   tree-bearing type).
2. Safe-area on more than `scroll` (page / shared band).
3. Optional: before-navigation hooks if any consumer needs cancelable
   transitions.

### P2 — quality / scale

1. **collection** recycling (or document "use list for large N").
2. Live flip for `text_field.is_secure` if apps need it (swap class on
   apply, or recreate the view).
3. Multi-column table only if a consumer needs spreadsheet semantics —
   today's `table` matches MAUI TableView's thin contract, not NSTableView
   columns.

### P3 — hygiene

1. Refresh MANIFEST prose so "cannot" sections match the empty
   `cannot-ledger` and the live substitute implementations.
2. Delete or fence dead `create_list` / `materialise_list` if the
   recycler is the only path.
3. Stage 4 item 7: examples under the new API.

---

## 7. How to re-verify

```bash
# contract still matches the map
python3 tools/gen_contract.py          # byte-idempotent
./tools/verify_contract.sh

# every verb answered
python3 tools/verb_coverage.py         # summary
python3 tools/verb_coverage.py --check # gate
python3 tools/verb_coverage.py --list  # per-bucket

# suites (from package dirs)
cd vendor/facet        && cpc test
cd vendor/facet_appkit && cpc test
```

---

## 8. Bottom line

| Question | Answer |
|---|---|
| Is the contract missing controls that MAUI seeded as ADOPT? | **No** — Stage 2 guards + iris gap pass closed the silent skips. |
| Is facet_appkit missing create/apply for a declared kind? | **No** — 42/42. |
| Is any declared verb neither implemented nor ledgered? | **No** — 0 absent, 0 never-fire. |
| Are there still *real* gaps? | **Yes, but qualitative:** 8 create-only, mobile verbs answered as desktop substitutes, collection materialises, 5 tier rows need contract carriers, examples not revived. |

For day-to-day app work (button, text input, list, scroll, split, menus,
window chrome): **the contract is there and AppKit implements it.** The
remaining work is carriers, scale, and honesty about Mac-shaped
substitutes — not a missing Button or TextField.
