# Stage 3 — the platform-free body

STUB. Verbatim from the plan of 2026-07-31, not yet revised. Decisions
recorded in `1.md` take precedence where the two disagree.

---

Ported from `vendor/facet.old` module by module, each reviewed against INTENT
and each its own commit: core (`Color`/`Style`/`Theme`, `Size`/`Rect`/`Point`,
`Node` + modifiers, the `@facet` Builder, leaves, containers, `Handle` +
`find`, `Cancellable`/`after`) → the `Renderer` vtable and mount walk, which is
the seam the backend fills → lifecycle (`Staged`, attach/detach), services,
screens (`Chrome`/`ScreenBox`), `nav`, the outlet router → `agent.cplus` →
`runtime.cplus` as the neutral facade only. The `runtime_macos.cplus` /
`runtime_linux.cplus` platform overrides do **not** come back this pass; facet
names no backend.
