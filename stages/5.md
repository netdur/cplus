# Stage 5 — records

STUB. Verbatim from the plan of 2026-07-31, not yet revised. Decisions
recorded in `1.md` take precedence where the two disagree.

---

`docs/contract.md` generated; ref/guide/tutorial ported to point at it rather
than hand-listing slots; `tools/verify_contract.sh` re-pointed at facet alone
(its `G2` facet_appkit check has to drop out while the backend is empty).
