# Stage 2 — re-aim the generator at declaration

STUB. Verbatim from the plan of 2026-07-31, not yet revised. Decisions
recorded in `1.md` take precedence where the two disagree.

---

Rewrite `tools/gen_contract.py` so `SLOTS` is *derived from the MAP's ADOPT
rows* instead of hand-typed, and the emission gate flips to "facet declares
it." It writes two files and only two: `vendor/facet/src/contract.cplus`
(generator-owned whole file — statics, registrars, `impl Handle` via EXT.2)
and `vendor/facet/docs/contract.md` (the manifest — one row per declared word
with its MAUI provenance). The `contract_appkit.cplus` emission is switched
off this pass.
