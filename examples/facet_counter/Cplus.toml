[package]
name    = "facet_counter"
version = "0.0.1"
edition = "2026"

# The worked example from `vendor/facet/SKILL.md` §2, as a program that runs.
# The skill file quotes this source; if you change one, change both — a
# reference whose examples do not compile is worse than no reference.

[macos]
entry = "src/main.cplus"

[dependencies]
stdlib        = "*"
facet         = "*"
facet_runtime = "*"
flex_layout   = "*"
events        = "*"

[macos.dependencies]
facet_appkit = "*"
objc         = "*"
appkit       = "*"
quartzcore   = "*"
webkit       = "*"
