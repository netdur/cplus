[package]
name    = "mathlib"
version = "0.1.0"
edition = "2026"

# Phase 5 reference: a C-callable library. `crate-type = "both"` produces
# both `target/<mode>/libmathlib.a` (static) and `libmathlib.{dylib,so}`
# (dynamic), plus `mathlib.h` for C consumers.
[lib]
crate-type = "both"
