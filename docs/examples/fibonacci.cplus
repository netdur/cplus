fn fib(n: i32) -> i32 {
    var a: i32 = 0;
    var b: i32 = 1;
    var i: i32 = 0;
    while i < n {
        let t = a + b;
        a = b;
        b = t;
        i = i + 1;
    }
    return a;
}

fn main() -> i32 {
    #println(fib(20));
    return 0;
}
