struct Point { x: i32, y: i32 }
struct Line { from: Point, to: Point }

fn main() -> i32 {
    let line: Line = Line {
        from: Point { x: 0, y: 0 },
        to: Point { x: 5, y: 12 },
    };
    println(line.to.x + line.to.y);
    return 0;
}
