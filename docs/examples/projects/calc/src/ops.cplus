// `ops.cplus` — arithmetic operations. Each is its own pub function
// so that other files can compose them by name.

pub fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

pub fn mul(a: i32, b: i32) -> i32 {
    return a * b;
}

pub fn neg(a: i32) -> i32 {
    return 0 -% a;
}
