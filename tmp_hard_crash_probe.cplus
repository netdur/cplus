fn leak() -> str {
    let owned: string = "x".to_string();
    let p: *u8 = #str_ptr(owned.as_str());
    return unsafe { #str_from_raw_parts(p, 1000000000 as usize) };
}

fn main() -> i32 {
    let s: str = leak();
    #println(s);
    return 0;
}
