// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// facet_appkit's generated backings: one impl per NATIVE-table slot,
// registered by install_generated() (called from the runtime facade
// beside backend::install()), each verified by its own round-trip
// probe test below — write through the Handle, read back through the
// raw getter. A slot that cannot pass its probe does not ship.

import "facet/facet" as facet;
import "facet/contract" as contract;
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "objc/synthesis" as synth;
import "appkit/appkit" as ak;
import "flex_layout/flex_layout" as flex;
import "./facet_appkit" as impl_mod;
import "./ui" as ui;

// Label.MaxLines -> setMaximumNumberOfLines:
fn g_set_max_lines(h: facet::Handle, v: i64) -> bool {
    let view: *u8 = h.view();
    if view == (0 as *u8) { return false; }
    let s: *u8 = rt::sel(#str_ptr("setMaximumNumberOfLines:\0"));
    if !impl_mod::slot_responds(view, s) { return false; }
    rt::msg_void_i64(view, s, v);
    impl_mod::slot_relayout(h, view);
    return true;
}

// Picker.SelectedIndex -> selectItemAtIndex:
fn g_set_selected_index(h: facet::Handle, v: i64) -> bool {
    let view: *u8 = h.view();
    if view == (0 as *u8) { return false; }
    let s: *u8 = rt::sel(#str_ptr("selectItemAtIndex:\0"));
    if !impl_mod::slot_responds(view, s) { return false; }
    rt::msg_void_i64(view, s, v);
    return true;
}

// Stepper.Increment -> setIncrement:
fn g_set_increment(h: facet::Handle, v: f64) -> bool {
    let view: *u8 = h.view();
    if view == (0 as *u8) { return false; }
    let s: *u8 = rt::sel(#str_ptr("setIncrement:\0"));
    if !impl_mod::slot_responds(view, s) { return false; }
    rt::msg_void_f64(view, s, v);
    return true;
}

// Image.IsAnimationPlaying -> setAnimates:
fn g_set_is_animation_playing(h: facet::Handle, v: bool) -> bool {
    let view: *u8 = h.view();
    if view == (0 as *u8) { return false; }
    let s: *u8 = rt::sel(#str_ptr("setAnimates:\0"));
    if !impl_mod::slot_responds(view, s) { return false; }
    var b: i8 = 0 as i8;
    if v { b = 1 as i8; }
    rt::msg_void_i8(view, s, b);
    return true;
}

// Register every generated backing. The runtime facade calls this right
// beside backend::install().
fn install_generated() {
    contract::set_max_lines_fn(g_set_max_lines);
    contract::set_selected_index_fn(g_set_selected_index);
    contract::set_increment_fn(g_set_increment);
    contract::set_is_animation_playing_fn(g_set_is_animation_playing);
    return;
}

#[test]
fn probe_set_max_lines_round_trips() {
    contract::set_max_lines_fn(g_set_max_lines);
    var probe_w: flex::Node = ui::label("probe", size: 13.0f64);
    let v: *u8 = probe_w.payload();
    let h: facet::Handle = facet::Handle::of(v, 0 as *u8, 0 as *u8, 0 as u32);
    let _r: facet::Handle = h.set_max_lines(3 as i64);
    assert rt::msg_i64(v, rt::sel(#str_ptr("maximumNumberOfLines\0"))) == (3 as i64);
}

#[test]
fn probe_set_selected_index_round_trips() {
    contract::set_selected_index_fn(g_set_selected_index);
    let probe_w: ak::PopUpButton = ak::PopUpButton::new(rt::Rect { x: 0.0, y: 0.0, w: 0.0, h: 0.0 }, false);
    let v: *u8 = probe_w.raw();
    rt::msg_void_id(v, rt::sel(#str_ptr("addItemWithTitle:\0")), bridge::nsstring("a"));
    rt::msg_void_id(v, rt::sel(#str_ptr("addItemWithTitle:\0")), bridge::nsstring("b"));
    rt::msg_void_id(v, rt::sel(#str_ptr("addItemWithTitle:\0")), bridge::nsstring("c"));
    let h: facet::Handle = facet::Handle::of(v, 0 as *u8, 0 as *u8, 0 as u32);
    let _r: facet::Handle = h.set_selected_index(1 as i64);
    assert rt::msg_i64(v, rt::sel(#str_ptr("indexOfSelectedItem\0"))) == (1 as i64);
}

#[test]
fn probe_set_increment_round_trips() {
    contract::set_increment_fn(g_set_increment);
    let probe_w: ak::Stepper = ak::Stepper::new();
    let v: *u8 = probe_w.raw();
    let h: facet::Handle = facet::Handle::of(v, 0 as *u8, 0 as *u8, 0 as u32);
    let _r: facet::Handle = h.set_increment(5.0f64);
    assert rt::msg_f64(v, rt::sel(#str_ptr("increment\0"))) == (5.0f64);
}

#[test]
fn probe_set_is_animation_playing_round_trips() {
    contract::set_is_animation_playing_fn(g_set_is_animation_playing);
    let probe_p: *u8 = synth::alloc_init_class(rt::get_class(#str_ptr("NSImageView\0")));
    let v: *u8 = probe_p;
    let h: facet::Handle = facet::Handle::of(v, 0 as *u8, 0 as *u8, 0 as u32);
    let _r: facet::Handle = h.set_is_animation_playing(true);
    assert (rt::msg_i8(v, rt::sel(#str_ptr("animates\0"))) != (0 as i8)) == (true);
}

