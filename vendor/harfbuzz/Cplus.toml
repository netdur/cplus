[package]
name    = "harfbuzz"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/share/gir-1.0/HarfBuzz-0.0.gir (namespace HarfBuzz 0.0)
# Reproduce: cpc-bindgen --gobject HarfBuzz --out vendor/harfbuzz --use GLib=glib --use GObject=gobject_gir --use GModule=gmodule --use Gio=gio --use cairo=cairo --use Graphene=graphene --use Pango=pango --use PangoCairo=pangocairo --use GdkPixbuf=gdkpixbuf --use Gdk=gdk --use Gsk=gsk --use Gtk=gtk4
# Coverage:  1077 items, 188 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"
glib        = "*"

[link]
libs = ["harfbuzz-gobject", "harfbuzz"]
