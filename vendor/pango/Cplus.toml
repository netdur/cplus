[package]
name    = "pango"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/share/gir-1.0/Pango-1.0.gir (namespace Pango 1.0)
# Reproduce: cpc-bindgen --gobject Pango --out vendor/pango --use GLib=glib --use GObject=gobject_gir --use GModule=gmodule --use Gio=gio --use cairo=cairo --use Graphene=graphene --use HarfBuzz=harfbuzz --use PangoCairo=pangocairo --use GdkPixbuf=gdkpixbuf --use Gdk=gdk --use Gsk=gsk --use Gtk=gtk4
# Coverage:  645 items, 85 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"
cairo       = "*"
gio         = "*"
glib        = "*"
gmodule     = "*"
gobject_gir = "*"
harfbuzz    = "*"

[link]
libs = ["pango-1.0"]
