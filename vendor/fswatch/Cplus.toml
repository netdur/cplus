[package]
name    = "fswatch"
version = "0.0.1"
edition = "2026"

# fswatch — owner-thread filesystem change events.
#
# The first backend targets macOS with kqueue. Platform-specific backends keep
# the public module stable when Linux and Windows support lands.

[dependencies]
stdlib = "*"
events = "*"
