[package]
name    = "gdk"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/share/gir-1.0/Gdk-4.0.gir (namespace Gdk 4.0)
# Reproduce: cpc-bindgen --gobject Gdk --out vendor/gdk --use GLib=glib --use GObject=gobject_gir --use GModule=gmodule --use Gio=gio --use cairo=cairo --use Graphene=graphene --use HarfBuzz=harfbuzz --use Pango=pango --use PangoCairo=pangocairo --use GdkPixbuf=gdkpixbuf --use Gsk=gsk --use Gtk=gtk4
# Coverage:  786 items, 52 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"
cairo       = "*"
gdkpixbuf   = "*"
gio         = "*"
glib        = "*"
gmodule     = "*"
gobject_gir = "*"
harfbuzz    = "*"
pango       = "*"
pangocairo  = "*"

[link]
libs = ["gtk-4"]
