[package]
name    = "gio"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/share/gir-1.0/Gio-2.0.gir (namespace Gio 2.0)
# Reproduce: cpc-bindgen --gobject Gio --out vendor/gio --use GLib=glib --use GObject=gobject_gir --use GModule=gmodule --use cairo=cairo --use Graphene=graphene --use HarfBuzz=harfbuzz --use Pango=pango --use PangoCairo=pangocairo --use GdkPixbuf=gdkpixbuf --use Gdk=gdk --use Gsk=gsk --use Gtk=gtk4
# Coverage:  2097 items, 384 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"
glib        = "*"
gobject_gir = "*"

[link]
libs = ["gio-2.0"]
