[package]
name    = "gobject_gir"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/share/gir-1.0/GObject-2.0.gir (namespace GObject 2.0)
# Reproduce: cpc-bindgen --gobject GObject --out vendor/gobject_gir --use GLib=glib --use GModule=gmodule --use Gio=gio --use cairo=cairo --use Graphene=graphene --use HarfBuzz=harfbuzz --use Pango=pango --use PangoCairo=pangocairo --use GdkPixbuf=gdkpixbuf --use Gdk=gdk --use Gsk=gsk --use Gtk=gtk4
# Coverage:  343 items, 117 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"
glib        = "*"

[link]
libs = ["gobject-2.0"]
