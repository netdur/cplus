// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/CoreML.framework/Headers/CoreML.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/vec" as vec;
import "stdlib/string_map" as string_map;
import "stdlib/string_set" as string_set;
import "stdlib/status" as status;
import "foundation/foundation" as foundation;

// `MLMultiArrayDataType` (NS_ENUM)
enum MultiArrayDataType {
    Double,
    Float64,
    Float32,
    Float16,
    Float,
    Int32,
    Int8,
}

fn multi_array_data_type_raw(v: MultiArrayDataType) -> i64 {
    return match v {
        MultiArrayDataType::Double => { 65600 as i64 },
        MultiArrayDataType::Float64 => { 65600 as i64 },
        MultiArrayDataType::Float32 => { 65568 as i64 },
        MultiArrayDataType::Float16 => { 65552 as i64 },
        MultiArrayDataType::Float => { 65568 as i64 },
        MultiArrayDataType::Int32 => { 131104 as i64 },
        MultiArrayDataType::Int8 => { 131080 as i64 },
    };
}

fn multi_array_data_type_from_raw(raw: i64) -> MultiArrayDataType {
    if raw == (65600 as i64) { return MultiArrayDataType::Float64; }
    if raw == (65568 as i64) { return MultiArrayDataType::Float32; }
    if raw == (65552 as i64) { return MultiArrayDataType::Float16; }
    if raw == (65568 as i64) { return MultiArrayDataType::Float; }
    if raw == (131104 as i64) { return MultiArrayDataType::Int32; }
    if raw == (131080 as i64) { return MultiArrayDataType::Int8; }
    return MultiArrayDataType::Double;
}

// `MLFeatureType` (NS_ENUM)
enum FeatureType {
    Invalid,
    Int64,
    Double,
    String,
    Image,
    MultiArray,
    Dictionary,
    Sequence,
    State,
}

fn feature_type_raw(v: FeatureType) -> i64 {
    return match v {
        FeatureType::Invalid => { 0 as i64 },
        FeatureType::Int64 => { 1 as i64 },
        FeatureType::Double => { 2 as i64 },
        FeatureType::String => { 3 as i64 },
        FeatureType::Image => { 4 as i64 },
        FeatureType::MultiArray => { 5 as i64 },
        FeatureType::Dictionary => { 6 as i64 },
        FeatureType::Sequence => { 7 as i64 },
        FeatureType::State => { 8 as i64 },
    };
}

fn feature_type_from_raw(raw: i64) -> FeatureType {
    if raw == (1 as i64) { return FeatureType::Int64; }
    if raw == (2 as i64) { return FeatureType::Double; }
    if raw == (3 as i64) { return FeatureType::String; }
    if raw == (4 as i64) { return FeatureType::Image; }
    if raw == (5 as i64) { return FeatureType::MultiArray; }
    if raw == (6 as i64) { return FeatureType::Dictionary; }
    if raw == (7 as i64) { return FeatureType::Sequence; }
    if raw == (8 as i64) { return FeatureType::State; }
    return FeatureType::Invalid;
}

// `CGImagePropertyOrientation` (NS_ENUM)
enum CGImagePropertyOrientation {
    KCGImagePropertyOrientationUp,
    KCGImagePropertyOrientationUpMirrored,
    KCGImagePropertyOrientationDown,
    KCGImagePropertyOrientationDownMirrored,
    KCGImagePropertyOrientationLeftMirrored,
    KCGImagePropertyOrientationRight,
    KCGImagePropertyOrientationRightMirrored,
    KCGImagePropertyOrientationLeft,
}

fn cg_image_property_orientation_raw(v: CGImagePropertyOrientation) -> i64 {
    return match v {
        CGImagePropertyOrientation::KCGImagePropertyOrientationUp => { 1 as i64 },
        CGImagePropertyOrientation::KCGImagePropertyOrientationUpMirrored => { 2 as i64 },
        CGImagePropertyOrientation::KCGImagePropertyOrientationDown => { 3 as i64 },
        CGImagePropertyOrientation::KCGImagePropertyOrientationDownMirrored => { 4 as i64 },
        CGImagePropertyOrientation::KCGImagePropertyOrientationLeftMirrored => { 5 as i64 },
        CGImagePropertyOrientation::KCGImagePropertyOrientationRight => { 6 as i64 },
        CGImagePropertyOrientation::KCGImagePropertyOrientationRightMirrored => { 7 as i64 },
        CGImagePropertyOrientation::KCGImagePropertyOrientationLeft => { 8 as i64 },
    };
}

fn cg_image_property_orientation_from_raw(raw: i64) -> CGImagePropertyOrientation {
    if raw == (2 as i64) { return CGImagePropertyOrientation::KCGImagePropertyOrientationUpMirrored; }
    if raw == (3 as i64) { return CGImagePropertyOrientation::KCGImagePropertyOrientationDown; }
    if raw == (4 as i64) { return CGImagePropertyOrientation::KCGImagePropertyOrientationDownMirrored; }
    if raw == (5 as i64) { return CGImagePropertyOrientation::KCGImagePropertyOrientationLeftMirrored; }
    if raw == (6 as i64) { return CGImagePropertyOrientation::KCGImagePropertyOrientationRight; }
    if raw == (7 as i64) { return CGImagePropertyOrientation::KCGImagePropertyOrientationRightMirrored; }
    if raw == (8 as i64) { return CGImagePropertyOrientation::KCGImagePropertyOrientationLeft; }
    return CGImagePropertyOrientation::KCGImagePropertyOrientationUp;
}

// `MLImageSizeConstraintType` (NS_ENUM)
enum ImageSizeConstraintType {
    Unspecified,
    Enumerated,
    Range,
}

fn image_size_constraint_type_raw(v: ImageSizeConstraintType) -> i64 {
    return match v {
        ImageSizeConstraintType::Unspecified => { 0 as i64 },
        ImageSizeConstraintType::Enumerated => { 2 as i64 },
        ImageSizeConstraintType::Range => { 3 as i64 },
    };
}

fn image_size_constraint_type_from_raw(raw: i64) -> ImageSizeConstraintType {
    if raw == (2 as i64) { return ImageSizeConstraintType::Enumerated; }
    if raw == (3 as i64) { return ImageSizeConstraintType::Range; }
    return ImageSizeConstraintType::Unspecified;
}

// `MLMultiArrayShapeConstraintType` (NS_ENUM)
enum MultiArrayShapeConstraintType {
    Unspecified,
    Enumerated,
    Range,
}

fn multi_array_shape_constraint_type_raw(v: MultiArrayShapeConstraintType) -> i64 {
    return match v {
        MultiArrayShapeConstraintType::Unspecified => { 1 as i64 },
        MultiArrayShapeConstraintType::Enumerated => { 2 as i64 },
        MultiArrayShapeConstraintType::Range => { 3 as i64 },
    };
}

fn multi_array_shape_constraint_type_from_raw(raw: i64) -> MultiArrayShapeConstraintType {
    if raw == (2 as i64) { return MultiArrayShapeConstraintType::Enumerated; }
    if raw == (3 as i64) { return MultiArrayShapeConstraintType::Range; }
    return MultiArrayShapeConstraintType::Unspecified;
}

// `MLReshapeFrequencyHint` (NS_ENUM)
enum ReshapeFrequencyHint {
    Frequent,
    Infrequent,
}

fn reshape_frequency_hint_raw(v: ReshapeFrequencyHint) -> i64 {
    return match v {
        ReshapeFrequencyHint::Frequent => { 0 as i64 },
        ReshapeFrequencyHint::Infrequent => { 1 as i64 },
    };
}

fn reshape_frequency_hint_from_raw(raw: i64) -> ReshapeFrequencyHint {
    if raw == (1 as i64) { return ReshapeFrequencyHint::Infrequent; }
    return ReshapeFrequencyHint::Frequent;
}

// `MLSpecializationStrategy` (NS_ENUM)
enum SpecializationStrategy {
    Default,
    FastPrediction,
}

fn specialization_strategy_raw(v: SpecializationStrategy) -> i64 {
    return match v {
        SpecializationStrategy::Default => { 0 as i64 },
        SpecializationStrategy::FastPrediction => { 1 as i64 },
    };
}

fn specialization_strategy_from_raw(raw: i64) -> SpecializationStrategy {
    if raw == (1 as i64) { return SpecializationStrategy::FastPrediction; }
    return SpecializationStrategy::Default;
}

// `MLComputeUnits` (NS_ENUM)
enum ComputeUnits {
    CPUOnly,
    CPUAndGPU,
    All,
    CPUAndNeuralEngine,
}

fn compute_units_raw(v: ComputeUnits) -> i64 {
    return match v {
        ComputeUnits::CPUOnly => { 0 as i64 },
        ComputeUnits::CPUAndGPU => { 1 as i64 },
        ComputeUnits::All => { 2 as i64 },
        ComputeUnits::CPUAndNeuralEngine => { 3 as i64 },
    };
}

fn compute_units_from_raw(raw: i64) -> ComputeUnits {
    if raw == (1 as i64) { return ComputeUnits::CPUAndGPU; }
    if raw == (2 as i64) { return ComputeUnits::All; }
    if raw == (3 as i64) { return ComputeUnits::CPUAndNeuralEngine; }
    return ComputeUnits::CPUOnly;
}

// `MLTaskState` (NS_ENUM)
enum TaskState {
    Suspended,
    Running,
    Cancelling,
    Completed,
    Failed,
}

fn task_state_raw(v: TaskState) -> i64 {
    return match v {
        TaskState::Suspended => { 1 as i64 },
        TaskState::Running => { 2 as i64 },
        TaskState::Cancelling => { 3 as i64 },
        TaskState::Completed => { 4 as i64 },
        TaskState::Failed => { 5 as i64 },
    };
}

fn task_state_from_raw(raw: i64) -> TaskState {
    if raw == (2 as i64) { return TaskState::Running; }
    if raw == (3 as i64) { return TaskState::Cancelling; }
    if raw == (4 as i64) { return TaskState::Completed; }
    if raw == (5 as i64) { return TaskState::Failed; }
    return TaskState::Suspended;
}

// `MLUpdateProgressEvent` (NS_OPTIONS) — bitfield; combine with `|`.
const update_progress_event_training_begin: u64 = 1 as u64;
const update_progress_event_epoch_end: u64 = 2 as u64;
const update_progress_event_mini_batch_end: u64 = 4 as u64;

// Untyped Objective-C object handle (`id` / `NSObject *` with no bound type).
// Non-owning; the caller manages lifetime.
struct AnyObject {
    opaque _obj: *u8,
}

impl AnyObject {
    fn raw(this) -> *u8 { return this._obj; }
    fn from_raw(ptr: *u8) -> AnyObject { return AnyObject { _obj: ptr }; }
}

#[link_name = "objc_msgSend"]
extern fn objc_msg_i8_id_id_id_id(recv: *u8, sel: *u8, a0: *u8, a1: *u8, a2: *u8, a3: *u8) -> i8;
#[link_name = "objc_msgSend"]
extern fn objc_msg_id_id_i64_i64(recv: *u8, sel: *u8, a0: *u8, a1: i64, a2: i64) -> *u8;
#[link_name = "objc_msgSend"]
extern fn objc_msg_id_id_i64_i64_i64_u32_id_id(recv: *u8, sel: *u8, a0: *u8, a1: i64, a2: i64, a3: i64, a4: u32, a5: *u8, a6: *u8) -> *u8;
#[link_name = "objc_msgSend"]
extern fn objc_msg_id_id_i64_i64_u32_id_id(recv: *u8, sel: *u8, a0: *u8, a1: i64, a2: i64, a3: u32, a4: *u8, a5: *u8) -> *u8;
#[link_name = "objc_msgSend"]
extern fn objc_msg_id_id_i64_id_id_id(recv: *u8, sel: *u8, a0: *u8, a1: i64, a2: *u8, a3: *u8, a4: *u8) -> *u8;

#[repr(C)]
struct MultiArray_get_bytes_with_handler_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, i64),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, i64),
    opaque ctx: *u8,
}

fn MultiArray_get_bytes_with_handler_invoke(block: *u8, a0: *u8, a1: i64) {
    let bl: *MultiArray_get_bytes_with_handler_block = { block as *MultiArray_get_bytes_with_handler_block };
    let f: fn(*u8, *u8, i64) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct MultiArray_get_mutable_bytes_with_handler_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, i64, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, i64, *u8),
    opaque ctx: *u8,
}

fn MultiArray_get_mutable_bytes_with_handler_invoke(block: *u8, a0: *u8, a1: i64, a2: *u8) {
    let bl: *MultiArray_get_mutable_bytes_with_handler_block = { block as *MultiArray_get_mutable_bytes_with_handler_block };
    let f: fn(*u8, *u8, i64, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1, a2);
    return;
}

#[repr(C)]
struct State_get_multi_array_for_state_named_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8),
    opaque ctx: *u8,
}

fn State_get_multi_array_for_state_named_invoke(block: *u8, a0: *u8) {
    let bl: *State_get_multi_array_for_state_named_block = { block as *State_get_multi_array_for_state_named_block };
    let f: fn(*u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0);
    return;
}

#[repr(C)]
struct ModelAsset_model_description_with_completion_handler_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn ModelAsset_model_description_with_completion_handler_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *ModelAsset_model_description_with_completion_handler_block = { block as *ModelAsset_model_description_with_completion_handler_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct ModelAsset_model_description_of_function_named_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn ModelAsset_model_description_of_function_named_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *ModelAsset_model_description_of_function_named_block = { block as *ModelAsset_model_description_of_function_named_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct ModelAsset_function_names_with_completion_handler_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn ModelAsset_function_names_with_completion_handler_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *ModelAsset_function_names_with_completion_handler_block = { block as *ModelAsset_function_names_with_completion_handler_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct Model_load_contents_of_url_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn Model_load_contents_of_url_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *Model_load_contents_of_url_block = { block as *Model_load_contents_of_url_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct Model_prediction_from_features_completion_handler_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn Model_prediction_from_features_completion_handler_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *Model_prediction_from_features_completion_handler_block = { block as *Model_prediction_from_features_completion_handler_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct Model_prediction_from_features_options_completion_handler_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn Model_prediction_from_features_options_completion_handler_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *Model_prediction_from_features_options_completion_handler_block = { block as *Model_prediction_from_features_options_completion_handler_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct Model_load_model_asset_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn Model_load_model_asset_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *Model_load_model_asset_block = { block as *Model_load_model_asset_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct Model_compile_model_at_url_completion_handler_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn Model_compile_model_at_url_completion_handler_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *Model_compile_model_at_url_completion_handler_block = { block as *Model_compile_model_at_url_completion_handler_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct Model_prediction_from_features_using_state_options_completion_handler_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn Model_prediction_from_features_using_state_options_completion_handler_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *Model_prediction_from_features_using_state_options_completion_handler_block = { block as *Model_prediction_from_features_using_state_options_completion_handler_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct ModelCollection_begin_accessing_model_collection_with_identifier_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn ModelCollection_begin_accessing_model_collection_with_identifier_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *ModelCollection_begin_accessing_model_collection_with_identifier_block = { block as *ModelCollection_begin_accessing_model_collection_with_identifier_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct ModelCollection_end_accessing_model_collection_with_identifier_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, i8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, i8, *u8),
    opaque ctx: *u8,
}

fn ModelCollection_end_accessing_model_collection_with_identifier_invoke(block: *u8, a0: i8, a1: *u8) {
    let bl: *ModelCollection_end_accessing_model_collection_with_identifier_block = { block as *ModelCollection_end_accessing_model_collection_with_identifier_block };
    let f: fn(*u8, i8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct ModelStructure_load_contents_of_url_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn ModelStructure_load_contents_of_url_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *ModelStructure_load_contents_of_url_block = { block as *ModelStructure_load_contents_of_url_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct ModelStructure_load_model_asset_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn ModelStructure_load_model_asset_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *ModelStructure_load_model_asset_block = { block as *ModelStructure_load_model_asset_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct ComputePlan_load_contents_of_url_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn ComputePlan_load_contents_of_url_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *ComputePlan_load_contents_of_url_block = { block as *ComputePlan_load_contents_of_url_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

#[repr(C)]
struct ComputePlan_load_model_asset_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8, *u8),
    opaque ctx: *u8,
}

fn ComputePlan_load_model_asset_invoke(block: *u8, a0: *u8, a1: *u8) {
    let bl: *ComputePlan_load_model_asset_block = { block as *ComputePlan_load_model_asset_block };
    let f: fn(*u8, *u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0, a1);
    return;
}

// `MLMultiArray` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct MultiArray {
    _obj: *u8,
}

impl MultiArray {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> MultiArray { return MultiArray { _obj: rt::retain(ptr) }; }

    fn data_pointer(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("dataPointer\0")));
    }

    fn data_type(this) -> MultiArrayDataType {
        return multi_array_data_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("dataType\0"))));
    }

    fn shape(this) -> vec::Vec[f64] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("shape\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[f64] = vec::Vec[f64]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let dv_sel: *u8 = rt::sel(#str_ptr("doubleValue\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let value: *u8 = rt::msg_id_u64(arr, at_sel, i);
            out.append(rt::msg_f64(value, dv_sel));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn strides(this) -> vec::Vec[f64] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("strides\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[f64] = vec::Vec[f64]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let dv_sel: *u8 = rt::sel(#str_ptr("doubleValue\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let value: *u8 = rt::msg_id_u64(arr, at_sel, i);
            out.append(rt::msg_f64(value, dv_sel));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn count(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("count\0")));
    }

    fn pixel_buffer(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("pixelBuffer\0")));
        return bridge::obj_option(obj);
    }

    fn new(shape: vec::Vec[f64], data_type: MultiArrayDataType, error: *u8) -> MultiArray {
        let cls: *u8 = rt::get_class(#str_ptr("MLMultiArray\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        let arr_shape: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_shape: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let num_cls_shape: *u8 = rt::get_class(#str_ptr("NSNumber\0"));
        let num_sel_shape: *u8 = rt::sel(#str_ptr("numberWithDouble:\0"));
        let n_shape: usize = shape.count();
        var i_shape: usize = 0 as usize;
        while i_shape < n_shape {
            match shape.at_ptr(i_shape) {
                option::Option[*f64]::Some(e_shape) => { rt::msg_void_id(arr_shape, add_sel_shape, rt::msg_id_f64(num_cls_shape, num_sel_shape, *e_shape)); }
                option::Option[*f64]::None => {}
            }
            i_shape = i_shape +% (1 as usize);
        }
        return MultiArray { _obj: rt::msg_id_id_i64_id(alloced, rt::sel(#str_ptr("initWithShape:dataType:error:\0")), arr_shape, multi_array_data_type_raw(data_type), error) };
    }

    fn new_with_shape_data_type_strides(shape: vec::Vec[f64], data_type: MultiArrayDataType, strides: vec::Vec[f64]) -> MultiArray {
        let cls: *u8 = rt::get_class(#str_ptr("MLMultiArray\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        let arr_shape: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_shape: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let num_cls_shape: *u8 = rt::get_class(#str_ptr("NSNumber\0"));
        let num_sel_shape: *u8 = rt::sel(#str_ptr("numberWithDouble:\0"));
        let n_shape: usize = shape.count();
        var i_shape: usize = 0 as usize;
        while i_shape < n_shape {
            match shape.at_ptr(i_shape) {
                option::Option[*f64]::Some(e_shape) => { rt::msg_void_id(arr_shape, add_sel_shape, rt::msg_id_f64(num_cls_shape, num_sel_shape, *e_shape)); }
                option::Option[*f64]::None => {}
            }
            i_shape = i_shape +% (1 as usize);
        }
        let arr_strides: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_strides: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let num_cls_strides: *u8 = rt::get_class(#str_ptr("NSNumber\0"));
        let num_sel_strides: *u8 = rt::sel(#str_ptr("numberWithDouble:\0"));
        let n_strides: usize = strides.count();
        var i_strides: usize = 0 as usize;
        while i_strides < n_strides {
            match strides.at_ptr(i_strides) {
                option::Option[*f64]::Some(e_strides) => { rt::msg_void_id(arr_strides, add_sel_strides, rt::msg_id_f64(num_cls_strides, num_sel_strides, *e_strides)); }
                option::Option[*f64]::None => {}
            }
            i_strides = i_strides +% (1 as usize);
        }
        return MultiArray { _obj: rt::msg_id_id_i64_id(alloced, rt::sel(#str_ptr("initWithShape:dataType:strides:\0")), arr_shape, multi_array_data_type_raw(data_type), arr_strides) };
    }

    // SKIPPED `initWithDataPointer:shape:dataType:strides:deallocator:error:`: params after the block not modelled
    fn new_with_pixel_buffer_shape(pixel_buffer: *u8, shape: vec::Vec[f64]) -> MultiArray {
        let cls: *u8 = rt::get_class(#str_ptr("MLMultiArray\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        let arr_shape: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_shape: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let num_cls_shape: *u8 = rt::get_class(#str_ptr("NSNumber\0"));
        let num_sel_shape: *u8 = rt::sel(#str_ptr("numberWithDouble:\0"));
        let n_shape: usize = shape.count();
        var i_shape: usize = 0 as usize;
        while i_shape < n_shape {
            match shape.at_ptr(i_shape) {
                option::Option[*f64]::Some(e_shape) => { rt::msg_void_id(arr_shape, add_sel_shape, rt::msg_id_f64(num_cls_shape, num_sel_shape, *e_shape)); }
                option::Option[*f64]::None => {}
            }
            i_shape = i_shape +% (1 as usize);
        }
        return MultiArray { _obj: rt::msg_id_id_id(alloced, rt::sel(#str_ptr("initWithPixelBuffer:shape:\0")), pixel_buffer, arr_shape) };
    }

    fn get_bytes_with_handler(this, cb: fn(*u8, *u8, i64), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: MultiArray_get_bytes_with_handler_block = MultiArray_get_bytes_with_handler_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: MultiArray_get_bytes_with_handler_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("getBytesWithHandler:\0")), bp);
        return;
    }

    fn get_mutable_bytes_with_handler(this, cb: fn(*u8, *u8, i64, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: MultiArray_get_mutable_bytes_with_handler_block = MultiArray_get_mutable_bytes_with_handler_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: MultiArray_get_mutable_bytes_with_handler_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("getMutableBytesWithHandler:\0")), bp);
        return;
    }

    fn multi_array_by_concatenating_multi_arrays(multi_arrays: vec::Vec[MultiArray], along_axis: i64, data_type: MultiArrayDataType) -> MultiArray {
        let arr_multi_arrays: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_multi_arrays: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let n_multi_arrays: usize = multi_arrays.count();
        var i_multi_arrays: usize = 0 as usize;
        while i_multi_arrays < n_multi_arrays {
            match multi_arrays.at_ptr(i_multi_arrays) {
                option::Option[*MultiArray]::Some(e_multi_arrays) => { rt::msg_void_id(arr_multi_arrays, add_sel_multi_arrays, (*e_multi_arrays).raw()); }
                option::Option[*MultiArray]::None => {}
            }
            i_multi_arrays = i_multi_arrays +% (1 as usize);
        }
        return MultiArray { _obj: rt::retain(objc_msg_id_id_i64_i64(rt::get_class(#str_ptr("MLMultiArray\0")), rt::sel(#str_ptr("multiArrayByConcatenatingMultiArrays:alongAxis:dataType:\0")), arr_multi_arrays, along_axis, multi_array_data_type_raw(data_type))) };
    }

    fn object_at_indexed_subscript(this, idx: i64) -> foundation::Number {
        return foundation::Number::from_raw(rt::msg_id_i64(this._obj, rt::sel(#str_ptr("objectAtIndexedSubscript:\0")), idx));
    }

    fn object_for_keyed_subscript(this, key: vec::Vec[f64]) -> foundation::Number {
        let arr_key: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_key: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let num_cls_key: *u8 = rt::get_class(#str_ptr("NSNumber\0"));
        let num_sel_key: *u8 = rt::sel(#str_ptr("numberWithDouble:\0"));
        let n_key: usize = key.count();
        var i_key: usize = 0 as usize;
        while i_key < n_key {
            match key.at_ptr(i_key) {
                option::Option[*f64]::Some(e_key) => { rt::msg_void_id(arr_key, add_sel_key, rt::msg_id_f64(num_cls_key, num_sel_key, *e_key)); }
                option::Option[*f64]::None => {}
            }
            i_key = i_key +% (1 as usize);
        }
        return foundation::Number::from_raw(rt::msg_id_id(this._obj, rt::sel(#str_ptr("objectForKeyedSubscript:\0")), arr_key));
    }

    fn set_object(this, obj: foundation::Number, at_indexed_subscript: i64) {
        rt::msg_void_id_i64(this._obj, rt::sel(#str_ptr("setObject:atIndexedSubscript:\0")), obj.raw(), at_indexed_subscript);
    }

    fn set_object_for_keyed_subscript(this, obj: foundation::Number, key: vec::Vec[f64]) {
        let arr_key: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_key: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let num_cls_key: *u8 = rt::get_class(#str_ptr("NSNumber\0"));
        let num_sel_key: *u8 = rt::sel(#str_ptr("numberWithDouble:\0"));
        let n_key: usize = key.count();
        var i_key: usize = 0 as usize;
        while i_key < n_key {
            match key.at_ptr(i_key) {
                option::Option[*f64]::Some(e_key) => { rt::msg_void_id(arr_key, add_sel_key, rt::msg_id_f64(num_cls_key, num_sel_key, *e_key)); }
                option::Option[*f64]::None => {}
            }
            i_key = i_key +% (1 as usize);
        }
        rt::msg_void_id_id(this._obj, rt::sel(#str_ptr("setObject:forKeyedSubscript:\0")), obj.raw(), arr_key);
    }

    fn transfer_to_multi_array(this, destination_multi_array: MultiArray) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("transferToMultiArray:\0")), destination_multi_array.raw());
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLSequence` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Sequence {
    opaque _obj: *u8,
}

impl Sequence {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Sequence { return Sequence { _obj: ptr }; }

    fn empty_sequence_with_type(type_: FeatureType) -> Sequence {
        return Sequence { _obj: rt::msg_id_i64(rt::get_class(#str_ptr("MLSequence\0")), rt::sel(#str_ptr("emptySequenceWithType:\0")), feature_type_raw(type_)) };
    }

    fn sequence_with_string_array(string_values: vec::Vec[text::Text]) -> Sequence {
        return Sequence { _obj: rt::msg_id_id(rt::get_class(#str_ptr("MLSequence\0")), rt::sel(#str_ptr("sequenceWithStringArray:\0")), bridge::nsarray_of_text(string_values)) };
    }

    fn sequence_with_int64_array(int64_values: vec::Vec[f64]) -> Sequence {
        let arr_int64_values: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_int64_values: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let num_cls_int64_values: *u8 = rt::get_class(#str_ptr("NSNumber\0"));
        let num_sel_int64_values: *u8 = rt::sel(#str_ptr("numberWithDouble:\0"));
        let n_int64_values: usize = int64_values.count();
        var i_int64_values: usize = 0 as usize;
        while i_int64_values < n_int64_values {
            match int64_values.at_ptr(i_int64_values) {
                option::Option[*f64]::Some(e_int64_values) => { rt::msg_void_id(arr_int64_values, add_sel_int64_values, rt::msg_id_f64(num_cls_int64_values, num_sel_int64_values, *e_int64_values)); }
                option::Option[*f64]::None => {}
            }
            i_int64_values = i_int64_values +% (1 as usize);
        }
        return Sequence { _obj: rt::msg_id_id(rt::get_class(#str_ptr("MLSequence\0")), rt::sel(#str_ptr("sequenceWithInt64Array:\0")), arr_int64_values) };
    }

    fn type_(this) -> FeatureType {
        return feature_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("type\0"))));
    }

    fn string_values(this) -> vec::Vec[text::Text] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("stringValues\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[text::Text] = vec::Vec[text::Text]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let value: *u8 = rt::msg_id_u64(arr, at_sel, i);
            out.append(bridge::to_text(value));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn int64_values(this) -> vec::Vec[f64] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("int64Values\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[f64] = vec::Vec[f64]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let dv_sel: *u8 = rt::sel(#str_ptr("doubleValue\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let value: *u8 = rt::msg_id_u64(arr, at_sel, i);
            out.append(rt::msg_f64(value, dv_sel));
            i = i +% (1 as u64);
        }
        return out;
    }

}

// `MLState` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct State {
    _obj: *u8,
}

impl State {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> State { return State { _obj: rt::retain(ptr) }; }

    fn get_multi_array_for_state_named(this, state_name: str, cb: fn(*u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: State_get_multi_array_for_state_named_block = State_get_multi_array_for_state_named_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: State_get_multi_array_for_state_named_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id_id(this._obj, rt::sel(#str_ptr("getMultiArrayForStateNamed:handler:\0")), bridge::nsstring(state_name), bp);
        return;
    }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> State {
        return State { _obj: rt::msg_id(rt::get_class(#str_ptr("MLState\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLFeatureValue` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct FeatureValue {
    opaque _obj: *u8,
}

impl FeatureValue {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> FeatureValue { return FeatureValue { _obj: ptr }; }

    fn feature_value_with_int64(value: i64) -> FeatureValue {
        return FeatureValue { _obj: rt::msg_id_i64(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithInt64:\0")), value) };
    }

    fn feature_value_with_double(value: f64) -> FeatureValue {
        return FeatureValue { _obj: rt::msg_id_f64(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithDouble:\0")), value) };
    }

    fn feature_value_with_string(value: str) -> FeatureValue {
        return FeatureValue { _obj: rt::msg_id_id(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithString:\0")), bridge::nsstring(value)) };
    }

    fn feature_value_with_multi_array(value: MultiArray) -> FeatureValue {
        return FeatureValue { _obj: rt::msg_id_id(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithMultiArray:\0")), value.raw()) };
    }

    fn feature_value_with_pixel_buffer(value: *u8) -> FeatureValue {
        return FeatureValue { _obj: rt::msg_id_id(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithPixelBuffer:\0")), value) };
    }

    fn feature_value_with_sequence(sequence: Sequence) -> FeatureValue {
        return FeatureValue { _obj: rt::msg_id_id(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithSequence:\0")), sequence.raw()) };
    }

    fn undefined_feature_value_with_type(type_: FeatureType) -> FeatureValue {
        return FeatureValue { _obj: rt::msg_id_i64(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("undefinedFeatureValueWithType:\0")), feature_type_raw(type_)) };
    }

    // SKIPPED `featureValueWithDictionary:error:`: param `NSDictionary<id,NSNumber *> * _Nonnull` — generic collection
    fn is_equal_to_feature_value(this, value: FeatureValue) -> bool {
        return rt::msg_i8_id(this._obj, rt::sel(#str_ptr("isEqualToFeatureValue:\0")), value.raw()) != (0 as i8);
    }

    fn type_(this) -> FeatureType {
        return feature_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("type\0"))));
    }

    fn is_undefined(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("isUndefined\0"))) != (0 as i8);
    }

    fn int64_value(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("int64Value\0")));
    }

    fn double_value(this) -> f64 {
        return rt::msg_f64(this._obj, rt::sel(#str_ptr("doubleValue\0")));
    }

    fn string_value(this) -> text::Text {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("stringValue\0")));
        return bridge::to_text(ns);
    }

    fn multi_array_value(this) -> option::Option[MultiArray] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("multiArrayValue\0")));
        if obj == { 0 as *u8 } {
            return option::Option[MultiArray]::None;
        }
        return option::some(MultiArray::from_raw(obj));
    }

    // SKIPPED `dictionaryValue`: return `NSDictionary<id,NSNumber *> * _Nonnull` — NSDictionary with non-string key `id`
    fn image_buffer_value(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("imageBufferValue\0")));
        return bridge::obj_option(obj);
    }

    fn sequence_value(this) -> option::Option[Sequence] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("sequenceValue\0")));
        if obj == { 0 as *u8 } {
            return option::Option[Sequence]::None;
        }
        return option::some(Sequence::from_raw(obj));
    }

    fn feature_value_with_image_at_url_pixels_wide(url: foundation::URL, pixels_wide: i64, pixels_high: i64, pixel_format_type: u32, options: string_map::StringMap[AnyObject], error: *u8) -> option::Option[FeatureValue] {
        let dict_options: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableDictionary\0")), rt::sel(#str_ptr("dictionary\0")));
        let dset_options: *u8 = rt::sel(#str_ptr("setObject:forKey:\0"));
        let dcap_options: usize = options.slot_capacity();
        var dsi_options: usize = 0 as usize;
        while dsi_options < dcap_options {
            match options.value_ptr_at_slot(dsi_options) {
                option::Option[*AnyObject]::Some(dvp_options) => {
                    match options.key_ptr_at_slot(dsi_options) {
                        option::Option[*text::Text]::Some(dkp_options) => {
                    let dval_options: *u8 = (*dvp_options).raw();
                            rt::msg_void_id_id(dict_options, dset_options, dval_options, bridge::nsstring((*dkp_options).view()));
                        }
                        option::Option[*text::Text]::None => {}
                    }
                }
                option::Option[*AnyObject]::None => {}
            }
            dsi_options = dsi_options +% (1 as usize);
        }
        let obj: *u8 = objc_msg_id_id_i64_i64_u32_id_id(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithImageAtURL:pixelsWide:pixelsHigh:pixelFormatType:options:error:\0")), url.raw(), pixels_wide, pixels_high, pixel_format_type, dict_options, error);
        if obj == { 0 as *u8 } {
            return option::Option[FeatureValue]::None;
        }
        return option::some(FeatureValue { _obj: obj });
    }

    fn feature_value_with_image_at_url(url: foundation::URL, constraint: ImageConstraint, options: string_map::StringMap[AnyObject], error: *u8) -> option::Option[FeatureValue] {
        let dict_options: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableDictionary\0")), rt::sel(#str_ptr("dictionary\0")));
        let dset_options: *u8 = rt::sel(#str_ptr("setObject:forKey:\0"));
        let dcap_options: usize = options.slot_capacity();
        var dsi_options: usize = 0 as usize;
        while dsi_options < dcap_options {
            match options.value_ptr_at_slot(dsi_options) {
                option::Option[*AnyObject]::Some(dvp_options) => {
                    match options.key_ptr_at_slot(dsi_options) {
                        option::Option[*text::Text]::Some(dkp_options) => {
                    let dval_options: *u8 = (*dvp_options).raw();
                            rt::msg_void_id_id(dict_options, dset_options, dval_options, bridge::nsstring((*dkp_options).view()));
                        }
                        option::Option[*text::Text]::None => {}
                    }
                }
                option::Option[*AnyObject]::None => {}
            }
            dsi_options = dsi_options +% (1 as usize);
        }
        let obj: *u8 = rt::msg_id_id_id_id_id(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithImageAtURL:constraint:options:error:\0")), url.raw(), constraint.raw(), dict_options, error);
        if obj == { 0 as *u8 } {
            return option::Option[FeatureValue]::None;
        }
        return option::some(FeatureValue { _obj: obj });
    }

    fn feature_value_with_cg_image_pixels_wide(cg_image: *u8, pixels_wide: i64, pixels_high: i64, pixel_format_type: u32, options: string_map::StringMap[AnyObject], error: *u8) -> option::Option[FeatureValue] {
        let dict_options: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableDictionary\0")), rt::sel(#str_ptr("dictionary\0")));
        let dset_options: *u8 = rt::sel(#str_ptr("setObject:forKey:\0"));
        let dcap_options: usize = options.slot_capacity();
        var dsi_options: usize = 0 as usize;
        while dsi_options < dcap_options {
            match options.value_ptr_at_slot(dsi_options) {
                option::Option[*AnyObject]::Some(dvp_options) => {
                    match options.key_ptr_at_slot(dsi_options) {
                        option::Option[*text::Text]::Some(dkp_options) => {
                    let dval_options: *u8 = (*dvp_options).raw();
                            rt::msg_void_id_id(dict_options, dset_options, dval_options, bridge::nsstring((*dkp_options).view()));
                        }
                        option::Option[*text::Text]::None => {}
                    }
                }
                option::Option[*AnyObject]::None => {}
            }
            dsi_options = dsi_options +% (1 as usize);
        }
        let obj: *u8 = objc_msg_id_id_i64_i64_u32_id_id(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithCGImage:pixelsWide:pixelsHigh:pixelFormatType:options:error:\0")), cg_image, pixels_wide, pixels_high, pixel_format_type, dict_options, error);
        if obj == { 0 as *u8 } {
            return option::Option[FeatureValue]::None;
        }
        return option::some(FeatureValue { _obj: obj });
    }

    fn feature_value_with_cg_image(cg_image: *u8, constraint: ImageConstraint, options: string_map::StringMap[AnyObject], error: *u8) -> option::Option[FeatureValue] {
        let dict_options: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableDictionary\0")), rt::sel(#str_ptr("dictionary\0")));
        let dset_options: *u8 = rt::sel(#str_ptr("setObject:forKey:\0"));
        let dcap_options: usize = options.slot_capacity();
        var dsi_options: usize = 0 as usize;
        while dsi_options < dcap_options {
            match options.value_ptr_at_slot(dsi_options) {
                option::Option[*AnyObject]::Some(dvp_options) => {
                    match options.key_ptr_at_slot(dsi_options) {
                        option::Option[*text::Text]::Some(dkp_options) => {
                    let dval_options: *u8 = (*dvp_options).raw();
                            rt::msg_void_id_id(dict_options, dset_options, dval_options, bridge::nsstring((*dkp_options).view()));
                        }
                        option::Option[*text::Text]::None => {}
                    }
                }
                option::Option[*AnyObject]::None => {}
            }
            dsi_options = dsi_options +% (1 as usize);
        }
        let obj: *u8 = rt::msg_id_id_id_id_id(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithCGImage:constraint:options:error:\0")), cg_image, constraint.raw(), dict_options, error);
        if obj == { 0 as *u8 } {
            return option::Option[FeatureValue]::None;
        }
        return option::some(FeatureValue { _obj: obj });
    }

    fn feature_value_with_image_at_url_orientation_pixels_wide(url: foundation::URL, orientation: CGImagePropertyOrientation, pixels_wide: i64, pixels_high: i64, pixel_format_type: u32, options: string_map::StringMap[AnyObject], error: *u8) -> option::Option[FeatureValue] {
        let dict_options: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableDictionary\0")), rt::sel(#str_ptr("dictionary\0")));
        let dset_options: *u8 = rt::sel(#str_ptr("setObject:forKey:\0"));
        let dcap_options: usize = options.slot_capacity();
        var dsi_options: usize = 0 as usize;
        while dsi_options < dcap_options {
            match options.value_ptr_at_slot(dsi_options) {
                option::Option[*AnyObject]::Some(dvp_options) => {
                    match options.key_ptr_at_slot(dsi_options) {
                        option::Option[*text::Text]::Some(dkp_options) => {
                    let dval_options: *u8 = (*dvp_options).raw();
                            rt::msg_void_id_id(dict_options, dset_options, dval_options, bridge::nsstring((*dkp_options).view()));
                        }
                        option::Option[*text::Text]::None => {}
                    }
                }
                option::Option[*AnyObject]::None => {}
            }
            dsi_options = dsi_options +% (1 as usize);
        }
        let obj: *u8 = objc_msg_id_id_i64_i64_i64_u32_id_id(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithImageAtURL:orientation:pixelsWide:pixelsHigh:pixelFormatType:options:error:\0")), url.raw(), cg_image_property_orientation_raw(orientation), pixels_wide, pixels_high, pixel_format_type, dict_options, error);
        if obj == { 0 as *u8 } {
            return option::Option[FeatureValue]::None;
        }
        return option::some(FeatureValue { _obj: obj });
    }

    fn feature_value_with_image_at_url_orientation(url: foundation::URL, orientation: CGImagePropertyOrientation, constraint: ImageConstraint, options: string_map::StringMap[AnyObject], error: *u8) -> option::Option[FeatureValue] {
        let dict_options: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableDictionary\0")), rt::sel(#str_ptr("dictionary\0")));
        let dset_options: *u8 = rt::sel(#str_ptr("setObject:forKey:\0"));
        let dcap_options: usize = options.slot_capacity();
        var dsi_options: usize = 0 as usize;
        while dsi_options < dcap_options {
            match options.value_ptr_at_slot(dsi_options) {
                option::Option[*AnyObject]::Some(dvp_options) => {
                    match options.key_ptr_at_slot(dsi_options) {
                        option::Option[*text::Text]::Some(dkp_options) => {
                    let dval_options: *u8 = (*dvp_options).raw();
                            rt::msg_void_id_id(dict_options, dset_options, dval_options, bridge::nsstring((*dkp_options).view()));
                        }
                        option::Option[*text::Text]::None => {}
                    }
                }
                option::Option[*AnyObject]::None => {}
            }
            dsi_options = dsi_options +% (1 as usize);
        }
        let obj: *u8 = objc_msg_id_id_i64_id_id_id(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithImageAtURL:orientation:constraint:options:error:\0")), url.raw(), cg_image_property_orientation_raw(orientation), constraint.raw(), dict_options, error);
        if obj == { 0 as *u8 } {
            return option::Option[FeatureValue]::None;
        }
        return option::some(FeatureValue { _obj: obj });
    }

    fn feature_value_with_cg_image_orientation_pixels_wide(cg_image: *u8, orientation: CGImagePropertyOrientation, pixels_wide: i64, pixels_high: i64, pixel_format_type: u32, options: string_map::StringMap[AnyObject], error: *u8) -> option::Option[FeatureValue] {
        let dict_options: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableDictionary\0")), rt::sel(#str_ptr("dictionary\0")));
        let dset_options: *u8 = rt::sel(#str_ptr("setObject:forKey:\0"));
        let dcap_options: usize = options.slot_capacity();
        var dsi_options: usize = 0 as usize;
        while dsi_options < dcap_options {
            match options.value_ptr_at_slot(dsi_options) {
                option::Option[*AnyObject]::Some(dvp_options) => {
                    match options.key_ptr_at_slot(dsi_options) {
                        option::Option[*text::Text]::Some(dkp_options) => {
                    let dval_options: *u8 = (*dvp_options).raw();
                            rt::msg_void_id_id(dict_options, dset_options, dval_options, bridge::nsstring((*dkp_options).view()));
                        }
                        option::Option[*text::Text]::None => {}
                    }
                }
                option::Option[*AnyObject]::None => {}
            }
            dsi_options = dsi_options +% (1 as usize);
        }
        let obj: *u8 = objc_msg_id_id_i64_i64_i64_u32_id_id(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithCGImage:orientation:pixelsWide:pixelsHigh:pixelFormatType:options:error:\0")), cg_image, cg_image_property_orientation_raw(orientation), pixels_wide, pixels_high, pixel_format_type, dict_options, error);
        if obj == { 0 as *u8 } {
            return option::Option[FeatureValue]::None;
        }
        return option::some(FeatureValue { _obj: obj });
    }

    fn feature_value_with_cg_image_orientation(cg_image: *u8, orientation: CGImagePropertyOrientation, constraint: ImageConstraint, options: string_map::StringMap[AnyObject], error: *u8) -> option::Option[FeatureValue] {
        let dict_options: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableDictionary\0")), rt::sel(#str_ptr("dictionary\0")));
        let dset_options: *u8 = rt::sel(#str_ptr("setObject:forKey:\0"));
        let dcap_options: usize = options.slot_capacity();
        var dsi_options: usize = 0 as usize;
        while dsi_options < dcap_options {
            match options.value_ptr_at_slot(dsi_options) {
                option::Option[*AnyObject]::Some(dvp_options) => {
                    match options.key_ptr_at_slot(dsi_options) {
                        option::Option[*text::Text]::Some(dkp_options) => {
                    let dval_options: *u8 = (*dvp_options).raw();
                            rt::msg_void_id_id(dict_options, dset_options, dval_options, bridge::nsstring((*dkp_options).view()));
                        }
                        option::Option[*text::Text]::None => {}
                    }
                }
                option::Option[*AnyObject]::None => {}
            }
            dsi_options = dsi_options +% (1 as usize);
        }
        let obj: *u8 = objc_msg_id_id_i64_id_id_id(rt::get_class(#str_ptr("MLFeatureValue\0")), rt::sel(#str_ptr("featureValueWithCGImage:orientation:constraint:options:error:\0")), cg_image, cg_image_property_orientation_raw(orientation), constraint.raw(), dict_options, error);
        if obj == { 0 as *u8 } {
            return option::Option[FeatureValue]::None;
        }
        return option::some(FeatureValue { _obj: obj });
    }

}

// `MLImageSize` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ImageSize {
    opaque _obj: *u8,
}

impl ImageSize {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ImageSize { return ImageSize { _obj: ptr }; }

    fn pixels_wide(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("pixelsWide\0")));
    }

    fn pixels_high(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("pixelsHigh\0")));
    }

}

// `MLImageSizeConstraint` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ImageSizeConstraint {
    opaque _obj: *u8,
}

impl ImageSizeConstraint {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ImageSizeConstraint { return ImageSizeConstraint { _obj: ptr }; }

    fn type_(this) -> ImageSizeConstraintType {
        return image_size_constraint_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("type\0"))));
    }

    fn pixels_wide_range(this) -> rt::Range {
        return rt::msg_range(this._obj, rt::sel(#str_ptr("pixelsWideRange\0")));
    }

    fn pixels_high_range(this) -> rt::Range {
        return rt::msg_range(this._obj, rt::sel(#str_ptr("pixelsHighRange\0")));
    }

    fn enumerated_image_sizes(this) -> vec::Vec[ImageSize] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("enumeratedImageSizes\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[ImageSize] = vec::Vec[ImageSize]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(ImageSize::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

}

// `MLImageConstraint` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ImageConstraint {
    _obj: *u8,
}

impl ImageConstraint {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ImageConstraint { return ImageConstraint { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn pixels_high(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("pixelsHigh\0")));
    }

    fn pixels_wide(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("pixelsWide\0")));
    }

    fn pixel_format_type(this) -> u32 {
        return rt::msg_u32(this._obj, rt::sel(#str_ptr("pixelFormatType\0")));
    }

    fn size_constraint(this) -> ImageSizeConstraint {
        return ImageSizeConstraint::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("sizeConstraint\0"))));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLDictionaryConstraint` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct DictionaryConstraint {
    opaque _obj: *u8,
}

impl DictionaryConstraint {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> DictionaryConstraint { return DictionaryConstraint { _obj: ptr }; }

    fn key_type(this) -> FeatureType {
        return feature_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("keyType\0"))));
    }

}

// `MLMultiArrayShapeConstraint` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct MultiArrayShapeConstraint {
    opaque _obj: *u8,
}

impl MultiArrayShapeConstraint {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> MultiArrayShapeConstraint { return MultiArrayShapeConstraint { _obj: ptr }; }

    fn type_(this) -> MultiArrayShapeConstraintType {
        return multi_array_shape_constraint_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("type\0"))));
    }

    // SKIPPED `sizeRangeForDimension`: NSArray return needs a single NSRange arg
    // SKIPPED `enumeratedShapes`: return `NSArray<NSArray<NSNumber *> *> * _Nonnull` — generic collection
}

// `MLMultiArrayConstraint` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct MultiArrayConstraint {
    opaque _obj: *u8,
}

impl MultiArrayConstraint {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> MultiArrayConstraint { return MultiArrayConstraint { _obj: ptr }; }

    fn shape(this) -> vec::Vec[f64] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("shape\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[f64] = vec::Vec[f64]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let dv_sel: *u8 = rt::sel(#str_ptr("doubleValue\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let value: *u8 = rt::msg_id_u64(arr, at_sel, i);
            out.append(rt::msg_f64(value, dv_sel));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn data_type(this) -> MultiArrayDataType {
        return multi_array_data_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("dataType\0"))));
    }

    fn shape_constraint(this) -> MultiArrayShapeConstraint {
        return MultiArrayShapeConstraint::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("shapeConstraint\0"))));
    }

}

// `MLFeatureDescription` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct FeatureDescription {
    opaque _obj: *u8,
}

impl FeatureDescription {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> FeatureDescription { return FeatureDescription { _obj: ptr }; }

    fn is_allowed_value(this, value: FeatureValue) -> bool {
        return rt::msg_i8_id(this._obj, rt::sel(#str_ptr("isAllowedValue:\0")), value.raw()) != (0 as i8);
    }

    fn name(this) -> text::Text {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("name\0")));
        return bridge::to_text(ns);
    }

    fn type_(this) -> FeatureType {
        return feature_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("type\0"))));
    }

    fn is_optional(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("isOptional\0"))) != (0 as i8);
    }

    fn multi_array_constraint(this) -> option::Option[MultiArrayConstraint] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("multiArrayConstraint\0")));
        if obj == { 0 as *u8 } {
            return option::Option[MultiArrayConstraint]::None;
        }
        return option::some(MultiArrayConstraint::from_raw(obj));
    }

    fn image_constraint(this) -> option::Option[ImageConstraint] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("imageConstraint\0")));
        if obj == { 0 as *u8 } {
            return option::Option[ImageConstraint]::None;
        }
        return option::some(ImageConstraint::from_raw(obj));
    }

    fn dictionary_constraint(this) -> option::Option[DictionaryConstraint] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("dictionaryConstraint\0")));
        if obj == { 0 as *u8 } {
            return option::Option[DictionaryConstraint]::None;
        }
        return option::some(DictionaryConstraint::from_raw(obj));
    }

    fn sequence_constraint(this) -> option::Option[SequenceConstraint] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("sequenceConstraint\0")));
        if obj == { 0 as *u8 } {
            return option::Option[SequenceConstraint]::None;
        }
        return option::some(SequenceConstraint::from_raw(obj));
    }

    fn state_constraint(this) -> option::Option[StateConstraint] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("stateConstraint\0")));
        if obj == { 0 as *u8 } {
            return option::Option[StateConstraint]::None;
        }
        return option::some(StateConstraint::from_raw(obj));
    }

}

// `MLSequenceConstraint` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct SequenceConstraint {
    opaque _obj: *u8,
}

impl SequenceConstraint {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> SequenceConstraint { return SequenceConstraint { _obj: ptr }; }

    fn value_description(this) -> FeatureDescription {
        return FeatureDescription::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("valueDescription\0"))));
    }

    fn count_range(this) -> rt::Range {
        return rt::msg_range(this._obj, rt::sel(#str_ptr("countRange\0")));
    }

}

// `MLStateConstraint` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct StateConstraint {
    opaque _obj: *u8,
}

impl StateConstraint {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> StateConstraint { return StateConstraint { _obj: ptr }; }

    fn buffer_shape(this) -> vec::Vec[f64] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("bufferShape\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[f64] = vec::Vec[f64]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let dv_sel: *u8 = rt::sel(#str_ptr("doubleValue\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let value: *u8 = rt::msg_id_u64(arr, at_sel, i);
            out.append(rt::msg_f64(value, dv_sel));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn data_type(this) -> MultiArrayDataType {
        return multi_array_data_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("dataType\0"))));
    }

}

// `MLDictionaryFeatureProvider` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct DictionaryFeatureProvider {
    _obj: *u8,
}

impl DictionaryFeatureProvider {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> DictionaryFeatureProvider { return DictionaryFeatureProvider { _obj: rt::retain(ptr) }; }

    fn new(dictionary: string_map::StringMap[AnyObject], error: *u8) -> DictionaryFeatureProvider {
        let cls: *u8 = rt::get_class(#str_ptr("MLDictionaryFeatureProvider\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        let dict_dictionary: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableDictionary\0")), rt::sel(#str_ptr("dictionary\0")));
        let dset_dictionary: *u8 = rt::sel(#str_ptr("setObject:forKey:\0"));
        let dcap_dictionary: usize = dictionary.slot_capacity();
        var dsi_dictionary: usize = 0 as usize;
        while dsi_dictionary < dcap_dictionary {
            match dictionary.value_ptr_at_slot(dsi_dictionary) {
                option::Option[*AnyObject]::Some(dvp_dictionary) => {
                    match dictionary.key_ptr_at_slot(dsi_dictionary) {
                        option::Option[*text::Text]::Some(dkp_dictionary) => {
                    let dval_dictionary: *u8 = (*dvp_dictionary).raw();
                            rt::msg_void_id_id(dict_dictionary, dset_dictionary, dval_dictionary, bridge::nsstring((*dkp_dictionary).view()));
                        }
                        option::Option[*text::Text]::None => {}
                    }
                }
                option::Option[*AnyObject]::None => {}
            }
            dsi_dictionary = dsi_dictionary +% (1 as usize);
        }
        return DictionaryFeatureProvider { _obj: rt::msg_id_id_id(alloced, rt::sel(#str_ptr("initWithDictionary:error:\0")), dict_dictionary, error) };
    }

    fn object_for_keyed_subscript(this, feature_name: str) -> option::Option[FeatureValue] {
        let obj: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("objectForKeyedSubscript:\0")), bridge::nsstring(feature_name));
        if obj == { 0 as *u8 } {
            return option::Option[FeatureValue]::None;
        }
        return option::some(FeatureValue::from_raw(obj));
    }

    fn dictionary(this) -> string_map::StringMap[FeatureValue] {
        let dict: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("dictionary\0")));
        let keys: *u8 = rt::msg_id(dict, rt::sel(#str_ptr("allKeys\0")));
        let n: u64 = rt::msg_u64(keys, rt::sel(#str_ptr("count\0")));
        var out: string_map::StringMap[FeatureValue] = string_map::new::[FeatureValue]();
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let obj_sel: *u8 = rt::sel(#str_ptr("objectForKey:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let nskey: *u8 = rt::msg_id_u64(keys, at_sel, i);
            let nsval: *u8 = rt::msg_id_id(dict, obj_sel, nskey);
            out.insert(bridge::to_text(nskey), FeatureValue::from_raw(nsval));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLArrayBatchProvider` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ArrayBatchProvider {
    _obj: *u8,
}

impl ArrayBatchProvider {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ArrayBatchProvider { return ArrayBatchProvider { _obj: rt::retain(ptr) }; }

    fn new(array: vec::Vec[FeatureProvider]) -> ArrayBatchProvider {
        let cls: *u8 = rt::get_class(#str_ptr("MLArrayBatchProvider\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        let arr_array: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_array: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let n_array: usize = array.count();
        var i_array: usize = 0 as usize;
        while i_array < n_array {
            match array.at_ptr(i_array) {
                option::Option[*FeatureProvider]::Some(e_array) => { rt::msg_void_id(arr_array, add_sel_array, (*e_array).raw()); }
                option::Option[*FeatureProvider]::None => {}
            }
            i_array = i_array +% (1 as usize);
        }
        return ArrayBatchProvider { _obj: rt::msg_id_id(alloced, rt::sel(#str_ptr("initWithFeatureProviderArray:\0")), arr_array) };
    }

    // SKIPPED `initWithDictionary:error:`: param `NSDictionary<NSString *,NSArray *> * _Nonnull` — generic collection
    fn array(this) -> vec::Vec[FeatureProvider] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("array\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[FeatureProvider] = vec::Vec[FeatureProvider]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(FeatureProvider::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLParameterKey` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ParameterKey {
    _obj: *u8,
}

impl ParameterKey {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ParameterKey { return ParameterKey { _obj: rt::retain(ptr) }; }

    // Upcast to `MLKey` (superclass). Safe typed reinterpret.
    fn as_key(this) -> Key { return Key::from_raw(this._obj); }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> *u8 {
        return rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("new\0")));
    }

    fn learning_rate() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("learningRate\0")))) };
    }

    fn momentum() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("momentum\0")))) };
    }

    fn mini_batch_size() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("miniBatchSize\0")))) };
    }

    fn beta1() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("beta1\0")))) };
    }

    fn beta2() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("beta2\0")))) };
    }

    fn eps() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("eps\0")))) };
    }

    fn epochs() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("epochs\0")))) };
    }

    fn shuffle() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("shuffle\0")))) };
    }

    fn seed() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("seed\0")))) };
    }

    fn number_of_neighbors() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("numberOfNeighbors\0")))) };
    }

    fn linked_model_file_name() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("linkedModelFileName\0")))) };
    }

    fn linked_model_search_path() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("linkedModelSearchPath\0")))) };
    }

    fn weights() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("weights\0")))) };
    }

    fn biases() -> ParameterKey {
        return ParameterKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLParameterKey\0")), rt::sel(#str_ptr("biases\0")))) };
    }

    fn scoped_to(this, scope: str) -> ParameterKey {
        return ParameterKey::from_raw(rt::msg_id_id(this._obj, rt::sel(#str_ptr("scopedTo:\0")), bridge::nsstring(scope)));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLParameterDescription` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ParameterDescription {
    opaque _obj: *u8,
}

impl ParameterDescription {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ParameterDescription { return ParameterDescription { _obj: ptr }; }

    fn key(this) -> ParameterKey {
        return ParameterKey::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("key\0"))));
    }

    fn default_value(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("defaultValue\0")));
    }

    fn numeric_constraint(this) -> option::Option[NumericConstraint] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("numericConstraint\0")));
        if obj == { 0 as *u8 } {
            return option::Option[NumericConstraint]::None;
        }
        return option::some(NumericConstraint::from_raw(obj));
    }

}

// `MLModelDescription` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ModelDescription {
    opaque _obj: *u8,
}

impl ModelDescription {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelDescription { return ModelDescription { _obj: ptr }; }

    fn input_descriptions_by_name(this) -> string_map::StringMap[FeatureDescription] {
        let dict: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("inputDescriptionsByName\0")));
        let keys: *u8 = rt::msg_id(dict, rt::sel(#str_ptr("allKeys\0")));
        let n: u64 = rt::msg_u64(keys, rt::sel(#str_ptr("count\0")));
        var out: string_map::StringMap[FeatureDescription] = string_map::new::[FeatureDescription]();
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let obj_sel: *u8 = rt::sel(#str_ptr("objectForKey:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let nskey: *u8 = rt::msg_id_u64(keys, at_sel, i);
            let nsval: *u8 = rt::msg_id_id(dict, obj_sel, nskey);
            out.insert(bridge::to_text(nskey), FeatureDescription::from_raw(nsval));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn output_descriptions_by_name(this) -> string_map::StringMap[FeatureDescription] {
        let dict: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("outputDescriptionsByName\0")));
        let keys: *u8 = rt::msg_id(dict, rt::sel(#str_ptr("allKeys\0")));
        let n: u64 = rt::msg_u64(keys, rt::sel(#str_ptr("count\0")));
        var out: string_map::StringMap[FeatureDescription] = string_map::new::[FeatureDescription]();
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let obj_sel: *u8 = rt::sel(#str_ptr("objectForKey:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let nskey: *u8 = rt::msg_id_u64(keys, at_sel, i);
            let nsval: *u8 = rt::msg_id_id(dict, obj_sel, nskey);
            out.insert(bridge::to_text(nskey), FeatureDescription::from_raw(nsval));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn state_descriptions_by_name(this) -> string_map::StringMap[FeatureDescription] {
        let dict: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("stateDescriptionsByName\0")));
        let keys: *u8 = rt::msg_id(dict, rt::sel(#str_ptr("allKeys\0")));
        let n: u64 = rt::msg_u64(keys, rt::sel(#str_ptr("count\0")));
        var out: string_map::StringMap[FeatureDescription] = string_map::new::[FeatureDescription]();
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let obj_sel: *u8 = rt::sel(#str_ptr("objectForKey:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let nskey: *u8 = rt::msg_id_u64(keys, at_sel, i);
            let nsval: *u8 = rt::msg_id_id(dict, obj_sel, nskey);
            out.insert(bridge::to_text(nskey), FeatureDescription::from_raw(nsval));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn predicted_feature_name(this) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("predictedFeatureName\0")));
        return bridge::to_text_option(ns);
    }

    fn predicted_probabilities_name(this) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("predictedProbabilitiesName\0")));
        return bridge::to_text_option(ns);
    }

    fn metadata(this) -> string_map::StringMap[AnyObject] {
        let dict: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("metadata\0")));
        let keys: *u8 = rt::msg_id(dict, rt::sel(#str_ptr("allKeys\0")));
        let n: u64 = rt::msg_u64(keys, rt::sel(#str_ptr("count\0")));
        var out: string_map::StringMap[AnyObject] = string_map::new::[AnyObject]();
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let obj_sel: *u8 = rt::sel(#str_ptr("objectForKey:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let nskey: *u8 = rt::msg_id_u64(keys, at_sel, i);
            let nsval: *u8 = rt::msg_id_id(dict, obj_sel, nskey);
            out.insert(bridge::to_text(nskey), AnyObject::from_raw(nsval));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn class_labels(this) -> vec::Vec[AnyObject] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("classLabels\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[AnyObject] = vec::Vec[AnyObject]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(AnyObject::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn is_updatable(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("isUpdatable\0"))) != (0 as i8);
    }

    fn training_input_descriptions_by_name(this) -> string_map::StringMap[FeatureDescription] {
        let dict: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("trainingInputDescriptionsByName\0")));
        let keys: *u8 = rt::msg_id(dict, rt::sel(#str_ptr("allKeys\0")));
        let n: u64 = rt::msg_u64(keys, rt::sel(#str_ptr("count\0")));
        var out: string_map::StringMap[FeatureDescription] = string_map::new::[FeatureDescription]();
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let obj_sel: *u8 = rt::sel(#str_ptr("objectForKey:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let nskey: *u8 = rt::msg_id_u64(keys, at_sel, i);
            let nsval: *u8 = rt::msg_id_id(dict, obj_sel, nskey);
            out.insert(bridge::to_text(nskey), FeatureDescription::from_raw(nsval));
            i = i +% (1 as u64);
        }
        return out;
    }

    // SKIPPED `parameterDescriptionsByKey`: return `NSDictionary<MLParameterKey *,MLParameterDescription *> * _Nonnull` — NSDictionary with non-string key `MLParameterKey *`
}

// `MLPredictionOptions` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct PredictionOptions {
    opaque _obj: *u8,
}

impl PredictionOptions {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> PredictionOptions { return PredictionOptions { _obj: ptr }; }

    fn uses_cpu_only(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("usesCPUOnly\0"))) != (0 as i8);
    }

    fn set_uses_cpu_only(this, uses_cpu_only: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setUsesCPUOnly:\0")), uses_cpu_only as i8);
    }

    fn output_backings(this) -> string_map::StringMap[AnyObject] {
        let dict: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("outputBackings\0")));
        let keys: *u8 = rt::msg_id(dict, rt::sel(#str_ptr("allKeys\0")));
        let n: u64 = rt::msg_u64(keys, rt::sel(#str_ptr("count\0")));
        var out: string_map::StringMap[AnyObject] = string_map::new::[AnyObject]();
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let obj_sel: *u8 = rt::sel(#str_ptr("objectForKey:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let nskey: *u8 = rt::msg_id_u64(keys, at_sel, i);
            let nsval: *u8 = rt::msg_id_id(dict, obj_sel, nskey);
            out.insert(bridge::to_text(nskey), AnyObject::from_raw(nsval));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn set_output_backings(this, output_backings: string_map::StringMap[AnyObject]) {
        let dict_output_backings: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableDictionary\0")), rt::sel(#str_ptr("dictionary\0")));
        let dset_output_backings: *u8 = rt::sel(#str_ptr("setObject:forKey:\0"));
        let dcap_output_backings: usize = output_backings.slot_capacity();
        var dsi_output_backings: usize = 0 as usize;
        while dsi_output_backings < dcap_output_backings {
            match output_backings.value_ptr_at_slot(dsi_output_backings) {
                option::Option[*AnyObject]::Some(dvp_output_backings) => {
                    match output_backings.key_ptr_at_slot(dsi_output_backings) {
                        option::Option[*text::Text]::Some(dkp_output_backings) => {
                    let dval_output_backings: *u8 = (*dvp_output_backings).raw();
                            rt::msg_void_id_id(dict_output_backings, dset_output_backings, dval_output_backings, bridge::nsstring((*dkp_output_backings).view()));
                        }
                        option::Option[*text::Text]::None => {}
                    }
                }
                option::Option[*AnyObject]::None => {}
            }
            dsi_output_backings = dsi_output_backings +% (1 as usize);
        }
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setOutputBackings:\0")), dict_output_backings);
    }

}

// `MLOptimizationHints` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct OptimizationHints {
    opaque _obj: *u8,
}

impl OptimizationHints {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> OptimizationHints { return OptimizationHints { _obj: ptr }; }

    fn reshape_frequency(this) -> ReshapeFrequencyHint {
        return reshape_frequency_hint_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("reshapeFrequency\0"))));
    }

    fn set_reshape_frequency(this, reshape_frequency: ReshapeFrequencyHint) {
        rt::msg_void_i64(this._obj, rt::sel(#str_ptr("setReshapeFrequency:\0")), reshape_frequency_hint_raw(reshape_frequency));
    }

    fn specialization_strategy(this) -> SpecializationStrategy {
        return specialization_strategy_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("specializationStrategy\0"))));
    }

    fn set_specialization_strategy(this, specialization_strategy: SpecializationStrategy) {
        rt::msg_void_i64(this._obj, rt::sel(#str_ptr("setSpecializationStrategy:\0")), specialization_strategy_raw(specialization_strategy));
    }

}

// `MLModelConfiguration` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ModelConfiguration {
    opaque _obj: *u8,
}

impl ModelConfiguration {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelConfiguration { return ModelConfiguration { _obj: ptr }; }

    fn model_display_name(this) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("modelDisplayName\0")));
        return bridge::to_text_option(ns);
    }

    fn set_model_display_name(this, model_display_name: str) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setModelDisplayName:\0")), bridge::nsstring(model_display_name));
    }

    fn compute_units(this) -> ComputeUnits {
        return compute_units_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("computeUnits\0"))));
    }

    fn set_compute_units(this, compute_units: ComputeUnits) {
        rt::msg_void_i64(this._obj, rt::sel(#str_ptr("setComputeUnits:\0")), compute_units_raw(compute_units));
    }

    fn optimization_hints(this) -> OptimizationHints {
        return OptimizationHints::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("optimizationHints\0"))));
    }

    fn set_optimization_hints(this, optimization_hints: OptimizationHints) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setOptimizationHints:\0")), optimization_hints.raw());
    }

    fn allow_low_precision_accumulation_on_gpu(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("allowLowPrecisionAccumulationOnGPU\0"))) != (0 as i8);
    }

    fn set_allow_low_precision_accumulation_on_gpu(this, allow_low_precision_accumulation_on_gpu: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setAllowLowPrecisionAccumulationOnGPU:\0")), allow_low_precision_accumulation_on_gpu as i8);
    }

    fn preferred_metal_device(this) -> option::Option[MTLDevice] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("preferredMetalDevice\0")));
        if obj == { 0 as *u8 } {
            return option::Option[MTLDevice]::None;
        }
        return option::some(MTLDevice::from_raw(obj));
    }

    fn set_preferred_metal_device(this, preferred_metal_device: MTLDevice) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setPreferredMetalDevice:\0")), preferred_metal_device.raw());
    }

    // SKIPPED `parameters`: return `NSDictionary<MLParameterKey *,id> * _Nullable` — NSDictionary with non-string key `MLParameterKey *`
    // SKIPPED `setParameters:`: param `NSDictionary<MLParameterKey *,id> * _Nullable` — generic collection
    fn function_name(this) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("functionName\0")));
        return bridge::to_text_option(ns);
    }

    fn set_function_name(this, function_name: str) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setFunctionName:\0")), bridge::nsstring(function_name));
    }

}

// `MLModelAsset` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelAsset {
    _obj: *u8,
}

impl ModelAsset {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelAsset { return ModelAsset { _obj: rt::retain(ptr) }; }

    fn model_asset_with_specification_data(specification_data: foundation::Data, error: *u8) -> option::Option[ModelAsset] {
        let obj: *u8 = rt::msg_id_id_id(rt::get_class(#str_ptr("MLModelAsset\0")), rt::sel(#str_ptr("modelAssetWithSpecificationData:error:\0")), specification_data.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[ModelAsset]::None;
        }
        return option::some(ModelAsset { _obj: rt::retain(obj) });
    }

    // SKIPPED `modelAssetWithSpecificationData:blobMapping:error:`: param `NSDictionary<NSURL *,NSData *> * _Nonnull` — generic collection
    fn model_asset_with_url(compiled_model_url: foundation::URL, error: *u8) -> option::Option[ModelAsset] {
        let obj: *u8 = rt::msg_id_id_id(rt::get_class(#str_ptr("MLModelAsset\0")), rt::sel(#str_ptr("modelAssetWithURL:error:\0")), compiled_model_url.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[ModelAsset]::None;
        }
        return option::some(ModelAsset { _obj: rt::retain(obj) });
    }

    fn model_description_with_completion_handler(this, cb: fn(*u8, *u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: ModelAsset_model_description_with_completion_handler_block = ModelAsset_model_description_with_completion_handler_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: ModelAsset_model_description_with_completion_handler_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("modelDescriptionWithCompletionHandler:\0")), bp);
        return;
    }

    fn model_description_of_function_named(this, function_name: str, cb: fn(*u8, *u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: ModelAsset_model_description_of_function_named_block = ModelAsset_model_description_of_function_named_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: ModelAsset_model_description_of_function_named_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id_id(this._obj, rt::sel(#str_ptr("modelDescriptionOfFunctionNamed:completionHandler:\0")), bridge::nsstring(function_name), bp);
        return;
    }

    fn function_names_with_completion_handler(this, cb: fn(*u8, *u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: ModelAsset_function_names_with_completion_handler_block = ModelAsset_function_names_with_completion_handler_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: ModelAsset_function_names_with_completion_handler_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("functionNamesWithCompletionHandler:\0")), bp);
        return;
    }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelAsset {
        return ModelAsset { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelAsset\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModel` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Model {
    opaque _obj: *u8,
}

impl Model {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Model { return Model { _obj: ptr }; }

    fn model_with_contents_of_url(url: foundation::URL, error: *u8) -> option::Option[Model] {
        let obj: *u8 = rt::msg_id_id_id(rt::get_class(#str_ptr("MLModel\0")), rt::sel(#str_ptr("modelWithContentsOfURL:error:\0")), url.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[Model]::None;
        }
        return option::some(Model { _obj: obj });
    }

    fn model_with_contents_of_url_configuration(url: foundation::URL, configuration: ModelConfiguration, error: *u8) -> option::Option[Model] {
        let obj: *u8 = rt::msg_id_id_id_id(rt::get_class(#str_ptr("MLModel\0")), rt::sel(#str_ptr("modelWithContentsOfURL:configuration:error:\0")), url.raw(), configuration.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[Model]::None;
        }
        return option::some(Model { _obj: obj });
    }

    fn load_contents_of_url(url: foundation::URL, configuration: ModelConfiguration, cb: fn(*u8, *u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: Model_load_contents_of_url_block = Model_load_contents_of_url_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: Model_load_contents_of_url_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id_id_id(rt::get_class(#str_ptr("MLModel\0")), rt::sel(#str_ptr("loadContentsOfURL:configuration:completionHandler:\0")), url.raw(), configuration.raw(), bp);
        return;
    }

    fn prediction_from_features(this, input: FeatureProvider, error: *u8) -> option::Option[FeatureProvider] {
        let obj: *u8 = rt::msg_id_id_id(this._obj, rt::sel(#str_ptr("predictionFromFeatures:error:\0")), input.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[FeatureProvider]::None;
        }
        return option::some(FeatureProvider::from_raw(obj));
    }

    fn prediction_from_features_options(this, input: FeatureProvider, options: PredictionOptions, error: *u8) -> option::Option[FeatureProvider] {
        let obj: *u8 = rt::msg_id_id_id_id(this._obj, rt::sel(#str_ptr("predictionFromFeatures:options:error:\0")), input.raw(), options.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[FeatureProvider]::None;
        }
        return option::some(FeatureProvider::from_raw(obj));
    }

    fn prediction_from_features_completion_handler(this, input: FeatureProvider, cb: fn(*u8, *u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: Model_prediction_from_features_completion_handler_block = Model_prediction_from_features_completion_handler_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: Model_prediction_from_features_completion_handler_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id_id(this._obj, rt::sel(#str_ptr("predictionFromFeatures:completionHandler:\0")), input.raw(), bp);
        return;
    }

    fn prediction_from_features_options_completion_handler(this, input: FeatureProvider, options: PredictionOptions, cb: fn(*u8, *u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: Model_prediction_from_features_options_completion_handler_block = Model_prediction_from_features_options_completion_handler_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: Model_prediction_from_features_options_completion_handler_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id_id_id(this._obj, rt::sel(#str_ptr("predictionFromFeatures:options:completionHandler:\0")), input.raw(), options.raw(), bp);
        return;
    }

    fn predictions_from_batch(this, input_batch: BatchProvider, error: *u8) -> option::Option[BatchProvider] {
        let obj: *u8 = rt::msg_id_id_id(this._obj, rt::sel(#str_ptr("predictionsFromBatch:error:\0")), input_batch.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[BatchProvider]::None;
        }
        return option::some(BatchProvider::from_raw(obj));
    }

    fn predictions_from_batch_options(this, input_batch: BatchProvider, options: PredictionOptions, error: *u8) -> option::Option[BatchProvider] {
        let obj: *u8 = rt::msg_id_id_id_id(this._obj, rt::sel(#str_ptr("predictionsFromBatch:options:error:\0")), input_batch.raw(), options.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[BatchProvider]::None;
        }
        return option::some(BatchProvider::from_raw(obj));
    }

    fn parameter_value_for_key(this, key: ParameterKey, error: *u8) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id_id_id(this._obj, rt::sel(#str_ptr("parameterValueForKey:error:\0")), key.raw(), error);
        return bridge::obj_option(obj);
    }

    fn load_model_asset(asset: ModelAsset, configuration: ModelConfiguration, cb: fn(*u8, *u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: Model_load_model_asset_block = Model_load_model_asset_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: Model_load_model_asset_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id_id_id(rt::get_class(#str_ptr("MLModel\0")), rt::sel(#str_ptr("loadModelAsset:configuration:completionHandler:\0")), asset.raw(), configuration.raw(), bp);
        return;
    }

    fn model_description(this) -> ModelDescription {
        return ModelDescription::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("modelDescription\0"))));
    }

    fn configuration(this) -> ModelConfiguration {
        return ModelConfiguration::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("configuration\0"))));
    }

    fn compile_model_at_url(model_url: foundation::URL, error: *u8) -> option::Option[foundation::URL] {
        let obj: *u8 = rt::msg_id_id_id(rt::get_class(#str_ptr("MLModel\0")), rt::sel(#str_ptr("compileModelAtURL:error:\0")), model_url.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[foundation::URL]::None;
        }
        return option::some(foundation::URL::from_raw(obj));
    }

    fn compile_model_at_url_completion_handler(model_url: foundation::URL, cb: fn(*u8, *u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: Model_compile_model_at_url_completion_handler_block = Model_compile_model_at_url_completion_handler_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: Model_compile_model_at_url_completion_handler_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id_id(rt::get_class(#str_ptr("MLModel\0")), rt::sel(#str_ptr("compileModelAtURL:completionHandler:\0")), model_url.raw(), bp);
        return;
    }

    fn available_compute_devices() -> vec::Vec[ComputeDeviceProtocol] {
        let arr: *u8 = rt::msg_id(rt::get_class(#str_ptr("MLModel\0")), rt::sel(#str_ptr("availableComputeDevices\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[ComputeDeviceProtocol] = vec::Vec[ComputeDeviceProtocol]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(ComputeDeviceProtocol::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn new_state(this) -> State {
        return State { _obj: rt::msg_id(this._obj, rt::sel(#str_ptr("newState\0"))) };
    }

    fn prediction_from_features_using_state(this, input_features: FeatureProvider, state: State, error: *u8) -> option::Option[FeatureProvider] {
        let obj: *u8 = rt::msg_id_id_id_id(this._obj, rt::sel(#str_ptr("predictionFromFeatures:usingState:error:\0")), input_features.raw(), state.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[FeatureProvider]::None;
        }
        return option::some(FeatureProvider::from_raw(obj));
    }

    fn prediction_from_features_using_state_options(this, input_features: FeatureProvider, state: State, options: PredictionOptions, error: *u8) -> option::Option[FeatureProvider] {
        let obj: *u8 = rt::msg_id_id_id_id_id(this._obj, rt::sel(#str_ptr("predictionFromFeatures:usingState:options:error:\0")), input_features.raw(), state.raw(), options.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[FeatureProvider]::None;
        }
        return option::some(FeatureProvider::from_raw(obj));
    }

    fn prediction_from_features_using_state_options_completion_handler(this, input_features: FeatureProvider, state: State, options: PredictionOptions, cb: fn(*u8, *u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: Model_prediction_from_features_using_state_options_completion_handler_block = Model_prediction_from_features_using_state_options_completion_handler_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: Model_prediction_from_features_using_state_options_completion_handler_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id_id_id_id(this._obj, rt::sel(#str_ptr("predictionFromFeatures:usingState:options:completionHandler:\0")), input_features.raw(), state.raw(), options.raw(), bp);
        return;
    }

}

// `MLKey` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct Key {
    _obj: *u8,
}

impl Key {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Key { return Key { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> *u8 {
        return rt::msg_id(rt::get_class(#str_ptr("MLKey\0")), rt::sel(#str_ptr("new\0")));
    }

    fn name(this) -> text::Text {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("name\0")));
        return bridge::to_text(ns);
    }

    fn scope(this) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("scope\0")));
        return bridge::to_text_option(ns);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLTask` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct Task {
    _obj: *u8,
}

impl Task {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Task { return Task { _obj: rt::retain(ptr) }; }

    fn resume(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("resume\0")));
    }

    fn cancel(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("cancel\0")));
    }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> *u8 {
        return rt::msg_id(rt::get_class(#str_ptr("MLTask\0")), rt::sel(#str_ptr("new\0")));
    }

    fn task_identifier(this) -> text::Text {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("taskIdentifier\0")));
        return bridge::to_text(ns);
    }

    fn state(this) -> TaskState {
        return task_state_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("state\0"))));
    }

    fn error(this) -> option::Option[foundation::Error] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("error\0")));
        if obj == { 0 as *u8 } {
            return option::Option[foundation::Error]::None;
        }
        return option::some(foundation::Error::from_raw(obj));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLUpdateContext` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct UpdateContext {
    opaque _obj: *u8,
}

impl UpdateContext {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> UpdateContext { return UpdateContext { _obj: ptr }; }

    fn task(this) -> UpdateTask {
        return UpdateTask::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("task\0"))));
    }

    fn model(this) -> Model {
        return Model::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("model\0"))));
    }

    fn event(this) -> u64 {
        return rt::msg_u64(this._obj, rt::sel(#str_ptr("event\0")));
    }

    // SKIPPED `metrics`: return `NSDictionary<MLMetricKey *,id> * _Nonnull` — NSDictionary with non-string key `MLMetricKey *`
    // SKIPPED `parameters`: return `NSDictionary<MLParameterKey *,id> * _Nonnull` — NSDictionary with non-string key `MLParameterKey *`
}

// `MLUpdateProgressHandlers` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct UpdateProgressHandlers {
    _obj: *u8,
}

impl UpdateProgressHandlers {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> UpdateProgressHandlers { return UpdateProgressHandlers { _obj: rt::retain(ptr) }; }

    // SKIPPED `initForEvents:progressHandler:completionHandler:`: params after the block not modelled
    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> *u8 {
        return rt::msg_id(rt::get_class(#str_ptr("MLUpdateProgressHandlers\0")), rt::sel(#str_ptr("new\0")));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLUpdateTask` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct UpdateTask {
    _obj: *u8,
}

impl UpdateTask {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> UpdateTask { return UpdateTask { _obj: rt::retain(ptr) }; }

    // Upcast to `MLTask` (superclass). Safe typed reinterpret.
    fn as_task(this) -> Task { return Task::from_raw(this._obj); }

    // SKIPPED `updateTaskForModelAtURL:trainingData:configuration:completionHandler:error:`: params after the block not modelled
    // SKIPPED `updateTaskForModelAtURL:trainingData:completionHandler:error:`: params after the block not modelled
    fn update_task_for_model_at_url_training_data_configuration_progress_handlers(model_url: foundation::URL, training_data: BatchProvider, configuration: ModelConfiguration, progress_handlers: UpdateProgressHandlers, error: *u8) -> option::Option[UpdateTask] {
        let obj: *u8 = rt::msg_id_id_id_id_id_id(rt::get_class(#str_ptr("MLUpdateTask\0")), rt::sel(#str_ptr("updateTaskForModelAtURL:trainingData:configuration:progressHandlers:error:\0")), model_url.raw(), training_data.raw(), configuration.raw(), progress_handlers.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[UpdateTask]::None;
        }
        return option::some(UpdateTask { _obj: rt::retain(obj) });
    }

    fn update_task_for_model_at_url_training_data(model_url: foundation::URL, training_data: BatchProvider, progress_handlers: UpdateProgressHandlers, error: *u8) -> option::Option[UpdateTask] {
        let obj: *u8 = rt::msg_id_id_id_id_id(rt::get_class(#str_ptr("MLUpdateTask\0")), rt::sel(#str_ptr("updateTaskForModelAtURL:trainingData:progressHandlers:error:\0")), model_url.raw(), training_data.raw(), progress_handlers.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[UpdateTask]::None;
        }
        return option::some(UpdateTask { _obj: rt::retain(obj) });
    }

    // SKIPPED `resumeWithParameters:`: param `NSDictionary<MLParameterKey *,id> * _Nonnull` — generic collection
    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> *u8 {
        return rt::msg_id(rt::get_class(#str_ptr("MLUpdateTask\0")), rt::sel(#str_ptr("new\0")));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLMetricKey` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct MetricKey {
    _obj: *u8,
}

impl MetricKey {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> MetricKey { return MetricKey { _obj: rt::retain(ptr) }; }

    // Upcast to `MLKey` (superclass). Safe typed reinterpret.
    fn as_key(this) -> Key { return Key::from_raw(this._obj); }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> *u8 {
        return rt::msg_id(rt::get_class(#str_ptr("MLMetricKey\0")), rt::sel(#str_ptr("new\0")));
    }

    fn loss_value() -> MetricKey {
        return MetricKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLMetricKey\0")), rt::sel(#str_ptr("lossValue\0")))) };
    }

    fn epoch_index() -> MetricKey {
        return MetricKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLMetricKey\0")), rt::sel(#str_ptr("epochIndex\0")))) };
    }

    fn mini_batch_index() -> MetricKey {
        return MetricKey { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MLMetricKey\0")), rt::sel(#str_ptr("miniBatchIndex\0")))) };
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLNumericConstraint` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct NumericConstraint {
    opaque _obj: *u8,
}

impl NumericConstraint {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> NumericConstraint { return NumericConstraint { _obj: ptr }; }

    fn min_number(this) -> foundation::Number {
        return foundation::Number::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("minNumber\0"))));
    }

    fn max_number(this) -> foundation::Number {
        return foundation::Number::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("maxNumber\0"))));
    }

    // SKIPPED `enumeratedNumbers`: return `NSSet<NSNumber *> * _Nullable` — generic collection
}

// `MLModelCollectionEntry` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelCollectionEntry {
    _obj: *u8,
}

impl ModelCollectionEntry {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelCollectionEntry { return ModelCollectionEntry { _obj: rt::retain(ptr) }; }

    fn is_equal_to_model_collection_entry(this, entry: ModelCollectionEntry) -> bool {
        return rt::msg_i8_id(this._obj, rt::sel(#str_ptr("isEqualToModelCollectionEntry:\0")), entry.raw()) != (0 as i8);
    }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> *u8 {
        return rt::msg_id(rt::get_class(#str_ptr("MLModelCollectionEntry\0")), rt::sel(#str_ptr("new\0")));
    }

    fn model_identifier(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("modelIdentifier\0")));
    }

    fn model_url(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("modelURL\0")));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelCollection` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelCollection {
    _obj: *u8,
}

impl ModelCollection {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelCollection { return ModelCollection { _obj: rt::retain(ptr) }; }

    fn begin_accessing_model_collection_with_identifier(identifier: str, cb: fn(*u8, *u8, *u8), ctx: *u8) -> foundation::Progress {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: ModelCollection_begin_accessing_model_collection_with_identifier_block = ModelCollection_begin_accessing_model_collection_with_identifier_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: ModelCollection_begin_accessing_model_collection_with_identifier_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        return foundation::Progress::from_raw(rt::msg_id_id_id(rt::get_class(#str_ptr("MLModelCollection\0")), rt::sel(#str_ptr("beginAccessingModelCollectionWithIdentifier:completionHandler:\0")), bridge::nsstring(identifier), bp));
    }

    fn end_accessing_model_collection_with_identifier(identifier: str, cb: fn(*u8, i8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: ModelCollection_end_accessing_model_collection_with_identifier_block = ModelCollection_end_accessing_model_collection_with_identifier_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: ModelCollection_end_accessing_model_collection_with_identifier_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id_id(rt::get_class(#str_ptr("MLModelCollection\0")), rt::sel(#str_ptr("endAccessingModelCollectionWithIdentifier:completionHandler:\0")), bridge::nsstring(identifier), bp);
        return;
    }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> *u8 {
        return rt::msg_id(rt::get_class(#str_ptr("MLModelCollection\0")), rt::sel(#str_ptr("new\0")));
    }

    fn identifier(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("identifier\0")));
    }

    fn entries(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("entries\0")));
    }

    fn deployment_id(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("deploymentID\0")));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLCPUComputeDevice` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct CPUComputeDevice {
    _obj: *u8,
}

impl CPUComputeDevice {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> CPUComputeDevice { return CPUComputeDevice { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> CPUComputeDevice {
        return CPUComputeDevice { _obj: rt::msg_id(rt::get_class(#str_ptr("MLCPUComputeDevice\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLGPUComputeDevice` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct GPUComputeDevice {
    _obj: *u8,
}

impl GPUComputeDevice {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> GPUComputeDevice { return GPUComputeDevice { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> GPUComputeDevice {
        return GPUComputeDevice { _obj: rt::msg_id(rt::get_class(#str_ptr("MLGPUComputeDevice\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn metal_device(this) -> MTLDevice {
        return MTLDevice::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("metalDevice\0"))));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLNeuralEngineComputeDevice` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct NeuralEngineComputeDevice {
    _obj: *u8,
}

impl NeuralEngineComputeDevice {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> NeuralEngineComputeDevice { return NeuralEngineComputeDevice { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> NeuralEngineComputeDevice {
        return NeuralEngineComputeDevice { _obj: rt::msg_id(rt::get_class(#str_ptr("MLNeuralEngineComputeDevice\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn total_core_count(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("totalCoreCount\0")));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelStructureNeuralNetwork` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelStructureNeuralNetwork {
    _obj: *u8,
}

impl ModelStructureNeuralNetwork {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelStructureNeuralNetwork { return ModelStructureNeuralNetwork { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelStructureNeuralNetwork {
        return ModelStructureNeuralNetwork { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelStructureNeuralNetwork\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn layers(this) -> vec::Vec[ModelStructureNeuralNetworkLayer] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("layers\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[ModelStructureNeuralNetworkLayer] = vec::Vec[ModelStructureNeuralNetworkLayer]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(ModelStructureNeuralNetworkLayer::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelStructureProgram` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelStructureProgram {
    _obj: *u8,
}

impl ModelStructureProgram {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelStructureProgram { return ModelStructureProgram { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelStructureProgram {
        return ModelStructureProgram { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelStructureProgram\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn functions(this) -> string_map::StringMap[ModelStructureProgramFunction] {
        let dict: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("functions\0")));
        let keys: *u8 = rt::msg_id(dict, rt::sel(#str_ptr("allKeys\0")));
        let n: u64 = rt::msg_u64(keys, rt::sel(#str_ptr("count\0")));
        var out: string_map::StringMap[ModelStructureProgramFunction] = string_map::new::[ModelStructureProgramFunction]();
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let obj_sel: *u8 = rt::sel(#str_ptr("objectForKey:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let nskey: *u8 = rt::msg_id_u64(keys, at_sel, i);
            let nsval: *u8 = rt::msg_id_id(dict, obj_sel, nskey);
            out.insert(bridge::to_text(nskey), ModelStructureProgramFunction::from_raw(nsval));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelStructurePipeline` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelStructurePipeline {
    _obj: *u8,
}

impl ModelStructurePipeline {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelStructurePipeline { return ModelStructurePipeline { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelStructurePipeline {
        return ModelStructurePipeline { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelStructurePipeline\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn sub_model_names(this) -> vec::Vec[text::Text] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("subModelNames\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[text::Text] = vec::Vec[text::Text]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let value: *u8 = rt::msg_id_u64(arr, at_sel, i);
            out.append(bridge::to_text(value));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn sub_models(this) -> vec::Vec[ModelStructure] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("subModels\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[ModelStructure] = vec::Vec[ModelStructure]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(ModelStructure::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelStructure` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelStructure {
    _obj: *u8,
}

impl ModelStructure {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelStructure { return ModelStructure { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelStructure {
        return ModelStructure { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelStructure\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn load_contents_of_url(url: foundation::URL, cb: fn(*u8, *u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: ModelStructure_load_contents_of_url_block = ModelStructure_load_contents_of_url_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: ModelStructure_load_contents_of_url_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id_id(rt::get_class(#str_ptr("MLModelStructure\0")), rt::sel(#str_ptr("loadContentsOfURL:completionHandler:\0")), url.raw(), bp);
        return;
    }

    fn load_model_asset(asset: ModelAsset, cb: fn(*u8, *u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: ModelStructure_load_model_asset_block = ModelStructure_load_model_asset_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: ModelStructure_load_model_asset_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id_id(rt::get_class(#str_ptr("MLModelStructure\0")), rt::sel(#str_ptr("loadModelAsset:completionHandler:\0")), asset.raw(), bp);
        return;
    }

    fn neural_network(this) -> option::Option[ModelStructureNeuralNetwork] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("neuralNetwork\0")));
        if obj == { 0 as *u8 } {
            return option::Option[ModelStructureNeuralNetwork]::None;
        }
        return option::some(ModelStructureNeuralNetwork::from_raw(obj));
    }

    fn program(this) -> option::Option[ModelStructureProgram] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("program\0")));
        if obj == { 0 as *u8 } {
            return option::Option[ModelStructureProgram]::None;
        }
        return option::some(ModelStructureProgram::from_raw(obj));
    }

    fn pipeline(this) -> option::Option[ModelStructurePipeline] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("pipeline\0")));
        if obj == { 0 as *u8 } {
            return option::Option[ModelStructurePipeline]::None;
        }
        return option::some(ModelStructurePipeline::from_raw(obj));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelStructureNeuralNetworkLayer` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelStructureNeuralNetworkLayer {
    _obj: *u8,
}

impl ModelStructureNeuralNetworkLayer {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelStructureNeuralNetworkLayer { return ModelStructureNeuralNetworkLayer { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelStructureNeuralNetworkLayer {
        return ModelStructureNeuralNetworkLayer { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelStructureNeuralNetworkLayer\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn name(this) -> text::Text {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("name\0")));
        return bridge::to_text(ns);
    }

    fn type_(this) -> text::Text {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("type\0")));
        return bridge::to_text(ns);
    }

    fn input_names(this) -> vec::Vec[text::Text] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("inputNames\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[text::Text] = vec::Vec[text::Text]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let value: *u8 = rt::msg_id_u64(arr, at_sel, i);
            out.append(bridge::to_text(value));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn output_names(this) -> vec::Vec[text::Text] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("outputNames\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[text::Text] = vec::Vec[text::Text]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let value: *u8 = rt::msg_id_u64(arr, at_sel, i);
            out.append(bridge::to_text(value));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelStructureProgramFunction` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelStructureProgramFunction {
    _obj: *u8,
}

impl ModelStructureProgramFunction {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelStructureProgramFunction { return ModelStructureProgramFunction { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelStructureProgramFunction {
        return ModelStructureProgramFunction { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelStructureProgramFunction\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn inputs(this) -> vec::Vec[ModelStructureProgramNamedValueType] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("inputs\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[ModelStructureProgramNamedValueType] = vec::Vec[ModelStructureProgramNamedValueType]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(ModelStructureProgramNamedValueType::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn block(this) -> ModelStructureProgramBlock {
        return ModelStructureProgramBlock::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("block\0"))));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelStructureProgramBinding` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelStructureProgramBinding {
    _obj: *u8,
}

impl ModelStructureProgramBinding {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelStructureProgramBinding { return ModelStructureProgramBinding { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelStructureProgramBinding {
        return ModelStructureProgramBinding { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelStructureProgramBinding\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn name(this) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("name\0")));
        return bridge::to_text_option(ns);
    }

    fn value(this) -> option::Option[ModelStructureProgramValue] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("value\0")));
        if obj == { 0 as *u8 } {
            return option::Option[ModelStructureProgramValue]::None;
        }
        return option::some(ModelStructureProgramValue::from_raw(obj));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelStructureProgramArgument` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelStructureProgramArgument {
    _obj: *u8,
}

impl ModelStructureProgramArgument {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelStructureProgramArgument { return ModelStructureProgramArgument { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelStructureProgramArgument {
        return ModelStructureProgramArgument { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelStructureProgramArgument\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn bindings(this) -> vec::Vec[ModelStructureProgramBinding] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("bindings\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[ModelStructureProgramBinding] = vec::Vec[ModelStructureProgramBinding]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(ModelStructureProgramBinding::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelStructureProgramValue` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelStructureProgramValue {
    _obj: *u8,
}

impl ModelStructureProgramValue {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelStructureProgramValue { return ModelStructureProgramValue { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelStructureProgramValue {
        return ModelStructureProgramValue { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelStructureProgramValue\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelStructureProgramNamedValueType` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelStructureProgramNamedValueType {
    _obj: *u8,
}

impl ModelStructureProgramNamedValueType {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelStructureProgramNamedValueType { return ModelStructureProgramNamedValueType { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelStructureProgramNamedValueType {
        return ModelStructureProgramNamedValueType { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelStructureProgramNamedValueType\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn name(this) -> text::Text {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("name\0")));
        return bridge::to_text(ns);
    }

    fn type_(this) -> ModelStructureProgramValueType {
        return ModelStructureProgramValueType::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("type\0"))));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelStructureProgramOperation` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelStructureProgramOperation {
    _obj: *u8,
}

impl ModelStructureProgramOperation {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelStructureProgramOperation { return ModelStructureProgramOperation { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelStructureProgramOperation {
        return ModelStructureProgramOperation { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelStructureProgramOperation\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn operator_name(this) -> text::Text {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("operatorName\0")));
        return bridge::to_text(ns);
    }

    fn inputs(this) -> string_map::StringMap[ModelStructureProgramArgument] {
        let dict: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("inputs\0")));
        let keys: *u8 = rt::msg_id(dict, rt::sel(#str_ptr("allKeys\0")));
        let n: u64 = rt::msg_u64(keys, rt::sel(#str_ptr("count\0")));
        var out: string_map::StringMap[ModelStructureProgramArgument] = string_map::new::[ModelStructureProgramArgument]();
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        let obj_sel: *u8 = rt::sel(#str_ptr("objectForKey:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let nskey: *u8 = rt::msg_id_u64(keys, at_sel, i);
            let nsval: *u8 = rt::msg_id_id(dict, obj_sel, nskey);
            out.insert(bridge::to_text(nskey), ModelStructureProgramArgument::from_raw(nsval));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn outputs(this) -> vec::Vec[ModelStructureProgramNamedValueType] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("outputs\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[ModelStructureProgramNamedValueType] = vec::Vec[ModelStructureProgramNamedValueType]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(ModelStructureProgramNamedValueType::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn blocks(this) -> vec::Vec[ModelStructureProgramBlock] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("blocks\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[ModelStructureProgramBlock] = vec::Vec[ModelStructureProgramBlock]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(ModelStructureProgramBlock::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelStructureProgramBlock` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelStructureProgramBlock {
    _obj: *u8,
}

impl ModelStructureProgramBlock {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelStructureProgramBlock { return ModelStructureProgramBlock { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelStructureProgramBlock {
        return ModelStructureProgramBlock { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelStructureProgramBlock\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn inputs(this) -> vec::Vec[ModelStructureProgramNamedValueType] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("inputs\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[ModelStructureProgramNamedValueType] = vec::Vec[ModelStructureProgramNamedValueType]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(ModelStructureProgramNamedValueType::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn output_names(this) -> vec::Vec[text::Text] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("outputNames\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[text::Text] = vec::Vec[text::Text]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let value: *u8 = rt::msg_id_u64(arr, at_sel, i);
            out.append(bridge::to_text(value));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn operations(this) -> vec::Vec[ModelStructureProgramOperation] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("operations\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[ModelStructureProgramOperation] = vec::Vec[ModelStructureProgramOperation]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(ModelStructureProgramOperation::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLModelStructureProgramValueType` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ModelStructureProgramValueType {
    _obj: *u8,
}

impl ModelStructureProgramValueType {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ModelStructureProgramValueType { return ModelStructureProgramValueType { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ModelStructureProgramValueType {
        return ModelStructureProgramValueType { _obj: rt::msg_id(rt::get_class(#str_ptr("MLModelStructureProgramValueType\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLComputePlanCost` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ComputePlanCost {
    _obj: *u8,
}

impl ComputePlanCost {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ComputePlanCost { return ComputePlanCost { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ComputePlanCost {
        return ComputePlanCost { _obj: rt::msg_id(rt::get_class(#str_ptr("MLComputePlanCost\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn weight(this) -> f64 {
        return rt::msg_f64(this._obj, rt::sel(#str_ptr("weight\0")));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLComputePlanDeviceUsage` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ComputePlanDeviceUsage {
    _obj: *u8,
}

impl ComputePlanDeviceUsage {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ComputePlanDeviceUsage { return ComputePlanDeviceUsage { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ComputePlanDeviceUsage {
        return ComputePlanDeviceUsage { _obj: rt::msg_id(rt::get_class(#str_ptr("MLComputePlanDeviceUsage\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn supported_compute_devices(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("supportedComputeDevices\0")));
    }

    // SKIPPED `preferredComputeDevice`: return `NS_SWIFT_NAME id<MLComputeDeviceProtocol>` — generic collection
    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLComputePlan` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ComputePlan {
    _obj: *u8,
}

impl ComputePlan {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ComputePlan { return ComputePlan { _obj: rt::retain(ptr) }; }

    // SKIPPED `init`: `NS_UNAVAILABLE` init, not a constructor
    fn new() -> ComputePlan {
        return ComputePlan { _obj: rt::msg_id(rt::get_class(#str_ptr("MLComputePlan\0")), rt::sel(#str_ptr("new\0"))) };
    }

    fn load_contents_of_url(url: foundation::URL, configuration: ModelConfiguration, cb: fn(*u8, *u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: ComputePlan_load_contents_of_url_block = ComputePlan_load_contents_of_url_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: ComputePlan_load_contents_of_url_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id_id_id(rt::get_class(#str_ptr("MLComputePlan\0")), rt::sel(#str_ptr("loadContentsOfURL:configuration:completionHandler:\0")), url.raw(), configuration.raw(), bp);
        return;
    }

    fn load_model_asset(asset: ModelAsset, configuration: ModelConfiguration, cb: fn(*u8, *u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: ComputePlan_load_model_asset_block = ComputePlan_load_model_asset_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: ComputePlan_load_model_asset_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id_id_id(rt::get_class(#str_ptr("MLComputePlan\0")), rt::sel(#str_ptr("loadModelAsset:configuration:completionHandler:\0")), asset.raw(), configuration.raw(), bp);
        return;
    }

    fn estimated_cost_of_ml_program_operation(this, operation: ModelStructureProgramOperation) -> option::Option[ComputePlanCost] {
        let obj: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("estimatedCostOfMLProgramOperation:\0")), operation.raw());
        if obj == { 0 as *u8 } {
            return option::Option[ComputePlanCost]::None;
        }
        return option::some(ComputePlanCost::from_raw(obj));
    }

    fn compute_device_usage_for_neural_network_layer(this, layer: ModelStructureNeuralNetworkLayer) -> option::Option[ComputePlanDeviceUsage] {
        let obj: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("computeDeviceUsageForNeuralNetworkLayer:\0")), layer.raw());
        if obj == { 0 as *u8 } {
            return option::Option[ComputePlanDeviceUsage]::None;
        }
        return option::some(ComputePlanDeviceUsage::from_raw(obj));
    }

    fn compute_device_usage_for_ml_program_operation(this, operation: ModelStructureProgramOperation) -> option::Option[ComputePlanDeviceUsage] {
        let obj: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("computeDeviceUsageForMLProgramOperation:\0")), operation.raw());
        if obj == { 0 as *u8 } {
            return option::Option[ComputePlanDeviceUsage]::None;
        }
        return option::some(ComputePlanDeviceUsage::from_raw(obj));
    }

    fn model_structure(this) -> ModelStructure {
        return ModelStructure::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("modelStructure\0"))));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MLFeatureProvider` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct FeatureProvider {
    opaque _obj: *u8,
}

impl FeatureProvider {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> FeatureProvider { return FeatureProvider { _obj: ptr }; }

    fn feature_value_for_name(this, feature_name: str) -> option::Option[FeatureValue] {
        let obj: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("featureValueForName:\0")), bridge::nsstring(feature_name));
        if obj == { 0 as *u8 } {
            return option::Option[FeatureValue]::None;
        }
        return option::some(FeatureValue::from_raw(obj));
    }

    fn feature_names(this) -> string_set::StringSet {
        let set: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("featureNames\0")));
        let arr: *u8 = rt::msg_id(set, rt::sel(#str_ptr("allObjects\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: string_set::StringSet = string_set::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let value: *u8 = rt::msg_id_u64(arr, at_sel, i);
            let _st: status::Status = out.insert(bridge::to_text(value));
            i = i +% (1 as u64);
        }
        return out;
    }

}

// `MLBatchProvider` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct BatchProvider {
    opaque _obj: *u8,
}

impl BatchProvider {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> BatchProvider { return BatchProvider { _obj: ptr }; }

    fn features_at_index(this, index: i64) -> FeatureProvider {
        return FeatureProvider::from_raw(rt::msg_id_i64(this._obj, rt::sel(#str_ptr("featuresAtIndex:\0")), index));
    }

    fn count(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("count\0")));
    }

}

// `MTLDevice` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct MTLDevice {
    opaque _obj: *u8,
}

impl MTLDevice {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> MTLDevice { return MTLDevice { _obj: ptr }; }

}

// `MLComputeDeviceProtocol` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct ComputeDeviceProtocol {
    opaque _obj: *u8,
}

impl ComputeDeviceProtocol {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ComputeDeviceProtocol { return ComputeDeviceProtocol { _obj: ptr }; }

}

// `MTLTexture` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct MTLTexture {
    opaque _obj: *u8,
}

impl MTLTexture {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> MTLTexture { return MTLTexture { _obj: ptr }; }

}

// `MTLCommandBuffer` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct MTLCommandBuffer {
    opaque _obj: *u8,
}

impl MTLCommandBuffer {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> MTLCommandBuffer { return MTLCommandBuffer { _obj: ptr }; }

}

// `MLCustomLayer` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct CustomLayer {
    opaque _obj: *u8,
}

impl CustomLayer {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> CustomLayer { return CustomLayer { _obj: ptr }; }

    fn init_with_parameter_dictionary_error(this, parameters: string_map::StringMap[AnyObject], error: *u8) -> *u8 {
        let dict_parameters: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableDictionary\0")), rt::sel(#str_ptr("dictionary\0")));
        let dset_parameters: *u8 = rt::sel(#str_ptr("setObject:forKey:\0"));
        let dcap_parameters: usize = parameters.slot_capacity();
        var dsi_parameters: usize = 0 as usize;
        while dsi_parameters < dcap_parameters {
            match parameters.value_ptr_at_slot(dsi_parameters) {
                option::Option[*AnyObject]::Some(dvp_parameters) => {
                    match parameters.key_ptr_at_slot(dsi_parameters) {
                        option::Option[*text::Text]::Some(dkp_parameters) => {
                    let dval_parameters: *u8 = (*dvp_parameters).raw();
                            rt::msg_void_id_id(dict_parameters, dset_parameters, dval_parameters, bridge::nsstring((*dkp_parameters).view()));
                        }
                        option::Option[*text::Text]::None => {}
                    }
                }
                option::Option[*AnyObject]::None => {}
            }
            dsi_parameters = dsi_parameters +% (1 as usize);
        }
        return rt::msg_id_id_id(this._obj, rt::sel(#str_ptr("initWithParameterDictionary:error:\0")), dict_parameters, error);
    }

    // SKIPPED `setWeightData:error:`: param `NSArray<NSData *> * _Nonnull` — generic collection
    // SKIPPED `outputShapesForInputShapes:error:`: param `NSArray<NSArray<NSNumber *> *> * _Nonnull` — generic collection
    fn evaluate_on_cpu_with_inputs(this, inputs: vec::Vec[MultiArray], outputs: vec::Vec[MultiArray], error: *u8) -> bool {
        let arr_inputs: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_inputs: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let n_inputs: usize = inputs.count();
        var i_inputs: usize = 0 as usize;
        while i_inputs < n_inputs {
            match inputs.at_ptr(i_inputs) {
                option::Option[*MultiArray]::Some(e_inputs) => { rt::msg_void_id(arr_inputs, add_sel_inputs, (*e_inputs).raw()); }
                option::Option[*MultiArray]::None => {}
            }
            i_inputs = i_inputs +% (1 as usize);
        }
        let arr_outputs: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_outputs: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let n_outputs: usize = outputs.count();
        var i_outputs: usize = 0 as usize;
        while i_outputs < n_outputs {
            match outputs.at_ptr(i_outputs) {
                option::Option[*MultiArray]::Some(e_outputs) => { rt::msg_void_id(arr_outputs, add_sel_outputs, (*e_outputs).raw()); }
                option::Option[*MultiArray]::None => {}
            }
            i_outputs = i_outputs +% (1 as usize);
        }
        return rt::msg_i8_id_id_id(this._obj, rt::sel(#str_ptr("evaluateOnCPUWithInputs:outputs:error:\0")), arr_inputs, arr_outputs, error) != (0 as i8);
    }

    fn encode_to_command_buffer(this, command_buffer: MTLCommandBuffer, inputs: vec::Vec[MTLTexture], outputs: vec::Vec[MTLTexture], error: *u8) -> bool {
        let arr_inputs: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_inputs: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let n_inputs: usize = inputs.count();
        var i_inputs: usize = 0 as usize;
        while i_inputs < n_inputs {
            match inputs.at_ptr(i_inputs) {
                option::Option[*MTLTexture]::Some(e_inputs) => { rt::msg_void_id(arr_inputs, add_sel_inputs, (*e_inputs).raw()); }
                option::Option[*MTLTexture]::None => {}
            }
            i_inputs = i_inputs +% (1 as usize);
        }
        let arr_outputs: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_outputs: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let n_outputs: usize = outputs.count();
        var i_outputs: usize = 0 as usize;
        while i_outputs < n_outputs {
            match outputs.at_ptr(i_outputs) {
                option::Option[*MTLTexture]::Some(e_outputs) => { rt::msg_void_id(arr_outputs, add_sel_outputs, (*e_outputs).raw()); }
                option::Option[*MTLTexture]::None => {}
            }
            i_outputs = i_outputs +% (1 as usize);
        }
        return objc_msg_i8_id_id_id_id(this._obj, rt::sel(#str_ptr("encodeToCommandBuffer:inputs:outputs:error:\0")), command_buffer.raw(), arr_inputs, arr_outputs, error) != (0 as i8);
    }

}

// `MLCustomModel` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct CustomModel {
    opaque _obj: *u8,
}

impl CustomModel {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> CustomModel { return CustomModel { _obj: ptr }; }

    fn init_with_model_description_parameter_dictionary_error(this, model_description: ModelDescription, parameters: string_map::StringMap[AnyObject], error: *u8) -> *u8 {
        let dict_parameters: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableDictionary\0")), rt::sel(#str_ptr("dictionary\0")));
        let dset_parameters: *u8 = rt::sel(#str_ptr("setObject:forKey:\0"));
        let dcap_parameters: usize = parameters.slot_capacity();
        var dsi_parameters: usize = 0 as usize;
        while dsi_parameters < dcap_parameters {
            match parameters.value_ptr_at_slot(dsi_parameters) {
                option::Option[*AnyObject]::Some(dvp_parameters) => {
                    match parameters.key_ptr_at_slot(dsi_parameters) {
                        option::Option[*text::Text]::Some(dkp_parameters) => {
                    let dval_parameters: *u8 = (*dvp_parameters).raw();
                            rt::msg_void_id_id(dict_parameters, dset_parameters, dval_parameters, bridge::nsstring((*dkp_parameters).view()));
                        }
                        option::Option[*text::Text]::None => {}
                    }
                }
                option::Option[*AnyObject]::None => {}
            }
            dsi_parameters = dsi_parameters +% (1 as usize);
        }
        return rt::msg_id_id_id_id(this._obj, rt::sel(#str_ptr("initWithModelDescription:parameterDictionary:error:\0")), model_description.raw(), dict_parameters, error);
    }

    fn prediction_from_features(this, input: FeatureProvider, options: PredictionOptions, error: *u8) -> option::Option[FeatureProvider] {
        let obj: *u8 = rt::msg_id_id_id_id(this._obj, rt::sel(#str_ptr("predictionFromFeatures:options:error:\0")), input.raw(), options.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[FeatureProvider]::None;
        }
        return option::some(FeatureProvider::from_raw(obj));
    }

    fn predictions_from_batch(this, input_batch: BatchProvider, options: PredictionOptions, error: *u8) -> option::Option[BatchProvider] {
        let obj: *u8 = rt::msg_id_id_id_id(this._obj, rt::sel(#str_ptr("predictionsFromBatch:options:error:\0")), input_batch.raw(), options.raw(), error);
        if obj == { 0 as *u8 } {
            return option::Option[BatchProvider]::None;
        }
        return option::some(BatchProvider::from_raw(obj));
    }

}

// `MLWritable` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct Writable {
    opaque _obj: *u8,
}

impl Writable {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Writable { return Writable { _obj: ptr }; }

    fn write_to_url(this, url: foundation::URL, error: *u8) -> bool {
        return rt::msg_i8_id_id(this._obj, rt::sel(#str_ptr("writeToURL:error:\0")), url.raw(), error) != (0 as i8);
    }

}

