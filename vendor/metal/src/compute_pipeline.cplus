// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLComputePipeline.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLShaderValidation` (NS_ENUM)
enum ShaderValidation {
    Default,
    Enabled,
    Disabled,
}

fn shader_validation_raw(v: ShaderValidation) -> i64 {
    return match v {
        ShaderValidation::Default => { 0 as i64 },
        ShaderValidation::Enabled => { 1 as i64 },
        ShaderValidation::Disabled => { 2 as i64 },
    };
}

fn shader_validation_from_raw(raw: i64) -> ShaderValidation {
    if raw == (1 as i64) { return ShaderValidation::Enabled; }
    if raw == (2 as i64) { return ShaderValidation::Disabled; }
    return ShaderValidation::Default;
}

// `MTLComputePipelineReflection` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ComputePipelineReflection {
    opaque _obj: *u8,
}

impl ComputePipelineReflection {
    fn raw(this) -> *u8 { return this._obj; }

    // SKIPPED `bindings`: return `NSArray<id<MTLBinding>> * _Nonnull` — generic collection
    // SKIPPED `arguments`: return `NSArray<MTLArgument *> * _Nonnull` — generic collection
}

// `MTLComputePipelineDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ComputePipelineDescriptor {
    opaque _obj: *u8,
}

impl ComputePipelineDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

    fn reset(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("reset\0")));
    }

    fn label(this) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("label\0")));
        return bridge::to_text_option(ns);
    }

    fn set_label(this, label: str) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setLabel:\0")), bridge::nsstring(label));
    }

    // SKIPPED `computeFunction`: return `id<MTLFunction> _Nullable` — generic collection
    // SKIPPED `setComputeFunction:`: param `id<MTLFunction> _Nullable` — generic collection
    fn thread_group_size_is_multiple_of_thread_execution_width(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("threadGroupSizeIsMultipleOfThreadExecutionWidth\0"))) != (0 as i8);
    }

    fn set_thread_group_size_is_multiple_of_thread_execution_width(this, thread_group_size_is_multiple_of_thread_execution_width: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setThreadGroupSizeIsMultipleOfThreadExecutionWidth:\0")), thread_group_size_is_multiple_of_thread_execution_width as i8);
    }

    fn max_total_threads_per_threadgroup(this) -> u64 {
        return rt::msg_u64(this._obj, rt::sel(#str_ptr("maxTotalThreadsPerThreadgroup\0")));
    }

    // SKIPPED `setMaxTotalThreadsPerThreadgroup:`: (return, arg) shape not yet modelled
    fn stage_input_descriptor(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("stageInputDescriptor\0")));
        return bridge::obj_option(obj);
    }

    fn set_stage_input_descriptor(this, stage_input_descriptor: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setStageInputDescriptor:\0")), stage_input_descriptor);
    }

    fn buffers(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("buffers\0")));
    }

    fn support_indirect_command_buffers(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("supportIndirectCommandBuffers\0"))) != (0 as i8);
    }

    fn set_support_indirect_command_buffers(this, support_indirect_command_buffers: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setSupportIndirectCommandBuffers:\0")), support_indirect_command_buffers as i8);
    }

    // SKIPPED `insertLibraries`: return `NSArray<id<MTLDynamicLibrary>> * _Nullable` — generic collection
    // SKIPPED `setInsertLibraries:`: param `NSArray<id<MTLDynamicLibrary>> * _Nullable` — generic collection
    // SKIPPED `preloadedLibraries`: return `NSArray<id<MTLDynamicLibrary>> * _Nonnull` — generic collection
    // SKIPPED `setPreloadedLibraries:`: param `NSArray<id<MTLDynamicLibrary>> * _Nonnull` — generic collection
    // SKIPPED `binaryArchives`: return `NSArray<id<MTLBinaryArchive>> * _Nullable` — generic collection
    // SKIPPED `setBinaryArchives:`: param `NSArray<id<MTLBinaryArchive>> * _Nullable` — generic collection
    fn linked_functions(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("linkedFunctions\0")));
        return bridge::obj_option(obj);
    }

    fn set_linked_functions(this, linked_functions: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setLinkedFunctions:\0")), linked_functions);
    }

    fn support_adding_binary_functions(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("supportAddingBinaryFunctions\0"))) != (0 as i8);
    }

    fn set_support_adding_binary_functions(this, support_adding_binary_functions: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setSupportAddingBinaryFunctions:\0")), support_adding_binary_functions as i8);
    }

    fn max_call_stack_depth(this) -> u64 {
        return rt::msg_u64(this._obj, rt::sel(#str_ptr("maxCallStackDepth\0")));
    }

    // SKIPPED `setMaxCallStackDepth:`: (return, arg) shape not yet modelled
    fn shader_validation(this) -> ShaderValidation {
        return shader_validation_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("shaderValidation\0"))));
    }

    // SKIPPED `setShaderValidation:`: (return, arg) shape not yet modelled
    // SKIPPED `requiredThreadsPerThreadgroup`: return `MTLSize` — unmapped type `MTLSize`
    // SKIPPED `setRequiredThreadsPerThreadgroup:`: param `MTLSize` — unmapped type `MTLSize`
}

// `MTLComputePipelineState` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `functionHandleWithName:`: return `id<MTLFunctionHandle> _Nullable` not modelled
// SKIPPED `functionHandleWithBinaryFunction:`: arg shape (only <=5 object args modelled)
// SKIPPED `newComputePipelineStateWithBinaryFunctions:error:`: return `id<MTLComputePipelineState> _Nullable` not modelled
// SKIPPED `imageblockMemoryLengthForDimensions:`: arg shape (only <=5 object args modelled)
// SKIPPED `functionHandleWithFunction:`: arg shape (only <=5 object args modelled)
// SKIPPED `newComputePipelineStateWithAdditionalBinaryFunctions:error:`: return `id<MTLComputePipelineState> _Nullable` not modelled
// SKIPPED `newVisibleFunctionTableWithDescriptor:`: return `id<MTLVisibleFunctionTable> _Nullable` not modelled
// SKIPPED `newIntersectionFunctionTableWithDescriptor:`: return `id<MTLIntersectionFunctionTable> _Nullable` not modelled
// SKIPPED `device`: return `id<MTLDevice> _Nonnull` not modelled
// SKIPPED `supportIndirectCommandBuffers`: no `i8`-return / 0-arg IMP shape
// SKIPPED `gpuResourceID`: return `MTLResourceID` not modelled
// SKIPPED `requiredThreadsPerThreadgroup`: return `MTLSize` not modelled
fn ComputePipelineState_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn ComputePipelineState_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }

struct ComputePipelineState {
    label: fn(*u8) -> *u8,
    reflection: fn(*u8) -> *u8,
    max_total_threads_per_threadgroup: fn(*u8) -> u64,
    thread_execution_width: fn(*u8) -> u64,
    static_threadgroup_memory_length: fn(*u8) -> u64,
    shader_validation: fn(*u8) -> u64,
    opaque user: *u8,
}

fn ComputePipelineState_new(user: *u8) -> ComputePipelineState {
    return ComputePipelineState { label: ComputePipelineState_noop_id_0, reflection: ComputePipelineState_noop_id_0, max_total_threads_per_threadgroup: ComputePipelineState_noop_u64_0, thread_execution_width: ComputePipelineState_noop_u64_0, static_threadgroup_memory_length: ComputePipelineState_noop_u64_0, shader_validation: ComputePipelineState_noop_u64_0, user: user };
}

impl ComputePipelineState {
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
    fn set_reflection(ref this, handler: fn(*u8) -> *u8) {
        this.reflection = handler;
        return;
    }
    fn set_max_total_threads_per_threadgroup(ref this, handler: fn(*u8) -> u64) {
        this.max_total_threads_per_threadgroup = handler;
        return;
    }
    fn set_thread_execution_width(ref this, handler: fn(*u8) -> u64) {
        this.thread_execution_width = handler;
        return;
    }
    fn set_static_threadgroup_memory_length(ref this, handler: fn(*u8) -> u64) {
        this.static_threadgroup_memory_length = handler;
        return;
    }
    fn set_shader_validation(ref this, handler: fn(*u8) -> u64) {
        this.shader_validation = handler;
        return;
    }
}

fn ComputePipelineState_key() -> *u8 { return { 28672 as *u8 }; }

fn ComputePipelineState_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *ComputePipelineState = { synth::get_associated(self_obj, ComputePipelineState_key()) as *ComputePipelineState };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn ComputePipelineState_reflection_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *ComputePipelineState = { synth::get_associated(self_obj, ComputePipelineState_key()) as *ComputePipelineState };
    let f: fn(*u8) -> *u8 = { (*c).reflection };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn ComputePipelineState_max_total_threads_per_threadgroup_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *ComputePipelineState = { synth::get_associated(self_obj, ComputePipelineState_key()) as *ComputePipelineState };
    let f: fn(*u8) -> u64 = { (*c).max_total_threads_per_threadgroup };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn ComputePipelineState_thread_execution_width_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *ComputePipelineState = { synth::get_associated(self_obj, ComputePipelineState_key()) as *ComputePipelineState };
    let f: fn(*u8) -> u64 = { (*c).thread_execution_width };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn ComputePipelineState_static_threadgroup_memory_length_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *ComputePipelineState = { synth::get_associated(self_obj, ComputePipelineState_key()) as *ComputePipelineState };
    let f: fn(*u8) -> u64 = { (*c).static_threadgroup_memory_length };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn ComputePipelineState_shader_validation_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *ComputePipelineState = { synth::get_associated(self_obj, ComputePipelineState_key()) as *ComputePipelineState };
    let f: fn(*u8) -> u64 = { (*c).shader_validation };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_ComputePipelineState(ctx: *ComputePipelineState) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_ComputePipelineState\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), ComputePipelineState_label_imp, #str_ptr("@@:\0"));
        let add_reflection: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("reflection\0")), ComputePipelineState_reflection_imp, #str_ptr("@@:\0"));
        let add_max_total_threads_per_threadgroup: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("maxTotalThreadsPerThreadgroup\0")), ComputePipelineState_max_total_threads_per_threadgroup_imp, #str_ptr("Q@:\0"));
        let add_thread_execution_width: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("threadExecutionWidth\0")), ComputePipelineState_thread_execution_width_imp, #str_ptr("Q@:\0"));
        let add_static_threadgroup_memory_length: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("staticThreadgroupMemoryLength\0")), ComputePipelineState_static_threadgroup_memory_length_imp, #str_ptr("Q@:\0"));
        let add_shader_validation: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("shaderValidation\0")), ComputePipelineState_shader_validation_imp, #str_ptr("Q@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, ComputePipelineState_key(), { ctx as *u8 });
    return d;
}

