// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLCaptureScope.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLCaptureScope` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `device`: return `id<MTLDevice> _Nonnull` not modelled
// SKIPPED `commandQueue`: return `id<MTLCommandQueue> _Nullable` not modelled
// SKIPPED `mtl4CommandQueue`: return `id<MTL4CommandQueue> _Nullable` not modelled
fn CaptureScope_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn CaptureScope_noop_v_0(user: *u8) { return; }
fn CaptureScope_noop_v_1(user: *u8, a0: *u8) { return; }

struct CaptureScope {
    begin_scope: fn(*u8),
    end_scope: fn(*u8),
    label: fn(*u8) -> *u8,
    set_label: fn(*u8, *u8),
    opaque user: *u8,
}

fn CaptureScope_new(user: *u8) -> CaptureScope {
    return CaptureScope { begin_scope: CaptureScope_noop_v_0, end_scope: CaptureScope_noop_v_0, label: CaptureScope_noop_id_0, set_label: CaptureScope_noop_v_1, user: user };
}

impl CaptureScope {
    fn set_begin_scope(ref this, handler: fn(*u8)) {
        this.begin_scope = handler;
        return;
    }
    fn set_end_scope(ref this, handler: fn(*u8)) {
        this.end_scope = handler;
        return;
    }
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
    fn set_set_label(ref this, handler: fn(*u8, *u8)) {
        this.set_label = handler;
        return;
    }
}

fn CaptureScope_key() -> *u8 { return { 28672 as *u8 }; }

fn CaptureScope_begin_scope_imp(self_obj: *u8, cmd: *u8) {
    let c: *CaptureScope = { synth::get_associated(self_obj, CaptureScope_key()) as *CaptureScope };
    let f: fn(*u8) = { (*c).begin_scope };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn CaptureScope_end_scope_imp(self_obj: *u8, cmd: *u8) {
    let c: *CaptureScope = { synth::get_associated(self_obj, CaptureScope_key()) as *CaptureScope };
    let f: fn(*u8) = { (*c).end_scope };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn CaptureScope_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *CaptureScope = { synth::get_associated(self_obj, CaptureScope_key()) as *CaptureScope };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn CaptureScope_set_label_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *CaptureScope = { synth::get_associated(self_obj, CaptureScope_key()) as *CaptureScope };
    let f: fn(*u8, *u8) = { (*c).set_label };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn create_CaptureScope(ctx: *CaptureScope) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_CaptureScope\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_begin_scope: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("beginScope\0")), CaptureScope_begin_scope_imp, #str_ptr("v@:\0"));
        let add_end_scope: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("endScope\0")), CaptureScope_end_scope_imp, #str_ptr("v@:\0"));
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), CaptureScope_label_imp, #str_ptr("@@:\0"));
        let add_set_label: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("setLabel:\0")), CaptureScope_set_label_imp, #str_ptr("v@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, CaptureScope_key(), { ctx as *u8 });
    return d;
}

