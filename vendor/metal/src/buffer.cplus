// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLBuffer.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLTextureDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextureDescriptor {
    opaque _obj: *u8,
}

impl TextureDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

}

// `MTLBuffer` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `didModifyRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `newTextureWithDescriptor:offset:bytesPerRow:`: arg shape (only <=5 object args modelled)
// SKIPPED `newTensorWithDescriptor:offset:error:`: arg shape (only <=5 object args modelled)
// SKIPPED `addDebugMarker:range:`: arg shape (only <=5 object args modelled)
// SKIPPED `newRemoteBufferViewForDevice:`: arg shape (only <=5 object args modelled)
// SKIPPED `remoteStorageBuffer`: return `API_AVAILABLE id<MTLBuffer>` not modelled
// SKIPPED `gpuAddress`: return `MTLGPUAddress` not modelled
fn Buffer_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn Buffer_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }
fn Buffer_noop_v_0(user: *u8) { return; }

struct Buffer {
    contents: fn(*u8) -> *u8,
    remove_all_debug_markers: fn(*u8),
    length: fn(*u8) -> u64,
    sparse_buffer_tier: fn(*u8) -> u64,
    opaque user: *u8,
}

fn Buffer_new(user: *u8) -> Buffer {
    return Buffer { contents: Buffer_noop_id_0, remove_all_debug_markers: Buffer_noop_v_0, length: Buffer_noop_u64_0, sparse_buffer_tier: Buffer_noop_u64_0, user: user };
}

impl Buffer {
    fn set_contents(ref this, handler: fn(*u8) -> *u8) {
        this.contents = handler;
        return;
    }
    fn set_remove_all_debug_markers(ref this, handler: fn(*u8)) {
        this.remove_all_debug_markers = handler;
        return;
    }
    fn set_length(ref this, handler: fn(*u8) -> u64) {
        this.length = handler;
        return;
    }
    fn set_sparse_buffer_tier(ref this, handler: fn(*u8) -> u64) {
        this.sparse_buffer_tier = handler;
        return;
    }
}

fn Buffer_key() -> *u8 { return { 28672 as *u8 }; }

fn Buffer_contents_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *Buffer = { synth::get_associated(self_obj, Buffer_key()) as *Buffer };
    let f: fn(*u8) -> *u8 = { (*c).contents };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Buffer_remove_all_debug_markers_imp(self_obj: *u8, cmd: *u8) {
    let c: *Buffer = { synth::get_associated(self_obj, Buffer_key()) as *Buffer };
    let f: fn(*u8) = { (*c).remove_all_debug_markers };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn Buffer_length_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Buffer = { synth::get_associated(self_obj, Buffer_key()) as *Buffer };
    let f: fn(*u8) -> u64 = { (*c).length };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Buffer_sparse_buffer_tier_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Buffer = { synth::get_associated(self_obj, Buffer_key()) as *Buffer };
    let f: fn(*u8) -> u64 = { (*c).sparse_buffer_tier };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_Buffer(ctx: *Buffer) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_Buffer\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_contents: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("contents\0")), Buffer_contents_imp, #str_ptr("@@:\0"));
        let add_remove_all_debug_markers: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("removeAllDebugMarkers\0")), Buffer_remove_all_debug_markers_imp, #str_ptr("v@:\0"));
        let add_length: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("length\0")), Buffer_length_imp, #str_ptr("Q@:\0"));
        let add_sparse_buffer_tier: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("sparseBufferTier\0")), Buffer_sparse_buffer_tier_imp, #str_ptr("Q@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, Buffer_key(), { ctx as *u8 });
    return d;
}

