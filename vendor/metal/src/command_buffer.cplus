// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLCommandBuffer.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLCommandBufferErrorOption` (NS_ENUM)
enum CommandBufferErrorOption {
    None,
    EncoderExecutionStatus,
}

fn command_buffer_error_option_raw(v: CommandBufferErrorOption) -> i64 {
    return match v {
        CommandBufferErrorOption::None => { 0 as i64 },
        CommandBufferErrorOption::EncoderExecutionStatus => { 1 as i64 },
    };
}

fn command_buffer_error_option_from_raw(raw: i64) -> CommandBufferErrorOption {
    if raw == (1 as i64) { return CommandBufferErrorOption::EncoderExecutionStatus; }
    return CommandBufferErrorOption::None;
}

// `MTLRenderPassDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct RenderPassDescriptor {
    opaque _obj: *u8,
}

impl RenderPassDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

}

// `MTLComputePassDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ComputePassDescriptor {
    opaque _obj: *u8,
}

impl ComputePassDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

}

// `MTLBlitPassDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct BlitPassDescriptor {
    opaque _obj: *u8,
}

impl BlitPassDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

}

// `MTLResourceStatePassDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ResourceStatePassDescriptor {
    opaque _obj: *u8,
}

impl ResourceStatePassDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

}

// `MTLAccelerationStructurePassDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct AccelerationStructurePassDescriptor {
    opaque _obj: *u8,
}

impl AccelerationStructurePassDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

}

// `MTLCommandBufferDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct CommandBufferDescriptor {
    opaque _obj: *u8,
}

impl CommandBufferDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

    fn retained_references(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("retainedReferences\0"))) != (0 as i8);
    }

    fn set_retained_references(this, retained_references: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setRetainedReferences:\0")), retained_references as i8);
    }

    fn error_options(this) -> CommandBufferErrorOption {
        return command_buffer_error_option_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("errorOptions\0"))));
    }

    // SKIPPED `setErrorOptions:`: (return, arg) shape not yet modelled
    // SKIPPED `logState`: return `id<MTLLogState> _Nullable` — generic collection
    // SKIPPED `setLogState:`: param `id<MTLLogState> _Nullable` — generic collection
}

// `MTLCommandBufferEncoderInfo` delegate protocol -> runtime class-synthesis helper.
fn CommandBufferEncoderInfo_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn CommandBufferEncoderInfo_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }

struct CommandBufferEncoderInfo {
    label: fn(*u8) -> *u8,
    debug_signposts: fn(*u8) -> *u8,
    error_state: fn(*u8) -> u64,
    opaque user: *u8,
}

fn CommandBufferEncoderInfo_new(user: *u8) -> CommandBufferEncoderInfo {
    return CommandBufferEncoderInfo { label: CommandBufferEncoderInfo_noop_id_0, debug_signposts: CommandBufferEncoderInfo_noop_id_0, error_state: CommandBufferEncoderInfo_noop_u64_0, user: user };
}

impl CommandBufferEncoderInfo {
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
    fn set_debug_signposts(ref this, handler: fn(*u8) -> *u8) {
        this.debug_signposts = handler;
        return;
    }
    fn set_error_state(ref this, handler: fn(*u8) -> u64) {
        this.error_state = handler;
        return;
    }
}

fn CommandBufferEncoderInfo_key() -> *u8 { return { 28672 as *u8 }; }

fn CommandBufferEncoderInfo_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *CommandBufferEncoderInfo = { synth::get_associated(self_obj, CommandBufferEncoderInfo_key()) as *CommandBufferEncoderInfo };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn CommandBufferEncoderInfo_debug_signposts_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *CommandBufferEncoderInfo = { synth::get_associated(self_obj, CommandBufferEncoderInfo_key()) as *CommandBufferEncoderInfo };
    let f: fn(*u8) -> *u8 = { (*c).debug_signposts };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn CommandBufferEncoderInfo_error_state_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *CommandBufferEncoderInfo = { synth::get_associated(self_obj, CommandBufferEncoderInfo_key()) as *CommandBufferEncoderInfo };
    let f: fn(*u8) -> u64 = { (*c).error_state };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_CommandBufferEncoderInfo(ctx: *CommandBufferEncoderInfo) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_CommandBufferEncoderInfo\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), CommandBufferEncoderInfo_label_imp, #str_ptr("@@:\0"));
        let add_debug_signposts: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("debugSignposts\0")), CommandBufferEncoderInfo_debug_signposts_imp, #str_ptr("@@:\0"));
        let add_error_state: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("errorState\0")), CommandBufferEncoderInfo_error_state_imp, #str_ptr("Q@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, CommandBufferEncoderInfo_key(), { ctx as *u8 });
    return d;
}

// `MTLCommandBuffer` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `addScheduledHandler:`: arg shape (only <=5 object args modelled)
// SKIPPED `presentDrawable:`: arg shape (only <=5 object args modelled)
// SKIPPED `presentDrawable:atTime:`: arg shape (only <=5 object args modelled)
// SKIPPED `presentDrawable:afterMinimumDuration:`: arg shape (only <=5 object args modelled)
// SKIPPED `addCompletedHandler:`: arg shape (only <=5 object args modelled)
// SKIPPED `blitCommandEncoder`: return `id<MTLBlitCommandEncoder> _Nullable` not modelled
// SKIPPED `renderCommandEncoderWithDescriptor:`: return `id<MTLRenderCommandEncoder> _Nullable` not modelled
// SKIPPED `computeCommandEncoderWithDescriptor:`: return `id<MTLComputeCommandEncoder> _Nullable` not modelled
// SKIPPED `blitCommandEncoderWithDescriptor:`: return `id<MTLBlitCommandEncoder> _Nullable` not modelled
// SKIPPED `computeCommandEncoder`: return `id<MTLComputeCommandEncoder> _Nullable` not modelled
// SKIPPED `computeCommandEncoderWithDispatchType:`: arg shape (only <=5 object args modelled)
// SKIPPED `encodeWaitForEvent:value:`: arg shape (only <=5 object args modelled)
// SKIPPED `encodeSignalEvent:value:`: arg shape (only <=5 object args modelled)
// SKIPPED `parallelRenderCommandEncoderWithDescriptor:`: return `id<MTLParallelRenderCommandEncoder> _Nullable` not modelled
// SKIPPED `resourceStateCommandEncoder`: return `id<MTLResourceStateCommandEncoder> _Nullable` not modelled
// SKIPPED `resourceStateCommandEncoderWithDescriptor:`: return `id<MTLResourceStateCommandEncoder> _Nullable` not modelled
// SKIPPED `accelerationStructureCommandEncoder`: return `id<MTLAccelerationStructureCommandEncoder> _Nullable` not modelled
// SKIPPED `accelerationStructureCommandEncoderWithDescriptor:`: return `id<MTLAccelerationStructureCommandEncoder> _Nonnull` not modelled
// SKIPPED `useResidencySet:`: arg shape (only <=5 object args modelled)
// SKIPPED `useResidencySets:count:`: arg shape (only <=5 object args modelled)
// SKIPPED `device`: return `id<MTLDevice> _Nonnull` not modelled
// SKIPPED `commandQueue`: return `id<MTLCommandQueue> _Nonnull` not modelled
// SKIPPED `retainedReferences`: no `i8`-return / 0-arg IMP shape
// SKIPPED `kernelStartTime`: return `CFTimeInterval` not modelled
// SKIPPED `kernelEndTime`: return `CFTimeInterval` not modelled
// SKIPPED `logs`: return `id<MTLLogContainer> _Nonnull` not modelled
// SKIPPED `GPUStartTime`: return `CFTimeInterval` not modelled
// SKIPPED `GPUEndTime`: return `CFTimeInterval` not modelled
fn CommandBuffer_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn CommandBuffer_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }
fn CommandBuffer_noop_v_0(user: *u8) { return; }
fn CommandBuffer_noop_v_1(user: *u8, a0: *u8) { return; }

struct CommandBuffer {
    enqueue: fn(*u8),
    commit: fn(*u8),
    wait_until_scheduled: fn(*u8),
    wait_until_completed: fn(*u8),
    push_debug_group: fn(*u8, *u8),
    pop_debug_group: fn(*u8),
    error_options: fn(*u8) -> u64,
    label: fn(*u8) -> *u8,
    set_label: fn(*u8, *u8),
    status: fn(*u8) -> u64,
    error: fn(*u8) -> *u8,
    opaque user: *u8,
}

fn CommandBuffer_new(user: *u8) -> CommandBuffer {
    return CommandBuffer { enqueue: CommandBuffer_noop_v_0, commit: CommandBuffer_noop_v_0, wait_until_scheduled: CommandBuffer_noop_v_0, wait_until_completed: CommandBuffer_noop_v_0, push_debug_group: CommandBuffer_noop_v_1, pop_debug_group: CommandBuffer_noop_v_0, error_options: CommandBuffer_noop_u64_0, label: CommandBuffer_noop_id_0, set_label: CommandBuffer_noop_v_1, status: CommandBuffer_noop_u64_0, error: CommandBuffer_noop_id_0, user: user };
}

impl CommandBuffer {
    fn set_enqueue(ref this, handler: fn(*u8)) {
        this.enqueue = handler;
        return;
    }
    fn set_commit(ref this, handler: fn(*u8)) {
        this.commit = handler;
        return;
    }
    fn set_wait_until_scheduled(ref this, handler: fn(*u8)) {
        this.wait_until_scheduled = handler;
        return;
    }
    fn set_wait_until_completed(ref this, handler: fn(*u8)) {
        this.wait_until_completed = handler;
        return;
    }
    fn set_push_debug_group(ref this, handler: fn(*u8, *u8)) {
        this.push_debug_group = handler;
        return;
    }
    fn set_pop_debug_group(ref this, handler: fn(*u8)) {
        this.pop_debug_group = handler;
        return;
    }
    fn set_error_options(ref this, handler: fn(*u8) -> u64) {
        this.error_options = handler;
        return;
    }
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
    fn set_set_label(ref this, handler: fn(*u8, *u8)) {
        this.set_label = handler;
        return;
    }
    fn set_status(ref this, handler: fn(*u8) -> u64) {
        this.status = handler;
        return;
    }
    fn set_error(ref this, handler: fn(*u8) -> *u8) {
        this.error = handler;
        return;
    }
}

fn CommandBuffer_key() -> *u8 { return { 28673 as *u8 }; }

fn CommandBuffer_enqueue_imp(self_obj: *u8, cmd: *u8) {
    let c: *CommandBuffer = { synth::get_associated(self_obj, CommandBuffer_key()) as *CommandBuffer };
    let f: fn(*u8) = { (*c).enqueue };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn CommandBuffer_commit_imp(self_obj: *u8, cmd: *u8) {
    let c: *CommandBuffer = { synth::get_associated(self_obj, CommandBuffer_key()) as *CommandBuffer };
    let f: fn(*u8) = { (*c).commit };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn CommandBuffer_wait_until_scheduled_imp(self_obj: *u8, cmd: *u8) {
    let c: *CommandBuffer = { synth::get_associated(self_obj, CommandBuffer_key()) as *CommandBuffer };
    let f: fn(*u8) = { (*c).wait_until_scheduled };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn CommandBuffer_wait_until_completed_imp(self_obj: *u8, cmd: *u8) {
    let c: *CommandBuffer = { synth::get_associated(self_obj, CommandBuffer_key()) as *CommandBuffer };
    let f: fn(*u8) = { (*c).wait_until_completed };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn CommandBuffer_push_debug_group_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *CommandBuffer = { synth::get_associated(self_obj, CommandBuffer_key()) as *CommandBuffer };
    let f: fn(*u8, *u8) = { (*c).push_debug_group };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn CommandBuffer_pop_debug_group_imp(self_obj: *u8, cmd: *u8) {
    let c: *CommandBuffer = { synth::get_associated(self_obj, CommandBuffer_key()) as *CommandBuffer };
    let f: fn(*u8) = { (*c).pop_debug_group };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn CommandBuffer_error_options_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *CommandBuffer = { synth::get_associated(self_obj, CommandBuffer_key()) as *CommandBuffer };
    let f: fn(*u8) -> u64 = { (*c).error_options };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn CommandBuffer_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *CommandBuffer = { synth::get_associated(self_obj, CommandBuffer_key()) as *CommandBuffer };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn CommandBuffer_set_label_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *CommandBuffer = { synth::get_associated(self_obj, CommandBuffer_key()) as *CommandBuffer };
    let f: fn(*u8, *u8) = { (*c).set_label };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn CommandBuffer_status_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *CommandBuffer = { synth::get_associated(self_obj, CommandBuffer_key()) as *CommandBuffer };
    let f: fn(*u8) -> u64 = { (*c).status };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn CommandBuffer_error_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *CommandBuffer = { synth::get_associated(self_obj, CommandBuffer_key()) as *CommandBuffer };
    let f: fn(*u8) -> *u8 = { (*c).error };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_CommandBuffer(ctx: *CommandBuffer) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_CommandBuffer\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_enqueue: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("enqueue\0")), CommandBuffer_enqueue_imp, #str_ptr("v@:\0"));
        let add_commit: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("commit\0")), CommandBuffer_commit_imp, #str_ptr("v@:\0"));
        let add_wait_until_scheduled: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("waitUntilScheduled\0")), CommandBuffer_wait_until_scheduled_imp, #str_ptr("v@:\0"));
        let add_wait_until_completed: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("waitUntilCompleted\0")), CommandBuffer_wait_until_completed_imp, #str_ptr("v@:\0"));
        let add_push_debug_group: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("pushDebugGroup:\0")), CommandBuffer_push_debug_group_imp, #str_ptr("v@:@\0"));
        let add_pop_debug_group: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("popDebugGroup\0")), CommandBuffer_pop_debug_group_imp, #str_ptr("v@:\0"));
        let add_error_options: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("errorOptions\0")), CommandBuffer_error_options_imp, #str_ptr("Q@:\0"));
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), CommandBuffer_label_imp, #str_ptr("@@:\0"));
        let add_set_label: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("setLabel:\0")), CommandBuffer_set_label_imp, #str_ptr("v@:@\0"));
        let add_status: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("status\0")), CommandBuffer_status_imp, #str_ptr("Q@:\0"));
        let add_error: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("error\0")), CommandBuffer_error_imp, #str_ptr("@@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, CommandBuffer_key(), { ctx as *u8 });
    return d;
}

