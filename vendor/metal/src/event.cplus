// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLEvent.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLSharedEventListener` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct SharedEventListener {
    _obj: *u8,
}

impl SharedEventListener {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> SharedEventListener { return SharedEventListener { _obj: ptr }; }

    fn new() -> SharedEventListener {
        let cls: *u8 = rt::get_class(#str_ptr("MTLSharedEventListener\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        return SharedEventListener { _obj: rt::msg_id(alloced, rt::sel(#str_ptr("init\0"))) };
    }

    // SKIPPED `initWithDispatchQueue:`: extra init variant (one `new` per type)
    fn shared_listener() -> SharedEventListener {
        return SharedEventListener { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("MTLSharedEventListener\0")), rt::sel(#str_ptr("sharedListener\0")))) };
    }

    // SKIPPED `dispatchQueue`: return `dispatch_queue_t _Nonnull` — unmapped type `dispatch_queue_t`
    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MTLSharedEventHandle` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct SharedEventHandle {
    opaque _obj: *u8,
}

impl SharedEventHandle {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> SharedEventHandle { return SharedEventHandle { _obj: ptr }; }

    fn label(this) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("label\0")));
        return bridge::to_text_option(ns);
    }

}

// `MTLEvent` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `device`: return `id<MTLDevice> _Nullable` not modelled
fn Event_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn Event_noop_v_1(user: *u8, a0: *u8) { return; }

struct Event {
    label: fn(*u8) -> *u8,
    set_label: fn(*u8, *u8),
    opaque user: *u8,
}

fn Event_new(user: *u8) -> Event {
    return Event { label: Event_noop_id_0, set_label: Event_noop_v_1, user: user };
}

impl Event {
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
    fn set_set_label(ref this, handler: fn(*u8, *u8)) {
        this.set_label = handler;
        return;
    }
}

fn Event_key() -> *u8 { return { 28672 as *u8 }; }

fn Event_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *Event = { synth::get_associated(self_obj, Event_key()) as *Event };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Event_set_label_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *Event = { synth::get_associated(self_obj, Event_key()) as *Event };
    let f: fn(*u8, *u8) = { (*c).set_label };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn create_Event(ctx: *Event) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_Event\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), Event_label_imp, #str_ptr("@@:\0"));
        let add_set_label: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("setLabel:\0")), Event_set_label_imp, #str_ptr("v@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, Event_key(), { ctx as *u8 });
    return d;
}

// `MTLSharedEvent` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `notifyListener:atValue:block:`: arg shape (only <=5 object args modelled)
// SKIPPED `waitUntilSignaledValue:timeoutMS:`: arg shape (only <=5 object args modelled)
// SKIPPED `signaledValue`: return `uint64_t` not modelled
// SKIPPED `setSignaledValue:`: arg shape (only <=5 object args modelled)
fn SharedEventProtocol_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }

struct SharedEventProtocol {
    new_shared_event_handle: fn(*u8) -> *u8,
    opaque user: *u8,
}

fn SharedEventProtocol_new(user: *u8) -> SharedEventProtocol {
    return SharedEventProtocol { new_shared_event_handle: SharedEventProtocol_noop_id_0, user: user };
}

impl SharedEventProtocol {
    fn set_new_shared_event_handle(ref this, handler: fn(*u8) -> *u8) {
        this.new_shared_event_handle = handler;
        return;
    }
}

fn SharedEventProtocol_key() -> *u8 { return { 28673 as *u8 }; }

fn SharedEventProtocol_new_shared_event_handle_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *SharedEventProtocol = { synth::get_associated(self_obj, SharedEventProtocol_key()) as *SharedEventProtocol };
    let f: fn(*u8) -> *u8 = { (*c).new_shared_event_handle };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_SharedEventProtocol(ctx: *SharedEventProtocol) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_SharedEventProtocol\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_new_shared_event_handle: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("newSharedEventHandle\0")), SharedEventProtocol_new_shared_event_handle_imp, #str_ptr("@@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, SharedEventProtocol_key(), { ctx as *u8 });
    return d;
}

