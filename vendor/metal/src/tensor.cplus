// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLTensor.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLTensorDataType` (NS_ENUM)
enum TensorDataType {
    None,
    Float32,
    Float16,
    BFloat16,
    Int8,
    UInt8,
    Int16,
    UInt16,
    Int32,
    UInt32,
    Int4,
    UInt4,
}

fn tensor_data_type_raw(v: TensorDataType) -> i64 {
    return match v {
        TensorDataType::None => { 0 as i64 },
        TensorDataType::Float32 => { 3 as i64 },
        TensorDataType::Float16 => { 16 as i64 },
        TensorDataType::BFloat16 => { 121 as i64 },
        TensorDataType::Int8 => { 45 as i64 },
        TensorDataType::UInt8 => { 49 as i64 },
        TensorDataType::Int16 => { 37 as i64 },
        TensorDataType::UInt16 => { 41 as i64 },
        TensorDataType::Int32 => { 29 as i64 },
        TensorDataType::UInt32 => { 33 as i64 },
        TensorDataType::Int4 => { 143 as i64 },
        TensorDataType::UInt4 => { 144 as i64 },
    };
}

fn tensor_data_type_from_raw(raw: i64) -> TensorDataType {
    if raw == (3 as i64) { return TensorDataType::Float32; }
    if raw == (16 as i64) { return TensorDataType::Float16; }
    if raw == (121 as i64) { return TensorDataType::BFloat16; }
    if raw == (45 as i64) { return TensorDataType::Int8; }
    if raw == (49 as i64) { return TensorDataType::UInt8; }
    if raw == (37 as i64) { return TensorDataType::Int16; }
    if raw == (41 as i64) { return TensorDataType::UInt16; }
    if raw == (29 as i64) { return TensorDataType::Int32; }
    if raw == (33 as i64) { return TensorDataType::UInt32; }
    if raw == (143 as i64) { return TensorDataType::Int4; }
    if raw == (144 as i64) { return TensorDataType::UInt4; }
    return TensorDataType::None;
}

// `MTLTensorUsage` (NS_OPTIONS) — bitfield; combine with `|`.
const tensor_usage_compute: u64 = 1 as u64;
const tensor_usage_render: u64 = 2 as u64;
const tensor_usage_machine_learning: u64 = 4 as u64;

// `MTLResourceOptions` (NS_OPTIONS) — bitfield; combine with `|`.
const resource_options_m_t_l_resource_c_p_u_cache_mode_default_cache: u64 = 0 as u64;
const resource_options_m_t_l_resource_c_p_u_cache_mode_write_combined: u64 = 1 as u64;
const resource_options_m_t_l_resource_storage_mode_shared: u64 = 0 as u64;
const resource_options_m_t_l_resource_storage_mode_managed: u64 = 16 as u64;
const resource_options_m_t_l_resource_storage_mode_private: u64 = 32 as u64;
const resource_options_m_t_l_resource_storage_mode_memoryless: u64 = 48 as u64;
const resource_options_m_t_l_resource_hazard_tracking_mode_default: u64 = 0 as u64;
const resource_options_m_t_l_resource_hazard_tracking_mode_untracked: u64 = 256 as u64;
const resource_options_m_t_l_resource_hazard_tracking_mode_tracked: u64 = 512 as u64;
const resource_options_m_t_l_resource_option_c_p_u_cache_mode_default: u64 = 0 as u64;
const resource_options_m_t_l_resource_option_c_p_u_cache_mode_write_combined: u64 = 1 as u64;

// `MTLCPUCacheMode` (NS_ENUM)
enum CPUCacheMode {
    DefaultCache,
    WriteCombined,
}

fn c_p_u_cache_mode_raw(v: CPUCacheMode) -> i64 {
    return match v {
        CPUCacheMode::DefaultCache => { 0 as i64 },
        CPUCacheMode::WriteCombined => { 1 as i64 },
    };
}

fn c_p_u_cache_mode_from_raw(raw: i64) -> CPUCacheMode {
    if raw == (1 as i64) { return CPUCacheMode::WriteCombined; }
    return CPUCacheMode::DefaultCache;
}

// `MTLStorageMode` (NS_ENUM)
enum StorageMode {
    Shared,
    Managed,
    Private,
    Memoryless,
}

fn storage_mode_raw(v: StorageMode) -> i64 {
    return match v {
        StorageMode::Shared => { 0 as i64 },
        StorageMode::Managed => { 1 as i64 },
        StorageMode::Private => { 2 as i64 },
        StorageMode::Memoryless => { 3 as i64 },
    };
}

fn storage_mode_from_raw(raw: i64) -> StorageMode {
    if raw == (1 as i64) { return StorageMode::Managed; }
    if raw == (2 as i64) { return StorageMode::Private; }
    if raw == (3 as i64) { return StorageMode::Memoryless; }
    return StorageMode::Shared;
}

// `MTLHazardTrackingMode` (NS_ENUM)
enum HazardTrackingMode {
    Default,
    Untracked,
    Tracked,
}

fn hazard_tracking_mode_raw(v: HazardTrackingMode) -> i64 {
    return match v {
        HazardTrackingMode::Default => { 0 as i64 },
        HazardTrackingMode::Untracked => { 1 as i64 },
        HazardTrackingMode::Tracked => { 2 as i64 },
    };
}

fn hazard_tracking_mode_from_raw(raw: i64) -> HazardTrackingMode {
    if raw == (1 as i64) { return HazardTrackingMode::Untracked; }
    if raw == (2 as i64) { return HazardTrackingMode::Tracked; }
    return HazardTrackingMode::Default;
}

// `MTLTensorExtents` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct TensorExtents {
    _obj: *u8,
}

impl TensorExtents {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TensorExtents { return TensorExtents { _obj: ptr }; }

    // SKIPPED `initWithRank:values:`: init arg shape not yet modelled
    // SKIPPED `extentAtDimensionIndex:`: (return, arg) shape not yet modelled
    fn rank(this) -> u64 {
        return rt::msg_u64(this._obj, rt::sel(#str_ptr("rank\0")));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `MTLTensorDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TensorDescriptor {
    opaque _obj: *u8,
}

impl TensorDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TensorDescriptor { return TensorDescriptor { _obj: ptr }; }

    fn dimensions(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("dimensions\0")));
    }

    fn set_dimensions(this, dimensions: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setDimensions:\0")), dimensions);
    }

    fn strides(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("strides\0")));
        return bridge::obj_option(obj);
    }

    fn set_strides(this, strides: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setStrides:\0")), strides);
    }

    fn data_type(this) -> TensorDataType {
        return tensor_data_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("dataType\0"))));
    }

    // SKIPPED `setDataType:`: (return, arg) shape not yet modelled
    fn usage(this) -> u64 {
        return rt::msg_u64(this._obj, rt::sel(#str_ptr("usage\0")));
    }

    // SKIPPED `setUsage:`: (return, arg) shape not yet modelled
    fn resource_options(this) -> u64 {
        return rt::msg_u64(this._obj, rt::sel(#str_ptr("resourceOptions\0")));
    }

    // SKIPPED `setResourceOptions:`: (return, arg) shape not yet modelled
    fn cpu_cache_mode(this) -> CPUCacheMode {
        return c_p_u_cache_mode_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("cpuCacheMode\0"))));
    }

    // SKIPPED `setCpuCacheMode:`: (return, arg) shape not yet modelled
    fn storage_mode(this) -> StorageMode {
        return storage_mode_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("storageMode\0"))));
    }

    // SKIPPED `setStorageMode:`: (return, arg) shape not yet modelled
    fn hazard_tracking_mode(this) -> HazardTrackingMode {
        return hazard_tracking_mode_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("hazardTrackingMode\0"))));
    }

    // SKIPPED `setHazardTrackingMode:`: (return, arg) shape not yet modelled
}

// `MTLTensor` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `gpuResourceID`: return `MTLResourceID` not modelled
// SKIPPED `buffer`: return `id<MTLBuffer> _Nullable` not modelled
fn Tensor_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn Tensor_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }
fn Tensor_noop_v_4(user: *u8, a0: *u8, a1: *u8, a2: *u8, a3: *u8) { return; }

struct Tensor {
    replace_slice_origin_slice_dimensions_with_bytes_strides: fn(*u8, *u8, *u8, *u8, *u8),
    get_bytes_strides_from_slice_origin_slice_dimensions: fn(*u8, *u8, *u8, *u8, *u8),
    buffer_offset: fn(*u8) -> u64,
    strides: fn(*u8) -> *u8,
    dimensions: fn(*u8) -> *u8,
    data_type: fn(*u8) -> u64,
    usage: fn(*u8) -> u64,
    opaque user: *u8,
}

fn Tensor_new(user: *u8) -> Tensor {
    return Tensor { replace_slice_origin_slice_dimensions_with_bytes_strides: Tensor_noop_v_4, get_bytes_strides_from_slice_origin_slice_dimensions: Tensor_noop_v_4, buffer_offset: Tensor_noop_u64_0, strides: Tensor_noop_id_0, dimensions: Tensor_noop_id_0, data_type: Tensor_noop_u64_0, usage: Tensor_noop_u64_0, user: user };
}

impl Tensor {
    fn set_replace_slice_origin_slice_dimensions_with_bytes_strides(ref this, handler: fn(*u8, *u8, *u8, *u8, *u8)) {
        this.replace_slice_origin_slice_dimensions_with_bytes_strides = handler;
        return;
    }
    fn set_get_bytes_strides_from_slice_origin_slice_dimensions(ref this, handler: fn(*u8, *u8, *u8, *u8, *u8)) {
        this.get_bytes_strides_from_slice_origin_slice_dimensions = handler;
        return;
    }
    fn set_buffer_offset(ref this, handler: fn(*u8) -> u64) {
        this.buffer_offset = handler;
        return;
    }
    fn set_strides(ref this, handler: fn(*u8) -> *u8) {
        this.strides = handler;
        return;
    }
    fn set_dimensions(ref this, handler: fn(*u8) -> *u8) {
        this.dimensions = handler;
        return;
    }
    fn set_data_type(ref this, handler: fn(*u8) -> u64) {
        this.data_type = handler;
        return;
    }
    fn set_usage(ref this, handler: fn(*u8) -> u64) {
        this.usage = handler;
        return;
    }
}

fn Tensor_key() -> *u8 { return { 28672 as *u8 }; }

fn Tensor_replace_slice_origin_slice_dimensions_with_bytes_strides_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8, a2: *u8, a3: *u8) {
    let c: *Tensor = { synth::get_associated(self_obj, Tensor_key()) as *Tensor };
    let f: fn(*u8, *u8, *u8, *u8, *u8) = { (*c).replace_slice_origin_slice_dimensions_with_bytes_strides };
    let u: *u8 = { (*c).user };
    f(u, a0, a1, a2, a3);
    return;
}

fn Tensor_get_bytes_strides_from_slice_origin_slice_dimensions_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8, a2: *u8, a3: *u8) {
    let c: *Tensor = { synth::get_associated(self_obj, Tensor_key()) as *Tensor };
    let f: fn(*u8, *u8, *u8, *u8, *u8) = { (*c).get_bytes_strides_from_slice_origin_slice_dimensions };
    let u: *u8 = { (*c).user };
    f(u, a0, a1, a2, a3);
    return;
}

fn Tensor_buffer_offset_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Tensor = { synth::get_associated(self_obj, Tensor_key()) as *Tensor };
    let f: fn(*u8) -> u64 = { (*c).buffer_offset };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Tensor_strides_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *Tensor = { synth::get_associated(self_obj, Tensor_key()) as *Tensor };
    let f: fn(*u8) -> *u8 = { (*c).strides };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Tensor_dimensions_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *Tensor = { synth::get_associated(self_obj, Tensor_key()) as *Tensor };
    let f: fn(*u8) -> *u8 = { (*c).dimensions };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Tensor_data_type_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Tensor = { synth::get_associated(self_obj, Tensor_key()) as *Tensor };
    let f: fn(*u8) -> u64 = { (*c).data_type };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Tensor_usage_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Tensor = { synth::get_associated(self_obj, Tensor_key()) as *Tensor };
    let f: fn(*u8) -> u64 = { (*c).usage };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_Tensor(ctx: *Tensor) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_Tensor\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_replace_slice_origin_slice_dimensions_with_bytes_strides: i8 = synth::add_method_v_4id(cls, rt::sel(#str_ptr("replaceSliceOrigin:sliceDimensions:withBytes:strides:\0")), Tensor_replace_slice_origin_slice_dimensions_with_bytes_strides_imp, #str_ptr("v@:@@@@\0"));
        let add_get_bytes_strides_from_slice_origin_slice_dimensions: i8 = synth::add_method_v_4id(cls, rt::sel(#str_ptr("getBytes:strides:fromSliceOrigin:sliceDimensions:\0")), Tensor_get_bytes_strides_from_slice_origin_slice_dimensions_imp, #str_ptr("v@:@@@@\0"));
        let add_buffer_offset: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("bufferOffset\0")), Tensor_buffer_offset_imp, #str_ptr("Q@:\0"));
        let add_strides: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("strides\0")), Tensor_strides_imp, #str_ptr("@@:\0"));
        let add_dimensions: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("dimensions\0")), Tensor_dimensions_imp, #str_ptr("@@:\0"));
        let add_data_type: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("dataType\0")), Tensor_data_type_imp, #str_ptr("Q@:\0"));
        let add_usage: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("usage\0")), Tensor_usage_imp, #str_ptr("Q@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, Tensor_key(), { ctx as *u8 });
    return d;
}

