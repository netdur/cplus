// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLCounters.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLStorageMode` (NS_ENUM)
enum StorageMode {
    Shared,
    Managed,
    Private,
    Memoryless,
}

fn storage_mode_raw(v: StorageMode) -> i64 {
    return match v {
        StorageMode::Shared => { 0 as i64 },
        StorageMode::Managed => { 1 as i64 },
        StorageMode::Private => { 2 as i64 },
        StorageMode::Memoryless => { 3 as i64 },
    };
}

fn storage_mode_from_raw(raw: i64) -> StorageMode {
    if raw == (1 as i64) { return StorageMode::Managed; }
    if raw == (2 as i64) { return StorageMode::Private; }
    if raw == (3 as i64) { return StorageMode::Memoryless; }
    return StorageMode::Shared;
}

// `MTLCounterSampleBufferDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct CounterSampleBufferDescriptor {
    opaque _obj: *u8,
}

impl CounterSampleBufferDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

    // SKIPPED `counterSet`: return `id<MTLCounterSet> _Nullable` — generic collection
    // SKIPPED `setCounterSet:`: param `id<MTLCounterSet> _Nullable` — generic collection
    fn label(this) -> text::Text {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("label\0")));
        return bridge::to_text(ns);
    }

    fn set_label(this, label: str) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setLabel:\0")), bridge::nsstring(label));
    }

    fn storage_mode(this) -> StorageMode {
        return storage_mode_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("storageMode\0"))));
    }

    // SKIPPED `setStorageMode:`: (return, arg) shape not yet modelled
    fn sample_count(this) -> u64 {
        return rt::msg_u64(this._obj, rt::sel(#str_ptr("sampleCount\0")));
    }

    // SKIPPED `setSampleCount:`: (return, arg) shape not yet modelled
}

// `MTLCounter` delegate protocol -> runtime class-synthesis helper.
fn Counter_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }

struct Counter {
    name: fn(*u8) -> *u8,
    opaque user: *u8,
}

fn Counter_new(user: *u8) -> Counter {
    return Counter { name: Counter_noop_id_0, user: user };
}

impl Counter {
    fn set_name(ref this, handler: fn(*u8) -> *u8) {
        this.name = handler;
        return;
    }
}

fn Counter_key() -> *u8 { return { 28672 as *u8 }; }

fn Counter_name_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *Counter = { synth::get_associated(self_obj, Counter_key()) as *Counter };
    let f: fn(*u8) -> *u8 = { (*c).name };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_Counter(ctx: *Counter) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_Counter\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_name: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("name\0")), Counter_name_imp, #str_ptr("@@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, Counter_key(), { ctx as *u8 });
    return d;
}

// `MTLCounterSet` delegate protocol -> runtime class-synthesis helper.
fn CounterSet_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }

struct CounterSet {
    name: fn(*u8) -> *u8,
    counters: fn(*u8) -> *u8,
    opaque user: *u8,
}

fn CounterSet_new(user: *u8) -> CounterSet {
    return CounterSet { name: CounterSet_noop_id_0, counters: CounterSet_noop_id_0, user: user };
}

impl CounterSet {
    fn set_name(ref this, handler: fn(*u8) -> *u8) {
        this.name = handler;
        return;
    }
    fn set_counters(ref this, handler: fn(*u8) -> *u8) {
        this.counters = handler;
        return;
    }
}

fn CounterSet_key() -> *u8 { return { 28673 as *u8 }; }

fn CounterSet_name_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *CounterSet = { synth::get_associated(self_obj, CounterSet_key()) as *CounterSet };
    let f: fn(*u8) -> *u8 = { (*c).name };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn CounterSet_counters_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *CounterSet = { synth::get_associated(self_obj, CounterSet_key()) as *CounterSet };
    let f: fn(*u8) -> *u8 = { (*c).counters };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_CounterSet(ctx: *CounterSet) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_CounterSet\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_name: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("name\0")), CounterSet_name_imp, #str_ptr("@@:\0"));
        let add_counters: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("counters\0")), CounterSet_counters_imp, #str_ptr("@@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, CounterSet_key(), { ctx as *u8 });
    return d;
}

// `MTLCounterSampleBuffer` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `resolveCounterRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `device`: return `id<MTLDevice> _Nonnull` not modelled
fn CounterSampleBuffer_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn CounterSampleBuffer_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }

struct CounterSampleBuffer {
    label: fn(*u8) -> *u8,
    sample_count: fn(*u8) -> u64,
    opaque user: *u8,
}

fn CounterSampleBuffer_new(user: *u8) -> CounterSampleBuffer {
    return CounterSampleBuffer { label: CounterSampleBuffer_noop_id_0, sample_count: CounterSampleBuffer_noop_u64_0, user: user };
}

impl CounterSampleBuffer {
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
    fn set_sample_count(ref this, handler: fn(*u8) -> u64) {
        this.sample_count = handler;
        return;
    }
}

fn CounterSampleBuffer_key() -> *u8 { return { 28674 as *u8 }; }

fn CounterSampleBuffer_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *CounterSampleBuffer = { synth::get_associated(self_obj, CounterSampleBuffer_key()) as *CounterSampleBuffer };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn CounterSampleBuffer_sample_count_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *CounterSampleBuffer = { synth::get_associated(self_obj, CounterSampleBuffer_key()) as *CounterSampleBuffer };
    let f: fn(*u8) -> u64 = { (*c).sample_count };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_CounterSampleBuffer(ctx: *CounterSampleBuffer) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_CounterSampleBuffer\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), CounterSampleBuffer_label_imp, #str_ptr("@@:\0"));
        let add_sample_count: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("sampleCount\0")), CounterSampleBuffer_sample_count_imp, #str_ptr("Q@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, CounterSampleBuffer_key(), { ctx as *u8 });
    return d;
}

