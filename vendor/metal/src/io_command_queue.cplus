// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLIOCommandQueue.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLIOPriority` (NS_ENUM)
enum IOPriority {
    High,
    Normal,
    Low,
}

fn i_o_priority_raw(v: IOPriority) -> i64 {
    return match v {
        IOPriority::High => { 0 as i64 },
        IOPriority::Normal => { 1 as i64 },
        IOPriority::Low => { 2 as i64 },
    };
}

fn i_o_priority_from_raw(raw: i64) -> IOPriority {
    if raw == (1 as i64) { return IOPriority::Normal; }
    if raw == (2 as i64) { return IOPriority::Low; }
    return IOPriority::High;
}

// `MTLIOCommandQueueType` (NS_ENUM)
enum IOCommandQueueType {
    Concurrent,
    Serial,
}

fn i_o_command_queue_type_raw(v: IOCommandQueueType) -> i64 {
    return match v {
        IOCommandQueueType::Concurrent => { 0 as i64 },
        IOCommandQueueType::Serial => { 1 as i64 },
    };
}

fn i_o_command_queue_type_from_raw(raw: i64) -> IOCommandQueueType {
    if raw == (1 as i64) { return IOCommandQueueType::Serial; }
    return IOCommandQueueType::Concurrent;
}

// `MTLIOCommandQueueDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct IOCommandQueueDescriptor {
    opaque _obj: *u8,
}

impl IOCommandQueueDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> IOCommandQueueDescriptor { return IOCommandQueueDescriptor { _obj: ptr }; }

    fn max_command_buffer_count(this) -> u64 {
        return rt::msg_u64(this._obj, rt::sel(#str_ptr("maxCommandBufferCount\0")));
    }

    // SKIPPED `setMaxCommandBufferCount:`: (return, arg) shape not yet modelled
    fn priority(this) -> IOPriority {
        return i_o_priority_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("priority\0"))));
    }

    // SKIPPED `setPriority:`: (return, arg) shape not yet modelled
    fn type_(this) -> IOCommandQueueType {
        return i_o_command_queue_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("type\0"))));
    }

    // SKIPPED `setType:`: (return, arg) shape not yet modelled
    fn max_commands_in_flight(this) -> u64 {
        return rt::msg_u64(this._obj, rt::sel(#str_ptr("maxCommandsInFlight\0")));
    }

    // SKIPPED `setMaxCommandsInFlight:`: (return, arg) shape not yet modelled
    // SKIPPED `scratchBufferAllocator`: return `id<MTLIOScratchBufferAllocator> _Nullable` — generic collection
    // SKIPPED `setScratchBufferAllocator:`: param `id<MTLIOScratchBufferAllocator> _Nullable` — generic collection
}

// `MTLIOCommandQueue` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `commandBuffer`: return `id<MTLIOCommandBuffer> _Nonnull` not modelled
// SKIPPED `commandBufferWithUnretainedReferences`: return `id<MTLIOCommandBuffer> _Nonnull` not modelled
fn IOCommandQueue_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn IOCommandQueue_noop_v_0(user: *u8) { return; }
fn IOCommandQueue_noop_v_1(user: *u8, a0: *u8) { return; }

struct IOCommandQueue {
    enqueue_barrier: fn(*u8),
    label: fn(*u8) -> *u8,
    set_label: fn(*u8, *u8),
    opaque user: *u8,
}

fn IOCommandQueue_new(user: *u8) -> IOCommandQueue {
    return IOCommandQueue { enqueue_barrier: IOCommandQueue_noop_v_0, label: IOCommandQueue_noop_id_0, set_label: IOCommandQueue_noop_v_1, user: user };
}

impl IOCommandQueue {
    fn set_enqueue_barrier(ref this, handler: fn(*u8)) {
        this.enqueue_barrier = handler;
        return;
    }
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
    fn set_set_label(ref this, handler: fn(*u8, *u8)) {
        this.set_label = handler;
        return;
    }
}

fn IOCommandQueue_key() -> *u8 { return { 28672 as *u8 }; }

fn IOCommandQueue_enqueue_barrier_imp(self_obj: *u8, cmd: *u8) {
    let c: *IOCommandQueue = { synth::get_associated(self_obj, IOCommandQueue_key()) as *IOCommandQueue };
    let f: fn(*u8) = { (*c).enqueue_barrier };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn IOCommandQueue_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *IOCommandQueue = { synth::get_associated(self_obj, IOCommandQueue_key()) as *IOCommandQueue };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn IOCommandQueue_set_label_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *IOCommandQueue = { synth::get_associated(self_obj, IOCommandQueue_key()) as *IOCommandQueue };
    let f: fn(*u8, *u8) = { (*c).set_label };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn create_IOCommandQueue(ctx: *IOCommandQueue) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_IOCommandQueue\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_enqueue_barrier: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("enqueueBarrier\0")), IOCommandQueue_enqueue_barrier_imp, #str_ptr("v@:\0"));
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), IOCommandQueue_label_imp, #str_ptr("@@:\0"));
        let add_set_label: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("setLabel:\0")), IOCommandQueue_set_label_imp, #str_ptr("v@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, IOCommandQueue_key(), { ctx as *u8 });
    return d;
}

// `MTLIOFileHandle` delegate protocol -> runtime class-synthesis helper.
fn IOFileHandle_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn IOFileHandle_noop_v_1(user: *u8, a0: *u8) { return; }

struct IOFileHandle {
    label: fn(*u8) -> *u8,
    set_label: fn(*u8, *u8),
    opaque user: *u8,
}

fn IOFileHandle_new(user: *u8) -> IOFileHandle {
    return IOFileHandle { label: IOFileHandle_noop_id_0, set_label: IOFileHandle_noop_v_1, user: user };
}

impl IOFileHandle {
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
    fn set_set_label(ref this, handler: fn(*u8, *u8)) {
        this.set_label = handler;
        return;
    }
}

fn IOFileHandle_key() -> *u8 { return { 28673 as *u8 }; }

fn IOFileHandle_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *IOFileHandle = { synth::get_associated(self_obj, IOFileHandle_key()) as *IOFileHandle };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn IOFileHandle_set_label_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *IOFileHandle = { synth::get_associated(self_obj, IOFileHandle_key()) as *IOFileHandle };
    let f: fn(*u8, *u8) = { (*c).set_label };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn create_IOFileHandle(ctx: *IOFileHandle) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_IOFileHandle\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), IOFileHandle_label_imp, #str_ptr("@@:\0"));
        let add_set_label: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("setLabel:\0")), IOFileHandle_set_label_imp, #str_ptr("v@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, IOFileHandle_key(), { ctx as *u8 });
    return d;
}

