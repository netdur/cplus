// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLCommandQueue.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLCommandQueueDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct CommandQueueDescriptor {
    opaque _obj: *u8,
}

impl CommandQueueDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

    fn max_command_buffer_count(this) -> u64 {
        return rt::msg_u64(this._obj, rt::sel(#str_ptr("maxCommandBufferCount\0")));
    }

    // SKIPPED `setMaxCommandBufferCount:`: (return, arg) shape not yet modelled
    // SKIPPED `logState`: return `id<MTLLogState> _Nullable` — generic collection
    // SKIPPED `setLogState:`: param `id<MTLLogState> _Nullable` — generic collection
}

// `MTLCommandQueue` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `commandBuffer`: return `id<MTLCommandBuffer> _Nullable` not modelled
// SKIPPED `commandBufferWithDescriptor:`: return `id<MTLCommandBuffer> _Nullable` not modelled
// SKIPPED `commandBufferWithUnretainedReferences`: return `id<MTLCommandBuffer> _Nullable` not modelled
// SKIPPED `addResidencySet:`: arg shape (only <=5 object args modelled)
// SKIPPED `addResidencySets:count:`: arg shape (only <=5 object args modelled)
// SKIPPED `removeResidencySet:`: arg shape (only <=5 object args modelled)
// SKIPPED `removeResidencySets:count:`: arg shape (only <=5 object args modelled)
// SKIPPED `device`: return `id<MTLDevice> _Nonnull` not modelled
fn CommandQueue_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn CommandQueue_noop_v_0(user: *u8) { return; }
fn CommandQueue_noop_v_1(user: *u8, a0: *u8) { return; }

struct CommandQueue {
    insert_debug_capture_boundary: fn(*u8),
    label: fn(*u8) -> *u8,
    set_label: fn(*u8, *u8),
    opaque user: *u8,
}

fn CommandQueue_new(user: *u8) -> CommandQueue {
    return CommandQueue { insert_debug_capture_boundary: CommandQueue_noop_v_0, label: CommandQueue_noop_id_0, set_label: CommandQueue_noop_v_1, user: user };
}

impl CommandQueue {
    fn set_insert_debug_capture_boundary(ref this, handler: fn(*u8)) {
        this.insert_debug_capture_boundary = handler;
        return;
    }
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
    fn set_set_label(ref this, handler: fn(*u8, *u8)) {
        this.set_label = handler;
        return;
    }
}

fn CommandQueue_key() -> *u8 { return { 28672 as *u8 }; }

fn CommandQueue_insert_debug_capture_boundary_imp(self_obj: *u8, cmd: *u8) {
    let c: *CommandQueue = { synth::get_associated(self_obj, CommandQueue_key()) as *CommandQueue };
    let f: fn(*u8) = { (*c).insert_debug_capture_boundary };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn CommandQueue_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *CommandQueue = { synth::get_associated(self_obj, CommandQueue_key()) as *CommandQueue };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn CommandQueue_set_label_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *CommandQueue = { synth::get_associated(self_obj, CommandQueue_key()) as *CommandQueue };
    let f: fn(*u8, *u8) = { (*c).set_label };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn create_CommandQueue(ctx: *CommandQueue) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_CommandQueue\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_insert_debug_capture_boundary: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("insertDebugCaptureBoundary\0")), CommandQueue_insert_debug_capture_boundary_imp, #str_ptr("v@:\0"));
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), CommandQueue_label_imp, #str_ptr("@@:\0"));
        let add_set_label: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("setLabel:\0")), CommandQueue_set_label_imp, #str_ptr("v@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, CommandQueue_key(), { ctx as *u8 });
    return d;
}

