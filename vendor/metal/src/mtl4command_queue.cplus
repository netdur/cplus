// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTL4CommandQueue.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTL4CommitOptions` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct MTL4CommitOptions {
    opaque _obj: *u8,
}

impl MTL4CommitOptions {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> MTL4CommitOptions { return MTL4CommitOptions { _obj: ptr }; }

    // SKIPPED `addFeedbackHandler:`: param `MTL4CommitFeedbackHandler _Nonnull` — unmapped type `MTL4CommitFeedbackHandler`
}

// `MTL4CommandQueueDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct MTL4CommandQueueDescriptor {
    opaque _obj: *u8,
}

impl MTL4CommandQueueDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> MTL4CommandQueueDescriptor { return MTL4CommandQueueDescriptor { _obj: ptr }; }

    fn label(this) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("label\0")));
        return bridge::to_text_option(ns);
    }

    fn set_label(this, label: str) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setLabel:\0")), bridge::nsstring(label));
    }

    // SKIPPED `feedbackQueue`: return `dispatch_queue_t _Nullable` — unmapped type `dispatch_queue_t`
    // SKIPPED `setFeedbackQueue:`: param `dispatch_queue_t _Nullable` — unmapped type `dispatch_queue_t`
}

// `MTL4CommandQueue` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `commit:count:`: arg shape (only <=5 object args modelled)
// SKIPPED `commit:count:options:`: arg shape (only <=5 object args modelled)
// SKIPPED `signalEvent:value:`: arg shape (only <=5 object args modelled)
// SKIPPED `waitForEvent:value:`: arg shape (only <=5 object args modelled)
// SKIPPED `signalDrawable:`: arg shape (only <=5 object args modelled)
// SKIPPED `waitForDrawable:`: arg shape (only <=5 object args modelled)
// SKIPPED `addResidencySet:`: arg shape (only <=5 object args modelled)
// SKIPPED `addResidencySets:count:`: arg shape (only <=5 object args modelled)
// SKIPPED `removeResidencySet:`: arg shape (only <=5 object args modelled)
// SKIPPED `removeResidencySets:count:`: arg shape (only <=5 object args modelled)
// SKIPPED `updateTextureMappings:heap:operations:count:`: arg shape (only <=5 object args modelled)
// SKIPPED `copyTextureMappingsFromTexture:toTexture:operations:count:`: arg shape (only <=5 object args modelled)
// SKIPPED `updateBufferMappings:heap:operations:count:`: arg shape (only <=5 object args modelled)
// SKIPPED `copyBufferMappingsFromBuffer:toBuffer:operations:count:`: arg shape (only <=5 object args modelled)
// SKIPPED `device`: return `id<MTLDevice> _Nonnull` not modelled
fn MTL4CommandQueue_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }

struct MTL4CommandQueue {
    label: fn(*u8) -> *u8,
    opaque user: *u8,
}

fn MTL4CommandQueue_new(user: *u8) -> MTL4CommandQueue {
    return MTL4CommandQueue { label: MTL4CommandQueue_noop_id_0, user: user };
}

impl MTL4CommandQueue {
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
}

fn MTL4CommandQueue_key() -> *u8 { return { 28672 as *u8 }; }

fn MTL4CommandQueue_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *MTL4CommandQueue = { synth::get_associated(self_obj, MTL4CommandQueue_key()) as *MTL4CommandQueue };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_MTL4CommandQueue(ctx: *MTL4CommandQueue) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_MTL4CommandQueue\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), MTL4CommandQueue_label_imp, #str_ptr("@@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, MTL4CommandQueue_key(), { ctx as *u8 });
    return d;
}

