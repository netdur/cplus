// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLArgumentEncoder.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLArgumentEncoder` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `setArgumentBuffer:offset:`: arg shape (only <=5 object args modelled)
// SKIPPED `setArgumentBuffer:startOffset:arrayElement:`: arg shape (only <=5 object args modelled)
// SKIPPED `setBuffer:offset:atIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `setBuffers:offsets:withRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `setTexture:atIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `setTextures:withRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `setSamplerState:atIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `setSamplerStates:withRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `constantDataAtIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `setRenderPipelineState:atIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `setRenderPipelineStates:withRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `setComputePipelineState:atIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `setComputePipelineStates:withRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `setIndirectCommandBuffer:atIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `setIndirectCommandBuffers:withRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `setAccelerationStructure:atIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `newArgumentEncoderForBufferAtIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `setVisibleFunctionTable:atIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `setVisibleFunctionTables:withRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `setIntersectionFunctionTable:atIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `setIntersectionFunctionTables:withRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `setDepthStencilState:atIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `setDepthStencilStates:withRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `device`: return `id<MTLDevice> _Nonnull` not modelled
fn ArgumentEncoder_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn ArgumentEncoder_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }
fn ArgumentEncoder_noop_v_1(user: *u8, a0: *u8) { return; }

struct ArgumentEncoder {
    label: fn(*u8) -> *u8,
    set_label: fn(*u8, *u8),
    encoded_length: fn(*u8) -> u64,
    alignment: fn(*u8) -> u64,
    opaque user: *u8,
}

fn ArgumentEncoder_new(user: *u8) -> ArgumentEncoder {
    return ArgumentEncoder { label: ArgumentEncoder_noop_id_0, set_label: ArgumentEncoder_noop_v_1, encoded_length: ArgumentEncoder_noop_u64_0, alignment: ArgumentEncoder_noop_u64_0, user: user };
}

impl ArgumentEncoder {
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
    fn set_set_label(ref this, handler: fn(*u8, *u8)) {
        this.set_label = handler;
        return;
    }
    fn set_encoded_length(ref this, handler: fn(*u8) -> u64) {
        this.encoded_length = handler;
        return;
    }
    fn set_alignment(ref this, handler: fn(*u8) -> u64) {
        this.alignment = handler;
        return;
    }
}

fn ArgumentEncoder_key() -> *u8 { return { 28672 as *u8 }; }

fn ArgumentEncoder_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *ArgumentEncoder = { synth::get_associated(self_obj, ArgumentEncoder_key()) as *ArgumentEncoder };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn ArgumentEncoder_set_label_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *ArgumentEncoder = { synth::get_associated(self_obj, ArgumentEncoder_key()) as *ArgumentEncoder };
    let f: fn(*u8, *u8) = { (*c).set_label };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn ArgumentEncoder_encoded_length_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *ArgumentEncoder = { synth::get_associated(self_obj, ArgumentEncoder_key()) as *ArgumentEncoder };
    let f: fn(*u8) -> u64 = { (*c).encoded_length };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn ArgumentEncoder_alignment_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *ArgumentEncoder = { synth::get_associated(self_obj, ArgumentEncoder_key()) as *ArgumentEncoder };
    let f: fn(*u8) -> u64 = { (*c).alignment };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_ArgumentEncoder(ctx: *ArgumentEncoder) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_ArgumentEncoder\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), ArgumentEncoder_label_imp, #str_ptr("@@:\0"));
        let add_set_label: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("setLabel:\0")), ArgumentEncoder_set_label_imp, #str_ptr("v@:@\0"));
        let add_encoded_length: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("encodedLength\0")), ArgumentEncoder_encoded_length_imp, #str_ptr("Q@:\0"));
        let add_alignment: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("alignment\0")), ArgumentEncoder_alignment_imp, #str_ptr("Q@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, ArgumentEncoder_key(), { ctx as *u8 });
    return d;
}

