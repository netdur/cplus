// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLFunctionLog.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLFunctionLogDebugLocation` delegate protocol -> runtime class-synthesis helper.
fn FunctionLogDebugLocation_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn FunctionLogDebugLocation_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }

struct FunctionLogDebugLocation {
    function_name: fn(*u8) -> *u8,
    u_r_l: fn(*u8) -> *u8,
    line: fn(*u8) -> u64,
    column: fn(*u8) -> u64,
    opaque user: *u8,
}

fn FunctionLogDebugLocation_new(user: *u8) -> FunctionLogDebugLocation {
    return FunctionLogDebugLocation { function_name: FunctionLogDebugLocation_noop_id_0, u_r_l: FunctionLogDebugLocation_noop_id_0, line: FunctionLogDebugLocation_noop_u64_0, column: FunctionLogDebugLocation_noop_u64_0, user: user };
}

impl FunctionLogDebugLocation {
    fn set_function_name(ref this, handler: fn(*u8) -> *u8) {
        this.function_name = handler;
        return;
    }
    fn set_u_r_l(ref this, handler: fn(*u8) -> *u8) {
        this.u_r_l = handler;
        return;
    }
    fn set_line(ref this, handler: fn(*u8) -> u64) {
        this.line = handler;
        return;
    }
    fn set_column(ref this, handler: fn(*u8) -> u64) {
        this.column = handler;
        return;
    }
}

fn FunctionLogDebugLocation_key() -> *u8 { return { 28672 as *u8 }; }

fn FunctionLogDebugLocation_function_name_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *FunctionLogDebugLocation = { synth::get_associated(self_obj, FunctionLogDebugLocation_key()) as *FunctionLogDebugLocation };
    let f: fn(*u8) -> *u8 = { (*c).function_name };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn FunctionLogDebugLocation_u_r_l_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *FunctionLogDebugLocation = { synth::get_associated(self_obj, FunctionLogDebugLocation_key()) as *FunctionLogDebugLocation };
    let f: fn(*u8) -> *u8 = { (*c).u_r_l };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn FunctionLogDebugLocation_line_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *FunctionLogDebugLocation = { synth::get_associated(self_obj, FunctionLogDebugLocation_key()) as *FunctionLogDebugLocation };
    let f: fn(*u8) -> u64 = { (*c).line };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn FunctionLogDebugLocation_column_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *FunctionLogDebugLocation = { synth::get_associated(self_obj, FunctionLogDebugLocation_key()) as *FunctionLogDebugLocation };
    let f: fn(*u8) -> u64 = { (*c).column };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_FunctionLogDebugLocation(ctx: *FunctionLogDebugLocation) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_FunctionLogDebugLocation\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_function_name: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("functionName\0")), FunctionLogDebugLocation_function_name_imp, #str_ptr("@@:\0"));
        let add_u_r_l: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("URL\0")), FunctionLogDebugLocation_u_r_l_imp, #str_ptr("@@:\0"));
        let add_line: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("line\0")), FunctionLogDebugLocation_line_imp, #str_ptr("Q@:\0"));
        let add_column: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("column\0")), FunctionLogDebugLocation_column_imp, #str_ptr("Q@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, FunctionLogDebugLocation_key(), { ctx as *u8 });
    return d;
}

// `MTLFunctionLog` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `function`: return `id<MTLFunction> _Nullable` not modelled
// SKIPPED `debugLocation`: return `id<MTLFunctionLogDebugLocation> _Nullable` not modelled
fn FunctionLog_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn FunctionLog_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }

struct FunctionLog {
    type_: fn(*u8) -> u64,
    encoder_label: fn(*u8) -> *u8,
    opaque user: *u8,
}

fn FunctionLog_new(user: *u8) -> FunctionLog {
    return FunctionLog { type_: FunctionLog_noop_u64_0, encoder_label: FunctionLog_noop_id_0, user: user };
}

impl FunctionLog {
    fn set_type_(ref this, handler: fn(*u8) -> u64) {
        this.type_ = handler;
        return;
    }
    fn set_encoder_label(ref this, handler: fn(*u8) -> *u8) {
        this.encoder_label = handler;
        return;
    }
}

fn FunctionLog_key() -> *u8 { return { 28673 as *u8 }; }

fn FunctionLog_type__imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *FunctionLog = { synth::get_associated(self_obj, FunctionLog_key()) as *FunctionLog };
    let f: fn(*u8) -> u64 = { (*c).type_ };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn FunctionLog_encoder_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *FunctionLog = { synth::get_associated(self_obj, FunctionLog_key()) as *FunctionLog };
    let f: fn(*u8) -> *u8 = { (*c).encoder_label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_FunctionLog(ctx: *FunctionLog) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_FunctionLog\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_type_: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("type\0")), FunctionLog_type__imp, #str_ptr("Q@:\0"));
        let add_encoder_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("encoderLabel\0")), FunctionLog_encoder_label_imp, #str_ptr("@@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, FunctionLog_key(), { ctx as *u8 });
    return d;
}

