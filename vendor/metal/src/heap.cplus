// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLHeap.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLStorageMode` (NS_ENUM)
enum StorageMode {
    Shared,
    Managed,
    Private,
    Memoryless,
}

fn storage_mode_raw(v: StorageMode) -> i64 {
    return match v {
        StorageMode::Shared => { 0 as i64 },
        StorageMode::Managed => { 1 as i64 },
        StorageMode::Private => { 2 as i64 },
        StorageMode::Memoryless => { 3 as i64 },
    };
}

fn storage_mode_from_raw(raw: i64) -> StorageMode {
    if raw == (1 as i64) { return StorageMode::Managed; }
    if raw == (2 as i64) { return StorageMode::Private; }
    if raw == (3 as i64) { return StorageMode::Memoryless; }
    return StorageMode::Shared;
}

// `MTLCPUCacheMode` (NS_ENUM)
enum CPUCacheMode {
    DefaultCache,
    WriteCombined,
}

fn c_p_u_cache_mode_raw(v: CPUCacheMode) -> i64 {
    return match v {
        CPUCacheMode::DefaultCache => { 0 as i64 },
        CPUCacheMode::WriteCombined => { 1 as i64 },
    };
}

fn c_p_u_cache_mode_from_raw(raw: i64) -> CPUCacheMode {
    if raw == (1 as i64) { return CPUCacheMode::WriteCombined; }
    return CPUCacheMode::DefaultCache;
}

// `MTLSparsePageSize` (NS_ENUM)
enum SparsePageSize {
    _16,
    _64,
    _256,
}

fn sparse_page_size_raw(v: SparsePageSize) -> i64 {
    return match v {
        SparsePageSize::_16 => { 101 as i64 },
        SparsePageSize::_64 => { 102 as i64 },
        SparsePageSize::_256 => { 103 as i64 },
    };
}

fn sparse_page_size_from_raw(raw: i64) -> SparsePageSize {
    if raw == (102 as i64) { return SparsePageSize::_64; }
    if raw == (103 as i64) { return SparsePageSize::_256; }
    return SparsePageSize::_16;
}

// `MTLHazardTrackingMode` (NS_ENUM)
enum HazardTrackingMode {
    Default,
    Untracked,
    Tracked,
}

fn hazard_tracking_mode_raw(v: HazardTrackingMode) -> i64 {
    return match v {
        HazardTrackingMode::Default => { 0 as i64 },
        HazardTrackingMode::Untracked => { 1 as i64 },
        HazardTrackingMode::Tracked => { 2 as i64 },
    };
}

fn hazard_tracking_mode_from_raw(raw: i64) -> HazardTrackingMode {
    if raw == (1 as i64) { return HazardTrackingMode::Untracked; }
    if raw == (2 as i64) { return HazardTrackingMode::Tracked; }
    return HazardTrackingMode::Default;
}

// `MTLResourceOptions` (NS_ENUM)
enum ResourceOptions {
    MTLResourceCPUCacheModeDefaultCache,
    MTLResourceCPUCacheModeWriteCombined,
    MTLResourceStorageModeShared,
    MTLResourceStorageModeManaged,
    MTLResourceStorageModePrivate,
    MTLResourceStorageModeMemoryless,
    MTLResourceHazardTrackingModeDefault,
    MTLResourceHazardTrackingModeUntracked,
    MTLResourceHazardTrackingModeTracked,
    MTLResourceOptionCPUCacheModeDefault,
    MTLResourceOptionCPUCacheModeWriteCombined,
}

fn resource_options_raw(v: ResourceOptions) -> i64 {
    return match v {
        ResourceOptions::MTLResourceCPUCacheModeDefaultCache => { 0 as i64 },
        ResourceOptions::MTLResourceCPUCacheModeWriteCombined => { 1 as i64 },
        ResourceOptions::MTLResourceStorageModeShared => { 0 as i64 },
        ResourceOptions::MTLResourceStorageModeManaged => { 16 as i64 },
        ResourceOptions::MTLResourceStorageModePrivate => { 32 as i64 },
        ResourceOptions::MTLResourceStorageModeMemoryless => { 48 as i64 },
        ResourceOptions::MTLResourceHazardTrackingModeDefault => { 0 as i64 },
        ResourceOptions::MTLResourceHazardTrackingModeUntracked => { 256 as i64 },
        ResourceOptions::MTLResourceHazardTrackingModeTracked => { 512 as i64 },
        ResourceOptions::MTLResourceOptionCPUCacheModeDefault => { 0 as i64 },
        ResourceOptions::MTLResourceOptionCPUCacheModeWriteCombined => { 1 as i64 },
    };
}

fn resource_options_from_raw(raw: i64) -> ResourceOptions {
    if raw == (1 as i64) { return ResourceOptions::MTLResourceCPUCacheModeWriteCombined; }
    if raw == (0 as i64) { return ResourceOptions::MTLResourceStorageModeShared; }
    if raw == (16 as i64) { return ResourceOptions::MTLResourceStorageModeManaged; }
    if raw == (32 as i64) { return ResourceOptions::MTLResourceStorageModePrivate; }
    if raw == (48 as i64) { return ResourceOptions::MTLResourceStorageModeMemoryless; }
    if raw == (0 as i64) { return ResourceOptions::MTLResourceHazardTrackingModeDefault; }
    if raw == (256 as i64) { return ResourceOptions::MTLResourceHazardTrackingModeUntracked; }
    if raw == (512 as i64) { return ResourceOptions::MTLResourceHazardTrackingModeTracked; }
    if raw == (0 as i64) { return ResourceOptions::MTLResourceOptionCPUCacheModeDefault; }
    if raw == (1 as i64) { return ResourceOptions::MTLResourceOptionCPUCacheModeWriteCombined; }
    return ResourceOptions::MTLResourceCPUCacheModeDefaultCache;
}

// `MTLHeapType` (NS_ENUM)
enum HeapType {
    Automatic,
    Placement,
    Sparse,
}

fn heap_type_raw(v: HeapType) -> i64 {
    return match v {
        HeapType::Automatic => { 0 as i64 },
        HeapType::Placement => { 1 as i64 },
        HeapType::Sparse => { 2 as i64 },
    };
}

fn heap_type_from_raw(raw: i64) -> HeapType {
    if raw == (1 as i64) { return HeapType::Placement; }
    if raw == (2 as i64) { return HeapType::Sparse; }
    return HeapType::Automatic;
}

// `MTLHeapDescriptor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct HeapDescriptor {
    opaque _obj: *u8,
}

impl HeapDescriptor {
    fn raw(this) -> *u8 { return this._obj; }

    fn size(this) -> u64 {
        return rt::msg_u64(this._obj, rt::sel(#str_ptr("size\0")));
    }

    // SKIPPED `setSize:`: (return, arg) shape not yet modelled
    fn storage_mode(this) -> StorageMode {
        return storage_mode_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("storageMode\0"))));
    }

    // SKIPPED `setStorageMode:`: (return, arg) shape not yet modelled
    fn cpu_cache_mode(this) -> CPUCacheMode {
        return c_p_u_cache_mode_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("cpuCacheMode\0"))));
    }

    // SKIPPED `setCpuCacheMode:`: (return, arg) shape not yet modelled
    fn sparse_page_size(this) -> SparsePageSize {
        return sparse_page_size_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("sparsePageSize\0"))));
    }

    // SKIPPED `setSparsePageSize:`: (return, arg) shape not yet modelled
    fn hazard_tracking_mode(this) -> HazardTrackingMode {
        return hazard_tracking_mode_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("hazardTrackingMode\0"))));
    }

    // SKIPPED `setHazardTrackingMode:`: (return, arg) shape not yet modelled
    fn resource_options(this) -> ResourceOptions {
        return resource_options_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("resourceOptions\0"))));
    }

    // SKIPPED `setResourceOptions:`: (return, arg) shape not yet modelled
    fn type_(this) -> HeapType {
        return heap_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("type\0"))));
    }

    // SKIPPED `setType:`: (return, arg) shape not yet modelled
    fn max_compatible_placement_sparse_page_size(this) -> SparsePageSize {
        return sparse_page_size_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("maxCompatiblePlacementSparsePageSize\0"))));
    }

    // SKIPPED `setMaxCompatiblePlacementSparsePageSize:`: (return, arg) shape not yet modelled
}

// `MTLHeap` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `maxAvailableSizeWithAlignment:`: arg shape (only <=5 object args modelled)
// SKIPPED `newBufferWithLength:options:`: arg shape (only <=5 object args modelled)
// SKIPPED `newTextureWithDescriptor:`: return `id<MTLTexture> _Nullable` not modelled
// SKIPPED `setPurgeableState:`: arg shape (only <=5 object args modelled)
// SKIPPED `newBufferWithLength:options:offset:`: arg shape (only <=5 object args modelled)
// SKIPPED `newTextureWithDescriptor:offset:`: arg shape (only <=5 object args modelled)
// SKIPPED `newAccelerationStructureWithSize:`: arg shape (only <=5 object args modelled)
// SKIPPED `newAccelerationStructureWithDescriptor:`: return `id<MTLAccelerationStructure> _Nullable` not modelled
// SKIPPED `newAccelerationStructureWithSize:offset:`: arg shape (only <=5 object args modelled)
// SKIPPED `newAccelerationStructureWithDescriptor:offset:`: arg shape (only <=5 object args modelled)
// SKIPPED `device`: return `id<MTLDevice> _Nonnull` not modelled
fn Heap_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn Heap_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }
fn Heap_noop_v_1(user: *u8, a0: *u8) { return; }

struct Heap {
    label: fn(*u8) -> *u8,
    set_label: fn(*u8, *u8),
    storage_mode: fn(*u8) -> u64,
    cpu_cache_mode: fn(*u8) -> u64,
    hazard_tracking_mode: fn(*u8) -> u64,
    resource_options: fn(*u8) -> u64,
    size: fn(*u8) -> u64,
    used_size: fn(*u8) -> u64,
    current_allocated_size: fn(*u8) -> u64,
    type_: fn(*u8) -> u64,
    opaque user: *u8,
}

fn Heap_new(user: *u8) -> Heap {
    return Heap { label: Heap_noop_id_0, set_label: Heap_noop_v_1, storage_mode: Heap_noop_u64_0, cpu_cache_mode: Heap_noop_u64_0, hazard_tracking_mode: Heap_noop_u64_0, resource_options: Heap_noop_u64_0, size: Heap_noop_u64_0, used_size: Heap_noop_u64_0, current_allocated_size: Heap_noop_u64_0, type_: Heap_noop_u64_0, user: user };
}

impl Heap {
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
    fn set_set_label(ref this, handler: fn(*u8, *u8)) {
        this.set_label = handler;
        return;
    }
    fn set_storage_mode(ref this, handler: fn(*u8) -> u64) {
        this.storage_mode = handler;
        return;
    }
    fn set_cpu_cache_mode(ref this, handler: fn(*u8) -> u64) {
        this.cpu_cache_mode = handler;
        return;
    }
    fn set_hazard_tracking_mode(ref this, handler: fn(*u8) -> u64) {
        this.hazard_tracking_mode = handler;
        return;
    }
    fn set_resource_options(ref this, handler: fn(*u8) -> u64) {
        this.resource_options = handler;
        return;
    }
    fn set_size(ref this, handler: fn(*u8) -> u64) {
        this.size = handler;
        return;
    }
    fn set_used_size(ref this, handler: fn(*u8) -> u64) {
        this.used_size = handler;
        return;
    }
    fn set_current_allocated_size(ref this, handler: fn(*u8) -> u64) {
        this.current_allocated_size = handler;
        return;
    }
    fn set_type_(ref this, handler: fn(*u8) -> u64) {
        this.type_ = handler;
        return;
    }
}

fn Heap_key() -> *u8 { return { 28672 as *u8 }; }

fn Heap_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *Heap = { synth::get_associated(self_obj, Heap_key()) as *Heap };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Heap_set_label_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *Heap = { synth::get_associated(self_obj, Heap_key()) as *Heap };
    let f: fn(*u8, *u8) = { (*c).set_label };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn Heap_storage_mode_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Heap = { synth::get_associated(self_obj, Heap_key()) as *Heap };
    let f: fn(*u8) -> u64 = { (*c).storage_mode };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Heap_cpu_cache_mode_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Heap = { synth::get_associated(self_obj, Heap_key()) as *Heap };
    let f: fn(*u8) -> u64 = { (*c).cpu_cache_mode };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Heap_hazard_tracking_mode_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Heap = { synth::get_associated(self_obj, Heap_key()) as *Heap };
    let f: fn(*u8) -> u64 = { (*c).hazard_tracking_mode };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Heap_resource_options_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Heap = { synth::get_associated(self_obj, Heap_key()) as *Heap };
    let f: fn(*u8) -> u64 = { (*c).resource_options };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Heap_size_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Heap = { synth::get_associated(self_obj, Heap_key()) as *Heap };
    let f: fn(*u8) -> u64 = { (*c).size };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Heap_used_size_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Heap = { synth::get_associated(self_obj, Heap_key()) as *Heap };
    let f: fn(*u8) -> u64 = { (*c).used_size };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Heap_current_allocated_size_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Heap = { synth::get_associated(self_obj, Heap_key()) as *Heap };
    let f: fn(*u8) -> u64 = { (*c).current_allocated_size };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Heap_type__imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Heap = { synth::get_associated(self_obj, Heap_key()) as *Heap };
    let f: fn(*u8) -> u64 = { (*c).type_ };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_Heap(ctx: *Heap) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_Heap\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), Heap_label_imp, #str_ptr("@@:\0"));
        let add_set_label: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("setLabel:\0")), Heap_set_label_imp, #str_ptr("v@:@\0"));
        let add_storage_mode: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("storageMode\0")), Heap_storage_mode_imp, #str_ptr("Q@:\0"));
        let add_cpu_cache_mode: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("cpuCacheMode\0")), Heap_cpu_cache_mode_imp, #str_ptr("Q@:\0"));
        let add_hazard_tracking_mode: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("hazardTrackingMode\0")), Heap_hazard_tracking_mode_imp, #str_ptr("Q@:\0"));
        let add_resource_options: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("resourceOptions\0")), Heap_resource_options_imp, #str_ptr("Q@:\0"));
        let add_size: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("size\0")), Heap_size_imp, #str_ptr("Q@:\0"));
        let add_used_size: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("usedSize\0")), Heap_used_size_imp, #str_ptr("Q@:\0"));
        let add_current_allocated_size: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("currentAllocatedSize\0")), Heap_current_allocated_size_imp, #str_ptr("Q@:\0"));
        let add_type_: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("type\0")), Heap_type__imp, #str_ptr("Q@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, Heap_key(), { ctx as *u8 });
    return d;
}

