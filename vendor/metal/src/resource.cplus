// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLResource.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLResource` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `setPurgeableState:`: arg shape (only <=5 object args modelled)
// SKIPPED `isAliasable`: no `i8`-return / 0-arg IMP shape
// SKIPPED `setOwnerWithIdentity:`: arg shape (only <=5 object args modelled)
// SKIPPED `device`: return `id<MTLDevice> _Nonnull` not modelled
// SKIPPED `heap`: return `id<MTLHeap> _Nullable` not modelled
fn Resource_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn Resource_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }
fn Resource_noop_v_0(user: *u8) { return; }
fn Resource_noop_v_1(user: *u8, a0: *u8) { return; }

struct Resource {
    make_aliasable: fn(*u8),
    label: fn(*u8) -> *u8,
    set_label: fn(*u8, *u8),
    cpu_cache_mode: fn(*u8) -> u64,
    storage_mode: fn(*u8) -> u64,
    hazard_tracking_mode: fn(*u8) -> u64,
    resource_options: fn(*u8) -> u64,
    heap_offset: fn(*u8) -> u64,
    allocated_size: fn(*u8) -> u64,
    opaque user: *u8,
}

fn Resource_new(user: *u8) -> Resource {
    return Resource { make_aliasable: Resource_noop_v_0, label: Resource_noop_id_0, set_label: Resource_noop_v_1, cpu_cache_mode: Resource_noop_u64_0, storage_mode: Resource_noop_u64_0, hazard_tracking_mode: Resource_noop_u64_0, resource_options: Resource_noop_u64_0, heap_offset: Resource_noop_u64_0, allocated_size: Resource_noop_u64_0, user: user };
}

impl Resource {
    fn set_make_aliasable(ref this, handler: fn(*u8)) {
        this.make_aliasable = handler;
        return;
    }
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
    fn set_set_label(ref this, handler: fn(*u8, *u8)) {
        this.set_label = handler;
        return;
    }
    fn set_cpu_cache_mode(ref this, handler: fn(*u8) -> u64) {
        this.cpu_cache_mode = handler;
        return;
    }
    fn set_storage_mode(ref this, handler: fn(*u8) -> u64) {
        this.storage_mode = handler;
        return;
    }
    fn set_hazard_tracking_mode(ref this, handler: fn(*u8) -> u64) {
        this.hazard_tracking_mode = handler;
        return;
    }
    fn set_resource_options(ref this, handler: fn(*u8) -> u64) {
        this.resource_options = handler;
        return;
    }
    fn set_heap_offset(ref this, handler: fn(*u8) -> u64) {
        this.heap_offset = handler;
        return;
    }
    fn set_allocated_size(ref this, handler: fn(*u8) -> u64) {
        this.allocated_size = handler;
        return;
    }
}

fn Resource_key() -> *u8 { return { 28672 as *u8 }; }

fn Resource_make_aliasable_imp(self_obj: *u8, cmd: *u8) {
    let c: *Resource = { synth::get_associated(self_obj, Resource_key()) as *Resource };
    let f: fn(*u8) = { (*c).make_aliasable };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn Resource_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *Resource = { synth::get_associated(self_obj, Resource_key()) as *Resource };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Resource_set_label_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *Resource = { synth::get_associated(self_obj, Resource_key()) as *Resource };
    let f: fn(*u8, *u8) = { (*c).set_label };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn Resource_cpu_cache_mode_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Resource = { synth::get_associated(self_obj, Resource_key()) as *Resource };
    let f: fn(*u8) -> u64 = { (*c).cpu_cache_mode };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Resource_storage_mode_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Resource = { synth::get_associated(self_obj, Resource_key()) as *Resource };
    let f: fn(*u8) -> u64 = { (*c).storage_mode };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Resource_hazard_tracking_mode_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Resource = { synth::get_associated(self_obj, Resource_key()) as *Resource };
    let f: fn(*u8) -> u64 = { (*c).hazard_tracking_mode };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Resource_resource_options_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Resource = { synth::get_associated(self_obj, Resource_key()) as *Resource };
    let f: fn(*u8) -> u64 = { (*c).resource_options };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Resource_heap_offset_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Resource = { synth::get_associated(self_obj, Resource_key()) as *Resource };
    let f: fn(*u8) -> u64 = { (*c).heap_offset };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Resource_allocated_size_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *Resource = { synth::get_associated(self_obj, Resource_key()) as *Resource };
    let f: fn(*u8) -> u64 = { (*c).allocated_size };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_Resource(ctx: *Resource) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_Resource\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_make_aliasable: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("makeAliasable\0")), Resource_make_aliasable_imp, #str_ptr("v@:\0"));
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), Resource_label_imp, #str_ptr("@@:\0"));
        let add_set_label: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("setLabel:\0")), Resource_set_label_imp, #str_ptr("v@:@\0"));
        let add_cpu_cache_mode: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("cpuCacheMode\0")), Resource_cpu_cache_mode_imp, #str_ptr("Q@:\0"));
        let add_storage_mode: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("storageMode\0")), Resource_storage_mode_imp, #str_ptr("Q@:\0"));
        let add_hazard_tracking_mode: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("hazardTrackingMode\0")), Resource_hazard_tracking_mode_imp, #str_ptr("Q@:\0"));
        let add_resource_options: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("resourceOptions\0")), Resource_resource_options_imp, #str_ptr("Q@:\0"));
        let add_heap_offset: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("heapOffset\0")), Resource_heap_offset_imp, #str_ptr("Q@:\0"));
        let add_allocated_size: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("allocatedSize\0")), Resource_allocated_size_imp, #str_ptr("Q@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, Resource_key(), { ctx as *u8 });
    return d;
}

