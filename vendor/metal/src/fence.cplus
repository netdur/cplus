// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/Metal.framework/Headers/MTLFence.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `MTLFence` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `device`: return `id<MTLDevice> _Nonnull` not modelled
fn Fence_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn Fence_noop_v_1(user: *u8, a0: *u8) { return; }

struct Fence {
    label: fn(*u8) -> *u8,
    set_label: fn(*u8, *u8),
    opaque user: *u8,
}

fn Fence_new(user: *u8) -> Fence {
    return Fence { label: Fence_noop_id_0, set_label: Fence_noop_v_1, user: user };
}

impl Fence {
    fn set_label(ref this, handler: fn(*u8) -> *u8) {
        this.label = handler;
        return;
    }
    fn set_set_label(ref this, handler: fn(*u8, *u8)) {
        this.set_label = handler;
        return;
    }
}

fn Fence_key() -> *u8 { return { 28672 as *u8 }; }

fn Fence_label_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *Fence = { synth::get_associated(self_obj, Fence_key()) as *Fence };
    let f: fn(*u8) -> *u8 = { (*c).label };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn Fence_set_label_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *Fence = { synth::get_associated(self_obj, Fence_key()) as *Fence };
    let f: fn(*u8, *u8) = { (*c).set_label };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn create_Fence(ctx: *Fence) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_Fence\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_label: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("label\0")), Fence_label_imp, #str_ptr("@@:\0"));
        let add_set_label: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("setLabel:\0")), Fence_set_label_imp, #str_ptr("v@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, Fence_key(), { ctx as *u8 });
    return d;
}

