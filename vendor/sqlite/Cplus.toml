[package]
name    = "sqlite"
version = "0.0.1"
edition = "2026"

# sqlite — idiomatic SQLite: Connection / Statement, Result errors, Drop.
# Raw C ABI lives in dependency `sqlite_ffi` (bindgen). See docs/.

[dependencies]
stdlib = "*"
sqlite_ffi = "*"
