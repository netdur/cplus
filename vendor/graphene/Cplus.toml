[package]
name    = "graphene"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/share/gir-1.0/Graphene-1.0.gir (namespace Graphene 1.0)
# Reproduce: cpc-bindgen --gobject Graphene --out vendor/graphene --use GLib=glib --use GObject=gobject_gir --use GModule=gmodule --use Gio=gio --use cairo=cairo --use HarfBuzz=harfbuzz --use Pango=pango --use PangoCairo=pangocairo --use GdkPixbuf=gdkpixbuf --use Gdk=gdk --use Gsk=gsk --use Gtk=gtk4
# Coverage:  271 items, 147 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"

[link]
libs = ["graphene-1.0"]
