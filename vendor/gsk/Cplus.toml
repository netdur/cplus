[package]
name    = "gsk"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/share/gir-1.0/Gsk-4.0.gir (namespace Gsk 4.0)
# Reproduce: cpc-bindgen --gobject Gsk --out vendor/gsk --use GLib=glib --use GObject=gobject_gir --use GModule=gmodule --use Gio=gio --use cairo=cairo --use Graphene=graphene --use HarfBuzz=harfbuzz --use Pango=pango --use PangoCairo=pangocairo --use GdkPixbuf=gdkpixbuf --use Gdk=gdk --use Gtk=gtk4
# Coverage:  371 items, 43 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"
cairo       = "*"
gdk         = "*"
gdkpixbuf   = "*"
gio         = "*"
glib        = "*"
gmodule     = "*"
gobject_gir = "*"
graphene    = "*"
harfbuzz    = "*"
pango       = "*"
pangocairo  = "*"

[link]
libs = ["gtk-4"]
