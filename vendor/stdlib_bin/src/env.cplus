// stdlib_bin/env — declarations only. Bodies live in ../lib/<arch>/stdlib_bin.a.

import "./result" as result;
import "./vec" as vec;

extern fn stdlib_env_var(name_ptr: *u8, name_len: usize, out_ptr: *u8, out_cap: usize, out_len: *usize) -> i32;
extern fn stdlib_env_args_count() -> usize;
extern fn stdlib_env_args_get(i: usize, out_ptr: *u8, out_cap: usize, out_len: *usize) -> i32;

pub fn var(name: str) -> result::Result[string, result::IoError] {
    // TODO: call stdlib_env_var; copy bytes into an owned string on success.
    return result::Result::Err(result::IoError::NotFound);
}

pub fn args() -> vec::Vec[string] {
    // TODO: walk indices 0..stdlib_env_args_count(), copy each into a Vec[string].
    return vec::Vec[string]::new();
}
