// stdlib_bin/net — declarations only. Bodies live in ../lib/<arch>/stdlib_bin.a.

import "./result" as result;
import "./vec" as vec;

extern fn stdlib_net_tcp_connect(host_ptr: *u8, host_len: usize, port: u16, out_fd: *i32) -> i32;
extern fn stdlib_net_tcp_read(fd: i32, buf_ptr: *u8, buf_cap: usize, out_read: *usize) -> i32;
extern fn stdlib_net_tcp_write(fd: i32, data_ptr: *u8, data_len: usize, out_written: *usize) -> i32;
extern fn stdlib_net_tcp_close(fd: i32);

extern fn stdlib_net_tcp_bind(host_ptr: *u8, host_len: usize, port: u16, out_fd: *i32) -> i32;
extern fn stdlib_net_tcp_accept(listener_fd: i32, out_fd: *i32) -> i32;

pub struct TcpStream {
    fd: i32,
}

impl TcpStream {
    pub fn connect(host: str, port: u16) -> result::Result[TcpStream, result::IoError] {
        // TODO: call stdlib_net_tcp_connect.
        return result::Result::Err(result::IoError::Other);
    }

    pub fn read(mut self, buf: vec::Vec[u8]) -> result::Result[usize, result::IoError] {
        // TODO: call stdlib_net_tcp_read.
        return result::Result::Err(result::IoError::Other);
    }

    pub fn write(mut self, data: vec::Vec[u8]) -> result::Result[usize, result::IoError] {
        // TODO: call stdlib_net_tcp_write.
        return result::Result::Err(result::IoError::Other);
    }

    pub fn close(move self) {
        // TODO: stdlib_net_tcp_close.
        return;
    }

    fn drop(mut self) {
        // TODO: if self.fd != -1: stdlib_net_tcp_close(self.fd).
        return;
    }
}

pub struct TcpListener {
    fd: i32,
}

impl TcpListener {
    pub fn bind(host: str, port: u16) -> result::Result[TcpListener, result::IoError] {
        // TODO: call stdlib_net_tcp_bind.
        return result::Result::Err(result::IoError::Other);
    }

    pub fn accept(mut self) -> result::Result[TcpStream, result::IoError] {
        // TODO: call stdlib_net_tcp_accept; wrap returned fd in TcpStream.
        return result::Result::Err(result::IoError::Other);
    }

    pub fn close(move self) {
        // TODO: stdlib_net_tcp_close.
        return;
    }

    fn drop(mut self) {
        // TODO: if self.fd != -1: stdlib_net_tcp_close(self.fd).
        return;
    }
}
