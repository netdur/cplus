// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// context_menu — MAUI MenuFlyout. 0 writes, 2 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------


// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn context_menu(
    key: str = "",
) -> core::Node {
    var p: props::ContextMenuProps = props::ContextMenuProps::new();
    return match box::new::[props::ContextMenuProps](p) {
        option::Option[box::Box[props::ContextMenuProps]]::Some(b) =>
            core::node_with(key, props::K_CONTEXT_MENU, b.into_raw(), props::release_context_menu_props),
        option::Option[box::Box[props::ContextMenuProps]]::None =>
            core::node_with(key, props::K_CONTEXT_MENU, 0 as *u8, props::release_context_menu_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct ContextMenu {
    opaque _p: *core::Node,
    _seen: u64,
}

impl ContextMenu {
    fn of(p: *core::Node) -> ContextMenu { return ContextMenu { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::ContextMenuProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::ContextMenuProps; }
        if { (*d).kind } != props::K_CONTEXT_MENU { return 0 as *props::ContextMenuProps; }
        return { (*d).props } as *props::ContextMenuProps;
    }

    fn count(this) -> i64 {
        let p: *props::ContextMenuProps = this._props();
        if p == (0 as *props::ContextMenuProps) { return 0 as i64; }
        return { (*p).count };
    }

    fn is_read_only(this) -> bool {
        let p: *props::ContextMenuProps = this._props();
        if p == (0 as *props::ContextMenuProps) { return false; }
        return { (*p).is_read_only };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> ContextMenu { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_is_enabled(this, v: bool) -> ContextMenu { core::set_is_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_is_visible(this, v: bool) -> ContextMenu { core::set_is_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> ContextMenu { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> ContextMenu { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> ContextMenu { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[ContextMenu] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[ContextMenu]::None; }
            if { (*d).kind } != props::K_CONTEXT_MENU { return option::Option[ContextMenu]::None; }
            option::Option[ContextMenu]::Some(ContextMenu::of(p))
        }
        option::Option[*core::Node]::None => option::Option[ContextMenu]::None,
    };
}
