// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// facet's generated contract module: hook statics + registrars
// (backend-facing — apps never import facet/contract) and the
// app-facing Handle methods, attached through a same-package impl
// extension (EXT.1). Regenerate with:
//   python3 tools/gen_contract.py
// The slot list is tools/gen_contract.py's SLOTS table, guarded by
// the curated MAP (tools/maui_map.py). docs/contract.md is the
// manifest twin of this file.

import "./facet" as core;

// VisualElement.Background — wired: setBackgroundColor:
static SET_BACKGROUND_COLOR_FN: fn(core::Handle, core::Color) -> bool = #zero::[fn(core::Handle, core::Color) -> bool]();
fn set_background_color_fn(f: fn(core::Handle, core::Color) -> bool) { SET_BACKGROUND_COLOR_FN = f; return; }

// VisualElement.IsEnabled — wired: setEnabled:
static SET_IS_ENABLED_FN: fn(core::Handle, bool) -> bool = #zero::[fn(core::Handle, bool) -> bool]();
fn set_is_enabled_fn(f: fn(core::Handle, bool) -> bool) { SET_IS_ENABLED_FN = f; return; }

// VisualElement.IsFocused — wired: responder chain (hand impl — focus is a behavior, not a setter)
static SET_IS_FOCUSED_FN: fn(core::Handle, bool) -> bool = #zero::[fn(core::Handle, bool) -> bool]();
fn set_is_focused_fn(f: fn(core::Handle, bool) -> bool) { SET_IS_FOCUSED_FN = f; return; }

// InputView.IsReadOnly — wired: setEditable: (inverted)
static SET_IS_READ_ONLY_FN: fn(core::Handle, bool) -> bool = #zero::[fn(core::Handle, bool) -> bool]();
fn set_is_read_only_fn(f: fn(core::Handle, bool) -> bool) { SET_IS_READ_ONLY_FN = f; return; }

// VisualElement.Opacity — wired: setAlphaValue:
static SET_OPACITY_FN: fn(core::Handle, f64) -> bool = #zero::[fn(core::Handle, f64) -> bool]();
fn set_opacity_fn(f: fn(core::Handle, f64) -> bool) { SET_OPACITY_FN = f; return; }

// Entry.Placeholder — wired: setPlaceholderString:
static SET_PLACEHOLDER_FN: fn(core::Handle, str) -> bool = #zero::[fn(core::Handle, str) -> bool]();
fn set_placeholder_fn(f: fn(core::Handle, str) -> bool) { SET_PLACEHOLDER_FN = f; return; }

// ProgressBar.Progress — wired: setDoubleValue:
static SET_PROGRESS_FN: fn(core::Handle, f64) -> bool = #zero::[fn(core::Handle, f64) -> bool]();
fn set_progress_fn(f: fn(core::Handle, f64) -> bool) { SET_PROGRESS_FN = f; return; }

// Label.TextColor — wired: setTextColor:
static SET_TEXT_COLOR_FN: fn(core::Handle, core::Color) -> bool = #zero::[fn(core::Handle, core::Color) -> bool]();
fn set_text_color_fn(f: fn(core::Handle, core::Color) -> bool) { SET_TEXT_COLOR_FN = f; return; }

// Button.Text — wired: setTitle:
static SET_TITLE_FN: fn(core::Handle, str) -> bool = #zero::[fn(core::Handle, str) -> bool]();
fn set_title_fn(f: fn(core::Handle, str) -> bool) { SET_TITLE_FN = f; return; }

// Slider.Minimum — wired: setMinValue:
static SET_MINIMUM_FN: fn(core::Handle, f64) -> bool = #zero::[fn(core::Handle, f64) -> bool]();
fn set_minimum_fn(f: fn(core::Handle, f64) -> bool) { SET_MINIMUM_FN = f; return; }

// Slider.Maximum — wired: setMaxValue:
static SET_MAXIMUM_FN: fn(core::Handle, f64) -> bool = #zero::[fn(core::Handle, f64) -> bool]();
fn set_maximum_fn(f: fn(core::Handle, f64) -> bool) { SET_MAXIMUM_FN = f; return; }

// Slider.MinimumTrackColor — wired: setTrackFillColor:
static SET_MINIMUM_TRACK_COLOR_FN: fn(core::Handle, core::Color) -> bool = #zero::[fn(core::Handle, core::Color) -> bool]();
fn set_minimum_track_color_fn(f: fn(core::Handle, core::Color) -> bool) { SET_MINIMUM_TRACK_COLOR_FN = f; return; }

// Slider.MaximumTrackColor — UNSUPPORTED on AppKit: NSSlider has no equivalent
static SET_MAXIMUM_TRACK_COLOR_FN: fn(core::Handle, core::Color) -> bool = #zero::[fn(core::Handle, core::Color) -> bool]();
fn set_maximum_track_color_fn(f: fn(core::Handle, core::Color) -> bool) { SET_MAXIMUM_TRACK_COLOR_FN = f; return; }

// Slider.ThumbColor — UNSUPPORTED on AppKit: NSSlider has no equivalent
static SET_THUMB_COLOR_FN: fn(core::Handle, core::Color) -> bool = #zero::[fn(core::Handle, core::Color) -> bool]();
fn set_thumb_color_fn(f: fn(core::Handle, core::Color) -> bool) { SET_THUMB_COLOR_FN = f; return; }

// Slider.ThumbImageSource — UNSUPPORTED on AppKit: NSSlider has no equivalent
static SET_THUMB_IMAGE_FN: fn(core::Handle, str) -> bool = #zero::[fn(core::Handle, str) -> bool]();
fn set_thumb_image_fn(f: fn(core::Handle, str) -> bool) { SET_THUMB_IMAGE_FN = f; return; }

impl core::Handle {
    // VisualElement.Background. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_background_color(this, v: core::Color) -> core::Handle {
        let f: fn(core::Handle, core::Color) -> bool = SET_BACKGROUND_COLOR_FN;
        if f != (0 as fn(core::Handle, core::Color) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // VisualElement.IsEnabled. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_is_enabled(this, v: bool) -> core::Handle {
        let f: fn(core::Handle, bool) -> bool = SET_IS_ENABLED_FN;
        if f != (0 as fn(core::Handle, bool) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // VisualElement.IsFocused. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_is_focused(this, v: bool) -> core::Handle {
        let f: fn(core::Handle, bool) -> bool = SET_IS_FOCUSED_FN;
        if f != (0 as fn(core::Handle, bool) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // InputView.IsReadOnly. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_is_read_only(this, v: bool) -> core::Handle {
        let f: fn(core::Handle, bool) -> bool = SET_IS_READ_ONLY_FN;
        if f != (0 as fn(core::Handle, bool) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // VisualElement.Opacity. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_opacity(this, v: f64) -> core::Handle {
        let f: fn(core::Handle, f64) -> bool = SET_OPACITY_FN;
        if f != (0 as fn(core::Handle, f64) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // Entry.Placeholder. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_placeholder(this, v: str) -> core::Handle {
        let f: fn(core::Handle, str) -> bool = SET_PLACEHOLDER_FN;
        if f != (0 as fn(core::Handle, str) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // ProgressBar.Progress. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_progress(this, v: f64) -> core::Handle {
        let f: fn(core::Handle, f64) -> bool = SET_PROGRESS_FN;
        if f != (0 as fn(core::Handle, f64) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // Label.TextColor. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_text_color(this, v: core::Color) -> core::Handle {
        let f: fn(core::Handle, core::Color) -> bool = SET_TEXT_COLOR_FN;
        if f != (0 as fn(core::Handle, core::Color) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // Button.Text. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_title(this, v: str) -> core::Handle {
        let f: fn(core::Handle, str) -> bool = SET_TITLE_FN;
        if f != (0 as fn(core::Handle, str) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // Slider.Minimum. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_minimum(this, v: f64) -> core::Handle {
        let f: fn(core::Handle, f64) -> bool = SET_MINIMUM_FN;
        if f != (0 as fn(core::Handle, f64) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // Slider.Maximum. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_maximum(this, v: f64) -> core::Handle {
        let f: fn(core::Handle, f64) -> bool = SET_MAXIMUM_FN;
        if f != (0 as fn(core::Handle, f64) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // Slider.MinimumTrackColor. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_minimum_track_color(this, v: core::Color) -> core::Handle {
        let f: fn(core::Handle, core::Color) -> bool = SET_MINIMUM_TRACK_COLOR_FN;
        if f != (0 as fn(core::Handle, core::Color) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // Slider.MaximumTrackColor. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_maximum_track_color(this, v: core::Color) -> core::Handle {
        let f: fn(core::Handle, core::Color) -> bool = SET_MAXIMUM_TRACK_COLOR_FN;
        if f != (0 as fn(core::Handle, core::Color) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // Slider.ThumbColor. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_thumb_color(this, v: core::Color) -> core::Handle {
        let f: fn(core::Handle, core::Color) -> bool = SET_THUMB_COLOR_FN;
        if f != (0 as fn(core::Handle, core::Color) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

    // Slider.ThumbImageSource. Chainable; a no-op on a miss, a kind
    // without the property, or a backend that has not wired it.
    fn set_thumb_image(this, v: str) -> core::Handle {
        let f: fn(core::Handle, str) -> bool = SET_THUMB_IMAGE_FN;
        if f != (0 as fn(core::Handle, str) -> bool) { let _ok: bool = f(this, v); }
        return this;
    }

}
