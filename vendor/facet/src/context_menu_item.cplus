// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// context_menu_item — MAUI MenuFlyoutItem. 0 writes, 0 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------


// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn context_menu_item(
    key: str = "",
) -> core::Node {
    var p: props::ContextMenuItemProps = props::ContextMenuItemProps::new();
    return match box::new::[props::ContextMenuItemProps](p) {
        option::Option[box::Box[props::ContextMenuItemProps]]::Some(b) =>
            core::node_with(key, props::K_CONTEXT_MENU_ITEM, b.into_raw(), props::release_context_menu_item_props),
        option::Option[box::Box[props::ContextMenuItemProps]]::None =>
            core::node_with(key, props::K_CONTEXT_MENU_ITEM, 0 as *u8, props::release_context_menu_item_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct ContextMenuItem {
    opaque _p: *core::Node,
    _seen: u64,
}

impl ContextMenuItem {
    fn of(p: *core::Node) -> ContextMenuItem { return ContextMenuItem { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::ContextMenuItemProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::ContextMenuItemProps; }
        if { (*d).kind } != props::K_CONTEXT_MENU_ITEM { return 0 as *props::ContextMenuItemProps; }
        return { (*d).props } as *props::ContextMenuItemProps;
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> ContextMenuItem { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_is_enabled(this, v: bool) -> ContextMenuItem { core::set_is_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_is_visible(this, v: bool) -> ContextMenuItem { core::set_is_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> ContextMenuItem { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> ContextMenuItem { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> ContextMenuItem { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[ContextMenuItem] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[ContextMenuItem]::None; }
            if { (*d).kind } != props::K_CONTEXT_MENU_ITEM { return option::Option[ContextMenuItem]::None; }
            option::Option[ContextMenuItem]::Some(ContextMenuItem::of(p))
        }
        option::Option[*core::Node]::None => option::Option[ContextMenuItem]::None,
    };
}
