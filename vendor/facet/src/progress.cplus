// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// progress — MAUI ProgressBar. 2 writes, 0 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_PROGRESS: u64 = 1u64;
const P_PROGRESS_COLOR: u64 = 2u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn progress(
    progress: f64,
    key: str = "",
    progress_color: vocab::Color = vocab::Color::clear(),
) -> core::Node {
    var p: props::ProgressProps = props::ProgressProps::new();
    p.progress = progress;
    p.progress_color = progress_color;
    return match box::new::[props::ProgressProps](p) {
        option::Option[box::Box[props::ProgressProps]]::Some(b) =>
            core::node_with(key, props::K_PROGRESS, b.into_raw(), props::release_progress_props),
        option::Option[box::Box[props::ProgressProps]]::None =>
            core::node_with(key, props::K_PROGRESS, 0 as *u8, props::release_progress_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Progress {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Progress {
    fn of(p: *core::Node) -> Progress { return Progress { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::ProgressProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::ProgressProps; }
        if { (*d).kind } != props::K_PROGRESS { return 0 as *props::ProgressProps; }
        return { (*d).props } as *props::ProgressProps;
    }

    // ProgressBar.Progress
    fn set_progress(this, v: f64) -> Progress {
        let p: *props::ProgressProps = this._props();
        if p == (0 as *props::ProgressProps) { return this; }
        { (*p).progress = v };
        core::touch(this._p, P_PROGRESS);
        return this;
    }

    // ProgressBar.ProgressColor
    fn set_progress_color(this, v: vocab::Color) -> Progress {
        let p: *props::ProgressProps = this._props();
        if p == (0 as *props::ProgressProps) { return this; }
        { (*p).progress_color = v };
        core::touch(this._p, P_PROGRESS_COLOR);
        return this;
    }

    fn progress(this) -> f64 {
        let p: *props::ProgressProps = this._props();
        if p == (0 as *props::ProgressProps) { return 0.0f64; }
        return { (*p).progress };
    }

    fn progress_color(this) -> vocab::Color {
        let p: *props::ProgressProps = this._props();
        if p == (0 as *props::ProgressProps) { return vocab::Color::clear(); }
        return { (*p).progress_color };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Progress { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_is_enabled(this, v: bool) -> Progress { core::set_is_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_is_visible(this, v: bool) -> Progress { core::set_is_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Progress { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Progress { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Progress { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Progress] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Progress]::None; }
            if { (*d).kind } != props::K_PROGRESS { return option::Option[Progress]::None; }
            option::Option[Progress]::Some(Progress::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Progress]::None,
    };
}
