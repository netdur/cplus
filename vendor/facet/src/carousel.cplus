// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// carousel — MAUI CarouselView. 12 writes, 3 reads, 3 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_IS_BOUNCE_ENABLED: u64 = 1u64;
const P_IS_SCROLL_ANIMATED: u64 = 2u64;
const P_IS_SCROLLING: u64 = 4u64;
const P_IS_SWIPE_ENABLED: u64 = 8u64;
const P_WRAPS: u64 = 16u64;
const P_PEEK_AREA_INSETS: u64 = 32u64;
const P_POSITION: u64 = 64u64;
const P_HORIZONTAL_SCROLL_BAR_VISIBILITY: u64 = 128u64;
const P_ITEMS_UPDATING_SCROLL_MODE: u64 = 256u64;
const P_REMAINING_ITEMS_THRESHOLD: u64 = 512u64;
const P_VERTICAL_SCROLL_BAR_VISIBILITY: u64 = 1024u64;
const P_ITEM_SIZING_STRATEGY: u64 = 2048u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn carousel(
    key: str = "",
    is_bounce_enabled: bool = false,
    is_scroll_animated: bool = false,
    is_scrolling: bool = false,
    is_swipe_enabled: bool = false,
    wraps: bool = false,
    peek_area_insets: vocab::Insets = vocab::Insets::zero(),
    position: i64 = 0 as i64,
    horizontal_scroll_bar_visibility: vocab::ScrollBars = vocab::ScrollBars::Always,
    items_updating_scroll_mode: vocab::ScrollAnchor = vocab::ScrollAnchor::KeepItemsInView,
    remaining_items_threshold: i64 = 0 as i64,
    vertical_scroll_bar_visibility: vocab::ScrollBars = vocab::ScrollBars::Always,
    item_sizing_strategy: vocab::ItemSizing = vocab::ItemSizing::MeasureAllItems,
    on_current_item_changed: fn(*u8, *u8) = props::no_handler,
    on_current_item_changed_ctx: *u8 = 0 as *u8,
    on_position_changed: fn(*u8, *u8) = props::no_handler,
    on_position_changed_ctx: *u8 = 0 as *u8,
    on_remaining_items_threshold_reached: fn(*u8, *u8) = props::no_handler,
    on_remaining_items_threshold_reached_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::CarouselProps = props::CarouselProps::new();
    p.is_bounce_enabled = is_bounce_enabled;
    p.is_scroll_animated = is_scroll_animated;
    p.is_scrolling = is_scrolling;
    p.is_swipe_enabled = is_swipe_enabled;
    p.wraps = wraps;
    p.peek_area_insets = peek_area_insets;
    p.position = position;
    p.horizontal_scroll_bar_visibility = horizontal_scroll_bar_visibility;
    p.items_updating_scroll_mode = items_updating_scroll_mode;
    p.remaining_items_threshold = remaining_items_threshold;
    p.vertical_scroll_bar_visibility = vertical_scroll_bar_visibility;
    p.item_sizing_strategy = item_sizing_strategy;
    p.on_current_item_changed = on_current_item_changed;
    p.on_current_item_changed_ctx = on_current_item_changed_ctx;
    p.on_position_changed = on_position_changed;
    p.on_position_changed_ctx = on_position_changed_ctx;
    p.on_remaining_items_threshold_reached = on_remaining_items_threshold_reached;
    p.on_remaining_items_threshold_reached_ctx = on_remaining_items_threshold_reached_ctx;
    return match box::new::[props::CarouselProps](p) {
        option::Option[box::Box[props::CarouselProps]]::Some(b) =>
            core::node_with(key, props::K_CAROUSEL, b.into_raw(), props::release_carousel_props),
        option::Option[box::Box[props::CarouselProps]]::None =>
            core::node_with(key, props::K_CAROUSEL, 0 as *u8, props::release_carousel_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Carousel {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Carousel {
    fn of(p: *core::Node) -> Carousel { return Carousel { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::CarouselProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::CarouselProps; }
        if { (*d).kind } != props::K_CAROUSEL { return 0 as *props::CarouselProps; }
        return { (*d).props } as *props::CarouselProps;
    }

    // CarouselView.IsBounceEnabled
    fn set_is_bounce_enabled(this, v: bool) -> Carousel {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return this; }
        { (*p).is_bounce_enabled = v };
        core::touch(this._p, P_IS_BOUNCE_ENABLED);
        return this;
    }

    // CarouselView.IsScrollAnimated
    fn set_is_scroll_animated(this, v: bool) -> Carousel {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return this; }
        { (*p).is_scroll_animated = v };
        core::touch(this._p, P_IS_SCROLL_ANIMATED);
        return this;
    }

    // CarouselView.IsScrolling
    fn set_is_scrolling(this, v: bool) -> Carousel {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return this; }
        { (*p).is_scrolling = v };
        core::touch(this._p, P_IS_SCROLLING);
        return this;
    }

    // CarouselView.IsSwipeEnabled
    fn set_is_swipe_enabled(this, v: bool) -> Carousel {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return this; }
        { (*p).is_swipe_enabled = v };
        core::touch(this._p, P_IS_SWIPE_ENABLED);
        return this;
    }

    // CarouselView.Loop
    fn set_wraps(this, v: bool) -> Carousel {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return this; }
        { (*p).wraps = v };
        core::touch(this._p, P_WRAPS);
        return this;
    }

    // CarouselView.PeekAreaInsets
    fn set_peek_area_insets(this, v: vocab::Insets) -> Carousel {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return this; }
        { (*p).peek_area_insets = v };
        core::touch(this._p, P_PEEK_AREA_INSETS);
        return this;
    }

    // CarouselView.Position
    fn set_position(this, v: i64) -> Carousel {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return this; }
        { (*p).position = v };
        core::touch(this._p, P_POSITION);
        return this;
    }

    // ItemsView.HorizontalScrollBarVisibility
    fn set_horizontal_scroll_bar_visibility(this, v: vocab::ScrollBars) -> Carousel {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return this; }
        { (*p).horizontal_scroll_bar_visibility = v };
        core::touch(this._p, P_HORIZONTAL_SCROLL_BAR_VISIBILITY);
        return this;
    }

    // ItemsView.ItemsUpdatingScrollMode
    fn set_items_updating_scroll_mode(this, v: vocab::ScrollAnchor) -> Carousel {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return this; }
        { (*p).items_updating_scroll_mode = v };
        core::touch(this._p, P_ITEMS_UPDATING_SCROLL_MODE);
        return this;
    }

    // ItemsView.RemainingItemsThreshold
    fn set_remaining_items_threshold(this, v: i64) -> Carousel {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return this; }
        { (*p).remaining_items_threshold = v };
        core::touch(this._p, P_REMAINING_ITEMS_THRESHOLD);
        return this;
    }

    // ItemsView.VerticalScrollBarVisibility
    fn set_vertical_scroll_bar_visibility(this, v: vocab::ScrollBars) -> Carousel {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return this; }
        { (*p).vertical_scroll_bar_visibility = v };
        core::touch(this._p, P_VERTICAL_SCROLL_BAR_VISIBILITY);
        return this;
    }

    // StructuredItemsView.ItemSizingStrategy
    fn set_item_sizing_strategy(this, v: vocab::ItemSizing) -> Carousel {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return this; }
        { (*p).item_sizing_strategy = v };
        core::touch(this._p, P_ITEM_SIZING_STRATEGY);
        return this;
    }

    fn is_bounce_enabled(this) -> bool {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return false; }
        return { (*p).is_bounce_enabled };
    }

    fn is_scroll_animated(this) -> bool {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return false; }
        return { (*p).is_scroll_animated };
    }

    fn is_scrolling(this) -> bool {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return false; }
        return { (*p).is_scrolling };
    }

    fn is_swipe_enabled(this) -> bool {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return false; }
        return { (*p).is_swipe_enabled };
    }

    fn wraps(this) -> bool {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return false; }
        return { (*p).wraps };
    }

    fn peek_area_insets(this) -> vocab::Insets {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return vocab::Insets::zero(); }
        return { (*p).peek_area_insets };
    }

    fn position(this) -> i64 {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return 0 as i64; }
        return { (*p).position };
    }

    fn horizontal_scroll_bar_visibility(this) -> vocab::ScrollBars {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return vocab::ScrollBars::Always; }
        return { (*p).horizontal_scroll_bar_visibility };
    }

    fn items_updating_scroll_mode(this) -> vocab::ScrollAnchor {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return vocab::ScrollAnchor::KeepItemsInView; }
        return { (*p).items_updating_scroll_mode };
    }

    fn remaining_items_threshold(this) -> i64 {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return 0 as i64; }
        return { (*p).remaining_items_threshold };
    }

    fn vertical_scroll_bar_visibility(this) -> vocab::ScrollBars {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return vocab::ScrollBars::Always; }
        return { (*p).vertical_scroll_bar_visibility };
    }

    fn item_sizing_strategy(this) -> vocab::ItemSizing {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return vocab::ItemSizing::MeasureAllItems; }
        return { (*p).item_sizing_strategy };
    }

    fn animate_current_item_changes(this) -> bool {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return false; }
        return { (*p).animate_current_item_changes };
    }

    fn animate_position_changes(this) -> bool {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return false; }
        return { (*p).animate_position_changes };
    }

    fn is_dragging(this) -> bool {
        let p: *props::CarouselProps = this._props();
        if p == (0 as *props::CarouselProps) { return false; }
        return { (*p).is_dragging };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Carousel { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_is_enabled(this, v: bool) -> Carousel { core::set_is_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_is_visible(this, v: bool) -> Carousel { core::set_is_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Carousel { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Carousel { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Carousel { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Carousel] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Carousel]::None; }
            if { (*d).kind } != props::K_CAROUSEL { return option::Option[Carousel]::None; }
            option::Option[Carousel]::Some(Carousel::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Carousel]::None,
    };
}
