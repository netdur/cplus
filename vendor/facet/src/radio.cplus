// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// radio — MAUI RadioButton. 12 writes, 0 reads, 1 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_BORDER_COLOR: u64 = 1u64;
const P_BORDER_WIDTH: u64 = 2u64;
const P_CHARACTER_SPACING: u64 = 4u64;
const P_CORNER_RADIUS: u64 = 8u64;
const P_FONT_ATTRIBUTES: u64 = 16u64;
const P_FONT_AUTO_SCALING_ENABLED: u64 = 32u64;
const P_FONT_FAMILY: u64 = 64u64;
const P_FONT_SIZE: u64 = 128u64;
const P_GROUP_NAME: u64 = 256u64;
const P_ON: u64 = 512u64;
const P_TEXT_COLOR: u64 = 1024u64;
const P_TEXT_TRANSFORM: u64 = 2048u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn radio(
    on: bool,
    key: str = "",
    border_color: vocab::Color = vocab::Color::clear(),
    border_width: f64 = 0.0f64,
    character_spacing: f64 = 0.0f64,
    corner_radius: vocab::Corners = vocab::Corners::none(),
    font_attributes: vocab::FontStyle = vocab::FontStyle::Bold,
    font_auto_scaling_enabled: bool = false,
    font_family: str = "",
    font_size: f64 = 0.0f64,
    group_name: str = "",
    text_color: vocab::Color = vocab::Color::clear(),
    text_transform: vocab::TextTransform = vocab::TextTransform::Default,
    on_checked_changed: fn(*u8, *u8) = props::no_handler,
    on_checked_changed_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::RadioProps = props::RadioProps::new();
    p.border_color = border_color;
    p.border_width = border_width;
    p.character_spacing = character_spacing;
    p.corner_radius = corner_radius;
    p.font_attributes = font_attributes;
    p.font_auto_scaling_enabled = font_auto_scaling_enabled;
    p.font_family = text::from_str(font_family);
    p.font_size = font_size;
    p.group_name = text::from_str(group_name);
    p.on = on;
    p.text_color = text_color;
    p.text_transform = text_transform;
    p.on_checked_changed = on_checked_changed;
    p.on_checked_changed_ctx = on_checked_changed_ctx;
    return match box::new::[props::RadioProps](p) {
        option::Option[box::Box[props::RadioProps]]::Some(b) =>
            core::node_with(key, props::K_RADIO, b.into_raw(), props::release_radio_props),
        option::Option[box::Box[props::RadioProps]]::None =>
            core::node_with(key, props::K_RADIO, 0 as *u8, props::release_radio_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Radio {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Radio {
    fn of(p: *core::Node) -> Radio { return Radio { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::RadioProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::RadioProps; }
        if { (*d).kind } != props::K_RADIO { return 0 as *props::RadioProps; }
        return { (*d).props } as *props::RadioProps;
    }

    // RadioButton.BorderColor
    fn set_border_color(this, v: vocab::Color) -> Radio {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return this; }
        { (*p).border_color = v };
        core::touch(this._p, P_BORDER_COLOR);
        return this;
    }

    // RadioButton.BorderWidth
    fn set_border_width(this, v: f64) -> Radio {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return this; }
        { (*p).border_width = v };
        core::touch(this._p, P_BORDER_WIDTH);
        return this;
    }

    // RadioButton.CharacterSpacing
    fn set_character_spacing(this, v: f64) -> Radio {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return this; }
        { (*p).character_spacing = v };
        core::touch(this._p, P_CHARACTER_SPACING);
        return this;
    }

    // RadioButton.CornerRadius
    fn set_corner_radius(this, v: vocab::Corners) -> Radio {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return this; }
        { (*p).corner_radius = v };
        core::touch(this._p, P_CORNER_RADIUS);
        return this;
    }

    // RadioButton.FontAttributes
    fn set_font_attributes(this, v: vocab::FontStyle) -> Radio {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return this; }
        { (*p).font_attributes = v };
        core::touch(this._p, P_FONT_ATTRIBUTES);
        return this;
    }

    // RadioButton.FontAutoScalingEnabled
    fn set_font_auto_scaling_enabled(this, v: bool) -> Radio {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return this; }
        { (*p).font_auto_scaling_enabled = v };
        core::touch(this._p, P_FONT_AUTO_SCALING_ENABLED);
        return this;
    }

    // RadioButton.FontFamily
    fn set_font_family(this, v: str) -> Radio {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return this; }
        { (*p).font_family = text::from_str(v) };
        core::touch(this._p, P_FONT_FAMILY);
        return this;
    }

    // RadioButton.FontSize
    fn set_font_size(this, v: f64) -> Radio {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return this; }
        { (*p).font_size = v };
        core::touch(this._p, P_FONT_SIZE);
        return this;
    }

    // RadioButton.GroupName
    fn set_group_name(this, v: str) -> Radio {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return this; }
        { (*p).group_name = text::from_str(v) };
        core::touch(this._p, P_GROUP_NAME);
        return this;
    }

    // RadioButton.IsChecked
    fn set_on(this, v: bool) -> Radio {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return this; }
        { (*p).on = v };
        core::touch(this._p, P_ON);
        return this;
    }

    // RadioButton.TextColor
    fn set_text_color(this, v: vocab::Color) -> Radio {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return this; }
        { (*p).text_color = v };
        core::touch(this._p, P_TEXT_COLOR);
        return this;
    }

    // RadioButton.TextTransform
    fn set_text_transform(this, v: vocab::TextTransform) -> Radio {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return this; }
        { (*p).text_transform = v };
        core::touch(this._p, P_TEXT_TRANSFORM);
        return this;
    }

    fn border_color(this) -> vocab::Color {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return vocab::Color::clear(); }
        return { (*p).border_color };
    }

    fn border_width(this) -> f64 {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return 0.0f64; }
        return { (*p).border_width };
    }

    fn character_spacing(this) -> f64 {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return 0.0f64; }
        return { (*p).character_spacing };
    }

    fn corner_radius(this) -> vocab::Corners {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return vocab::Corners::none(); }
        return { (*p).corner_radius };
    }

    fn font_attributes(this) -> vocab::FontStyle {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return vocab::FontStyle::Bold; }
        return { (*p).font_attributes };
    }

    fn font_auto_scaling_enabled(this) -> bool {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return false; }
        return { (*p).font_auto_scaling_enabled };
    }

    fn font_family(this) -> str {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return ""; }
        return { (*p).font_family.view() };
    }

    fn font_size(this) -> f64 {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return 0.0f64; }
        return { (*p).font_size };
    }

    fn group_name(this) -> str {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return ""; }
        return { (*p).group_name.view() };
    }

    fn on(this) -> bool {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return false; }
        return { (*p).on };
    }

    fn text_color(this) -> vocab::Color {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return vocab::Color::clear(); }
        return { (*p).text_color };
    }

    fn text_transform(this) -> vocab::TextTransform {
        let p: *props::RadioProps = this._props();
        if p == (0 as *props::RadioProps) { return vocab::TextTransform::Default; }
        return { (*p).text_transform };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Radio { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_enabled(this, v: bool) -> Radio { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> Radio { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Radio { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Radio { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Radio { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Radio] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Radio]::None; }
            if { (*d).kind } != props::K_RADIO { return option::Option[Radio]::None; }
            option::Option[Radio]::Some(Radio::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Radio]::None,
    };
}
