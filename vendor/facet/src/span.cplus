// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// span — MAUI Span. 11 writes, 0 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_BACKGROUND_COLOR: u64 = 1u64;
const P_CHARACTER_SPACING: u64 = 2u64;
const P_FONT_ATTRIBUTES: u64 = 4u64;
const P_FONT_AUTO_SCALING_ENABLED: u64 = 8u64;
const P_FONT_FAMILY: u64 = 16u64;
const P_FONT_SIZE: u64 = 32u64;
const P_LINE_HEIGHT: u64 = 64u64;
const P_TEXT: u64 = 128u64;
const P_TEXT_COLOR: u64 = 256u64;
const P_TEXT_DECORATIONS: u64 = 512u64;
const P_TEXT_TRANSFORM: u64 = 1024u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn span(
    text: str,
    key: str = "",
    background_color: vocab::Color = vocab::Color::clear(),
    character_spacing: f64 = 0.0f64,
    font_attributes: vocab::FontStyle = vocab::FontStyle::Bold,
    font_auto_scaling_enabled: bool = false,
    font_family: str = "",
    font_size: f64 = 0.0f64,
    line_height: f64 = 0.0f64,
    text_color: vocab::Color = vocab::Color::clear(),
    text_decorations: vocab::TextDecoration = vocab::TextDecoration::None,
    text_transform: vocab::TextTransform = vocab::TextTransform::Default,
) -> core::Node {
    var p: props::SpanProps = props::SpanProps::new();
    p.background_color = background_color;
    p.character_spacing = character_spacing;
    p.font_attributes = font_attributes;
    p.font_auto_scaling_enabled = font_auto_scaling_enabled;
    p.font_family = text::from_str(font_family);
    p.font_size = font_size;
    p.line_height = line_height;
    p.text = text::from_str(text);
    p.text_color = text_color;
    p.text_decorations = text_decorations;
    p.text_transform = text_transform;
    return match box::new::[props::SpanProps](p) {
        option::Option[box::Box[props::SpanProps]]::Some(b) =>
            core::node_with(key, props::K_SPAN, b.into_raw(), props::release_span_props),
        option::Option[box::Box[props::SpanProps]]::None =>
            core::node_with(key, props::K_SPAN, 0 as *u8, props::release_span_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Span {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Span {
    fn of(p: *core::Node) -> Span { return Span { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::SpanProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::SpanProps; }
        if { (*d).kind } != props::K_SPAN { return 0 as *props::SpanProps; }
        return { (*d).props } as *props::SpanProps;
    }

    // Span.BackgroundColor
    fn set_background_color(this, v: vocab::Color) -> Span {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return this; }
        { (*p).background_color = v };
        core::touch(this._p, P_BACKGROUND_COLOR);
        return this;
    }

    // Span.CharacterSpacing
    fn set_character_spacing(this, v: f64) -> Span {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return this; }
        { (*p).character_spacing = v };
        core::touch(this._p, P_CHARACTER_SPACING);
        return this;
    }

    // Span.FontAttributes
    fn set_font_attributes(this, v: vocab::FontStyle) -> Span {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return this; }
        { (*p).font_attributes = v };
        core::touch(this._p, P_FONT_ATTRIBUTES);
        return this;
    }

    // Span.FontAutoScalingEnabled
    fn set_font_auto_scaling_enabled(this, v: bool) -> Span {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return this; }
        { (*p).font_auto_scaling_enabled = v };
        core::touch(this._p, P_FONT_AUTO_SCALING_ENABLED);
        return this;
    }

    // Span.FontFamily
    fn set_font_family(this, v: str) -> Span {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return this; }
        { (*p).font_family = text::from_str(v) };
        core::touch(this._p, P_FONT_FAMILY);
        return this;
    }

    // Span.FontSize
    fn set_font_size(this, v: f64) -> Span {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return this; }
        { (*p).font_size = v };
        core::touch(this._p, P_FONT_SIZE);
        return this;
    }

    // Span.LineHeight
    fn set_line_height(this, v: f64) -> Span {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return this; }
        { (*p).line_height = v };
        core::touch(this._p, P_LINE_HEIGHT);
        return this;
    }

    // Span.Text
    fn set_text(this, v: str) -> Span {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return this; }
        { (*p).text = text::from_str(v) };
        core::touch(this._p, P_TEXT);
        return this;
    }

    // Span.TextColor
    fn set_text_color(this, v: vocab::Color) -> Span {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return this; }
        { (*p).text_color = v };
        core::touch(this._p, P_TEXT_COLOR);
        return this;
    }

    // Span.TextDecorations
    fn set_text_decorations(this, v: vocab::TextDecoration) -> Span {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return this; }
        { (*p).text_decorations = v };
        core::touch(this._p, P_TEXT_DECORATIONS);
        return this;
    }

    // Span.TextTransform
    fn set_text_transform(this, v: vocab::TextTransform) -> Span {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return this; }
        { (*p).text_transform = v };
        core::touch(this._p, P_TEXT_TRANSFORM);
        return this;
    }

    fn background_color(this) -> vocab::Color {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return vocab::Color::clear(); }
        return { (*p).background_color };
    }

    fn character_spacing(this) -> f64 {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return 0.0f64; }
        return { (*p).character_spacing };
    }

    fn font_attributes(this) -> vocab::FontStyle {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return vocab::FontStyle::Bold; }
        return { (*p).font_attributes };
    }

    fn font_auto_scaling_enabled(this) -> bool {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return false; }
        return { (*p).font_auto_scaling_enabled };
    }

    fn font_family(this) -> str {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return ""; }
        return { (*p).font_family.view() };
    }

    fn font_size(this) -> f64 {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return 0.0f64; }
        return { (*p).font_size };
    }

    fn line_height(this) -> f64 {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return 0.0f64; }
        return { (*p).line_height };
    }

    fn text(this) -> str {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return ""; }
        return { (*p).text.view() };
    }

    fn text_color(this) -> vocab::Color {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return vocab::Color::clear(); }
        return { (*p).text_color };
    }

    fn text_decorations(this) -> vocab::TextDecoration {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return vocab::TextDecoration::None; }
        return { (*p).text_decorations };
    }

    fn text_transform(this) -> vocab::TextTransform {
        let p: *props::SpanProps = this._props();
        if p == (0 as *props::SpanProps) { return vocab::TextTransform::Default; }
        return { (*p).text_transform };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Span { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_is_enabled(this, v: bool) -> Span { core::set_is_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_is_visible(this, v: bool) -> Span { core::set_is_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Span { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Span { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Span { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Span] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Span]::None; }
            if { (*d).kind } != props::K_SPAN { return option::Option[Span]::None; }
            option::Option[Span]::Some(Span::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Span]::None,
    };
}
