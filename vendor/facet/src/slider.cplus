// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// slider — MAUI Slider. 7 writes, 0 reads, 3 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_MAXIMUM: u64 = 1u64;
const P_MAXIMUM_TRACK_COLOR: u64 = 2u64;
const P_MINIMUM: u64 = 4u64;
const P_MINIMUM_TRACK_COLOR: u64 = 8u64;
const P_THUMB_COLOR: u64 = 16u64;
const P_THUMB_IMAGE_SOURCE: u64 = 32u64;
const P_VALUE: u64 = 64u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn slider(
    value: f64,
    key: str = "",
    maximum: f64 = 0.0f64,
    maximum_track_color: vocab::Color = vocab::Color::clear(),
    minimum: f64 = 0.0f64,
    minimum_track_color: vocab::Color = vocab::Color::clear(),
    thumb_color: vocab::Color = vocab::Color::clear(),
    thumb_image_source: str = "",
    on_drag_completed: fn(*u8, *u8) = props::no_handler,
    on_drag_completed_ctx: *u8 = 0 as *u8,
    on_drag_started: fn(*u8, *u8) = props::no_handler,
    on_drag_started_ctx: *u8 = 0 as *u8,
    on_value_changed: fn(*u8, *u8) = props::no_handler,
    on_value_changed_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::SliderProps = props::SliderProps::new();
    p.maximum = maximum;
    p.maximum_track_color = maximum_track_color;
    p.minimum = minimum;
    p.minimum_track_color = minimum_track_color;
    p.thumb_color = thumb_color;
    p.thumb_image_source = text::from_str(thumb_image_source);
    p.value = value;
    p.on_drag_completed = on_drag_completed;
    p.on_drag_completed_ctx = on_drag_completed_ctx;
    p.on_drag_started = on_drag_started;
    p.on_drag_started_ctx = on_drag_started_ctx;
    p.on_value_changed = on_value_changed;
    p.on_value_changed_ctx = on_value_changed_ctx;
    return match box::new::[props::SliderProps](p) {
        option::Option[box::Box[props::SliderProps]]::Some(b) =>
            core::node_with(key, props::K_SLIDER, b.into_raw(), props::release_slider_props),
        option::Option[box::Box[props::SliderProps]]::None =>
            core::node_with(key, props::K_SLIDER, 0 as *u8, props::release_slider_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Slider {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Slider {
    fn of(p: *core::Node) -> Slider { return Slider { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::SliderProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::SliderProps; }
        if { (*d).kind } != props::K_SLIDER { return 0 as *props::SliderProps; }
        return { (*d).props } as *props::SliderProps;
    }

    // Slider.Maximum
    fn set_maximum(this, v: f64) -> Slider {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return this; }
        { (*p).maximum = v };
        core::touch(this._p, P_MAXIMUM);
        return this;
    }

    // Slider.MaximumTrackColor
    fn set_maximum_track_color(this, v: vocab::Color) -> Slider {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return this; }
        { (*p).maximum_track_color = v };
        core::touch(this._p, P_MAXIMUM_TRACK_COLOR);
        return this;
    }

    // Slider.Minimum
    fn set_minimum(this, v: f64) -> Slider {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return this; }
        { (*p).minimum = v };
        core::touch(this._p, P_MINIMUM);
        return this;
    }

    // Slider.MinimumTrackColor
    fn set_minimum_track_color(this, v: vocab::Color) -> Slider {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return this; }
        { (*p).minimum_track_color = v };
        core::touch(this._p, P_MINIMUM_TRACK_COLOR);
        return this;
    }

    // Slider.ThumbColor
    fn set_thumb_color(this, v: vocab::Color) -> Slider {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return this; }
        { (*p).thumb_color = v };
        core::touch(this._p, P_THUMB_COLOR);
        return this;
    }

    // Slider.ThumbImageSource
    fn set_thumb_image_source(this, v: str) -> Slider {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return this; }
        { (*p).thumb_image_source = text::from_str(v) };
        core::touch(this._p, P_THUMB_IMAGE_SOURCE);
        return this;
    }

    // Slider.Value
    fn set_value(this, v: f64) -> Slider {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return this; }
        { (*p).value = v };
        core::touch(this._p, P_VALUE);
        return this;
    }

    fn maximum(this) -> f64 {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return 0.0f64; }
        return { (*p).maximum };
    }

    fn maximum_track_color(this) -> vocab::Color {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return vocab::Color::clear(); }
        return { (*p).maximum_track_color };
    }

    fn minimum(this) -> f64 {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return 0.0f64; }
        return { (*p).minimum };
    }

    fn minimum_track_color(this) -> vocab::Color {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return vocab::Color::clear(); }
        return { (*p).minimum_track_color };
    }

    fn thumb_color(this) -> vocab::Color {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return vocab::Color::clear(); }
        return { (*p).thumb_color };
    }

    fn thumb_image_source(this) -> str {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return ""; }
        return { (*p).thumb_image_source.view() };
    }

    fn value(this) -> f64 {
        let p: *props::SliderProps = this._props();
        if p == (0 as *props::SliderProps) { return 0.0f64; }
        return { (*p).value };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Slider { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_enabled(this, v: bool) -> Slider { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> Slider { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Slider { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Slider { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Slider { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Slider] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Slider]::None; }
            if { (*d).kind } != props::K_SLIDER { return option::Option[Slider]::None; }
            option::Option[Slider]::Some(Slider::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Slider]::None,
    };
}
