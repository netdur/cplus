// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// swipeable — MAUI SwipeView. 1 writes, 0 reads, 1 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_REVEAL_THRESHOLD: u64 = 1u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn swipeable(
    key: str = "",
    reveal_threshold: f64 = 0.0f64,
    on_swipe_started: fn(*u8, *u8) = props::no_handler,
    on_swipe_started_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::SwipeableProps = props::SwipeableProps::new();
    p.reveal_threshold = reveal_threshold;
    p.on_swipe_started = on_swipe_started;
    p.on_swipe_started_ctx = on_swipe_started_ctx;
    return match box::new::[props::SwipeableProps](p) {
        option::Option[box::Box[props::SwipeableProps]]::Some(b) =>
            core::node_with(key, props::K_SWIPEABLE, b.into_raw(), props::release_swipeable_props),
        option::Option[box::Box[props::SwipeableProps]]::None =>
            core::node_with(key, props::K_SWIPEABLE, 0 as *u8, props::release_swipeable_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Swipeable {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Swipeable {
    fn of(p: *core::Node) -> Swipeable { return Swipeable { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::SwipeableProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::SwipeableProps; }
        if { (*d).kind } != props::K_SWIPEABLE { return 0 as *props::SwipeableProps; }
        return { (*d).props } as *props::SwipeableProps;
    }

    // SwipeView.Threshold
    fn set_reveal_threshold(this, v: f64) -> Swipeable {
        let p: *props::SwipeableProps = this._props();
        if p == (0 as *props::SwipeableProps) { return this; }
        { (*p).reveal_threshold = v };
        core::touch(this._p, P_REVEAL_THRESHOLD);
        return this;
    }

    fn reveal_threshold(this) -> f64 {
        let p: *props::SwipeableProps = this._props();
        if p == (0 as *props::SwipeableProps) { return 0.0f64; }
        return { (*p).reveal_threshold };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Swipeable { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_is_enabled(this, v: bool) -> Swipeable { core::set_is_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_is_visible(this, v: bool) -> Swipeable { core::set_is_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Swipeable { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Swipeable { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Swipeable { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Swipeable] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Swipeable]::None; }
            if { (*d).kind } != props::K_SWIPEABLE { return option::Option[Swipeable]::None; }
            option::Option[Swipeable]::Some(Swipeable::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Swipeable]::None,
    };
}
