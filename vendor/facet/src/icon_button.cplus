// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// icon_button — the ledger ImageButton. 6 writes, 2 reads, 3 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;
import "./mount" as mount;

// ---- dirty bits --------------------------------------------------------
// One bit per write, then one per owned prop and one per command. The
// top 16 bits of the word are the shared band's (props::C_*).

const P_FIT: u64 = 1u64;
const P_BORDER_COLOR: u64 = 2u64;
const P_BORDER_WIDTH: u64 = 4u64;
const P_CORNER_RADIUS: u64 = 8u64;
const P_IS_OPAQUE: u64 = 16u64;
const P_SOURCE: u64 = 32u64;
const P_TOGGLES: u64 = 64u64;
const P_ON: u64 = 128u64;
const P_BORDERED: u64 = 256u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn icon_button(
    source: str,
    key: str = "",
    fit: vocab::ImageFit = vocab::ImageFit::AspectFit,
    border_color: vocab::Color = vocab::Color::clear(),
    border_width: f64 = 0.0f64,
    corner_radius: vocab::Corners = vocab::Corners::none(),
    is_opaque: bool = false,
    on_clicked: fn(*u8, *u8) = props::no_handler,
    on_clicked_ctx: *u8 = 0 as *u8,
    on_pressed: fn(*u8, *u8) = props::no_handler,
    on_pressed_ctx: *u8 = 0 as *u8,
    on_released: fn(*u8, *u8) = props::no_handler,
    on_released_ctx: *u8 = 0 as *u8,
    toggles: bool = false,
    on: bool = false,
    bordered: bool = true,
) -> core::Node {
    var p: props::IconButtonProps = props::IconButtonProps::new();
    p.fit = fit;
    p.border_color = border_color;
    p.border_width = border_width;
    p.corner_radius = corner_radius;
    p.is_opaque = is_opaque;
    p.source = text::from_str(source);
    p.on_clicked = on_clicked;
    p.on_clicked_ctx = on_clicked_ctx;
    p.on_pressed = on_pressed;
    p.on_pressed_ctx = on_pressed_ctx;
    p.on_released = on_released;
    p.on_released_ctx = on_released_ctx;
    p.toggles = toggles;
    p.on = on;
    p.bordered = bordered;
    return match box::new::[props::IconButtonProps](p) {
        option::Option[box::Box[props::IconButtonProps]]::Some(b) =>
            core::node_with(key, props::K_ICON_BUTTON, b.into_raw(), props::release_icon_button_props),
        option::Option[box::Box[props::IconButtonProps]]::None =>
            core::node_with(key, props::K_ICON_BUTTON, 0 as *u8, props::release_icon_button_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct IconButton {
    opaque _p: *core::Node,
    _seen: u64,
}

impl IconButton {
    // Unchecked, and module-private for that reason: it trusts the
    // caller about the kind. `from` is the public door.
    fn _of(p: *core::Node) -> IconButton { return IconButton { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::IconButtonProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::IconButtonProps; }
        if { (*d).kind } != props::K_ICON_BUTTON { return 0 as *props::IconButtonProps; }
        return { (*d).props } as *props::IconButtonProps;
    }

    // ImageButton.Aspect
    fn set_fit(this, v: vocab::ImageFit) -> IconButton {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return this; }
        { (*p).fit = v };
        core::touch(this._p, P_FIT);
        return this;
    }

    // ImageButton.BorderColor
    fn set_border_color(this, v: vocab::Color) -> IconButton {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return this; }
        { (*p).border_color = v };
        core::touch(this._p, P_BORDER_COLOR);
        return this;
    }

    // ImageButton.BorderWidth
    fn set_border_width(this, v: f64) -> IconButton {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return this; }
        { (*p).border_width = v };
        core::touch(this._p, P_BORDER_WIDTH);
        return this;
    }

    // ImageButton.CornerRadius
    fn set_corner_radius(this, v: vocab::Corners) -> IconButton {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return this; }
        { (*p).corner_radius = v };
        core::touch(this._p, P_CORNER_RADIUS);
        return this;
    }

    // ImageButton.IsOpaque
    fn set_is_opaque(this, v: bool) -> IconButton {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return this; }
        { (*p).is_opaque = v };
        core::touch(this._p, P_IS_OPAQUE);
        return this;
    }

    // ImageButton.Source
    fn set_source(this, v: str) -> IconButton {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return this; }
        { (*p).source = text::from_str(v) };
        core::touch(this._p, P_SOURCE);
        return this;
    }

    fn fit(this) -> vocab::ImageFit {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return vocab::ImageFit::AspectFit; }
        return { (*p).fit };
    }

    fn border_color(this) -> vocab::Color {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return vocab::Color::clear(); }
        return { (*p).border_color };
    }

    fn border_width(this) -> f64 {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return 0.0f64; }
        return { (*p).border_width };
    }

    fn corner_radius(this) -> vocab::Corners {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return vocab::Corners::none(); }
        return { (*p).corner_radius };
    }

    fn is_opaque(this) -> bool {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return false; }
        return { (*p).is_opaque };
    }

    fn source(this) -> str {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return ""; }
        return { (*p).source.view() };
    }

    fn is_loading(this) -> bool {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return false; }
        return { (*p).is_loading };
    }

    fn is_pressed(this) -> bool {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return false; }
        return { (*p).is_pressed };
    }

    // facet's own words. The ledger has no button that FLIPS — its toggle is a
    // CheckBox or a Switch, which draw their own control and cannot be a
    // plain word or an icon — and no way to switch a border off without
    // losing its width. Both gaps fill together: a chip is a button that
    // toggles and shows a border when it is on.
    //
    // `toggles` hands the state to AppKit's PushOnPushOff, so `is_on()`
    // cannot drift from what the user sees.
    fn set_toggles(this, v: bool) -> IconButton {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return this; }
        { (*p).toggles = v };
        core::touch(this._p, P_TOGGLES);
        return this;
    }

    fn toggles(this) -> bool {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return false; }
        return { (*p).toggles };
    }

    fn set_on(this, v: bool) -> IconButton {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return this; }
        { (*p).on = v };
        core::touch(this._p, P_ON);
        return this;
    }

    fn is_on(this) -> bool {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return false; }
        return { (*p).on };
    }

    // The border is DESCRIBED by border_color / border_width /
    // corner_radius and SWITCHED by this. Off does not forget: a width of
    // 0 would have meant restating the border every time it came back.
    fn set_bordered(this, v: bool) -> IconButton {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return this; }
        { (*p).bordered = v };
        core::touch(this._p, P_BORDERED);
        return this;
    }

    fn is_bordered(this) -> bool {
        let p: *props::IconButtonProps = this._props();
        if p == (0 as *props::IconButtonProps) { return false; }
        return { (*p).bordered };
    }

    // The flex node this cursor points at. The DOM division, and the
    // reason facet declares no layout verb of its own:
    //
    //   the control's own   b.set_title("Saving…")     this module
    //   the shared band     b.set_opacity(0.5)         facet.cplus, once
    //   layout              { (*b.node()).set_width(…) }  ALL of flex
    //
    // Reaching flex through the node rather than forwarding its ~40 verbs
    // onto 42 cursors is what keeps facet from growing a second layout
    // vocabulary that can disagree with the first.
    fn node(this) -> *core::Node { return this._p; }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> IconButton { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_background_color(this, v: vocab::Color) -> IconButton { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_background(this, v: vocab::Brush) -> IconButton { core::set_background(this._p, v); return this; }
    fn background(this) -> vocab::Brush { return core::background(this._p); }
    fn set_background_image(this, v: str) -> IconButton { core::set_background_image(this._p, v); return this; }
    fn background_image(this) -> str { return core::background_image(this._p); }
    fn set_safe_area(this, v: vocab::SafeArea) -> IconButton { core::set_safe_area(this._p, v); return this; }
    fn safe_area(this) -> vocab::SafeArea { return core::safe_area(this._p); }
    fn set_shadow(this, v: vocab::Shadow) -> IconButton { core::set_shadow(this._p, v); return this; }
    fn shadow(this) -> vocab::Shadow { return core::shadow(this._p); }
    fn set_clip(this, v: vocab::Shape) -> IconButton { core::set_clip(this._p, v); return this; }
    fn clip(this) -> vocab::Shape { return core::clip(this._p); }
    fn set_enabled(this, v: bool) -> IconButton { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> IconButton { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_display(this, v: flex::Display) -> IconButton { core::set_display(this._p, v); return this; }
    fn display(this) -> flex::Display { return core::display_of(this._p); }
    fn set_shown(this, v: bool) -> IconButton { core::set_shown(this._p, v); return this; }
    fn is_hidden(this) -> bool { return core::is_hidden(this._p); }
    fn set_input_transparent(this, v: bool) -> IconButton { core::set_input_transparent(this._p, v); return this; }
    fn input_transparent(this) -> bool { return core::input_transparent(this._p); }
    fn set_flow_direction(this, v: vocab::FlowDirection) -> IconButton { core::set_flow_direction(this._p, v); return this; }
    fn flow_direction(this) -> vocab::FlowDirection { return core::flow_direction(this._p); }
    fn set_rotation(this, v: f64) -> IconButton { core::set_rotation(this._p, v); return this; }
    fn rotation(this) -> f64 { return core::rotation(this._p); }
    fn set_rotation_x(this, v: f64) -> IconButton { core::set_rotation_x(this._p, v); return this; }
    fn rotation_x(this) -> f64 { return core::rotation_x(this._p); }
    fn set_rotation_y(this, v: f64) -> IconButton { core::set_rotation_y(this._p, v); return this; }
    fn rotation_y(this) -> f64 { return core::rotation_y(this._p); }
    fn set_scale(this, v: f64) -> IconButton { core::set_scale(this._p, v); return this; }
    fn scale(this) -> f64 { return core::scale(this._p); }
    fn set_scale_x(this, v: f64) -> IconButton { core::set_scale_x(this._p, v); return this; }
    fn scale_x(this) -> f64 { return core::scale_x(this._p); }
    fn set_scale_y(this, v: f64) -> IconButton { core::set_scale_y(this._p, v); return this; }
    fn scale_y(this) -> f64 { return core::scale_y(this._p); }
    fn set_translation_x(this, v: f64) -> IconButton { core::set_translation_x(this._p, v); return this; }
    fn translation_x(this) -> f64 { return core::translation_x(this._p); }
    fn set_translation_y(this, v: f64) -> IconButton { core::set_translation_y(this._p, v); return this; }
    fn translation_y(this) -> f64 { return core::translation_y(this._p); }
    fn set_anchor_x(this, v: f64) -> IconButton { core::set_anchor_x(this._p, v); return this; }
    fn anchor_x(this) -> f64 { return core::anchor_x(this._p); }
    fn set_anchor_y(this, v: f64) -> IconButton { core::set_anchor_y(this._p, v); return this; }
    fn anchor_y(this) -> f64 { return core::anchor_y(this._p); }
    fn set_z_index(this, v: i64) -> IconButton { core::set_z_index(this._p, v); return this; }
    fn z_index(this) -> i64 { return core::z_index(this._p); }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn bounds(this) -> vocab::Rect { return core::bounds_of(this._p); }
    fn is_focused(this) -> bool { return core::is_focused(this._p); }
    fn is_attached(this) -> bool { return core::is_attached(this._p); }
    fn window(this) -> vocab::WindowRef { return core::window_of(this._p); }
    fn native(this) -> *u8 { return core::native(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn child(this, at: usize) -> option::Option[*core::Node] { return core::child_of(this._p, at); }
    fn set_grow(this, v: f64) -> IconButton { core::set_grow(this._p, v); return this; }
    fn set_shrink(this, v: f64) -> IconButton { core::set_shrink(this._p, v); return this; }
    fn set_width(this, v: f64) -> IconButton { core::set_width(this._p, v); return this; }
    fn set_height(this, v: f64) -> IconButton { core::set_height(this._p, v); return this; }
    fn set_width_percent(this, v: f64) -> IconButton { core::set_width_percent(this._p, v); return this; }
    fn set_height_percent(this, v: f64) -> IconButton { core::set_height_percent(this._p, v); return this; }
    fn set_padding(this, v: f64) -> IconButton { core::set_padding(this._p, v); return this; }
    fn set_margin(this, v: f64) -> IconButton { core::set_margin(this._p, v); return this; }
    fn set_padding_edge(this, edge: flex::Edge, v: f64) -> IconButton { core::set_padding_edge(this._p, edge, v); return this; }
    fn set_margin_edge(this, edge: flex::Edge, v: f64) -> IconButton { core::set_margin_edge(this._p, edge, v); return this; }
    fn set_gap(this, v: f64) -> IconButton { core::set_gap(this._p, v); return this; }
    fn set_justify(this, v: flex::Justify) -> IconButton { core::set_justify(this._p, v); return this; }
    fn set_align(this, v: flex::Align) -> IconButton { core::set_align(this._p, v); return this; }
    fn set_wrap(this, v: flex::Wrap) -> IconButton { core::set_wrap(this._p, v); return this; }
    fn focus(this) -> IconButton { core::focus(this._p); return this; }
    fn blur(this) -> IconButton { core::blur(this._p); return this; }
    fn relayout(this) -> IconButton { core::relayout(this._p); return this; }
    fn measure(this, width: f64, height: f64) -> vocab::Size { return core::measure(this._p, width, height); }
    fn begin_updates(this) -> IconButton { core::begin_updates(this._p); return this; }
    fn end_updates(this) -> IconButton { core::end_updates(this._p); return this; }
    fn on_focus(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> IconButton { core::set_on_focus(this._p, f, ctx); return this; }
    fn on_blur(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> IconButton { core::set_on_blur(this._p, f, ctx); return this; }
    fn on_attach(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> IconButton { core::set_on_attach(this._p, f, ctx); return this; }
    fn on_detach(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> IconButton { core::set_on_detach(this._p, f, ctx); return this; }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup ------------------------------------------------------------
// `from` narrows a node to this control, the DOM step: a node carries what
// EVERY node has, and a checked cast adds what only a icon_button has. `None` on
// a wrong kind — never a cursor whose every verb silently does nothing.

fn from(p: *core::Node) -> option::Option[IconButton] {
    if p == (0 as *core::Node) { return option::Option[IconButton]::None; }
    let d: *core::Data = { (*p).data() };
    if d == (0 as *core::Data) { return option::Option[IconButton]::None; }
    if { (*d).kind } != props::K_ICON_BUTTON { return option::Option[IconButton]::None; }
    return option::Option[IconButton]::Some(IconButton::_of(p));
}

// Resolve a key, then narrow through the same checked door.

// `within:` and not `in:` — `in` is a C+ keyword. Bare `find` is the
// DOM rule: the running app's windows in open order, first match wins;
// pass `within:` to scope a subtree (or disambiguate across windows).
fn find(key: str, within: *core::Node = 0 as *core::Node) -> option::Option[IconButton] {
    if within != (0 as *core::Node) {
        return match core::find_in(within, key) {
            option::Option[*core::Node]::Some(p) => from(p),
            option::Option[*core::Node]::None => option::Option[IconButton]::None,
        };
    }
    return match mount::find_node(key) {
        option::Option[*core::Node]::Some(p) => from(p),
        option::Option[*core::Node]::None => option::Option[IconButton]::None,
    };
}
