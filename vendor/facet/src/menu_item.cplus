// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// menu_item — MAUI MenuItem. 4 writes, 0 reads, 1 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_ICON_IMAGE_SOURCE: u64 = 1u64;
const P_IS_DESTRUCTIVE: u64 = 2u64;
const P_IS_ENABLED: u64 = 4u64;
const P_TEXT: u64 = 8u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn menu_item(
    text: str,
    key: str = "",
    icon_image_source: str = "",
    destructive: bool = false,
    enabled: bool = false,
    on_clicked: fn(*u8, *u8) = props::no_handler,
    on_clicked_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::MenuItemProps = props::MenuItemProps::new();
    p.icon_image_source = text::from_str(icon_image_source);
    p.is_destructive = destructive;
    p.is_enabled = enabled;
    p.text = text::from_str(text);
    p.on_clicked = on_clicked;
    p.on_clicked_ctx = on_clicked_ctx;
    return match box::new::[props::MenuItemProps](p) {
        option::Option[box::Box[props::MenuItemProps]]::Some(b) =>
            core::node_with(key, props::K_MENU_ITEM, b.into_raw(), props::release_menu_item_props),
        option::Option[box::Box[props::MenuItemProps]]::None =>
            core::node_with(key, props::K_MENU_ITEM, 0 as *u8, props::release_menu_item_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct MenuItem {
    opaque _p: *core::Node,
    _seen: u64,
}

impl MenuItem {
    fn of(p: *core::Node) -> MenuItem { return MenuItem { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::MenuItemProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::MenuItemProps; }
        if { (*d).kind } != props::K_MENU_ITEM { return 0 as *props::MenuItemProps; }
        return { (*d).props } as *props::MenuItemProps;
    }

    // MenuItem.IconImageSource
    fn set_icon_image_source(this, v: str) -> MenuItem {
        let p: *props::MenuItemProps = this._props();
        if p == (0 as *props::MenuItemProps) { return this; }
        { (*p).icon_image_source = text::from_str(v) };
        core::touch(this._p, P_ICON_IMAGE_SOURCE);
        return this;
    }

    // MenuItem.IsDestructive
    fn set_destructive(this, v: bool) -> MenuItem {
        let p: *props::MenuItemProps = this._props();
        if p == (0 as *props::MenuItemProps) { return this; }
        { (*p).is_destructive = v };
        core::touch(this._p, P_IS_DESTRUCTIVE);
        return this;
    }

    // MenuItem.IsEnabled
    fn set_enabled(this, v: bool) -> MenuItem {
        let p: *props::MenuItemProps = this._props();
        if p == (0 as *props::MenuItemProps) { return this; }
        { (*p).is_enabled = v };
        core::touch(this._p, P_IS_ENABLED);
        return this;
    }

    // MenuItem.Text
    fn set_text(this, v: str) -> MenuItem {
        let p: *props::MenuItemProps = this._props();
        if p == (0 as *props::MenuItemProps) { return this; }
        { (*p).text = text::from_str(v) };
        core::touch(this._p, P_TEXT);
        return this;
    }

    fn icon_image_source(this) -> str {
        let p: *props::MenuItemProps = this._props();
        if p == (0 as *props::MenuItemProps) { return ""; }
        return { (*p).icon_image_source.view() };
    }

    fn is_destructive(this) -> bool {
        let p: *props::MenuItemProps = this._props();
        if p == (0 as *props::MenuItemProps) { return false; }
        return { (*p).is_destructive };
    }

    fn is_enabled(this) -> bool {
        let p: *props::MenuItemProps = this._props();
        if p == (0 as *props::MenuItemProps) { return false; }
        return { (*p).is_enabled };
    }

    fn text(this) -> str {
        let p: *props::MenuItemProps = this._props();
        if p == (0 as *props::MenuItemProps) { return ""; }
        return { (*p).text.view() };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> MenuItem { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_enabled(this, v: bool) -> MenuItem { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> MenuItem { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> MenuItem { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> MenuItem { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> MenuItem { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[MenuItem] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[MenuItem]::None; }
            if { (*d).kind } != props::K_MENU_ITEM { return option::Option[MenuItem]::None; }
            option::Option[MenuItem]::Some(MenuItem::of(p))
        }
        option::Option[*core::Node]::None => option::Option[MenuItem]::None,
    };
}
