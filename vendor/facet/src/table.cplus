// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// table — MAUI TableView. 3 writes, 0 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;
import "./mount" as mount;

// ---- dirty bits --------------------------------------------------------
// One bit per write, then one per owned prop and one per command. The
// top 16 bits of the word are the shared band's (props::C_*).

const P_HAS_UNEVEN_ROWS: u64 = 1u64;
const P_STYLE: u64 = 2u64;
const P_ROW_HEIGHT: u64 = 4u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn table(
    key: str = "",
    uneven_rows: bool = false,
    style: vocab::TableStyle = vocab::TableStyle::Data,
    row_height: i64 = 0 as i64,
) -> core::Node {
    var p: props::TableProps = props::TableProps::new();
    p.has_uneven_rows = uneven_rows;
    p.style = style;
    p.row_height = row_height;
    return match box::new::[props::TableProps](p) {
        option::Option[box::Box[props::TableProps]]::Some(b) =>
            core::node_with(key, props::K_TABLE, b.into_raw(), props::release_table_props),
        option::Option[box::Box[props::TableProps]]::None =>
            core::node_with(key, props::K_TABLE, 0 as *u8, props::release_table_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Table {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Table {
    // Unchecked, and module-private for that reason: it trusts the
    // caller about the kind. `from` is the public door.
    fn _of(p: *core::Node) -> Table { return Table { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::TableProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::TableProps; }
        if { (*d).kind } != props::K_TABLE { return 0 as *props::TableProps; }
        return { (*d).props } as *props::TableProps;
    }

    // TableView.HasUnevenRows
    fn set_uneven_rows(this, v: bool) -> Table {
        let p: *props::TableProps = this._props();
        if p == (0 as *props::TableProps) { return this; }
        { (*p).has_uneven_rows = v };
        core::touch(this._p, P_HAS_UNEVEN_ROWS);
        return this;
    }

    // TableView.Intent
    fn set_style(this, v: vocab::TableStyle) -> Table {
        let p: *props::TableProps = this._props();
        if p == (0 as *props::TableProps) { return this; }
        { (*p).style = v };
        core::touch(this._p, P_STYLE);
        return this;
    }

    // TableView.RowHeight
    fn set_row_height(this, v: i64) -> Table {
        let p: *props::TableProps = this._props();
        if p == (0 as *props::TableProps) { return this; }
        { (*p).row_height = v };
        core::touch(this._p, P_ROW_HEIGHT);
        return this;
    }

    fn has_uneven_rows(this) -> bool {
        let p: *props::TableProps = this._props();
        if p == (0 as *props::TableProps) { return false; }
        return { (*p).has_uneven_rows };
    }

    fn style(this) -> vocab::TableStyle {
        let p: *props::TableProps = this._props();
        if p == (0 as *props::TableProps) { return vocab::TableStyle::Data; }
        return { (*p).style };
    }

    fn row_height(this) -> i64 {
        let p: *props::TableProps = this._props();
        if p == (0 as *props::TableProps) { return 0 as i64; }
        return { (*p).row_height };
    }

    // TableView.Root — a named child, not a property. `@` keys are
    // facet's; an application's key never starts with one.
    fn set_content(this, take n: core::Node) -> Table {
        core::set_slot(this._p, "@content", n);
        return this;
    }

    fn content(this) -> option::Option[*core::Node] {
        return core::slot(this._p, "@content");
    }

    // The flex node this cursor points at. The DOM division, and the
    // reason facet declares no layout verb of its own:
    //
    //   the control's own   b.set_title("Saving…")     this module
    //   the shared band     b.set_opacity(0.5)         facet.cplus, once
    //   layout              { (*b.node()).set_width(…) }  ALL of flex
    //
    // Reaching flex through the node rather than forwarding its ~40 verbs
    // onto 42 cursors is what keeps facet from growing a second layout
    // vocabulary that can disagree with the first.
    fn node(this) -> *core::Node { return this._p; }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Table { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Table { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_background(this, v: vocab::Brush) -> Table { core::set_background(this._p, v); return this; }
    fn background(this) -> vocab::Brush { return core::background(this._p); }
    fn set_shadow(this, v: vocab::Shadow) -> Table { core::set_shadow(this._p, v); return this; }
    fn shadow(this) -> vocab::Shadow { return core::shadow(this._p); }
    fn set_clip(this, v: vocab::Shape) -> Table { core::set_clip(this._p, v); return this; }
    fn clip(this) -> vocab::Shape { return core::clip(this._p); }
    fn set_enabled(this, v: bool) -> Table { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> Table { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_input_transparent(this, v: bool) -> Table { core::set_input_transparent(this._p, v); return this; }
    fn input_transparent(this) -> bool { return core::input_transparent(this._p); }
    fn set_flow_direction(this, v: vocab::FlowDirection) -> Table { core::set_flow_direction(this._p, v); return this; }
    fn flow_direction(this) -> vocab::FlowDirection { return core::flow_direction(this._p); }
    fn set_rotation(this, v: f64) -> Table { core::set_rotation(this._p, v); return this; }
    fn rotation(this) -> f64 { return core::rotation(this._p); }
    fn set_rotation_x(this, v: f64) -> Table { core::set_rotation_x(this._p, v); return this; }
    fn rotation_x(this) -> f64 { return core::rotation_x(this._p); }
    fn set_rotation_y(this, v: f64) -> Table { core::set_rotation_y(this._p, v); return this; }
    fn rotation_y(this) -> f64 { return core::rotation_y(this._p); }
    fn set_scale(this, v: f64) -> Table { core::set_scale(this._p, v); return this; }
    fn scale(this) -> f64 { return core::scale(this._p); }
    fn set_scale_x(this, v: f64) -> Table { core::set_scale_x(this._p, v); return this; }
    fn scale_x(this) -> f64 { return core::scale_x(this._p); }
    fn set_scale_y(this, v: f64) -> Table { core::set_scale_y(this._p, v); return this; }
    fn scale_y(this) -> f64 { return core::scale_y(this._p); }
    fn set_translation_x(this, v: f64) -> Table { core::set_translation_x(this._p, v); return this; }
    fn translation_x(this) -> f64 { return core::translation_x(this._p); }
    fn set_translation_y(this, v: f64) -> Table { core::set_translation_y(this._p, v); return this; }
    fn translation_y(this) -> f64 { return core::translation_y(this._p); }
    fn set_anchor_x(this, v: f64) -> Table { core::set_anchor_x(this._p, v); return this; }
    fn anchor_x(this) -> f64 { return core::anchor_x(this._p); }
    fn set_anchor_y(this, v: f64) -> Table { core::set_anchor_y(this._p, v); return this; }
    fn anchor_y(this) -> f64 { return core::anchor_y(this._p); }
    fn set_z_index(this, v: i64) -> Table { core::set_z_index(this._p, v); return this; }
    fn z_index(this) -> i64 { return core::z_index(this._p); }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn bounds(this) -> vocab::Rect { return core::bounds_of(this._p); }
    fn is_focused(this) -> bool { return core::is_focused(this._p); }
    fn is_attached(this) -> bool { return core::is_attached(this._p); }
    fn window(this) -> vocab::WindowRef { return core::window_of(this._p); }
    fn native(this) -> *u8 { return core::native(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn child(this, at: usize) -> option::Option[*core::Node] { return core::child_of(this._p, at); }
    fn set_grow(this, v: f64) -> Table { core::set_grow(this._p, v); return this; }
    fn set_shrink(this, v: f64) -> Table { core::set_shrink(this._p, v); return this; }
    fn set_width(this, v: f64) -> Table { core::set_width(this._p, v); return this; }
    fn set_height(this, v: f64) -> Table { core::set_height(this._p, v); return this; }
    fn set_width_percent(this, v: f64) -> Table { core::set_width_percent(this._p, v); return this; }
    fn set_height_percent(this, v: f64) -> Table { core::set_height_percent(this._p, v); return this; }
    fn set_padding(this, v: f64) -> Table { core::set_padding(this._p, v); return this; }
    fn set_margin(this, v: f64) -> Table { core::set_margin(this._p, v); return this; }
    fn set_gap(this, v: f64) -> Table { core::set_gap(this._p, v); return this; }
    fn set_justify(this, v: flex::Justify) -> Table { core::set_justify(this._p, v); return this; }
    fn set_align(this, v: flex::Align) -> Table { core::set_align(this._p, v); return this; }
    fn set_wrap(this, v: flex::Wrap) -> Table { core::set_wrap(this._p, v); return this; }
    fn focus(this) -> Table { core::focus(this._p); return this; }
    fn blur(this) -> Table { core::blur(this._p); return this; }
    fn relayout(this) -> Table { core::relayout(this._p); return this; }
    fn measure(this, width: f64, height: f64) -> vocab::Size { return core::measure(this._p, width, height); }
    fn begin_updates(this) -> Table { core::begin_updates(this._p); return this; }
    fn end_updates(this) -> Table { core::end_updates(this._p); return this; }
    fn on_focus(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Table { core::set_on_focus(this._p, f, ctx); return this; }
    fn on_blur(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Table { core::set_on_blur(this._p, f, ctx); return this; }
    fn on_attach(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Table { core::set_on_attach(this._p, f, ctx); return this; }
    fn on_detach(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Table { core::set_on_detach(this._p, f, ctx); return this; }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup ------------------------------------------------------------
// `from` narrows a node to this control, the DOM step: a node carries what
// EVERY node has, and a checked cast adds what only a table has. `None` on
// a wrong kind — never a cursor whose every verb silently does nothing.

fn from(p: *core::Node) -> option::Option[Table] {
    if p == (0 as *core::Node) { return option::Option[Table]::None; }
    let d: *core::Data = { (*p).data() };
    if d == (0 as *core::Data) { return option::Option[Table]::None; }
    if { (*d).kind } != props::K_TABLE { return option::Option[Table]::None; }
    return option::Option[Table]::Some(Table::_of(p));
}

// Resolve a key, then narrow through the same checked door.

// `within:` and not `in:` — `in` is a C+ keyword. Bare `find` is the
// DOM rule: the running app's windows in open order, first match wins;
// pass `within:` to scope a subtree (or disambiguate across windows).
fn find(key: str, within: *core::Node = 0 as *core::Node) -> option::Option[Table] {
    if within != (0 as *core::Node) {
        return match core::find_in(within, key) {
            option::Option[*core::Node]::Some(p) => from(p),
            option::Option[*core::Node]::None => option::Option[Table]::None,
        };
    }
    return match mount::find_node(key) {
        option::Option[*core::Node]::Some(p) => from(p),
        option::Option[*core::Node]::None => option::Option[Table]::None,
    };
}
