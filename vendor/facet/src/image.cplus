// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// image — MAUI Image. 4 writes, 1 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_ASPECT: u64 = 1u64;
const P_IS_ANIMATION_PLAYING: u64 = 2u64;
const P_IS_OPAQUE: u64 = 4u64;
const P_SOURCE: u64 = 8u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn image(
    source: str,
    key: str = "",
    aspect: vocab::ImageFit = vocab::ImageFit::AspectFill,
    animation_playing: bool = false,
    is_opaque: bool = false,
) -> core::Node {
    var p: props::ImageProps = props::ImageProps::new();
    p.aspect = aspect;
    p.is_animation_playing = animation_playing;
    p.is_opaque = is_opaque;
    p.source = text::from_str(source);
    return match box::new::[props::ImageProps](p) {
        option::Option[box::Box[props::ImageProps]]::Some(b) =>
            core::node_with(key, props::K_IMAGE, b.into_raw(), props::release_image_props),
        option::Option[box::Box[props::ImageProps]]::None =>
            core::node_with(key, props::K_IMAGE, 0 as *u8, props::release_image_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Image {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Image {
    fn of(p: *core::Node) -> Image { return Image { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::ImageProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::ImageProps; }
        if { (*d).kind } != props::K_IMAGE { return 0 as *props::ImageProps; }
        return { (*d).props } as *props::ImageProps;
    }

    // Image.Aspect
    fn set_aspect(this, v: vocab::ImageFit) -> Image {
        let p: *props::ImageProps = this._props();
        if p == (0 as *props::ImageProps) { return this; }
        { (*p).aspect = v };
        core::touch(this._p, P_ASPECT);
        return this;
    }

    // Image.IsAnimationPlaying
    fn set_animation_playing(this, v: bool) -> Image {
        let p: *props::ImageProps = this._props();
        if p == (0 as *props::ImageProps) { return this; }
        { (*p).is_animation_playing = v };
        core::touch(this._p, P_IS_ANIMATION_PLAYING);
        return this;
    }

    // Image.IsOpaque
    fn set_is_opaque(this, v: bool) -> Image {
        let p: *props::ImageProps = this._props();
        if p == (0 as *props::ImageProps) { return this; }
        { (*p).is_opaque = v };
        core::touch(this._p, P_IS_OPAQUE);
        return this;
    }

    // Image.Source
    fn set_source(this, v: str) -> Image {
        let p: *props::ImageProps = this._props();
        if p == (0 as *props::ImageProps) { return this; }
        { (*p).source = text::from_str(v) };
        core::touch(this._p, P_SOURCE);
        return this;
    }

    fn aspect(this) -> vocab::ImageFit {
        let p: *props::ImageProps = this._props();
        if p == (0 as *props::ImageProps) { return vocab::ImageFit::AspectFill; }
        return { (*p).aspect };
    }

    fn is_animation_playing(this) -> bool {
        let p: *props::ImageProps = this._props();
        if p == (0 as *props::ImageProps) { return false; }
        return { (*p).is_animation_playing };
    }

    fn is_opaque(this) -> bool {
        let p: *props::ImageProps = this._props();
        if p == (0 as *props::ImageProps) { return false; }
        return { (*p).is_opaque };
    }

    fn source(this) -> str {
        let p: *props::ImageProps = this._props();
        if p == (0 as *props::ImageProps) { return ""; }
        return { (*p).source.view() };
    }

    fn is_loading(this) -> bool {
        let p: *props::ImageProps = this._props();
        if p == (0 as *props::ImageProps) { return false; }
        return { (*p).is_loading };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Image { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_enabled(this, v: bool) -> Image { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> Image { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Image { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Image { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Image { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Image] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Image]::None; }
            if { (*d).kind } != props::K_IMAGE { return option::Option[Image]::None; }
            option::Option[Image]::Some(Image::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Image]::None,
    };
}
