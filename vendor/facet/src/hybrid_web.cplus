// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// hybrid_web — MAUI HybridWebView. 2 writes, 0 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_DEFAULT_FILE: u64 = 1u64;
const P_HYBRID_ROOT: u64 = 2u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn hybrid_web(
    key: str = "",
    default_file: str = "",
    hybrid_root: str = "",
) -> core::Node {
    var p: props::HybridWebProps = props::HybridWebProps::new();
    p.default_file = text::from_str(default_file);
    p.hybrid_root = text::from_str(hybrid_root);
    return match box::new::[props::HybridWebProps](p) {
        option::Option[box::Box[props::HybridWebProps]]::Some(b) =>
            core::node_with(key, props::K_HYBRID_WEB, b.into_raw(), props::release_hybrid_web_props),
        option::Option[box::Box[props::HybridWebProps]]::None =>
            core::node_with(key, props::K_HYBRID_WEB, 0 as *u8, props::release_hybrid_web_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct HybridWeb {
    opaque _p: *core::Node,
    _seen: u64,
}

impl HybridWeb {
    fn of(p: *core::Node) -> HybridWeb { return HybridWeb { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::HybridWebProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::HybridWebProps; }
        if { (*d).kind } != props::K_HYBRID_WEB { return 0 as *props::HybridWebProps; }
        return { (*d).props } as *props::HybridWebProps;
    }

    // HybridWebView.DefaultFile
    fn set_default_file(this, v: str) -> HybridWeb {
        let p: *props::HybridWebProps = this._props();
        if p == (0 as *props::HybridWebProps) { return this; }
        { (*p).default_file = text::from_str(v) };
        core::touch(this._p, P_DEFAULT_FILE);
        return this;
    }

    // HybridWebView.HybridRoot
    fn set_hybrid_root(this, v: str) -> HybridWeb {
        let p: *props::HybridWebProps = this._props();
        if p == (0 as *props::HybridWebProps) { return this; }
        { (*p).hybrid_root = text::from_str(v) };
        core::touch(this._p, P_HYBRID_ROOT);
        return this;
    }

    fn default_file(this) -> str {
        let p: *props::HybridWebProps = this._props();
        if p == (0 as *props::HybridWebProps) { return ""; }
        return { (*p).default_file.view() };
    }

    fn hybrid_root(this) -> str {
        let p: *props::HybridWebProps = this._props();
        if p == (0 as *props::HybridWebProps) { return ""; }
        return { (*p).hybrid_root.view() };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> HybridWeb { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_enabled(this, v: bool) -> HybridWeb { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> HybridWeb { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> HybridWeb { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> HybridWeb { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> HybridWeb { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[HybridWeb] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[HybridWeb]::None; }
            if { (*d).kind } != props::K_HYBRID_WEB { return option::Option[HybridWeb]::None; }
            option::Option[HybridWeb]::Some(HybridWeb::of(p))
        }
        option::Option[*core::Node]::None => option::Option[HybridWeb]::None,
    };
}
