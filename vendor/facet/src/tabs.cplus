// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// tabs — the ledger TabbedPage. 5 writes, 0 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;
import "./mount" as mount;

// ---- dirty bits --------------------------------------------------------
// One bit per write, then one per owned prop and one per command. The
// top 16 bits of the word are the shared band's (props::C_*).

const P_BAR_BACKGROUND: u64 = 1u64;
const P_BAR_BACKGROUND_COLOR: u64 = 2u64;
const P_BAR_TEXT_COLOR: u64 = 4u64;
const P_SELECTED_TAB_COLOR: u64 = 8u64;
const P_UNSELECTED_TAB_COLOR: u64 = 16u64;
const P_SELECTED_INDEX: u64 = 32u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn tabs(
    key: str = "",
    bar_background: vocab::Brush = vocab::Brush::none(),
    bar_background_color: vocab::Color = vocab::Color::clear(),
    bar_text_color: vocab::Color = vocab::Color::clear(),
    selected_tab_color: vocab::Color = vocab::Color::clear(),
    unselected_tab_color: vocab::Color = vocab::Color::clear(),
    selected_index: i64 = 0 as i64,
    on_tab_changed: fn(*u8, *u8) = props::no_handler,
    on_tab_changed_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::TabsProps = props::TabsProps::new();
    p.bar_background = bar_background;
    p.bar_background_color = bar_background_color;
    p.bar_text_color = bar_text_color;
    p.selected_tab_color = selected_tab_color;
    p.unselected_tab_color = unselected_tab_color;
    p.selected_index = selected_index;
    p.on_tab_changed = on_tab_changed;
    p.on_tab_changed_ctx = on_tab_changed_ctx;
    return match box::new::[props::TabsProps](p) {
        option::Option[box::Box[props::TabsProps]]::Some(b) =>
            core::node_with(key, props::K_TABS, b.into_raw(), props::release_tabs_props),
        option::Option[box::Box[props::TabsProps]]::None =>
            core::node_with(key, props::K_TABS, 0 as *u8, props::release_tabs_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Tabs {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Tabs {
    // Unchecked, and module-private for that reason: it trusts the
    // caller about the kind. `from` is the public door.
    fn _of(p: *core::Node) -> Tabs { return Tabs { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::TabsProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::TabsProps; }
        if { (*d).kind } != props::K_TABS { return 0 as *props::TabsProps; }
        return { (*d).props } as *props::TabsProps;
    }

    // TabbedPage.BarBackground
    fn set_bar_background(this, v: vocab::Brush) -> Tabs {
        let p: *props::TabsProps = this._props();
        if p == (0 as *props::TabsProps) { return this; }
        { (*p).bar_background = v };
        core::touch(this._p, P_BAR_BACKGROUND);
        return this;
    }

    // TabbedPage.BarBackgroundColor
    fn set_bar_background_color(this, v: vocab::Color) -> Tabs {
        let p: *props::TabsProps = this._props();
        if p == (0 as *props::TabsProps) { return this; }
        { (*p).bar_background_color = v };
        core::touch(this._p, P_BAR_BACKGROUND_COLOR);
        return this;
    }

    // TabbedPage.BarTextColor
    fn set_bar_text_color(this, v: vocab::Color) -> Tabs {
        let p: *props::TabsProps = this._props();
        if p == (0 as *props::TabsProps) { return this; }
        { (*p).bar_text_color = v };
        core::touch(this._p, P_BAR_TEXT_COLOR);
        return this;
    }

    // TabbedPage.SelectedTabColor
    fn set_selected_tab_color(this, v: vocab::Color) -> Tabs {
        let p: *props::TabsProps = this._props();
        if p == (0 as *props::TabsProps) { return this; }
        { (*p).selected_tab_color = v };
        core::touch(this._p, P_SELECTED_TAB_COLOR);
        return this;
    }

    // TabbedPage.UnselectedTabColor
    fn set_unselected_tab_color(this, v: vocab::Color) -> Tabs {
        let p: *props::TabsProps = this._props();
        if p == (0 as *props::TabsProps) { return this; }
        { (*p).unselected_tab_color = v };
        core::touch(this._p, P_UNSELECTED_TAB_COLOR);
        return this;
    }

    fn bar_background(this) -> vocab::Brush {
        let p: *props::TabsProps = this._props();
        if p == (0 as *props::TabsProps) { return vocab::Brush::none(); }
        return { (*p).bar_background };
    }

    fn bar_background_color(this) -> vocab::Color {
        let p: *props::TabsProps = this._props();
        if p == (0 as *props::TabsProps) { return vocab::Color::clear(); }
        return { (*p).bar_background_color };
    }

    fn bar_text_color(this) -> vocab::Color {
        let p: *props::TabsProps = this._props();
        if p == (0 as *props::TabsProps) { return vocab::Color::clear(); }
        return { (*p).bar_text_color };
    }

    fn selected_tab_color(this) -> vocab::Color {
        let p: *props::TabsProps = this._props();
        if p == (0 as *props::TabsProps) { return vocab::Color::clear(); }
        return { (*p).selected_tab_color };
    }

    fn unselected_tab_color(this) -> vocab::Color {
        let p: *props::TabsProps = this._props();
        if p == (0 as *props::TabsProps) { return vocab::Color::clear(); }
        return { (*p).unselected_tab_color };
    }

    // facet's own word. The ledger's TabbedPage describes a STRIP and says
    // nothing about which tab is showing — `CurrentPage` lives on
    // MultiPage<T>, outside the manifest slice. So the five colour
    // verbs decorated a control nothing could drive.
    //
    // An agent has no hands, and a tab strip it cannot switch is a
    // strip it cannot use. This is that switch.
    fn set_selected_index(this, v: i64) -> Tabs {
        let p: *props::TabsProps = this._props();
        if p == (0 as *props::TabsProps) { return this; }
        { (*p).selected_index = v };
        core::touch(this._p, P_SELECTED_INDEX);
        return this;
    }

    fn selected_index(this) -> i64 {
        let p: *props::TabsProps = this._props();
        if p == (0 as *props::TabsProps) { return 0 as i64; }
        return { (*p).selected_index };
    }

    // The flex node this cursor points at. The DOM division, and the
    // reason facet declares no layout verb of its own:
    //
    //   the control's own   b.set_title("Saving…")     this module
    //   the shared band     b.set_opacity(0.5)         facet.cplus, once
    //   layout              { (*b.node()).set_width(…) }  ALL of flex
    //
    // Reaching flex through the node rather than forwarding its ~40 verbs
    // onto 42 cursors is what keeps facet from growing a second layout
    // vocabulary that can disagree with the first.
    fn node(this) -> *core::Node { return this._p; }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Tabs { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Tabs { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_background(this, v: vocab::Brush) -> Tabs { core::set_background(this._p, v); return this; }
    fn background(this) -> vocab::Brush { return core::background(this._p); }
    fn set_background_image(this, v: str) -> Tabs { core::set_background_image(this._p, v); return this; }
    fn background_image(this) -> str { return core::background_image(this._p); }
    fn set_safe_area(this, v: vocab::SafeArea) -> Tabs { core::set_safe_area(this._p, v); return this; }
    fn safe_area(this) -> vocab::SafeArea { return core::safe_area(this._p); }
    fn set_shadow(this, v: vocab::Shadow) -> Tabs { core::set_shadow(this._p, v); return this; }
    fn shadow(this) -> vocab::Shadow { return core::shadow(this._p); }
    fn set_clip(this, v: vocab::Shape) -> Tabs { core::set_clip(this._p, v); return this; }
    fn clip(this) -> vocab::Shape { return core::clip(this._p); }
    fn set_enabled(this, v: bool) -> Tabs { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> Tabs { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_display(this, v: flex::Display) -> Tabs { core::set_display(this._p, v); return this; }
    fn display(this) -> flex::Display { return core::display_of(this._p); }
    fn set_shown(this, v: bool) -> Tabs { core::set_shown(this._p, v); return this; }
    fn is_hidden(this) -> bool { return core::is_hidden(this._p); }
    fn set_input_transparent(this, v: bool) -> Tabs { core::set_input_transparent(this._p, v); return this; }
    fn input_transparent(this) -> bool { return core::input_transparent(this._p); }
    fn set_flow_direction(this, v: vocab::FlowDirection) -> Tabs { core::set_flow_direction(this._p, v); return this; }
    fn flow_direction(this) -> vocab::FlowDirection { return core::flow_direction(this._p); }
    fn set_agent(this, v: vocab::Agent) -> Tabs { core::set_agent(this._p, v); return this; }
    fn agent(this) -> vocab::Agent { return core::agent(this._p); }
    fn set_rotation(this, v: f64) -> Tabs { core::set_rotation(this._p, v); return this; }
    fn rotation(this) -> f64 { return core::rotation(this._p); }
    fn set_rotation_x(this, v: f64) -> Tabs { core::set_rotation_x(this._p, v); return this; }
    fn rotation_x(this) -> f64 { return core::rotation_x(this._p); }
    fn set_rotation_y(this, v: f64) -> Tabs { core::set_rotation_y(this._p, v); return this; }
    fn rotation_y(this) -> f64 { return core::rotation_y(this._p); }
    fn set_scale(this, v: f64) -> Tabs { core::set_scale(this._p, v); return this; }
    fn scale(this) -> f64 { return core::scale(this._p); }
    fn set_scale_x(this, v: f64) -> Tabs { core::set_scale_x(this._p, v); return this; }
    fn scale_x(this) -> f64 { return core::scale_x(this._p); }
    fn set_scale_y(this, v: f64) -> Tabs { core::set_scale_y(this._p, v); return this; }
    fn scale_y(this) -> f64 { return core::scale_y(this._p); }
    fn set_translation_x(this, v: f64) -> Tabs { core::set_translation_x(this._p, v); return this; }
    fn translation_x(this) -> f64 { return core::translation_x(this._p); }
    fn set_translation_y(this, v: f64) -> Tabs { core::set_translation_y(this._p, v); return this; }
    fn translation_y(this) -> f64 { return core::translation_y(this._p); }
    fn set_anchor_x(this, v: f64) -> Tabs { core::set_anchor_x(this._p, v); return this; }
    fn anchor_x(this) -> f64 { return core::anchor_x(this._p); }
    fn set_anchor_y(this, v: f64) -> Tabs { core::set_anchor_y(this._p, v); return this; }
    fn anchor_y(this) -> f64 { return core::anchor_y(this._p); }
    fn set_z_index(this, v: i64) -> Tabs { core::set_z_index(this._p, v); return this; }
    fn z_index(this) -> i64 { return core::z_index(this._p); }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn bounds(this) -> vocab::Rect { return core::bounds_of(this._p); }
    fn is_focused(this) -> bool { return core::is_focused(this._p); }
    fn is_attached(this) -> bool { return core::is_attached(this._p); }
    fn window(this) -> vocab::WindowRef { return core::window_of(this._p); }
    fn native(this) -> *u8 { return core::native(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn child(this, at: usize) -> option::Option[*core::Node] { return core::child_of(this._p, at); }
    fn set_grow(this, v: f64) -> Tabs { core::set_grow(this._p, v); return this; }
    fn set_shrink(this, v: f64) -> Tabs { core::set_shrink(this._p, v); return this; }
    fn set_width(this, v: f64) -> Tabs { core::set_width(this._p, v); return this; }
    fn set_height(this, v: f64) -> Tabs { core::set_height(this._p, v); return this; }
    fn set_width_percent(this, v: f64) -> Tabs { core::set_width_percent(this._p, v); return this; }
    fn set_height_percent(this, v: f64) -> Tabs { core::set_height_percent(this._p, v); return this; }
    fn set_padding(this, v: f64) -> Tabs { core::set_padding(this._p, v); return this; }
    fn set_margin(this, v: f64) -> Tabs { core::set_margin(this._p, v); return this; }
    fn set_padding_edge(this, edge: flex::Edge, v: f64) -> Tabs { core::set_padding_edge(this._p, edge, v); return this; }
    fn set_margin_edge(this, edge: flex::Edge, v: f64) -> Tabs { core::set_margin_edge(this._p, edge, v); return this; }
    fn set_gap(this, v: f64) -> Tabs { core::set_gap(this._p, v); return this; }
    fn set_justify(this, v: flex::Justify) -> Tabs { core::set_justify(this._p, v); return this; }
    fn set_align(this, v: flex::Align) -> Tabs { core::set_align(this._p, v); return this; }
    fn set_wrap(this, v: flex::Wrap) -> Tabs { core::set_wrap(this._p, v); return this; }
    fn focus(this) -> Tabs { core::focus(this._p); return this; }
    fn blur(this) -> Tabs { core::blur(this._p); return this; }
    fn relayout(this) -> Tabs { core::relayout(this._p); return this; }
    fn measure(this, width: f64, height: f64) -> vocab::Size { return core::measure(this._p, width, height); }
    fn begin_updates(this) -> Tabs { core::begin_updates(this._p); return this; }
    fn end_updates(this) -> Tabs { core::end_updates(this._p); return this; }
    fn on_focus(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Tabs { core::set_on_focus(this._p, f, ctx); return this; }
    fn on_blur(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Tabs { core::set_on_blur(this._p, f, ctx); return this; }
    fn on_attach(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Tabs { core::set_on_attach(this._p, f, ctx); return this; }
    fn on_detach(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Tabs { core::set_on_detach(this._p, f, ctx); return this; }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup ------------------------------------------------------------
// `from` narrows a node to this control, the DOM step: a node carries what
// EVERY node has, and a checked cast adds what only a tabs has. `None` on
// a wrong kind — never a cursor whose every verb silently does nothing.

fn from(p: *core::Node) -> option::Option[Tabs] {
    if p == (0 as *core::Node) { return option::Option[Tabs]::None; }
    let d: *core::Data = { (*p).data() };
    if d == (0 as *core::Data) { return option::Option[Tabs]::None; }
    if { (*d).kind } != props::K_TABS { return option::Option[Tabs]::None; }
    return option::Option[Tabs]::Some(Tabs::_of(p));
}

// Resolve a key, then narrow through the same checked door.

// `within:` and not `in:` — `in` is a C+ keyword. Bare `find` is the
// DOM rule: the running app's windows in open order, first match wins;
// pass `within:` to scope a subtree (or disambiguate across windows).
fn find(key: str, within: *core::Node = 0 as *core::Node) -> option::Option[Tabs] {
    if within != (0 as *core::Node) {
        return match core::find_in(within, key) {
            option::Option[*core::Node]::Some(p) => { _report_kind(key, p); from(p) }
            option::Option[*core::Node]::None => option::Option[Tabs]::None,
        };
    }
    return match mount::find_node(key) {
        option::Option[*core::Node]::Some(p) => { _report_kind(key, p); from(p) }
        option::Option[*core::Node]::None => option::Option[Tabs]::None,
    };
}

// The debug posture's third distinction (FACET_DEBUG_FIND=1): the key
// RESOLVED, and `from` is about to answer None anyway because the node is
// another control. `_`-private; costs one flag read when the posture is off.
fn _report_kind(key: str, p: *core::Node) {
    if !mount::find_debugging() { return; }
    if p == (0 as *core::Node) { return; }
    let d: *core::Data = { (*p).data() };
    if d == (0 as *core::Data) || { (*d).kind } != props::K_TABS {
        mount::debug_find_wrong_kind(key, "tabs");
    }
    return;
}
