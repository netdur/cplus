// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// text_field — MAUI Entry. 22 writes, 0 reads, 2 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_CLEAR_BUTTON_VISIBILITY: u64 = 1u64;
const P_HORIZONTAL_TEXT_ALIGNMENT: u64 = 2u64;
const P_IS_SECURE: u64 = 4u64;
const P_RETURN_TYPE: u64 = 8u64;
const P_VERTICAL_TEXT_ALIGNMENT: u64 = 16u64;
const P_CHARACTER_SPACING: u64 = 32u64;
const P_CURSOR_POSITION: u64 = 64u64;
const P_FONT_ATTRIBUTES: u64 = 128u64;
const P_FONT_AUTO_SCALING_ENABLED: u64 = 256u64;
const P_FONT_FAMILY: u64 = 512u64;
const P_FONT_SIZE: u64 = 1024u64;
const P_IS_READ_ONLY: u64 = 2048u64;
const P_IS_SPELL_CHECK_ENABLED: u64 = 4096u64;
const P_IS_TEXT_PREDICTION_ENABLED: u64 = 8192u64;
const P_KEYBOARD: u64 = 16384u64;
const P_MAX_LENGTH: u64 = 32768u64;
const P_PLACEHOLDER: u64 = 65536u64;
const P_PLACEHOLDER_COLOR: u64 = 131072u64;
const P_SELECTION_LENGTH: u64 = 262144u64;
const P_TEXT: u64 = 524288u64;
const P_TEXT_COLOR: u64 = 1048576u64;
const P_TEXT_TRANSFORM: u64 = 2097152u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn text_field(
    text: str,
    key: str = "",
    clear_button_visibility: vocab::ClearButton = vocab::ClearButton::Never,
    horizontal_text_alignment: vocab::TextAlign = vocab::TextAlign::Center,
    is_secure: bool = false,
    return_type: vocab::ReturnKey = vocab::ReturnKey::Default,
    vertical_text_alignment: vocab::TextAlign = vocab::TextAlign::Center,
    character_spacing: f64 = 0.0f64,
    cursor_position: i64 = 0 as i64,
    font_attributes: vocab::FontStyle = vocab::FontStyle::Bold,
    font_auto_scaling_enabled: bool = false,
    font_family: str = "",
    font_size: f64 = 0.0f64,
    is_read_only: bool = false,
    is_spell_check_enabled: bool = false,
    is_text_prediction_enabled: bool = false,
    keyboard: vocab::Keyboard = vocab::Keyboard::Default,
    max_length: i64 = 0 as i64,
    placeholder: str = "",
    placeholder_color: vocab::Color = vocab::Color::clear(),
    selection_length: i64 = 0 as i64,
    text_color: vocab::Color = vocab::Color::clear(),
    text_transform: vocab::TextTransform = vocab::TextTransform::Default,
    on_submit: fn(*u8, *u8) = props::no_handler,
    on_submit_ctx: *u8 = 0 as *u8,
    on_text_changed: fn(*u8, *u8) = props::no_handler,
    on_text_changed_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::TextFieldProps = props::TextFieldProps::new();
    p.clear_button_visibility = clear_button_visibility;
    p.horizontal_text_alignment = horizontal_text_alignment;
    p.is_secure = is_secure;
    p.return_type = return_type;
    p.vertical_text_alignment = vertical_text_alignment;
    p.character_spacing = character_spacing;
    p.cursor_position = cursor_position;
    p.font_attributes = font_attributes;
    p.font_auto_scaling_enabled = font_auto_scaling_enabled;
    p.font_family = text::from_str(font_family);
    p.font_size = font_size;
    p.is_read_only = is_read_only;
    p.is_spell_check_enabled = is_spell_check_enabled;
    p.is_text_prediction_enabled = is_text_prediction_enabled;
    p.keyboard = keyboard;
    p.max_length = max_length;
    p.placeholder = text::from_str(placeholder);
    p.placeholder_color = placeholder_color;
    p.selection_length = selection_length;
    p.text = text::from_str(text);
    p.text_color = text_color;
    p.text_transform = text_transform;
    p.on_submit = on_submit;
    p.on_submit_ctx = on_submit_ctx;
    p.on_text_changed = on_text_changed;
    p.on_text_changed_ctx = on_text_changed_ctx;
    return match box::new::[props::TextFieldProps](p) {
        option::Option[box::Box[props::TextFieldProps]]::Some(b) =>
            core::node_with(key, props::K_TEXT_FIELD, b.into_raw(), props::release_text_field_props),
        option::Option[box::Box[props::TextFieldProps]]::None =>
            core::node_with(key, props::K_TEXT_FIELD, 0 as *u8, props::release_text_field_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct TextField {
    opaque _p: *core::Node,
    _seen: u64,
}

impl TextField {
    fn of(p: *core::Node) -> TextField { return TextField { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::TextFieldProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::TextFieldProps; }
        if { (*d).kind } != props::K_TEXT_FIELD { return 0 as *props::TextFieldProps; }
        return { (*d).props } as *props::TextFieldProps;
    }

    // Entry.ClearButtonVisibility
    fn set_clear_button_visibility(this, v: vocab::ClearButton) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).clear_button_visibility = v };
        core::touch(this._p, P_CLEAR_BUTTON_VISIBILITY);
        return this;
    }

    // Entry.HorizontalTextAlignment
    fn set_horizontal_text_alignment(this, v: vocab::TextAlign) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).horizontal_text_alignment = v };
        core::touch(this._p, P_HORIZONTAL_TEXT_ALIGNMENT);
        return this;
    }

    // Entry.IsPassword
    fn set_is_secure(this, v: bool) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).is_secure = v };
        core::touch(this._p, P_IS_SECURE);
        return this;
    }

    // Entry.ReturnType
    fn set_return_type(this, v: vocab::ReturnKey) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).return_type = v };
        core::touch(this._p, P_RETURN_TYPE);
        return this;
    }

    // Entry.VerticalTextAlignment
    fn set_vertical_text_alignment(this, v: vocab::TextAlign) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).vertical_text_alignment = v };
        core::touch(this._p, P_VERTICAL_TEXT_ALIGNMENT);
        return this;
    }

    // InputView.CharacterSpacing
    fn set_character_spacing(this, v: f64) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).character_spacing = v };
        core::touch(this._p, P_CHARACTER_SPACING);
        return this;
    }

    // InputView.CursorPosition
    fn set_cursor_position(this, v: i64) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).cursor_position = v };
        core::touch(this._p, P_CURSOR_POSITION);
        return this;
    }

    // InputView.FontAttributes
    fn set_font_attributes(this, v: vocab::FontStyle) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).font_attributes = v };
        core::touch(this._p, P_FONT_ATTRIBUTES);
        return this;
    }

    // InputView.FontAutoScalingEnabled
    fn set_font_auto_scaling_enabled(this, v: bool) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).font_auto_scaling_enabled = v };
        core::touch(this._p, P_FONT_AUTO_SCALING_ENABLED);
        return this;
    }

    // InputView.FontFamily
    fn set_font_family(this, v: str) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).font_family = text::from_str(v) };
        core::touch(this._p, P_FONT_FAMILY);
        return this;
    }

    // InputView.FontSize
    fn set_font_size(this, v: f64) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).font_size = v };
        core::touch(this._p, P_FONT_SIZE);
        return this;
    }

    // InputView.IsReadOnly
    fn set_is_read_only(this, v: bool) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).is_read_only = v };
        core::touch(this._p, P_IS_READ_ONLY);
        return this;
    }

    // InputView.IsSpellCheckEnabled
    fn set_is_spell_check_enabled(this, v: bool) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).is_spell_check_enabled = v };
        core::touch(this._p, P_IS_SPELL_CHECK_ENABLED);
        return this;
    }

    // InputView.IsTextPredictionEnabled
    fn set_is_text_prediction_enabled(this, v: bool) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).is_text_prediction_enabled = v };
        core::touch(this._p, P_IS_TEXT_PREDICTION_ENABLED);
        return this;
    }

    // InputView.Keyboard
    fn set_keyboard(this, v: vocab::Keyboard) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).keyboard = v };
        core::touch(this._p, P_KEYBOARD);
        return this;
    }

    // InputView.MaxLength
    fn set_max_length(this, v: i64) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).max_length = v };
        core::touch(this._p, P_MAX_LENGTH);
        return this;
    }

    // InputView.Placeholder
    fn set_placeholder(this, v: str) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).placeholder = text::from_str(v) };
        core::touch(this._p, P_PLACEHOLDER);
        return this;
    }

    // InputView.PlaceholderColor
    fn set_placeholder_color(this, v: vocab::Color) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).placeholder_color = v };
        core::touch(this._p, P_PLACEHOLDER_COLOR);
        return this;
    }

    // InputView.SelectionLength
    fn set_selection_length(this, v: i64) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).selection_length = v };
        core::touch(this._p, P_SELECTION_LENGTH);
        return this;
    }

    // InputView.Text
    fn set_text(this, v: str) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).text = text::from_str(v) };
        core::touch(this._p, P_TEXT);
        return this;
    }

    // InputView.TextColor
    fn set_text_color(this, v: vocab::Color) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).text_color = v };
        core::touch(this._p, P_TEXT_COLOR);
        return this;
    }

    // InputView.TextTransform
    fn set_text_transform(this, v: vocab::TextTransform) -> TextField {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return this; }
        { (*p).text_transform = v };
        core::touch(this._p, P_TEXT_TRANSFORM);
        return this;
    }

    fn clear_button_visibility(this) -> vocab::ClearButton {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return vocab::ClearButton::Never; }
        return { (*p).clear_button_visibility };
    }

    fn horizontal_text_alignment(this) -> vocab::TextAlign {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return vocab::TextAlign::Center; }
        return { (*p).horizontal_text_alignment };
    }

    fn is_secure(this) -> bool {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return false; }
        return { (*p).is_secure };
    }

    fn return_type(this) -> vocab::ReturnKey {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return vocab::ReturnKey::Default; }
        return { (*p).return_type };
    }

    fn vertical_text_alignment(this) -> vocab::TextAlign {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return vocab::TextAlign::Center; }
        return { (*p).vertical_text_alignment };
    }

    fn character_spacing(this) -> f64 {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return 0.0f64; }
        return { (*p).character_spacing };
    }

    fn cursor_position(this) -> i64 {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return 0 as i64; }
        return { (*p).cursor_position };
    }

    fn font_attributes(this) -> vocab::FontStyle {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return vocab::FontStyle::Bold; }
        return { (*p).font_attributes };
    }

    fn font_auto_scaling_enabled(this) -> bool {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return false; }
        return { (*p).font_auto_scaling_enabled };
    }

    fn font_family(this) -> str {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return ""; }
        return { (*p).font_family.view() };
    }

    fn font_size(this) -> f64 {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return 0.0f64; }
        return { (*p).font_size };
    }

    fn is_read_only(this) -> bool {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return false; }
        return { (*p).is_read_only };
    }

    fn is_spell_check_enabled(this) -> bool {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return false; }
        return { (*p).is_spell_check_enabled };
    }

    fn is_text_prediction_enabled(this) -> bool {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return false; }
        return { (*p).is_text_prediction_enabled };
    }

    fn keyboard(this) -> vocab::Keyboard {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return vocab::Keyboard::Default; }
        return { (*p).keyboard };
    }

    fn max_length(this) -> i64 {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return 0 as i64; }
        return { (*p).max_length };
    }

    fn placeholder(this) -> str {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return ""; }
        return { (*p).placeholder.view() };
    }

    fn placeholder_color(this) -> vocab::Color {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return vocab::Color::clear(); }
        return { (*p).placeholder_color };
    }

    fn selection_length(this) -> i64 {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return 0 as i64; }
        return { (*p).selection_length };
    }

    fn text(this) -> str {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return ""; }
        return { (*p).text.view() };
    }

    fn text_color(this) -> vocab::Color {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return vocab::Color::clear(); }
        return { (*p).text_color };
    }

    fn text_transform(this) -> vocab::TextTransform {
        let p: *props::TextFieldProps = this._props();
        if p == (0 as *props::TextFieldProps) { return vocab::TextTransform::Default; }
        return { (*p).text_transform };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> TextField { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_is_enabled(this, v: bool) -> TextField { core::set_is_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_is_visible(this, v: bool) -> TextField { core::set_is_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> TextField { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> TextField { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> TextField { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[TextField] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[TextField]::None; }
            if { (*d).kind } != props::K_TEXT_FIELD { return option::Option[TextField]::None; }
            option::Option[TextField]::Some(TextField::of(p))
        }
        option::Option[*core::Node]::None => option::Option[TextField]::None,
    };
}
