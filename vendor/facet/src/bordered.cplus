// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// bordered — MAUI Border. 8 writes, 0 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;
import "./mount" as mount;

// ---- dirty bits --------------------------------------------------------
// One bit per write, then one per owned prop and one per command. The
// top 16 bits of the word are the shared band's (props::C_*).

const P_STROKE: u64 = 1u64;
const P_STROKE_DASH: u64 = 2u64;
const P_STROKE_DASH_OFFSET: u64 = 4u64;
const P_STROKE_CAP: u64 = 8u64;
const P_STROKE_JOIN: u64 = 16u64;
const P_STROKE_MITER_LIMIT: u64 = 32u64;
const P_STROKE_SHAPE: u64 = 64u64;
const P_STROKE_WIDTH: u64 = 128u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn bordered(
    key: str = "",
    stroke: vocab::Brush = vocab::Brush::none(),
    stroke_dash: vocab::Dashes = vocab::Dashes::none(),
    stroke_dash_offset: f64 = 0.0f64,
    stroke_cap: vocab::LineCap = vocab::LineCap::Flat,
    stroke_join: vocab::LineJoin = vocab::LineJoin::Miter,
    stroke_miter_limit: f64 = 0.0f64,
    stroke_shape: vocab::Shape = vocab::Shape::none(),
    stroke_width: f64 = 0.0f64,
) -> core::Node {
    var p: props::BorderedProps = props::BorderedProps::new();
    p.stroke = stroke;
    p.stroke_dash = stroke_dash;
    p.stroke_dash_offset = stroke_dash_offset;
    p.stroke_cap = stroke_cap;
    p.stroke_join = stroke_join;
    p.stroke_miter_limit = stroke_miter_limit;
    p.stroke_shape = stroke_shape;
    p.stroke_width = stroke_width;
    return match box::new::[props::BorderedProps](p) {
        option::Option[box::Box[props::BorderedProps]]::Some(b) =>
            core::node_with(key, props::K_BORDERED, b.into_raw(), props::release_bordered_props),
        option::Option[box::Box[props::BorderedProps]]::None =>
            core::node_with(key, props::K_BORDERED, 0 as *u8, props::release_bordered_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Bordered {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Bordered {
    // Unchecked, and module-private for that reason: it trusts the
    // caller about the kind. `from` is the public door.
    fn _of(p: *core::Node) -> Bordered { return Bordered { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::BorderedProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::BorderedProps; }
        if { (*d).kind } != props::K_BORDERED { return 0 as *props::BorderedProps; }
        return { (*d).props } as *props::BorderedProps;
    }

    // Border.Stroke
    fn set_stroke(this, v: vocab::Brush) -> Bordered {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return this; }
        { (*p).stroke = v };
        core::touch(this._p, P_STROKE);
        return this;
    }

    // Border.StrokeDashArray
    fn set_stroke_dash(this, v: vocab::Dashes) -> Bordered {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return this; }
        { (*p).stroke_dash = v };
        core::touch(this._p, P_STROKE_DASH);
        return this;
    }

    // Border.StrokeDashOffset
    fn set_stroke_dash_offset(this, v: f64) -> Bordered {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return this; }
        { (*p).stroke_dash_offset = v };
        core::touch(this._p, P_STROKE_DASH_OFFSET);
        return this;
    }

    // Border.StrokeLineCap
    fn set_stroke_cap(this, v: vocab::LineCap) -> Bordered {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return this; }
        { (*p).stroke_cap = v };
        core::touch(this._p, P_STROKE_CAP);
        return this;
    }

    // Border.StrokeLineJoin
    fn set_stroke_join(this, v: vocab::LineJoin) -> Bordered {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return this; }
        { (*p).stroke_join = v };
        core::touch(this._p, P_STROKE_JOIN);
        return this;
    }

    // Border.StrokeMiterLimit
    fn set_stroke_miter_limit(this, v: f64) -> Bordered {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return this; }
        { (*p).stroke_miter_limit = v };
        core::touch(this._p, P_STROKE_MITER_LIMIT);
        return this;
    }

    // Border.StrokeShape
    fn set_stroke_shape(this, v: vocab::Shape) -> Bordered {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return this; }
        { (*p).stroke_shape = v };
        core::touch(this._p, P_STROKE_SHAPE);
        return this;
    }

    // Border.StrokeThickness
    fn set_stroke_width(this, v: f64) -> Bordered {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return this; }
        { (*p).stroke_width = v };
        core::touch(this._p, P_STROKE_WIDTH);
        return this;
    }

    fn stroke(this) -> vocab::Brush {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return vocab::Brush::none(); }
        return { (*p).stroke };
    }

    fn stroke_dash(this) -> vocab::Dashes {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return vocab::Dashes::none(); }
        return { (*p).stroke_dash };
    }

    fn stroke_dash_offset(this) -> f64 {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return 0.0f64; }
        return { (*p).stroke_dash_offset };
    }

    fn stroke_cap(this) -> vocab::LineCap {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return vocab::LineCap::Flat; }
        return { (*p).stroke_cap };
    }

    fn stroke_join(this) -> vocab::LineJoin {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return vocab::LineJoin::Miter; }
        return { (*p).stroke_join };
    }

    fn stroke_miter_limit(this) -> f64 {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return 0.0f64; }
        return { (*p).stroke_miter_limit };
    }

    fn stroke_shape(this) -> vocab::Shape {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return vocab::Shape::none(); }
        return { (*p).stroke_shape };
    }

    fn stroke_width(this) -> f64 {
        let p: *props::BorderedProps = this._props();
        if p == (0 as *props::BorderedProps) { return 0.0f64; }
        return { (*p).stroke_width };
    }

    // Border.Content — a named child, not a property. `@` keys are
    // facet's; an application's key never starts with one.
    fn set_content(this, take n: core::Node) -> Bordered {
        core::set_slot(this._p, "@content", n);
        return this;
    }

    fn content(this) -> option::Option[*core::Node] {
        return core::slot(this._p, "@content");
    }

    // The flex node this cursor points at. The DOM division, and the
    // reason facet declares no layout verb of its own:
    //
    //   the control's own   b.set_title("Saving…")     this module
    //   the shared band     b.set_opacity(0.5)         facet.cplus, once
    //   layout              { (*b.node()).set_width(…) }  ALL of flex
    //
    // Reaching flex through the node rather than forwarding its ~40 verbs
    // onto 42 cursors is what keeps facet from growing a second layout
    // vocabulary that can disagree with the first.
    fn node(this) -> *core::Node { return this._p; }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Bordered { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Bordered { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_background(this, v: vocab::Brush) -> Bordered { core::set_background(this._p, v); return this; }
    fn background(this) -> vocab::Brush { return core::background(this._p); }
    fn set_shadow(this, v: vocab::Shadow) -> Bordered { core::set_shadow(this._p, v); return this; }
    fn shadow(this) -> vocab::Shadow { return core::shadow(this._p); }
    fn set_clip(this, v: vocab::Shape) -> Bordered { core::set_clip(this._p, v); return this; }
    fn clip(this) -> vocab::Shape { return core::clip(this._p); }
    fn set_enabled(this, v: bool) -> Bordered { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> Bordered { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_input_transparent(this, v: bool) -> Bordered { core::set_input_transparent(this._p, v); return this; }
    fn input_transparent(this) -> bool { return core::input_transparent(this._p); }
    fn set_flow_direction(this, v: vocab::FlowDirection) -> Bordered { core::set_flow_direction(this._p, v); return this; }
    fn flow_direction(this) -> vocab::FlowDirection { return core::flow_direction(this._p); }
    fn set_rotation(this, v: f64) -> Bordered { core::set_rotation(this._p, v); return this; }
    fn rotation(this) -> f64 { return core::rotation(this._p); }
    fn set_rotation_x(this, v: f64) -> Bordered { core::set_rotation_x(this._p, v); return this; }
    fn rotation_x(this) -> f64 { return core::rotation_x(this._p); }
    fn set_rotation_y(this, v: f64) -> Bordered { core::set_rotation_y(this._p, v); return this; }
    fn rotation_y(this) -> f64 { return core::rotation_y(this._p); }
    fn set_scale(this, v: f64) -> Bordered { core::set_scale(this._p, v); return this; }
    fn scale(this) -> f64 { return core::scale(this._p); }
    fn set_scale_x(this, v: f64) -> Bordered { core::set_scale_x(this._p, v); return this; }
    fn scale_x(this) -> f64 { return core::scale_x(this._p); }
    fn set_scale_y(this, v: f64) -> Bordered { core::set_scale_y(this._p, v); return this; }
    fn scale_y(this) -> f64 { return core::scale_y(this._p); }
    fn set_translation_x(this, v: f64) -> Bordered { core::set_translation_x(this._p, v); return this; }
    fn translation_x(this) -> f64 { return core::translation_x(this._p); }
    fn set_translation_y(this, v: f64) -> Bordered { core::set_translation_y(this._p, v); return this; }
    fn translation_y(this) -> f64 { return core::translation_y(this._p); }
    fn set_anchor_x(this, v: f64) -> Bordered { core::set_anchor_x(this._p, v); return this; }
    fn anchor_x(this) -> f64 { return core::anchor_x(this._p); }
    fn set_anchor_y(this, v: f64) -> Bordered { core::set_anchor_y(this._p, v); return this; }
    fn anchor_y(this) -> f64 { return core::anchor_y(this._p); }
    fn set_z_index(this, v: i64) -> Bordered { core::set_z_index(this._p, v); return this; }
    fn z_index(this) -> i64 { return core::z_index(this._p); }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn bounds(this) -> vocab::Rect { return core::bounds_of(this._p); }
    fn is_focused(this) -> bool { return core::is_focused(this._p); }
    fn is_attached(this) -> bool { return core::is_attached(this._p); }
    fn window(this) -> vocab::WindowRef { return core::window_of(this._p); }
    fn native(this) -> *u8 { return core::native(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn child(this, at: usize) -> option::Option[*core::Node] { return core::child_of(this._p, at); }
    fn set_grow(this, v: f64) -> Bordered { core::set_grow(this._p, v); return this; }
    fn set_shrink(this, v: f64) -> Bordered { core::set_shrink(this._p, v); return this; }
    fn set_width(this, v: f64) -> Bordered { core::set_width(this._p, v); return this; }
    fn set_height(this, v: f64) -> Bordered { core::set_height(this._p, v); return this; }
    fn set_width_percent(this, v: f64) -> Bordered { core::set_width_percent(this._p, v); return this; }
    fn set_height_percent(this, v: f64) -> Bordered { core::set_height_percent(this._p, v); return this; }
    fn set_padding(this, v: f64) -> Bordered { core::set_padding(this._p, v); return this; }
    fn set_margin(this, v: f64) -> Bordered { core::set_margin(this._p, v); return this; }
    fn set_gap(this, v: f64) -> Bordered { core::set_gap(this._p, v); return this; }
    fn set_justify(this, v: flex::Justify) -> Bordered { core::set_justify(this._p, v); return this; }
    fn set_align(this, v: flex::Align) -> Bordered { core::set_align(this._p, v); return this; }
    fn set_wrap(this, v: flex::Wrap) -> Bordered { core::set_wrap(this._p, v); return this; }
    fn focus(this) -> Bordered { core::focus(this._p); return this; }
    fn blur(this) -> Bordered { core::blur(this._p); return this; }
    fn relayout(this) -> Bordered { core::relayout(this._p); return this; }
    fn measure(this, width: f64, height: f64) -> vocab::Size { return core::measure(this._p, width, height); }
    fn begin_updates(this) -> Bordered { core::begin_updates(this._p); return this; }
    fn end_updates(this) -> Bordered { core::end_updates(this._p); return this; }
    fn on_focus(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Bordered { core::set_on_focus(this._p, f, ctx); return this; }
    fn on_blur(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Bordered { core::set_on_blur(this._p, f, ctx); return this; }
    fn on_attach(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Bordered { core::set_on_attach(this._p, f, ctx); return this; }
    fn on_detach(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Bordered { core::set_on_detach(this._p, f, ctx); return this; }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup ------------------------------------------------------------
// `from` narrows a node to this control, the DOM step: a node carries what
// EVERY node has, and a checked cast adds what only a bordered has. `None` on
// a wrong kind — never a cursor whose every verb silently does nothing.

fn from(p: *core::Node) -> option::Option[Bordered] {
    if p == (0 as *core::Node) { return option::Option[Bordered]::None; }
    let d: *core::Data = { (*p).data() };
    if d == (0 as *core::Data) { return option::Option[Bordered]::None; }
    if { (*d).kind } != props::K_BORDERED { return option::Option[Bordered]::None; }
    return option::Option[Bordered]::Some(Bordered::_of(p));
}

// Resolve a key, then narrow through the same checked door.

// `within:` and not `in:` — `in` is a C+ keyword. Bare `find` is the
// DOM rule: the running app's windows in open order, first match wins;
// pass `within:` to scope a subtree (or disambiguate across windows).
fn find(key: str, within: *core::Node = 0 as *core::Node) -> option::Option[Bordered] {
    if within != (0 as *core::Node) {
        return match core::find_in(within, key) {
            option::Option[*core::Node]::Some(p) => from(p),
            option::Option[*core::Node]::None => option::Option[Bordered]::None,
        };
    }
    return match mount::find_node(key) {
        option::Option[*core::Node]::Some(p) => from(p),
        option::Option[*core::Node]::None => option::Option[Bordered]::None,
    };
}
