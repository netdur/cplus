// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// time_picker — MAUI TimePicker. 10 writes, 0 reads, 3 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;
import "./mount" as mount;

// ---- dirty bits --------------------------------------------------------
// One bit per write, then one per owned prop and one per command. The
// top 16 bits of the word are the shared band's (props::C_*).

const P_CHARACTER_SPACING: u64 = 1u64;
const P_FONT_WEIGHT: u64 = 2u64;
const P_IS_ITALIC: u64 = 4u64;
const P_FONT_SCALES: u64 = 8u64;
const P_FONT_FAMILY: u64 = 16u64;
const P_FONT_SIZE: u64 = 32u64;
const P_FORMAT: u64 = 64u64;
const P_IS_OPEN: u64 = 128u64;
const P_TEXT_COLOR: u64 = 256u64;
const P_TIME: u64 = 512u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn time_picker(
    time: vocab::Duration,
    key: str = "",
    character_spacing: f64 = 0.0f64,
    font_weight: vocab::FontWeight = vocab::FontWeight::Default,
    italic: bool = false,
    font_scales: bool = false,
    font_family: str = "",
    font_size: f64 = 0.0f64,
    format: str = "",
    open: bool = false,
    text_color: vocab::Color = vocab::Color::clear(),
    on_closed: fn(*u8, *u8) = props::no_handler,
    on_closed_ctx: *u8 = 0 as *u8,
    on_opened: fn(*u8, *u8) = props::no_handler,
    on_opened_ctx: *u8 = 0 as *u8,
    on_time_selected: fn(*u8, *u8) = props::no_handler,
    on_time_selected_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::TimePickerProps = props::TimePickerProps::new();
    p.character_spacing = character_spacing;
    p.font_weight = font_weight;
    p.is_italic = italic;
    p.font_scales = font_scales;
    p.font_family = text::from_str(font_family);
    p.font_size = font_size;
    p.format = text::from_str(format);
    p.is_open = open;
    p.text_color = text_color;
    p.time = time;
    p.on_closed = on_closed;
    p.on_closed_ctx = on_closed_ctx;
    p.on_opened = on_opened;
    p.on_opened_ctx = on_opened_ctx;
    p.on_time_selected = on_time_selected;
    p.on_time_selected_ctx = on_time_selected_ctx;
    return match box::new::[props::TimePickerProps](p) {
        option::Option[box::Box[props::TimePickerProps]]::Some(b) =>
            core::node_with(key, props::K_TIME_PICKER, b.into_raw(), props::release_time_picker_props),
        option::Option[box::Box[props::TimePickerProps]]::None =>
            core::node_with(key, props::K_TIME_PICKER, 0 as *u8, props::release_time_picker_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct TimePicker {
    opaque _p: *core::Node,
    _seen: u64,
}

impl TimePicker {
    // Unchecked, and module-private for that reason: it trusts the
    // caller about the kind. `from` is the public door.
    fn _of(p: *core::Node) -> TimePicker { return TimePicker { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::TimePickerProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::TimePickerProps; }
        if { (*d).kind } != props::K_TIME_PICKER { return 0 as *props::TimePickerProps; }
        return { (*d).props } as *props::TimePickerProps;
    }

    // TimePicker.CharacterSpacing
    fn set_character_spacing(this, v: f64) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).character_spacing = v };
        core::touch(this._p, P_CHARACTER_SPACING);
        return this;
    }

    // TimePicker.FontAttributes
    fn set_font_weight(this, v: vocab::FontWeight) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).font_weight = v };
        core::touch(this._p, P_FONT_WEIGHT);
        return this;
    }

    // TimePicker.FontAttributes (italic axis)
    fn set_italic(this, v: bool) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).is_italic = v };
        core::touch(this._p, P_IS_ITALIC);
        return this;
    }

    // TimePicker.FontAutoScalingEnabled
    fn set_font_scales(this, v: bool) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).font_scales = v };
        core::touch(this._p, P_FONT_SCALES);
        return this;
    }

    // TimePicker.FontFamily
    fn set_font_family(this, v: str) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).font_family = text::from_str(v) };
        core::touch(this._p, P_FONT_FAMILY);
        return this;
    }

    // TimePicker.FontSize
    fn set_font_size(this, v: f64) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).font_size = v };
        core::touch(this._p, P_FONT_SIZE);
        return this;
    }

    // TimePicker.Format
    fn set_format(this, v: str) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).format = text::from_str(v) };
        core::touch(this._p, P_FORMAT);
        return this;
    }

    // TimePicker.IsOpen
    fn set_open(this, v: bool) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).is_open = v };
        core::touch(this._p, P_IS_OPEN);
        return this;
    }

    // TimePicker.TextColor
    fn set_text_color(this, v: vocab::Color) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).text_color = v };
        core::touch(this._p, P_TEXT_COLOR);
        return this;
    }

    // TimePicker.Time
    fn set_time(this, v: vocab::Duration) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).time = v };
        core::touch(this._p, P_TIME);
        return this;
    }

    fn character_spacing(this) -> f64 {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return 0.0f64; }
        return { (*p).character_spacing };
    }

    fn font_weight(this) -> vocab::FontWeight {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return vocab::FontWeight::Default; }
        return { (*p).font_weight };
    }

    fn is_italic(this) -> bool {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return false; }
        return { (*p).is_italic };
    }

    fn font_scales(this) -> bool {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return false; }
        return { (*p).font_scales };
    }

    fn font_family(this) -> str {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return ""; }
        return { (*p).font_family.view() };
    }

    fn font_size(this) -> f64 {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return 0.0f64; }
        return { (*p).font_size };
    }

    fn format(this) -> str {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return ""; }
        return { (*p).format.view() };
    }

    fn is_open(this) -> bool {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return false; }
        return { (*p).is_open };
    }

    fn text_color(this) -> vocab::Color {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return vocab::Color::clear(); }
        return { (*p).text_color };
    }

    fn time(this) -> vocab::Duration {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return vocab::Duration::zero(); }
        return { (*p).time };
    }

    // The flex node this cursor points at. The DOM division, and the
    // reason facet declares no layout verb of its own:
    //
    //   the control's own   b.set_title("Saving…")     this module
    //   the shared band     b.set_opacity(0.5)         facet.cplus, once
    //   layout              { (*b.node()).set_width(…) }  ALL of flex
    //
    // Reaching flex through the node rather than forwarding its ~40 verbs
    // onto 42 cursors is what keeps facet from growing a second layout
    // vocabulary that can disagree with the first.
    fn node(this) -> *core::Node { return this._p; }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> TimePicker { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_background_color(this, v: vocab::Color) -> TimePicker { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_background(this, v: vocab::Brush) -> TimePicker { core::set_background(this._p, v); return this; }
    fn background(this) -> vocab::Brush { return core::background(this._p); }
    fn set_shadow(this, v: vocab::Shadow) -> TimePicker { core::set_shadow(this._p, v); return this; }
    fn shadow(this) -> vocab::Shadow { return core::shadow(this._p); }
    fn set_clip(this, v: vocab::Shape) -> TimePicker { core::set_clip(this._p, v); return this; }
    fn clip(this) -> vocab::Shape { return core::clip(this._p); }
    fn set_enabled(this, v: bool) -> TimePicker { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> TimePicker { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_input_transparent(this, v: bool) -> TimePicker { core::set_input_transparent(this._p, v); return this; }
    fn input_transparent(this) -> bool { return core::input_transparent(this._p); }
    fn set_flow_direction(this, v: vocab::FlowDirection) -> TimePicker { core::set_flow_direction(this._p, v); return this; }
    fn flow_direction(this) -> vocab::FlowDirection { return core::flow_direction(this._p); }
    fn set_rotation(this, v: f64) -> TimePicker { core::set_rotation(this._p, v); return this; }
    fn rotation(this) -> f64 { return core::rotation(this._p); }
    fn set_rotation_x(this, v: f64) -> TimePicker { core::set_rotation_x(this._p, v); return this; }
    fn rotation_x(this) -> f64 { return core::rotation_x(this._p); }
    fn set_rotation_y(this, v: f64) -> TimePicker { core::set_rotation_y(this._p, v); return this; }
    fn rotation_y(this) -> f64 { return core::rotation_y(this._p); }
    fn set_scale(this, v: f64) -> TimePicker { core::set_scale(this._p, v); return this; }
    fn scale(this) -> f64 { return core::scale(this._p); }
    fn set_scale_x(this, v: f64) -> TimePicker { core::set_scale_x(this._p, v); return this; }
    fn scale_x(this) -> f64 { return core::scale_x(this._p); }
    fn set_scale_y(this, v: f64) -> TimePicker { core::set_scale_y(this._p, v); return this; }
    fn scale_y(this) -> f64 { return core::scale_y(this._p); }
    fn set_translation_x(this, v: f64) -> TimePicker { core::set_translation_x(this._p, v); return this; }
    fn translation_x(this) -> f64 { return core::translation_x(this._p); }
    fn set_translation_y(this, v: f64) -> TimePicker { core::set_translation_y(this._p, v); return this; }
    fn translation_y(this) -> f64 { return core::translation_y(this._p); }
    fn set_anchor_x(this, v: f64) -> TimePicker { core::set_anchor_x(this._p, v); return this; }
    fn anchor_x(this) -> f64 { return core::anchor_x(this._p); }
    fn set_anchor_y(this, v: f64) -> TimePicker { core::set_anchor_y(this._p, v); return this; }
    fn anchor_y(this) -> f64 { return core::anchor_y(this._p); }
    fn set_z_index(this, v: i64) -> TimePicker { core::set_z_index(this._p, v); return this; }
    fn z_index(this) -> i64 { return core::z_index(this._p); }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn bounds(this) -> vocab::Rect { return core::bounds_of(this._p); }
    fn is_focused(this) -> bool { return core::is_focused(this._p); }
    fn is_attached(this) -> bool { return core::is_attached(this._p); }
    fn window(this) -> vocab::WindowRef { return core::window_of(this._p); }
    fn native(this) -> *u8 { return core::native(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn child(this, at: usize) -> option::Option[*core::Node] { return core::child_of(this._p, at); }
    fn set_grow(this, v: f64) -> TimePicker { core::set_grow(this._p, v); return this; }
    fn set_shrink(this, v: f64) -> TimePicker { core::set_shrink(this._p, v); return this; }
    fn set_width(this, v: f64) -> TimePicker { core::set_width(this._p, v); return this; }
    fn set_height(this, v: f64) -> TimePicker { core::set_height(this._p, v); return this; }
    fn set_width_percent(this, v: f64) -> TimePicker { core::set_width_percent(this._p, v); return this; }
    fn set_height_percent(this, v: f64) -> TimePicker { core::set_height_percent(this._p, v); return this; }
    fn set_padding(this, v: f64) -> TimePicker { core::set_padding(this._p, v); return this; }
    fn set_margin(this, v: f64) -> TimePicker { core::set_margin(this._p, v); return this; }
    fn set_gap(this, v: f64) -> TimePicker { core::set_gap(this._p, v); return this; }
    fn set_justify(this, v: flex::Justify) -> TimePicker { core::set_justify(this._p, v); return this; }
    fn set_align(this, v: flex::Align) -> TimePicker { core::set_align(this._p, v); return this; }
    fn set_wrap(this, v: flex::Wrap) -> TimePicker { core::set_wrap(this._p, v); return this; }
    fn focus(this) -> TimePicker { core::focus(this._p); return this; }
    fn blur(this) -> TimePicker { core::blur(this._p); return this; }
    fn relayout(this) -> TimePicker { core::relayout(this._p); return this; }
    fn measure(this, width: f64, height: f64) -> vocab::Size { return core::measure(this._p, width, height); }
    fn begin_updates(this) -> TimePicker { core::begin_updates(this._p); return this; }
    fn end_updates(this) -> TimePicker { core::end_updates(this._p); return this; }
    fn on_focus(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> TimePicker { core::set_on_focus(this._p, f, ctx); return this; }
    fn on_blur(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> TimePicker { core::set_on_blur(this._p, f, ctx); return this; }
    fn on_attach(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> TimePicker { core::set_on_attach(this._p, f, ctx); return this; }
    fn on_detach(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> TimePicker { core::set_on_detach(this._p, f, ctx); return this; }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup ------------------------------------------------------------
// `from` narrows a node to this control, the DOM step: a node carries what
// EVERY node has, and a checked cast adds what only a time_picker has. `None` on
// a wrong kind — never a cursor whose every verb silently does nothing.

fn from(p: *core::Node) -> option::Option[TimePicker] {
    if p == (0 as *core::Node) { return option::Option[TimePicker]::None; }
    let d: *core::Data = { (*p).data() };
    if d == (0 as *core::Data) { return option::Option[TimePicker]::None; }
    if { (*d).kind } != props::K_TIME_PICKER { return option::Option[TimePicker]::None; }
    return option::Option[TimePicker]::Some(TimePicker::_of(p));
}

// Resolve a key, then narrow through the same checked door.

// `within:` and not `in:` — `in` is a C+ keyword. Defaults to the
// mounted tree, so an agent holding only a key can address it; pass a
// subtree to scope the search (stages/3, the global find).
fn find(key: str, within: *core::Node = mount::mounted_root()) -> option::Option[TimePicker] {
    if within == (0 as *core::Node) { return option::Option[TimePicker]::None; }
    return match core::find_in(within, key) {
        option::Option[*core::Node]::Some(p) => from(p),
        option::Option[*core::Node]::None => option::Option[TimePicker]::None,
    };
}
