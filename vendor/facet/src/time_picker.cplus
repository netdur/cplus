// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// time_picker — MAUI TimePicker. 9 writes, 0 reads, 3 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_CHARACTER_SPACING: u64 = 1u64;
const P_FONT_ATTRIBUTES: u64 = 2u64;
const P_FONT_AUTO_SCALING_ENABLED: u64 = 4u64;
const P_FONT_FAMILY: u64 = 8u64;
const P_FONT_SIZE: u64 = 16u64;
const P_FORMAT: u64 = 32u64;
const P_IS_OPEN: u64 = 64u64;
const P_TEXT_COLOR: u64 = 128u64;
const P_TIME: u64 = 256u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn time_picker(
    time: vocab::Duration,
    key: str = "",
    character_spacing: f64 = 0.0f64,
    font_attributes: vocab::FontStyle = vocab::FontStyle::Bold,
    font_auto_scaling_enabled: bool = false,
    font_family: str = "",
    font_size: f64 = 0.0f64,
    format: str = "",
    is_open: bool = false,
    text_color: vocab::Color = vocab::Color::clear(),
    on_closed: fn(*u8, *u8) = props::no_handler,
    on_closed_ctx: *u8 = 0 as *u8,
    on_opened: fn(*u8, *u8) = props::no_handler,
    on_opened_ctx: *u8 = 0 as *u8,
    on_time_selected: fn(*u8, *u8) = props::no_handler,
    on_time_selected_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::TimePickerProps = props::TimePickerProps::new();
    p.character_spacing = character_spacing;
    p.font_attributes = font_attributes;
    p.font_auto_scaling_enabled = font_auto_scaling_enabled;
    p.font_family = text::from_str(font_family);
    p.font_size = font_size;
    p.format = text::from_str(format);
    p.is_open = is_open;
    p.text_color = text_color;
    p.time = time;
    p.on_closed = on_closed;
    p.on_closed_ctx = on_closed_ctx;
    p.on_opened = on_opened;
    p.on_opened_ctx = on_opened_ctx;
    p.on_time_selected = on_time_selected;
    p.on_time_selected_ctx = on_time_selected_ctx;
    return match box::new::[props::TimePickerProps](p) {
        option::Option[box::Box[props::TimePickerProps]]::Some(b) =>
            core::node_with(key, props::K_TIME_PICKER, b.into_raw(), props::release_time_picker_props),
        option::Option[box::Box[props::TimePickerProps]]::None =>
            core::node_with(key, props::K_TIME_PICKER, 0 as *u8, props::release_time_picker_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct TimePicker {
    opaque _p: *core::Node,
    _seen: u64,
}

impl TimePicker {
    fn of(p: *core::Node) -> TimePicker { return TimePicker { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::TimePickerProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::TimePickerProps; }
        if { (*d).kind } != props::K_TIME_PICKER { return 0 as *props::TimePickerProps; }
        return { (*d).props } as *props::TimePickerProps;
    }

    // TimePicker.CharacterSpacing
    fn set_character_spacing(this, v: f64) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).character_spacing = v };
        core::touch(this._p, P_CHARACTER_SPACING);
        return this;
    }

    // TimePicker.FontAttributes
    fn set_font_attributes(this, v: vocab::FontStyle) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).font_attributes = v };
        core::touch(this._p, P_FONT_ATTRIBUTES);
        return this;
    }

    // TimePicker.FontAutoScalingEnabled
    fn set_font_auto_scaling_enabled(this, v: bool) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).font_auto_scaling_enabled = v };
        core::touch(this._p, P_FONT_AUTO_SCALING_ENABLED);
        return this;
    }

    // TimePicker.FontFamily
    fn set_font_family(this, v: str) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).font_family = text::from_str(v) };
        core::touch(this._p, P_FONT_FAMILY);
        return this;
    }

    // TimePicker.FontSize
    fn set_font_size(this, v: f64) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).font_size = v };
        core::touch(this._p, P_FONT_SIZE);
        return this;
    }

    // TimePicker.Format
    fn set_format(this, v: str) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).format = text::from_str(v) };
        core::touch(this._p, P_FORMAT);
        return this;
    }

    // TimePicker.IsOpen
    fn set_is_open(this, v: bool) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).is_open = v };
        core::touch(this._p, P_IS_OPEN);
        return this;
    }

    // TimePicker.TextColor
    fn set_text_color(this, v: vocab::Color) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).text_color = v };
        core::touch(this._p, P_TEXT_COLOR);
        return this;
    }

    // TimePicker.Time
    fn set_time(this, v: vocab::Duration) -> TimePicker {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return this; }
        { (*p).time = v };
        core::touch(this._p, P_TIME);
        return this;
    }

    fn character_spacing(this) -> f64 {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return 0.0f64; }
        return { (*p).character_spacing };
    }

    fn font_attributes(this) -> vocab::FontStyle {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return vocab::FontStyle::Bold; }
        return { (*p).font_attributes };
    }

    fn font_auto_scaling_enabled(this) -> bool {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return false; }
        return { (*p).font_auto_scaling_enabled };
    }

    fn font_family(this) -> str {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return ""; }
        return { (*p).font_family.view() };
    }

    fn font_size(this) -> f64 {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return 0.0f64; }
        return { (*p).font_size };
    }

    fn format(this) -> str {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return ""; }
        return { (*p).format.view() };
    }

    fn is_open(this) -> bool {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return false; }
        return { (*p).is_open };
    }

    fn text_color(this) -> vocab::Color {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return vocab::Color::clear(); }
        return { (*p).text_color };
    }

    fn time(this) -> vocab::Duration {
        let p: *props::TimePickerProps = this._props();
        if p == (0 as *props::TimePickerProps) { return vocab::Duration::zero(); }
        return { (*p).time };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> TimePicker { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_is_enabled(this, v: bool) -> TimePicker { core::set_is_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_is_visible(this, v: bool) -> TimePicker { core::set_is_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> TimePicker { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> TimePicker { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> TimePicker { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[TimePicker] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[TimePicker]::None; }
            if { (*d).kind } != props::K_TIME_PICKER { return option::Option[TimePicker]::None; }
            option::Option[TimePicker]::Some(TimePicker::of(p))
        }
        option::Option[*core::Node]::None => option::Option[TimePicker]::None,
    };
}
