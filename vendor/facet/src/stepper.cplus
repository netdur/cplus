// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// stepper — MAUI Stepper. 4 writes, 0 reads, 1 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_INCREMENT: u64 = 1u64;
const P_MAXIMUM: u64 = 2u64;
const P_MINIMUM: u64 = 4u64;
const P_VALUE: u64 = 8u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn stepper(
    value: f64,
    key: str = "",
    increment: f64 = 0.0f64,
    maximum: f64 = 0.0f64,
    minimum: f64 = 0.0f64,
    on_value_changed: fn(*u8, *u8) = props::no_handler,
    on_value_changed_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::StepperProps = props::StepperProps::new();
    p.increment = increment;
    p.maximum = maximum;
    p.minimum = minimum;
    p.value = value;
    p.on_value_changed = on_value_changed;
    p.on_value_changed_ctx = on_value_changed_ctx;
    return match box::new::[props::StepperProps](p) {
        option::Option[box::Box[props::StepperProps]]::Some(b) =>
            core::node_with(key, props::K_STEPPER, b.into_raw(), props::release_stepper_props),
        option::Option[box::Box[props::StepperProps]]::None =>
            core::node_with(key, props::K_STEPPER, 0 as *u8, props::release_stepper_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Stepper {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Stepper {
    fn of(p: *core::Node) -> Stepper { return Stepper { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::StepperProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::StepperProps; }
        if { (*d).kind } != props::K_STEPPER { return 0 as *props::StepperProps; }
        return { (*d).props } as *props::StepperProps;
    }

    // Stepper.Increment
    fn set_increment(this, v: f64) -> Stepper {
        let p: *props::StepperProps = this._props();
        if p == (0 as *props::StepperProps) { return this; }
        { (*p).increment = v };
        core::touch(this._p, P_INCREMENT);
        return this;
    }

    // Stepper.Maximum
    fn set_maximum(this, v: f64) -> Stepper {
        let p: *props::StepperProps = this._props();
        if p == (0 as *props::StepperProps) { return this; }
        { (*p).maximum = v };
        core::touch(this._p, P_MAXIMUM);
        return this;
    }

    // Stepper.Minimum
    fn set_minimum(this, v: f64) -> Stepper {
        let p: *props::StepperProps = this._props();
        if p == (0 as *props::StepperProps) { return this; }
        { (*p).minimum = v };
        core::touch(this._p, P_MINIMUM);
        return this;
    }

    // Stepper.Value
    fn set_value(this, v: f64) -> Stepper {
        let p: *props::StepperProps = this._props();
        if p == (0 as *props::StepperProps) { return this; }
        { (*p).value = v };
        core::touch(this._p, P_VALUE);
        return this;
    }

    fn increment(this) -> f64 {
        let p: *props::StepperProps = this._props();
        if p == (0 as *props::StepperProps) { return 0.0f64; }
        return { (*p).increment };
    }

    fn maximum(this) -> f64 {
        let p: *props::StepperProps = this._props();
        if p == (0 as *props::StepperProps) { return 0.0f64; }
        return { (*p).maximum };
    }

    fn minimum(this) -> f64 {
        let p: *props::StepperProps = this._props();
        if p == (0 as *props::StepperProps) { return 0.0f64; }
        return { (*p).minimum };
    }

    fn value(this) -> f64 {
        let p: *props::StepperProps = this._props();
        if p == (0 as *props::StepperProps) { return 0.0f64; }
        return { (*p).value };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Stepper { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_enabled(this, v: bool) -> Stepper { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> Stepper { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Stepper { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Stepper { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Stepper { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Stepper] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Stepper]::None; }
            if { (*d).kind } != props::K_STEPPER { return option::Option[Stepper]::None; }
            option::Option[Stepper]::Some(Stepper::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Stepper]::None,
    };
}
