// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// box — MAUI BoxView. 2 writes, 0 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_COLOR: u64 = 1u64;
const P_CORNER_RADIUS: u64 = 2u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn box(
    color: vocab::Color,
    key: str = "",
    corner_radius: vocab::Corners = vocab::Corners::none(),
) -> core::Node {
    var p: props::BoxProps = props::BoxProps::new();
    p.color = color;
    p.corner_radius = corner_radius;
    return match box::new::[props::BoxProps](p) {
        option::Option[box::Box[props::BoxProps]]::Some(b) =>
            core::node_with(key, props::K_BOX, b.into_raw(), props::release_box_props),
        option::Option[box::Box[props::BoxProps]]::None =>
            core::node_with(key, props::K_BOX, 0 as *u8, props::release_box_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Box {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Box {
    fn of(p: *core::Node) -> Box { return Box { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::BoxProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::BoxProps; }
        if { (*d).kind } != props::K_BOX { return 0 as *props::BoxProps; }
        return { (*d).props } as *props::BoxProps;
    }

    // BoxView.Color
    fn set_color(this, v: vocab::Color) -> Box {
        let p: *props::BoxProps = this._props();
        if p == (0 as *props::BoxProps) { return this; }
        { (*p).color = v };
        core::touch(this._p, P_COLOR);
        return this;
    }

    // BoxView.CornerRadius
    fn set_corner_radius(this, v: vocab::Corners) -> Box {
        let p: *props::BoxProps = this._props();
        if p == (0 as *props::BoxProps) { return this; }
        { (*p).corner_radius = v };
        core::touch(this._p, P_CORNER_RADIUS);
        return this;
    }

    fn color(this) -> vocab::Color {
        let p: *props::BoxProps = this._props();
        if p == (0 as *props::BoxProps) { return vocab::Color::clear(); }
        return { (*p).color };
    }

    fn corner_radius(this) -> vocab::Corners {
        let p: *props::BoxProps = this._props();
        if p == (0 as *props::BoxProps) { return vocab::Corners::none(); }
        return { (*p).corner_radius };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Box { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_enabled(this, v: bool) -> Box { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> Box { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Box { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Box { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Box { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Box] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Box]::None; }
            if { (*d).kind } != props::K_BOX { return option::Option[Box]::None; }
            option::Option[Box]::Some(Box::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Box]::None,
    };
}
