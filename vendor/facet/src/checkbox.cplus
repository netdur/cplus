// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// checkbox — MAUI CheckBox. 2 writes, 1 reads, 1 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_COLOR: u64 = 1u64;
const P_ON: u64 = 2u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn checkbox(
    on: bool,
    key: str = "",
    color: vocab::Color = vocab::Color::clear(),
    on_checked_changed: fn(*u8, *u8) = props::no_handler,
    on_checked_changed_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::CheckboxProps = props::CheckboxProps::new();
    p.color = color;
    p.on = on;
    p.on_checked_changed = on_checked_changed;
    p.on_checked_changed_ctx = on_checked_changed_ctx;
    return match box::new::[props::CheckboxProps](p) {
        option::Option[box::Box[props::CheckboxProps]]::Some(b) =>
            core::node_with(key, props::K_CHECKBOX, b.into_raw(), props::release_checkbox_props),
        option::Option[box::Box[props::CheckboxProps]]::None =>
            core::node_with(key, props::K_CHECKBOX, 0 as *u8, props::release_checkbox_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Checkbox {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Checkbox {
    fn of(p: *core::Node) -> Checkbox { return Checkbox { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::CheckboxProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::CheckboxProps; }
        if { (*d).kind } != props::K_CHECKBOX { return 0 as *props::CheckboxProps; }
        return { (*d).props } as *props::CheckboxProps;
    }

    // CheckBox.Color
    fn set_color(this, v: vocab::Color) -> Checkbox {
        let p: *props::CheckboxProps = this._props();
        if p == (0 as *props::CheckboxProps) { return this; }
        { (*p).color = v };
        core::touch(this._p, P_COLOR);
        return this;
    }

    // CheckBox.IsChecked
    fn set_on(this, v: bool) -> Checkbox {
        let p: *props::CheckboxProps = this._props();
        if p == (0 as *props::CheckboxProps) { return this; }
        { (*p).on = v };
        core::touch(this._p, P_ON);
        return this;
    }

    fn color(this) -> vocab::Color {
        let p: *props::CheckboxProps = this._props();
        if p == (0 as *props::CheckboxProps) { return vocab::Color::clear(); }
        return { (*p).color };
    }

    fn on(this) -> bool {
        let p: *props::CheckboxProps = this._props();
        if p == (0 as *props::CheckboxProps) { return false; }
        return { (*p).on };
    }

    fn foreground(this) -> vocab::Brush {
        let p: *props::CheckboxProps = this._props();
        if p == (0 as *props::CheckboxProps) { return vocab::Brush::none(); }
        return { (*p).foreground };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Checkbox { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_is_enabled(this, v: bool) -> Checkbox { core::set_is_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_is_visible(this, v: bool) -> Checkbox { core::set_is_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Checkbox { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Checkbox { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Checkbox { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Checkbox] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Checkbox]::None; }
            if { (*d).kind } != props::K_CHECKBOX { return option::Option[Checkbox]::None; }
            option::Option[Checkbox]::Some(Checkbox::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Checkbox]::None,
    };
}
