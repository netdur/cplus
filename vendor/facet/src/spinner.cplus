// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// spinner — MAUI ActivityIndicator. 2 writes, 0 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_COLOR: u64 = 1u64;
const P_IS_RUNNING: u64 = 2u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn spinner(
    color: vocab::Color,
    key: str = "",
    is_running: bool = false,
) -> core::Node {
    var p: props::SpinnerProps = props::SpinnerProps::new();
    p.color = color;
    p.is_running = is_running;
    return match box::new::[props::SpinnerProps](p) {
        option::Option[box::Box[props::SpinnerProps]]::Some(b) =>
            core::node_with(key, props::K_SPINNER, b.into_raw(), props::release_spinner_props),
        option::Option[box::Box[props::SpinnerProps]]::None =>
            core::node_with(key, props::K_SPINNER, 0 as *u8, props::release_spinner_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Spinner {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Spinner {
    fn of(p: *core::Node) -> Spinner { return Spinner { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::SpinnerProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::SpinnerProps; }
        if { (*d).kind } != props::K_SPINNER { return 0 as *props::SpinnerProps; }
        return { (*d).props } as *props::SpinnerProps;
    }

    // ActivityIndicator.Color
    fn set_color(this, v: vocab::Color) -> Spinner {
        let p: *props::SpinnerProps = this._props();
        if p == (0 as *props::SpinnerProps) { return this; }
        { (*p).color = v };
        core::touch(this._p, P_COLOR);
        return this;
    }

    // ActivityIndicator.IsRunning
    fn set_is_running(this, v: bool) -> Spinner {
        let p: *props::SpinnerProps = this._props();
        if p == (0 as *props::SpinnerProps) { return this; }
        { (*p).is_running = v };
        core::touch(this._p, P_IS_RUNNING);
        return this;
    }

    fn color(this) -> vocab::Color {
        let p: *props::SpinnerProps = this._props();
        if p == (0 as *props::SpinnerProps) { return vocab::Color::clear(); }
        return { (*p).color };
    }

    fn is_running(this) -> bool {
        let p: *props::SpinnerProps = this._props();
        if p == (0 as *props::SpinnerProps) { return false; }
        return { (*p).is_running };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Spinner { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_is_enabled(this, v: bool) -> Spinner { core::set_is_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_is_visible(this, v: bool) -> Spinner { core::set_is_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Spinner { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Spinner { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Spinner { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Spinner] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Spinner]::None; }
            if { (*d).kind } != props::K_SPINNER { return option::Option[Spinner]::None; }
            option::Option[Spinner]::Some(Spinner::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Spinner]::None,
    };
}
