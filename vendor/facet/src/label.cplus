// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// label — MAUI Label. 16 writes, 0 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_CHARACTER_SPACING: u64 = 1u64;
const P_FONT_ATTRIBUTES: u64 = 2u64;
const P_FONT_AUTO_SCALING_ENABLED: u64 = 4u64;
const P_FONT_FAMILY: u64 = 8u64;
const P_FONT_SIZE: u64 = 16u64;
const P_FORMATTED_TEXT: u64 = 32u64;
const P_HORIZONTAL_TEXT_ALIGNMENT: u64 = 64u64;
const P_LINE_BREAK_MODE: u64 = 128u64;
const P_LINE_HEIGHT: u64 = 256u64;
const P_MAX_LINES: u64 = 512u64;
const P_TEXT: u64 = 1024u64;
const P_TEXT_COLOR: u64 = 2048u64;
const P_TEXT_DECORATIONS: u64 = 4096u64;
const P_TEXT_TRANSFORM: u64 = 8192u64;
const P_TEXT_TYPE: u64 = 16384u64;
const P_VERTICAL_TEXT_ALIGNMENT: u64 = 32768u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn label(
    text: str,
    key: str = "",
    character_spacing: f64 = 0.0f64,
    font_attributes: vocab::FontStyle = vocab::FontStyle::Bold,
    font_auto_scaling_enabled: bool = false,
    font_family: str = "",
    font_size: f64 = 0.0f64,
    formatted_text: vocab::Spans = vocab::Spans::none(),
    horizontal_text_alignment: vocab::TextAlign = vocab::TextAlign::Center,
    line_break_mode: vocab::LineBreak = vocab::LineBreak::CharacterWrap,
    line_height: f64 = 0.0f64,
    max_lines: i64 = 0 as i64,
    text_color: vocab::Color = vocab::Color::clear(),
    text_decorations: vocab::TextDecoration = vocab::TextDecoration::None,
    text_transform: vocab::TextTransform = vocab::TextTransform::Default,
    text_type: vocab::TextType = vocab::TextType::Html,
    vertical_text_alignment: vocab::TextAlign = vocab::TextAlign::Center,
) -> core::Node {
    var p: props::LabelProps = props::LabelProps::new();
    p.character_spacing = character_spacing;
    p.font_attributes = font_attributes;
    p.font_auto_scaling_enabled = font_auto_scaling_enabled;
    p.font_family = text::from_str(font_family);
    p.font_size = font_size;
    p.formatted_text = formatted_text;
    p.horizontal_text_alignment = horizontal_text_alignment;
    p.line_break_mode = line_break_mode;
    p.line_height = line_height;
    p.max_lines = max_lines;
    p.text = text::from_str(text);
    p.text_color = text_color;
    p.text_decorations = text_decorations;
    p.text_transform = text_transform;
    p.text_type = text_type;
    p.vertical_text_alignment = vertical_text_alignment;
    return match box::new::[props::LabelProps](p) {
        option::Option[box::Box[props::LabelProps]]::Some(b) =>
            core::node_with(key, props::K_LABEL, b.into_raw(), props::release_label_props),
        option::Option[box::Box[props::LabelProps]]::None =>
            core::node_with(key, props::K_LABEL, 0 as *u8, props::release_label_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Label {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Label {
    fn of(p: *core::Node) -> Label { return Label { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::LabelProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::LabelProps; }
        if { (*d).kind } != props::K_LABEL { return 0 as *props::LabelProps; }
        return { (*d).props } as *props::LabelProps;
    }

    // Label.CharacterSpacing
    fn set_character_spacing(this, v: f64) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).character_spacing = v };
        core::touch(this._p, P_CHARACTER_SPACING);
        return this;
    }

    // Label.FontAttributes
    fn set_font_attributes(this, v: vocab::FontStyle) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).font_attributes = v };
        core::touch(this._p, P_FONT_ATTRIBUTES);
        return this;
    }

    // Label.FontAutoScalingEnabled
    fn set_font_auto_scaling_enabled(this, v: bool) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).font_auto_scaling_enabled = v };
        core::touch(this._p, P_FONT_AUTO_SCALING_ENABLED);
        return this;
    }

    // Label.FontFamily
    fn set_font_family(this, v: str) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).font_family = text::from_str(v) };
        core::touch(this._p, P_FONT_FAMILY);
        return this;
    }

    // Label.FontSize
    fn set_font_size(this, v: f64) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).font_size = v };
        core::touch(this._p, P_FONT_SIZE);
        return this;
    }

    // Label.FormattedText
    fn set_formatted_text(this, v: vocab::Spans) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).formatted_text = v };
        core::touch(this._p, P_FORMATTED_TEXT);
        return this;
    }

    // Label.HorizontalTextAlignment
    fn set_horizontal_text_alignment(this, v: vocab::TextAlign) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).horizontal_text_alignment = v };
        core::touch(this._p, P_HORIZONTAL_TEXT_ALIGNMENT);
        return this;
    }

    // Label.LineBreakMode
    fn set_line_break_mode(this, v: vocab::LineBreak) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).line_break_mode = v };
        core::touch(this._p, P_LINE_BREAK_MODE);
        return this;
    }

    // Label.LineHeight
    fn set_line_height(this, v: f64) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).line_height = v };
        core::touch(this._p, P_LINE_HEIGHT);
        return this;
    }

    // Label.MaxLines
    fn set_max_lines(this, v: i64) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).max_lines = v };
        core::touch(this._p, P_MAX_LINES);
        return this;
    }

    // Label.Text
    fn set_text(this, v: str) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).text = text::from_str(v) };
        core::touch(this._p, P_TEXT);
        return this;
    }

    // Label.TextColor
    fn set_text_color(this, v: vocab::Color) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).text_color = v };
        core::touch(this._p, P_TEXT_COLOR);
        return this;
    }

    // Label.TextDecorations
    fn set_text_decorations(this, v: vocab::TextDecoration) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).text_decorations = v };
        core::touch(this._p, P_TEXT_DECORATIONS);
        return this;
    }

    // Label.TextTransform
    fn set_text_transform(this, v: vocab::TextTransform) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).text_transform = v };
        core::touch(this._p, P_TEXT_TRANSFORM);
        return this;
    }

    // Label.TextType
    fn set_text_type(this, v: vocab::TextType) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).text_type = v };
        core::touch(this._p, P_TEXT_TYPE);
        return this;
    }

    // Label.VerticalTextAlignment
    fn set_vertical_text_alignment(this, v: vocab::TextAlign) -> Label {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return this; }
        { (*p).vertical_text_alignment = v };
        core::touch(this._p, P_VERTICAL_TEXT_ALIGNMENT);
        return this;
    }

    fn character_spacing(this) -> f64 {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return 0.0f64; }
        return { (*p).character_spacing };
    }

    fn font_attributes(this) -> vocab::FontStyle {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return vocab::FontStyle::Bold; }
        return { (*p).font_attributes };
    }

    fn font_auto_scaling_enabled(this) -> bool {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return false; }
        return { (*p).font_auto_scaling_enabled };
    }

    fn font_family(this) -> str {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return ""; }
        return { (*p).font_family.view() };
    }

    fn font_size(this) -> f64 {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return 0.0f64; }
        return { (*p).font_size };
    }

    fn formatted_text(this) -> vocab::Spans {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return vocab::Spans::none(); }
        return { (*p).formatted_text };
    }

    fn horizontal_text_alignment(this) -> vocab::TextAlign {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return vocab::TextAlign::Center; }
        return { (*p).horizontal_text_alignment };
    }

    fn line_break_mode(this) -> vocab::LineBreak {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return vocab::LineBreak::CharacterWrap; }
        return { (*p).line_break_mode };
    }

    fn line_height(this) -> f64 {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return 0.0f64; }
        return { (*p).line_height };
    }

    fn max_lines(this) -> i64 {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return 0 as i64; }
        return { (*p).max_lines };
    }

    fn text(this) -> str {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return ""; }
        return { (*p).text.view() };
    }

    fn text_color(this) -> vocab::Color {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return vocab::Color::clear(); }
        return { (*p).text_color };
    }

    fn text_decorations(this) -> vocab::TextDecoration {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return vocab::TextDecoration::None; }
        return { (*p).text_decorations };
    }

    fn text_transform(this) -> vocab::TextTransform {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return vocab::TextTransform::Default; }
        return { (*p).text_transform };
    }

    fn text_type(this) -> vocab::TextType {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return vocab::TextType::Html; }
        return { (*p).text_type };
    }

    fn vertical_text_alignment(this) -> vocab::TextAlign {
        let p: *props::LabelProps = this._props();
        if p == (0 as *props::LabelProps) { return vocab::TextAlign::Center; }
        return { (*p).vertical_text_alignment };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Label { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_is_enabled(this, v: bool) -> Label { core::set_is_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_is_visible(this, v: bool) -> Label { core::set_is_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Label { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Label { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Label { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Label] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Label]::None; }
            if { (*d).kind } != props::K_LABEL { return option::Option[Label]::None; }
            option::Option[Label]::Some(Label::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Label]::None,
    };
}
