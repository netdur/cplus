// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// scroll — MAUI ScrollView. 6 writes, 3 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_CASCADE_INPUT_TRANSPARENT: u64 = 1u64;
const P_HORIZONTAL_SCROLL_BAR_VISIBILITY: u64 = 2u64;
const P_LAYOUT_AREA_OVERRIDE: u64 = 4u64;
const P_ORIENTATION: u64 = 8u64;
const P_SAFE_AREA_EDGES: u64 = 16u64;
const P_VERTICAL_SCROLL_BAR_VISIBILITY: u64 = 32u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn scroll(
    key: str = "",
    cascade_input_transparent: bool = false,
    horizontal_scroll_bar_visibility: vocab::ScrollBars = vocab::ScrollBars::Always,
    layout_area_override: vocab::Rect = vocab::Rect::zero(),
    orientation: vocab::ScrollAxis = vocab::ScrollAxis::Both,
    safe_area_edges: vocab::SafeArea = vocab::SafeArea::Default,
    vertical_scroll_bar_visibility: vocab::ScrollBars = vocab::ScrollBars::Always,
) -> core::Node {
    var p: props::ScrollProps = props::ScrollProps::new();
    p.cascade_input_transparent = cascade_input_transparent;
    p.horizontal_scroll_bar_visibility = horizontal_scroll_bar_visibility;
    p.layout_area_override = layout_area_override;
    p.orientation = orientation;
    p.safe_area_edges = safe_area_edges;
    p.vertical_scroll_bar_visibility = vertical_scroll_bar_visibility;
    return match box::new::[props::ScrollProps](p) {
        option::Option[box::Box[props::ScrollProps]]::Some(b) =>
            core::node_with(key, props::K_SCROLL, b.into_raw(), props::release_scroll_props),
        option::Option[box::Box[props::ScrollProps]]::None =>
            core::node_with(key, props::K_SCROLL, 0 as *u8, props::release_scroll_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Scroll {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Scroll {
    fn of(p: *core::Node) -> Scroll { return Scroll { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::ScrollProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::ScrollProps; }
        if { (*d).kind } != props::K_SCROLL { return 0 as *props::ScrollProps; }
        return { (*d).props } as *props::ScrollProps;
    }

    // ScrollView.CascadeInputTransparent
    fn set_cascade_input_transparent(this, v: bool) -> Scroll {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return this; }
        { (*p).cascade_input_transparent = v };
        core::touch(this._p, P_CASCADE_INPUT_TRANSPARENT);
        return this;
    }

    // ScrollView.HorizontalScrollBarVisibility
    fn set_horizontal_scroll_bar_visibility(this, v: vocab::ScrollBars) -> Scroll {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return this; }
        { (*p).horizontal_scroll_bar_visibility = v };
        core::touch(this._p, P_HORIZONTAL_SCROLL_BAR_VISIBILITY);
        return this;
    }

    // ScrollView.LayoutAreaOverride
    fn set_layout_area_override(this, v: vocab::Rect) -> Scroll {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return this; }
        { (*p).layout_area_override = v };
        core::touch(this._p, P_LAYOUT_AREA_OVERRIDE);
        return this;
    }

    // ScrollView.Orientation
    fn set_orientation(this, v: vocab::ScrollAxis) -> Scroll {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return this; }
        { (*p).orientation = v };
        core::touch(this._p, P_ORIENTATION);
        return this;
    }

    // ScrollView.SafeAreaEdges
    fn set_safe_area_edges(this, v: vocab::SafeArea) -> Scroll {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return this; }
        { (*p).safe_area_edges = v };
        core::touch(this._p, P_SAFE_AREA_EDGES);
        return this;
    }

    // ScrollView.VerticalScrollBarVisibility
    fn set_vertical_scroll_bar_visibility(this, v: vocab::ScrollBars) -> Scroll {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return this; }
        { (*p).vertical_scroll_bar_visibility = v };
        core::touch(this._p, P_VERTICAL_SCROLL_BAR_VISIBILITY);
        return this;
    }

    fn cascade_input_transparent(this) -> bool {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return false; }
        return { (*p).cascade_input_transparent };
    }

    fn horizontal_scroll_bar_visibility(this) -> vocab::ScrollBars {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return vocab::ScrollBars::Always; }
        return { (*p).horizontal_scroll_bar_visibility };
    }

    fn layout_area_override(this) -> vocab::Rect {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return vocab::Rect::zero(); }
        return { (*p).layout_area_override };
    }

    fn orientation(this) -> vocab::ScrollAxis {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return vocab::ScrollAxis::Both; }
        return { (*p).orientation };
    }

    fn safe_area_edges(this) -> vocab::SafeArea {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return vocab::SafeArea::Default; }
        return { (*p).safe_area_edges };
    }

    fn vertical_scroll_bar_visibility(this) -> vocab::ScrollBars {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return vocab::ScrollBars::Always; }
        return { (*p).vertical_scroll_bar_visibility };
    }

    fn content_size(this) -> vocab::Size {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return vocab::Size::zero(); }
        return { (*p).content_size };
    }

    fn scroll_x(this) -> f64 {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return 0.0f64; }
        return { (*p).scroll_x };
    }

    fn scroll_y(this) -> f64 {
        let p: *props::ScrollProps = this._props();
        if p == (0 as *props::ScrollProps) { return 0.0f64; }
        return { (*p).scroll_y };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Scroll { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_enabled(this, v: bool) -> Scroll { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> Scroll { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Scroll { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Scroll { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Scroll { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Scroll] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Scroll]::None; }
            if { (*d).kind } != props::K_SCROLL { return option::Option[Scroll]::None; }
            option::Option[Scroll]::Some(Scroll::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Scroll]::None,
    };
}
