// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// toggle — MAUI Switch. 4 writes, 0 reads, 1 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_ON: u64 = 1u64;
const P_OFF_COLOR: u64 = 2u64;
const P_ON_COLOR: u64 = 4u64;
const P_THUMB_COLOR: u64 = 8u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn toggle(
    on: bool,
    key: str = "",
    off_color: vocab::Color = vocab::Color::clear(),
    on_color: vocab::Color = vocab::Color::clear(),
    thumb_color: vocab::Color = vocab::Color::clear(),
    on_toggled: fn(*u8, *u8) = props::no_handler,
    on_toggled_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::ToggleProps = props::ToggleProps::new();
    p.on = on;
    p.off_color = off_color;
    p.on_color = on_color;
    p.thumb_color = thumb_color;
    p.on_toggled = on_toggled;
    p.on_toggled_ctx = on_toggled_ctx;
    return match box::new::[props::ToggleProps](p) {
        option::Option[box::Box[props::ToggleProps]]::Some(b) =>
            core::node_with(key, props::K_TOGGLE, b.into_raw(), props::release_toggle_props),
        option::Option[box::Box[props::ToggleProps]]::None =>
            core::node_with(key, props::K_TOGGLE, 0 as *u8, props::release_toggle_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Toggle {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Toggle {
    fn of(p: *core::Node) -> Toggle { return Toggle { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::ToggleProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::ToggleProps; }
        if { (*d).kind } != props::K_TOGGLE { return 0 as *props::ToggleProps; }
        return { (*d).props } as *props::ToggleProps;
    }

    // Switch.IsToggled
    fn set_on(this, v: bool) -> Toggle {
        let p: *props::ToggleProps = this._props();
        if p == (0 as *props::ToggleProps) { return this; }
        { (*p).on = v };
        core::touch(this._p, P_ON);
        return this;
    }

    // Switch.OffColor
    fn set_off_color(this, v: vocab::Color) -> Toggle {
        let p: *props::ToggleProps = this._props();
        if p == (0 as *props::ToggleProps) { return this; }
        { (*p).off_color = v };
        core::touch(this._p, P_OFF_COLOR);
        return this;
    }

    // Switch.OnColor
    fn set_on_color(this, v: vocab::Color) -> Toggle {
        let p: *props::ToggleProps = this._props();
        if p == (0 as *props::ToggleProps) { return this; }
        { (*p).on_color = v };
        core::touch(this._p, P_ON_COLOR);
        return this;
    }

    // Switch.ThumbColor
    fn set_thumb_color(this, v: vocab::Color) -> Toggle {
        let p: *props::ToggleProps = this._props();
        if p == (0 as *props::ToggleProps) { return this; }
        { (*p).thumb_color = v };
        core::touch(this._p, P_THUMB_COLOR);
        return this;
    }

    fn on(this) -> bool {
        let p: *props::ToggleProps = this._props();
        if p == (0 as *props::ToggleProps) { return false; }
        return { (*p).on };
    }

    fn off_color(this) -> vocab::Color {
        let p: *props::ToggleProps = this._props();
        if p == (0 as *props::ToggleProps) { return vocab::Color::clear(); }
        return { (*p).off_color };
    }

    fn on_color(this) -> vocab::Color {
        let p: *props::ToggleProps = this._props();
        if p == (0 as *props::ToggleProps) { return vocab::Color::clear(); }
        return { (*p).on_color };
    }

    fn thumb_color(this) -> vocab::Color {
        let p: *props::ToggleProps = this._props();
        if p == (0 as *props::ToggleProps) { return vocab::Color::clear(); }
        return { (*p).thumb_color };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Toggle { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_is_enabled(this, v: bool) -> Toggle { core::set_is_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_is_visible(this, v: bool) -> Toggle { core::set_is_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Toggle { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Toggle { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Toggle { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Toggle] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Toggle]::None; }
            if { (*d).kind } != props::K_TOGGLE { return option::Option[Toggle]::None; }
            option::Option[Toggle]::Some(Toggle::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Toggle]::None,
    };
}
