// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// web — the ledger WebView. 2 writes, 2 reads, 3 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;
import "./mount" as mount;

// ---- dirty bits --------------------------------------------------------
// One bit per write, then one per owned prop and one per command. The
// top 16 bits of the word are the shared band's (props::C_*).

const P_SOURCE: u64 = 1u64;
const P_USER_AGENT: u64 = 2u64;
const P_EVAL: u64 = 4u64;
const P_GO_BACK: u64 = 8u64;
const P_GO_FORWARD: u64 = 16u64;
const P_RELOAD: u64 = 32u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn web(
    source: str,
    key: str = "",
    user_agent: str = "",
    on_navigated: fn(*u8, *u8) = props::no_handler,
    on_navigated_ctx: *u8 = 0 as *u8,
    on_navigating: fn(*u8, *u8) = props::no_handler,
    on_navigating_ctx: *u8 = 0 as *u8,
    on_process_terminated: fn(*u8, *u8) = props::no_handler,
    on_process_terminated_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::WebProps = props::WebProps::new();
    p.source = text::from_str(source);
    p.user_agent = text::from_str(user_agent);
    p.on_navigated = on_navigated;
    p.on_navigated_ctx = on_navigated_ctx;
    p.on_navigating = on_navigating;
    p.on_navigating_ctx = on_navigating_ctx;
    p.on_process_terminated = on_process_terminated;
    p.on_process_terminated_ctx = on_process_terminated_ctx;
    return match box::new::[props::WebProps](p) {
        option::Option[box::Box[props::WebProps]]::Some(b) =>
            core::node_with(key, props::K_WEB, b.into_raw(), props::release_web_props),
        option::Option[box::Box[props::WebProps]]::None =>
            core::node_with(key, props::K_WEB, 0 as *u8, props::release_web_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Web {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Web {
    // Unchecked, and module-private for that reason: it trusts the
    // caller about the kind. `from` is the public door.
    fn _of(p: *core::Node) -> Web { return Web { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::WebProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::WebProps; }
        if { (*d).kind } != props::K_WEB { return 0 as *props::WebProps; }
        return { (*d).props } as *props::WebProps;
    }

    // WebView.Source
    fn set_source(this, v: str) -> Web {
        let p: *props::WebProps = this._props();
        if p == (0 as *props::WebProps) { return this; }
        { (*p).source = text::from_str(v) };
        core::touch(this._p, P_SOURCE);
        return this;
    }

    // WebView.UserAgent
    fn set_user_agent(this, v: str) -> Web {
        let p: *props::WebProps = this._props();
        if p == (0 as *props::WebProps) { return this; }
        { (*p).user_agent = text::from_str(v) };
        core::touch(this._p, P_USER_AGENT);
        return this;
    }

    fn source(this) -> str {
        let p: *props::WebProps = this._props();
        if p == (0 as *props::WebProps) { return ""; }
        return { (*p).source.view() };
    }

    fn user_agent(this) -> str {
        let p: *props::WebProps = this._props();
        if p == (0 as *props::WebProps) { return ""; }
        return { (*p).user_agent.view() };
    }

    fn can_go_back(this) -> bool {
        let p: *props::WebProps = this._props();
        if p == (0 as *props::WebProps) { return false; }
        return { (*p).can_go_back };
    }

    fn can_go_forward(this) -> bool {
        let p: *props::WebProps = this._props();
        if p == (0 as *props::WebProps) { return false; }
        return { (*p).can_go_forward };
    }

    // WebView.Eval
    fn eval(this, script: str) -> Web {
        let p: *props::WebProps = this._props();
        if p == (0 as *props::WebProps) { return this; }
        { (*p).eval_script = text::from_str(script) };
        core::touch(this._p, P_EVAL);
        return this;
    }

    // WebView.GoBack
    fn go_back(this) -> Web {
        let p: *props::WebProps = this._props();
        if p == (0 as *props::WebProps) { return this; }
        core::touch(this._p, P_GO_BACK);
        return this;
    }

    // WebView.GoForward
    fn go_forward(this) -> Web {
        let p: *props::WebProps = this._props();
        if p == (0 as *props::WebProps) { return this; }
        core::touch(this._p, P_GO_FORWARD);
        return this;
    }

    // WebView.Reload
    fn reload(this) -> Web {
        let p: *props::WebProps = this._props();
        if p == (0 as *props::WebProps) { return this; }
        core::touch(this._p, P_RELOAD);
        return this;
    }

    // The flex node this cursor points at. The DOM division, and the
    // reason facet declares no layout verb of its own:
    //
    //   the control's own   b.set_title("Saving…")     this module
    //   the shared band     b.set_opacity(0.5)         facet.cplus, once
    //   layout              { (*b.node()).set_width(…) }  ALL of flex
    //
    // Reaching flex through the node rather than forwarding its ~40 verbs
    // onto 42 cursors is what keeps facet from growing a second layout
    // vocabulary that can disagree with the first.
    fn node(this) -> *core::Node { return this._p; }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Web { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Web { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_background(this, v: vocab::Brush) -> Web { core::set_background(this._p, v); return this; }
    fn background(this) -> vocab::Brush { return core::background(this._p); }
    fn set_background_image(this, v: str) -> Web { core::set_background_image(this._p, v); return this; }
    fn background_image(this) -> str { return core::background_image(this._p); }
    fn set_safe_area(this, v: vocab::SafeArea) -> Web { core::set_safe_area(this._p, v); return this; }
    fn safe_area(this) -> vocab::SafeArea { return core::safe_area(this._p); }
    fn set_shadow(this, v: vocab::Shadow) -> Web { core::set_shadow(this._p, v); return this; }
    fn shadow(this) -> vocab::Shadow { return core::shadow(this._p); }
    fn set_clip(this, v: vocab::Shape) -> Web { core::set_clip(this._p, v); return this; }
    fn clip(this) -> vocab::Shape { return core::clip(this._p); }
    fn set_enabled(this, v: bool) -> Web { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> Web { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_display(this, v: flex::Display) -> Web { core::set_display(this._p, v); return this; }
    fn display(this) -> flex::Display { return core::display_of(this._p); }
    fn set_shown(this, v: bool) -> Web { core::set_shown(this._p, v); return this; }
    fn is_hidden(this) -> bool { return core::is_hidden(this._p); }
    fn set_input_transparent(this, v: bool) -> Web { core::set_input_transparent(this._p, v); return this; }
    fn input_transparent(this) -> bool { return core::input_transparent(this._p); }
    fn set_flow_direction(this, v: vocab::FlowDirection) -> Web { core::set_flow_direction(this._p, v); return this; }
    fn flow_direction(this) -> vocab::FlowDirection { return core::flow_direction(this._p); }
    fn set_agent(this, v: vocab::Agent) -> Web { core::set_agent(this._p, v); return this; }
    fn agent(this) -> vocab::Agent { return core::agent(this._p); }
    fn set_rotation(this, v: f64) -> Web { core::set_rotation(this._p, v); return this; }
    fn rotation(this) -> f64 { return core::rotation(this._p); }
    fn set_rotation_x(this, v: f64) -> Web { core::set_rotation_x(this._p, v); return this; }
    fn rotation_x(this) -> f64 { return core::rotation_x(this._p); }
    fn set_rotation_y(this, v: f64) -> Web { core::set_rotation_y(this._p, v); return this; }
    fn rotation_y(this) -> f64 { return core::rotation_y(this._p); }
    fn set_scale(this, v: f64) -> Web { core::set_scale(this._p, v); return this; }
    fn scale(this) -> f64 { return core::scale(this._p); }
    fn set_scale_x(this, v: f64) -> Web { core::set_scale_x(this._p, v); return this; }
    fn scale_x(this) -> f64 { return core::scale_x(this._p); }
    fn set_scale_y(this, v: f64) -> Web { core::set_scale_y(this._p, v); return this; }
    fn scale_y(this) -> f64 { return core::scale_y(this._p); }
    fn set_translation_x(this, v: f64) -> Web { core::set_translation_x(this._p, v); return this; }
    fn translation_x(this) -> f64 { return core::translation_x(this._p); }
    fn set_translation_y(this, v: f64) -> Web { core::set_translation_y(this._p, v); return this; }
    fn translation_y(this) -> f64 { return core::translation_y(this._p); }
    fn set_anchor_x(this, v: f64) -> Web { core::set_anchor_x(this._p, v); return this; }
    fn anchor_x(this) -> f64 { return core::anchor_x(this._p); }
    fn set_anchor_y(this, v: f64) -> Web { core::set_anchor_y(this._p, v); return this; }
    fn anchor_y(this) -> f64 { return core::anchor_y(this._p); }
    fn set_z_index(this, v: i64) -> Web { core::set_z_index(this._p, v); return this; }
    fn z_index(this) -> i64 { return core::z_index(this._p); }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn bounds(this) -> vocab::Rect { return core::bounds_of(this._p); }
    fn is_focused(this) -> bool { return core::is_focused(this._p); }
    fn is_attached(this) -> bool { return core::is_attached(this._p); }
    fn window(this) -> vocab::WindowRef { return core::window_of(this._p); }
    fn native(this) -> *u8 { return core::native(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn child(this, at: usize) -> option::Option[*core::Node] { return core::child_of(this._p, at); }
    fn set_grow(this, v: f64) -> Web { core::set_grow(this._p, v); return this; }
    fn set_shrink(this, v: f64) -> Web { core::set_shrink(this._p, v); return this; }
    fn set_width(this, v: f64) -> Web { core::set_width(this._p, v); return this; }
    fn set_height(this, v: f64) -> Web { core::set_height(this._p, v); return this; }
    fn set_width_percent(this, v: f64) -> Web { core::set_width_percent(this._p, v); return this; }
    fn set_height_percent(this, v: f64) -> Web { core::set_height_percent(this._p, v); return this; }
    fn set_padding(this, v: f64) -> Web { core::set_padding(this._p, v); return this; }
    fn set_margin(this, v: f64) -> Web { core::set_margin(this._p, v); return this; }
    fn set_padding_edge(this, edge: flex::Edge, v: f64) -> Web { core::set_padding_edge(this._p, edge, v); return this; }
    fn set_margin_edge(this, edge: flex::Edge, v: f64) -> Web { core::set_margin_edge(this._p, edge, v); return this; }
    fn set_gap(this, v: f64) -> Web { core::set_gap(this._p, v); return this; }
    fn set_justify(this, v: flex::Justify) -> Web { core::set_justify(this._p, v); return this; }
    fn set_align(this, v: flex::Align) -> Web { core::set_align(this._p, v); return this; }
    fn set_wrap(this, v: flex::Wrap) -> Web { core::set_wrap(this._p, v); return this; }
    fn focus(this) -> Web { core::focus(this._p); return this; }
    fn blur(this) -> Web { core::blur(this._p); return this; }
    fn animate_opacity(this, to: f64, duration: vocab::Duration = vocab::Duration::animation(), easing: vocab::Easing = vocab::Easing::SinInOut) -> Web { core::animate_opacity(this._p, to, duration, easing); return this; }
    fn animate_scale(this, to: f64, duration: vocab::Duration = vocab::Duration::animation(), easing: vocab::Easing = vocab::Easing::SinInOut) -> Web { core::animate_scale(this._p, to, duration, easing); return this; }
    fn animate_scale_x(this, to: f64, duration: vocab::Duration = vocab::Duration::animation(), easing: vocab::Easing = vocab::Easing::SinInOut) -> Web { core::animate_scale_x(this._p, to, duration, easing); return this; }
    fn animate_scale_y(this, to: f64, duration: vocab::Duration = vocab::Duration::animation(), easing: vocab::Easing = vocab::Easing::SinInOut) -> Web { core::animate_scale_y(this._p, to, duration, easing); return this; }
    fn animate_rotation(this, to: f64, duration: vocab::Duration = vocab::Duration::animation(), easing: vocab::Easing = vocab::Easing::SinInOut) -> Web { core::animate_rotation(this._p, to, duration, easing); return this; }
    fn animate_rotation_x(this, to: f64, duration: vocab::Duration = vocab::Duration::animation(), easing: vocab::Easing = vocab::Easing::SinInOut) -> Web { core::animate_rotation_x(this._p, to, duration, easing); return this; }
    fn animate_rotation_y(this, to: f64, duration: vocab::Duration = vocab::Duration::animation(), easing: vocab::Easing = vocab::Easing::SinInOut) -> Web { core::animate_rotation_y(this._p, to, duration, easing); return this; }
    fn animate_translation(this, x: f64, y: f64, duration: vocab::Duration = vocab::Duration::animation(), easing: vocab::Easing = vocab::Easing::SinInOut) -> Web { core::animate_translation(this._p, x, y, duration, easing); return this; }
    fn cancel_animations(this) -> Web { core::cancel_animations(this._p); return this; }
    fn relayout(this) -> Web { core::relayout(this._p); return this; }
    fn measure(this, width: f64, height: f64) -> vocab::Size { return core::measure(this._p, width, height); }
    fn begin_updates(this) -> Web { core::begin_updates(this._p); return this; }
    fn end_updates(this) -> Web { core::end_updates(this._p); return this; }
    fn on_focus(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Web { core::set_on_focus(this._p, f, ctx); return this; }
    fn on_blur(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Web { core::set_on_blur(this._p, f, ctx); return this; }
    fn on_attach(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Web { core::set_on_attach(this._p, f, ctx); return this; }
    fn on_detach(this, f: fn(*u8, *u8), ctx: *u8 = 0 as *u8) -> Web { core::set_on_detach(this._p, f, ctx); return this; }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup ------------------------------------------------------------
// `from` narrows a node to this control, the DOM step: a node carries what
// EVERY node has, and a checked cast adds what only a web has. `None` on
// a wrong kind — never a cursor whose every verb silently does nothing.

fn from(p: *core::Node) -> option::Option[Web] {
    if p == (0 as *core::Node) { return option::Option[Web]::None; }
    let d: *core::Data = { (*p).data() };
    if d == (0 as *core::Data) { return option::Option[Web]::None; }
    if { (*d).kind } != props::K_WEB { return option::Option[Web]::None; }
    return option::Option[Web]::Some(Web::_of(p));
}

// Resolve a key, then narrow through the same checked door.

// `within:` and not `in:` — `in` is a C+ keyword. Bare `find` is the
// DOM rule: the running app's windows in open order, first match wins;
// pass `within:` to scope a subtree (or disambiguate across windows).
fn find(key: str, within: *core::Node = 0 as *core::Node) -> option::Option[Web] {
    if within != (0 as *core::Node) {
        return match core::find_in(within, key) {
            option::Option[*core::Node]::Some(p) => { _report_kind(key, p); from(p) }
            option::Option[*core::Node]::None => option::Option[Web]::None,
        };
    }
    return match mount::find_node(key) {
        option::Option[*core::Node]::Some(p) => { _report_kind(key, p); from(p) }
        option::Option[*core::Node]::None => option::Option[Web]::None,
    };
}

// The debug posture's third distinction (FACET_DEBUG_FIND=1): the key
// RESOLVED, and `from` is about to answer None anyway because the node is
// another control. `_`-private; costs one flag read when the posture is off.
fn _report_kind(key: str, p: *core::Node) {
    if !mount::find_debugging() { return; }
    if p == (0 as *core::Node) { return; }
    let d: *core::Data = { (*p).data() };
    if d == (0 as *core::Data) || { (*d).kind } != props::K_WEB {
        mount::debug_find_wrong_kind(key, "web");
    }
    return;
}
