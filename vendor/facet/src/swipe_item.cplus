// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// swipe_item — MAUI SwipeItem. 2 writes, 0 reads, 0 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_BACKGROUND_COLOR: u64 = 1u64;
const P_IS_VISIBLE: u64 = 2u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn swipe_item(
    key: str = "",
    background_color: vocab::Color = vocab::Color::clear(),
    visible: bool = false,
) -> core::Node {
    var p: props::SwipeItemProps = props::SwipeItemProps::new();
    p.background_color = background_color;
    p.is_visible = visible;
    return match box::new::[props::SwipeItemProps](p) {
        option::Option[box::Box[props::SwipeItemProps]]::Some(b) =>
            core::node_with(key, props::K_SWIPE_ITEM, b.into_raw(), props::release_swipe_item_props),
        option::Option[box::Box[props::SwipeItemProps]]::None =>
            core::node_with(key, props::K_SWIPE_ITEM, 0 as *u8, props::release_swipe_item_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct SwipeItem {
    opaque _p: *core::Node,
    _seen: u64,
}

impl SwipeItem {
    fn of(p: *core::Node) -> SwipeItem { return SwipeItem { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::SwipeItemProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::SwipeItemProps; }
        if { (*d).kind } != props::K_SWIPE_ITEM { return 0 as *props::SwipeItemProps; }
        return { (*d).props } as *props::SwipeItemProps;
    }

    // SwipeItem.BackgroundColor
    fn set_background_color(this, v: vocab::Color) -> SwipeItem {
        let p: *props::SwipeItemProps = this._props();
        if p == (0 as *props::SwipeItemProps) { return this; }
        { (*p).background_color = v };
        core::touch(this._p, P_BACKGROUND_COLOR);
        return this;
    }

    // SwipeItem.IsVisible
    fn set_visible(this, v: bool) -> SwipeItem {
        let p: *props::SwipeItemProps = this._props();
        if p == (0 as *props::SwipeItemProps) { return this; }
        { (*p).is_visible = v };
        core::touch(this._p, P_IS_VISIBLE);
        return this;
    }

    fn background_color(this) -> vocab::Color {
        let p: *props::SwipeItemProps = this._props();
        if p == (0 as *props::SwipeItemProps) { return vocab::Color::clear(); }
        return { (*p).background_color };
    }

    fn is_visible(this) -> bool {
        let p: *props::SwipeItemProps = this._props();
        if p == (0 as *props::SwipeItemProps) { return false; }
        return { (*p).is_visible };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> SwipeItem { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_enabled(this, v: bool) -> SwipeItem { core::set_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_visible(this, v: bool) -> SwipeItem { core::set_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> SwipeItem { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> SwipeItem { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> SwipeItem { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[SwipeItem] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[SwipeItem]::None; }
            if { (*d).kind } != props::K_SWIPE_ITEM { return option::Option[SwipeItem]::None; }
            option::Option[SwipeItem]::Some(SwipeItem::of(p))
        }
        option::Option[*core::Node]::None => option::Option[SwipeItem]::None,
    };
}
