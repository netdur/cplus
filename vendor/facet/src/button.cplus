// GENERATED by tools/gen_contract.py — DO NOT EDIT.
// button — MAUI Button. 14 writes, 1 reads, 3 events.
//
// Importing this module is what makes these verbs exist; without it a call
// is E0324, not a silent no-op.

import "flex_layout/flex_layout" as flex;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/box" as box;
import "./facet" as core;
import "./props" as props;
import "./vocabulary" as vocab;

// ---- dirty bits --------------------------------------------------------

const P_BORDER_COLOR: u64 = 1u64;
const P_BORDER_WIDTH: u64 = 2u64;
const P_CHARACTER_SPACING: u64 = 4u64;
const P_CONTENT_LAYOUT: u64 = 8u64;
const P_CORNER_RADIUS: u64 = 16u64;
const P_FONT_ATTRIBUTES: u64 = 32u64;
const P_FONT_AUTO_SCALING_ENABLED: u64 = 64u64;
const P_FONT_FAMILY: u64 = 128u64;
const P_FONT_SIZE: u64 = 256u64;
const P_IMAGE_SOURCE: u64 = 512u64;
const P_LINE_BREAK_MODE: u64 = 1024u64;
const P_TITLE: u64 = 2048u64;
const P_TEXT_COLOR: u64 = 4096u64;
const P_TEXT_TRANSFORM: u64 = 8192u64;

// ---- build -------------------------------------------------------------
// Typed by the parameter list: a verb another control owns is not a
// parameter here, so passing it is a compile error.

fn button(
    title: str,
    key: str = "",
    border_color: vocab::Color = vocab::Color::clear(),
    border_width: f64 = 0.0f64,
    character_spacing: f64 = 0.0f64,
    content_layout: vocab::ContentLayout = vocab::ContentLayout::ImageLeft,
    corner_radius: vocab::Corners = vocab::Corners::none(),
    font_attributes: vocab::FontStyle = vocab::FontStyle::Bold,
    font_auto_scaling_enabled: bool = false,
    font_family: str = "",
    font_size: f64 = 0.0f64,
    image_source: str = "",
    line_break_mode: vocab::LineBreak = vocab::LineBreak::CharacterWrap,
    text_color: vocab::Color = vocab::Color::clear(),
    text_transform: vocab::TextTransform = vocab::TextTransform::Default,
    on_click: fn(*u8, *u8) = props::no_handler,
    on_click_ctx: *u8 = 0 as *u8,
    on_pressed: fn(*u8, *u8) = props::no_handler,
    on_pressed_ctx: *u8 = 0 as *u8,
    on_released: fn(*u8, *u8) = props::no_handler,
    on_released_ctx: *u8 = 0 as *u8,
) -> core::Node {
    var p: props::ButtonProps = props::ButtonProps::new();
    p.border_color = border_color;
    p.border_width = border_width;
    p.character_spacing = character_spacing;
    p.content_layout = content_layout;
    p.corner_radius = corner_radius;
    p.font_attributes = font_attributes;
    p.font_auto_scaling_enabled = font_auto_scaling_enabled;
    p.font_family = text::from_str(font_family);
    p.font_size = font_size;
    p.image_source = text::from_str(image_source);
    p.line_break_mode = line_break_mode;
    p.title = text::from_str(title);
    p.text_color = text_color;
    p.text_transform = text_transform;
    p.on_click = on_click;
    p.on_click_ctx = on_click_ctx;
    p.on_pressed = on_pressed;
    p.on_pressed_ctx = on_pressed_ctx;
    p.on_released = on_released;
    p.on_released_ctx = on_released_ctx;
    return match box::new::[props::ButtonProps](p) {
        option::Option[box::Box[props::ButtonProps]]::Some(b) =>
            core::node_with(key, props::K_BUTTON, b.into_raw(), props::release_button_props),
        option::Option[box::Box[props::ButtonProps]]::None =>
            core::node_with(key, props::K_BUTTON, 0 as *u8, props::release_button_props),
    };
}

// ---- live --------------------------------------------------------------
// The typed handle `find` hands back. Copy, non-owning. `_seen` is flex's
// removal_count() at lookup: while it matches, no node anywhere has been
// removed, so `_p` is provably live without dereferencing it.

struct Button {
    opaque _p: *core::Node,
    _seen: u64,
}

impl Button {
    fn of(p: *core::Node) -> Button { return Button { _p: p, _seen: flex::removal_count() }; }
    fn is_live(this) -> bool { return this._seen == flex::removal_count(); }

    fn _props(this) -> *props::ButtonProps {
        let d: *core::Data = { (*this._p).data() };
        if d == (0 as *core::Data) { return 0 as *props::ButtonProps; }
        if { (*d).kind } != props::K_BUTTON { return 0 as *props::ButtonProps; }
        return { (*d).props } as *props::ButtonProps;
    }

    // Button.BorderColor
    fn set_border_color(this, v: vocab::Color) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).border_color = v };
        core::touch(this._p, P_BORDER_COLOR);
        return this;
    }

    // Button.BorderWidth
    fn set_border_width(this, v: f64) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).border_width = v };
        core::touch(this._p, P_BORDER_WIDTH);
        return this;
    }

    // Button.CharacterSpacing
    fn set_character_spacing(this, v: f64) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).character_spacing = v };
        core::touch(this._p, P_CHARACTER_SPACING);
        return this;
    }

    // Button.ContentLayout
    fn set_content_layout(this, v: vocab::ContentLayout) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).content_layout = v };
        core::touch(this._p, P_CONTENT_LAYOUT);
        return this;
    }

    // Button.CornerRadius
    fn set_corner_radius(this, v: vocab::Corners) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).corner_radius = v };
        core::touch(this._p, P_CORNER_RADIUS);
        return this;
    }

    // Button.FontAttributes
    fn set_font_attributes(this, v: vocab::FontStyle) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).font_attributes = v };
        core::touch(this._p, P_FONT_ATTRIBUTES);
        return this;
    }

    // Button.FontAutoScalingEnabled
    fn set_font_auto_scaling_enabled(this, v: bool) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).font_auto_scaling_enabled = v };
        core::touch(this._p, P_FONT_AUTO_SCALING_ENABLED);
        return this;
    }

    // Button.FontFamily
    fn set_font_family(this, v: str) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).font_family = text::from_str(v) };
        core::touch(this._p, P_FONT_FAMILY);
        return this;
    }

    // Button.FontSize
    fn set_font_size(this, v: f64) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).font_size = v };
        core::touch(this._p, P_FONT_SIZE);
        return this;
    }

    // Button.ImageSource
    fn set_image_source(this, v: str) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).image_source = text::from_str(v) };
        core::touch(this._p, P_IMAGE_SOURCE);
        return this;
    }

    // Button.LineBreakMode
    fn set_line_break_mode(this, v: vocab::LineBreak) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).line_break_mode = v };
        core::touch(this._p, P_LINE_BREAK_MODE);
        return this;
    }

    // Button.Text
    fn set_title(this, v: str) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).title = text::from_str(v) };
        core::touch(this._p, P_TITLE);
        return this;
    }

    // Button.TextColor
    fn set_text_color(this, v: vocab::Color) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).text_color = v };
        core::touch(this._p, P_TEXT_COLOR);
        return this;
    }

    // Button.TextTransform
    fn set_text_transform(this, v: vocab::TextTransform) -> Button {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return this; }
        { (*p).text_transform = v };
        core::touch(this._p, P_TEXT_TRANSFORM);
        return this;
    }

    fn border_color(this) -> vocab::Color {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return vocab::Color::clear(); }
        return { (*p).border_color };
    }

    fn border_width(this) -> f64 {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return 0.0f64; }
        return { (*p).border_width };
    }

    fn character_spacing(this) -> f64 {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return 0.0f64; }
        return { (*p).character_spacing };
    }

    fn content_layout(this) -> vocab::ContentLayout {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return vocab::ContentLayout::ImageLeft; }
        return { (*p).content_layout };
    }

    fn corner_radius(this) -> vocab::Corners {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return vocab::Corners::none(); }
        return { (*p).corner_radius };
    }

    fn font_attributes(this) -> vocab::FontStyle {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return vocab::FontStyle::Bold; }
        return { (*p).font_attributes };
    }

    fn font_auto_scaling_enabled(this) -> bool {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return false; }
        return { (*p).font_auto_scaling_enabled };
    }

    fn font_family(this) -> str {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return ""; }
        return { (*p).font_family.view() };
    }

    fn font_size(this) -> f64 {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return 0.0f64; }
        return { (*p).font_size };
    }

    fn image_source(this) -> str {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return ""; }
        return { (*p).image_source.view() };
    }

    fn line_break_mode(this) -> vocab::LineBreak {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return vocab::LineBreak::CharacterWrap; }
        return { (*p).line_break_mode };
    }

    fn title(this) -> str {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return ""; }
        return { (*p).title.view() };
    }

    fn text_color(this) -> vocab::Color {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return vocab::Color::clear(); }
        return { (*p).text_color };
    }

    fn text_transform(this) -> vocab::TextTransform {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return vocab::TextTransform::Default; }
        return { (*p).text_transform };
    }

    fn is_pressed(this) -> bool {
        let p: *props::ButtonProps = this._props();
        if p == (0 as *props::ButtonProps) { return false; }
        return { (*p).is_pressed };
    }

    // --- the shared band: one-line forwards, no generic element type ---
    fn set_opacity(this, v: f64) -> Button { core::set_opacity(this._p, v); return this; }
    fn opacity(this) -> f64 { return core::opacity(this._p); }
    fn set_is_enabled(this, v: bool) -> Button { core::set_is_enabled(this._p, v); return this; }
    fn is_enabled(this) -> bool { return core::is_enabled(this._p); }
    fn set_is_visible(this, v: bool) -> Button { core::set_is_visible(this._p, v); return this; }
    fn is_visible(this) -> bool { return core::is_visible(this._p); }
    fn set_background_color(this, v: vocab::Color) -> Button { core::set_background_color(this._p, v); return this; }
    fn background_color(this) -> vocab::Color { return core::background_color(this._p); }
    fn set_rotation(this, v: f64) -> Button { core::set_rotation(this._p, v); return this; }
    fn set_scale(this, v: f64) -> Button { core::set_scale(this._p, v); return this; }
    fn width(this) -> f64 { return core::width_of(this._p); }
    fn height(this) -> f64 { return core::height_of(this._p); }
    fn x(this) -> f64 { return core::x_of(this._p); }
    fn y(this) -> f64 { return core::y_of(this._p); }
    fn child_count(this) -> usize { return core::child_count_of(this._p); }
    fn frame(this) -> core::Frame { return core::frame_of(this._p); }
}

// ---- lookup: resolve the key, then check the kind -----------------------

fn find(root: *core::Node, key: str) -> option::Option[Button] {
    return match core::find_in(root, key) {
        option::Option[*core::Node]::Some(p) => {
            let d: *core::Data = { (*p).data() };
            if d == (0 as *core::Data) { return option::Option[Button]::None; }
            if { (*d).kind } != props::K_BUTTON { return option::Option[Button]::None; }
            option::Option[Button]::Some(Button::of(p))
        }
        option::Option[*core::Node]::None => option::Option[Button]::None,
    };
}
