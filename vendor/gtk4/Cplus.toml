[package]
name    = "gtk4"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/share/gir-1.0/Gtk-4.0.gir (namespace Gtk 4.0)
# Reproduce: cpc-bindgen --gobject Gtk --out vendor/gtk4 --use GLib=glib --use GObject=gobject_gir --use GModule=gmodule --use Gio=gio --use cairo=cairo --use Graphene=graphene --use HarfBuzz=harfbuzz --use Pango=pango --use PangoCairo=pangocairo --use GdkPixbuf=gdkpixbuf --use Gdk=gdk --use Gsk=gsk
# Coverage:  5150 items, 361 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"
cairo       = "*"
gdk         = "*"
gdkpixbuf   = "*"
gio         = "*"
glib        = "*"
gobject_gir = "*"
graphene    = "*"
gsk         = "*"
harfbuzz    = "*"
pango       = "*"

[link]
libs = ["gtk-4"]
