[package]
name    = "facet_appkit"
version = "0.0.1"
edition = "2026"

# facet_appkit — the AppKit construction renderer for facet. Walks a facet::Node
# tree (identity -> native), creating NSView/NSStackView/NSButton/NSTextField,
# and `run()`s the app. FACET.1 discovery spike.

[dependencies]
stdlib = "*"
appkit = "*"
facet  = "*"
