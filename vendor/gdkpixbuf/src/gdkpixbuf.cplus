// Auto-generated by cpc-bindgen --gobject. DO NOT EDIT.
// GIR: /usr/share/gir-1.0/GdkPixbuf-2.0.gir
// Namespace: GdkPixbuf
//
// GObject bindings: methods are direct C symbols; lifetime, signals, and
// gchar*<->Text conversion come from `vendor/gobject`.

import "gobject/runtime" as g;
import "gobject/signal" as sig;
import "gobject/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;

import "gio/gio" as gio;
import "glib/glib" as glib;
import "gobject_gir/gobject_gir" as gobject_gir;

// === Enumerations & flags ===

// enum `Colorspace` — constants as i32.
fn colorspace_rgb() -> i32 { return 0 as i32; }

// enum `InterpType` — constants as i32.
fn interp_type_nearest() -> i32 { return 0 as i32; }
fn interp_type_tiles() -> i32 { return 1 as i32; }
fn interp_type_bilinear() -> i32 { return 2 as i32; }
fn interp_type_hyper() -> i32 { return 3 as i32; }

// enum `PixbufAlphaMode` — constants as i32.
fn pixbuf_alpha_mode_bilevel() -> i32 { return 0 as i32; }
fn pixbuf_alpha_mode_full() -> i32 { return 1 as i32; }

// enum `PixbufError` — constants as i32.
fn pixbuf_error_corrupt_image() -> i32 { return 0 as i32; }
fn pixbuf_error_insufficient_memory() -> i32 { return 1 as i32; }
fn pixbuf_error_bad_option() -> i32 { return 2 as i32; }
fn pixbuf_error_unknown_type() -> i32 { return 3 as i32; }
fn pixbuf_error_unsupported_operation() -> i32 { return 4 as i32; }
fn pixbuf_error_failed() -> i32 { return 5 as i32; }
fn pixbuf_error_incomplete_animation() -> i32 { return 6 as i32; }

// enum `PixbufRotation` — constants as i32.
fn pixbuf_rotation_none() -> i32 { return 0 as i32; }
fn pixbuf_rotation_counterclockwise() -> i32 { return 90 as i32; }
fn pixbuf_rotation_upsidedown() -> i32 { return 180 as i32; }
fn pixbuf_rotation_clockwise() -> i32 { return 270 as i32; }

// flags `PixbufFormatFlags` — constants as u32.
fn pixbuf_format_flags_writable() -> u32 { return 1 as u32; }
fn pixbuf_format_flags_scalable() -> u32 { return 2 as u32; }
fn pixbuf_format_flags_threadsafe() -> u32 { return 4 as u32; }


// === Free functions ===

#[link_name = "gdk_pixbuf_error_quark"]
extern fn __c_gdk_pixbuf_error_quark() -> u32;
fn pixbuf_error_quark() -> u32 {
    let __r: u32 = { __c_gdk_pixbuf_error_quark() };
    return __r;
}


// === Interfaces ===


// === Boxed records ===

#[link_name = "gdk_pixbuf_format_copy"]
extern fn __c_gdk_pixbuf_format_copy(__recv: *u8) -> *u8;
#[link_name = "gdk_pixbuf_format_free"]
extern fn __c_gdk_pixbuf_format_free(__recv: *u8);
#[link_name = "gdk_pixbuf_format_get_description"]
extern fn __c_gdk_pixbuf_format_get_description(__recv: *u8) -> *u8;
// SKIPPED method `PixbufFormat::get_extensions`: return type — no <type> (array, callback, or varargs)
#[link_name = "gdk_pixbuf_format_get_license"]
extern fn __c_gdk_pixbuf_format_get_license(__recv: *u8) -> *u8;
// SKIPPED method `PixbufFormat::get_mime_types`: return type — no <type> (array, callback, or varargs)
#[link_name = "gdk_pixbuf_format_get_name"]
extern fn __c_gdk_pixbuf_format_get_name(__recv: *u8) -> *u8;
#[link_name = "gdk_pixbuf_format_is_disabled"]
extern fn __c_gdk_pixbuf_format_is_disabled(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_format_is_save_option_supported"]
extern fn __c_gdk_pixbuf_format_is_save_option_supported(__recv: *u8, option_key: *u8) -> i32;
#[link_name = "gdk_pixbuf_format_is_scalable"]
extern fn __c_gdk_pixbuf_format_is_scalable(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_format_is_writable"]
extern fn __c_gdk_pixbuf_format_is_writable(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_format_set_disabled"]
extern fn __c_gdk_pixbuf_format_set_disabled(__recv: *u8, disabled: i32);
// `PixbufFormat` — boxed record (non-owning handle).
struct PixbufFormat {
    opaque _raw: *u8,
}

impl PixbufFormat {
    fn raw(this) -> *u8 { return this._raw; }
    fn from_raw(ptr: *u8) -> PixbufFormat { return PixbufFormat { _raw: ptr }; }

    fn copy(this) -> PixbufFormat {
        let __r: *u8 = { __c_gdk_pixbuf_format_copy(this._raw) };
        return PixbufFormat::from_raw(__r);
    }

    fn free(this) {
        { __c_gdk_pixbuf_format_free(this._raw) };
        return;
    }

    fn get_description(this) -> text::Text {
        let __r: *u8 = { __c_gdk_pixbuf_format_get_description(this._raw) };
        return bridge::cstr_to_text_full(__r);
    }

    fn get_license(this) -> text::Text {
        let __r: *u8 = { __c_gdk_pixbuf_format_get_license(this._raw) };
        return bridge::cstr_to_text_full(__r);
    }

    fn get_name(this) -> text::Text {
        let __r: *u8 = { __c_gdk_pixbuf_format_get_name(this._raw) };
        return bridge::cstr_to_text_full(__r);
    }

    fn is_disabled(this) -> bool {
        let __r: i32 = { __c_gdk_pixbuf_format_is_disabled(this._raw) };
        return __r != (0 as i32);
    }

    fn is_save_option_supported(this, option_key: str) -> bool {
        let __cs_option_key: *u8 = bridge::str_to_cstring(option_key);
        let __r: i32 = { __c_gdk_pixbuf_format_is_save_option_supported(this._raw, __cs_option_key) };
        bridge::free_cstring(__cs_option_key);
        return __r != (0 as i32);
    }

    fn is_scalable(this) -> bool {
        let __r: i32 = { __c_gdk_pixbuf_format_is_scalable(this._raw) };
        return __r != (0 as i32);
    }

    fn is_writable(this) -> bool {
        let __r: i32 = { __c_gdk_pixbuf_format_is_writable(this._raw) };
        return __r != (0 as i32);
    }

    fn set_disabled(this, disabled: bool) {
        { __c_gdk_pixbuf_format_set_disabled(this._raw, (if disabled { 1 as i32 } else { 0 as i32 })) };
        return;
    }

}


// === Classes ===

#[link_name = "gdk_pixbuf_new"]
extern fn __c_gdk_pixbuf_new(colorspace: i32, has_alpha: i32, bits_per_sample: i32, width: i32, height: i32) -> *u8;
#[link_name = "gdk_pixbuf_new_from_bytes"]
extern fn __c_gdk_pixbuf_new_from_bytes(data: *u8, colorspace: i32, has_alpha: i32, bits_per_sample: i32, width: i32, height: i32, rowstride: i32) -> *u8;
// SKIPPED ctor `Pixbuf::new_from_data`: param `data` — no <type> (array, callback, or varargs)
#[link_name = "gdk_pixbuf_new_from_file"]
extern fn __c_gdk_pixbuf_new_from_file(filename: *u8) -> *u8;
#[link_name = "gdk_pixbuf_new_from_file_at_scale"]
extern fn __c_gdk_pixbuf_new_from_file_at_scale(filename: *u8, width: i32, height: i32, preserve_aspect_ratio: i32) -> *u8;
#[link_name = "gdk_pixbuf_new_from_file_at_size"]
extern fn __c_gdk_pixbuf_new_from_file_at_size(filename: *u8, width: i32, height: i32) -> *u8;
// SKIPPED ctor `Pixbuf::new_from_inline`: param `data` — no <type> (array, callback, or varargs)
#[link_name = "gdk_pixbuf_new_from_resource"]
extern fn __c_gdk_pixbuf_new_from_resource(resource_path: *u8) -> *u8;
#[link_name = "gdk_pixbuf_new_from_resource_at_scale"]
extern fn __c_gdk_pixbuf_new_from_resource_at_scale(resource_path: *u8, width: i32, height: i32, preserve_aspect_ratio: i32) -> *u8;
#[link_name = "gdk_pixbuf_new_from_stream"]
extern fn __c_gdk_pixbuf_new_from_stream(stream: *u8, cancellable: *u8) -> *u8;
#[link_name = "gdk_pixbuf_new_from_stream_at_scale"]
extern fn __c_gdk_pixbuf_new_from_stream_at_scale(stream: *u8, width: i32, height: i32, preserve_aspect_ratio: i32, cancellable: *u8) -> *u8;
#[link_name = "gdk_pixbuf_new_from_stream_finish"]
extern fn __c_gdk_pixbuf_new_from_stream_finish(async_result: *u8) -> *u8;
// SKIPPED ctor `Pixbuf::new_from_xpm_data`: param `data` — no <type> (array, callback, or varargs)
#[link_name = "gdk_pixbuf_add_alpha"]
extern fn __c_gdk_pixbuf_add_alpha(__recv: *u8, substitute_color: i32, r: u8, g: u8, b: u8) -> *u8;
#[link_name = "gdk_pixbuf_apply_embedded_orientation"]
extern fn __c_gdk_pixbuf_apply_embedded_orientation(__recv: *u8) -> *u8;
#[link_name = "gdk_pixbuf_composite"]
extern fn __c_gdk_pixbuf_composite(__recv: *u8, dest: *u8, dest_x: i32, dest_y: i32, dest_width: i32, dest_height: i32, offset_x: f64, offset_y: f64, scale_x: f64, scale_y: f64, interp_type: i32, overall_alpha: i32);
#[link_name = "gdk_pixbuf_composite_color"]
extern fn __c_gdk_pixbuf_composite_color(__recv: *u8, dest: *u8, dest_x: i32, dest_y: i32, dest_width: i32, dest_height: i32, offset_x: f64, offset_y: f64, scale_x: f64, scale_y: f64, interp_type: i32, overall_alpha: i32, check_x: i32, check_y: i32, check_size: i32, color1: u32, color2: u32);
#[link_name = "gdk_pixbuf_composite_color_simple"]
extern fn __c_gdk_pixbuf_composite_color_simple(__recv: *u8, dest_width: i32, dest_height: i32, interp_type: i32, overall_alpha: i32, check_size: i32, color1: u32, color2: u32) -> *u8;
#[link_name = "gdk_pixbuf_copy"]
extern fn __c_gdk_pixbuf_copy(__recv: *u8) -> *u8;
#[link_name = "gdk_pixbuf_copy_area"]
extern fn __c_gdk_pixbuf_copy_area(__recv: *u8, src_x: i32, src_y: i32, width: i32, height: i32, dest_pixbuf: *u8, dest_x: i32, dest_y: i32);
#[link_name = "gdk_pixbuf_copy_options"]
extern fn __c_gdk_pixbuf_copy_options(__recv: *u8, dest_pixbuf: *u8) -> i32;
#[link_name = "gdk_pixbuf_fill"]
extern fn __c_gdk_pixbuf_fill(__recv: *u8, pixel: u32);
#[link_name = "gdk_pixbuf_flip"]
extern fn __c_gdk_pixbuf_flip(__recv: *u8, horizontal: i32) -> *u8;
#[link_name = "gdk_pixbuf_get_bits_per_sample"]
extern fn __c_gdk_pixbuf_get_bits_per_sample(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_get_byte_length"]
extern fn __c_gdk_pixbuf_get_byte_length(__recv: *u8) -> usize;
#[link_name = "gdk_pixbuf_get_colorspace"]
extern fn __c_gdk_pixbuf_get_colorspace(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_get_has_alpha"]
extern fn __c_gdk_pixbuf_get_has_alpha(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_get_height"]
extern fn __c_gdk_pixbuf_get_height(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_get_n_channels"]
extern fn __c_gdk_pixbuf_get_n_channels(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_get_option"]
extern fn __c_gdk_pixbuf_get_option(__recv: *u8, key: *u8) -> *u8;
#[link_name = "gdk_pixbuf_get_options"]
extern fn __c_gdk_pixbuf_get_options(__recv: *u8) -> *u8;
// SKIPPED method `Pixbuf::get_pixels`: return type — no <type> (array, callback, or varargs)
// SKIPPED method `Pixbuf::get_pixels_with_length`: return type — no <type> (array, callback, or varargs)
#[link_name = "gdk_pixbuf_get_rowstride"]
extern fn __c_gdk_pixbuf_get_rowstride(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_get_width"]
extern fn __c_gdk_pixbuf_get_width(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_new_subpixbuf"]
extern fn __c_gdk_pixbuf_new_subpixbuf(__recv: *u8, src_x: i32, src_y: i32, width: i32, height: i32) -> *u8;
#[link_name = "gdk_pixbuf_read_pixel_bytes"]
extern fn __c_gdk_pixbuf_read_pixel_bytes(__recv: *u8) -> *u8;
#[link_name = "gdk_pixbuf_read_pixels"]
extern fn __c_gdk_pixbuf_read_pixels(__recv: *u8) -> u8;
#[link_name = "gdk_pixbuf_ref"]
extern fn __c_gdk_pixbuf_ref(__recv: *u8) -> *u8;
#[link_name = "gdk_pixbuf_remove_option"]
extern fn __c_gdk_pixbuf_remove_option(__recv: *u8, key: *u8) -> i32;
#[link_name = "gdk_pixbuf_rotate_simple"]
extern fn __c_gdk_pixbuf_rotate_simple(__recv: *u8, angle: i32) -> *u8;
#[link_name = "gdk_pixbuf_saturate_and_pixelate"]
extern fn __c_gdk_pixbuf_saturate_and_pixelate(__recv: *u8, dest: *u8, saturation: f32, pixelate: i32);
// SKIPPED method `Pixbuf::save`: variadic
// SKIPPED method `Pixbuf::save_to_buffer`: param `buffer` — no <type> (array, callback, or varargs)
// SKIPPED method `Pixbuf::save_to_bufferv`: param `buffer` — no <type> (array, callback, or varargs)
// SKIPPED method `Pixbuf::save_to_callback`: param `save_func` — unmapped type `PixbufSaveFunc`
// SKIPPED method `Pixbuf::save_to_callbackv`: param `save_func` — unmapped type `PixbufSaveFunc`
// SKIPPED method `Pixbuf::save_to_stream`: variadic
// SKIPPED method `Pixbuf::save_to_stream_async`: param `callback` — unmapped type `Gio.AsyncReadyCallback`
// SKIPPED method `Pixbuf::save_to_streamv`: param `option_keys` — no <type> (array, callback, or varargs)
// SKIPPED method `Pixbuf::save_to_streamv_async`: param `option_keys` — no <type> (array, callback, or varargs)
// SKIPPED method `Pixbuf::savev`: param `option_keys` — no <type> (array, callback, or varargs)
#[link_name = "gdk_pixbuf_scale"]
extern fn __c_gdk_pixbuf_scale(__recv: *u8, dest: *u8, dest_x: i32, dest_y: i32, dest_width: i32, dest_height: i32, offset_x: f64, offset_y: f64, scale_x: f64, scale_y: f64, interp_type: i32);
#[link_name = "gdk_pixbuf_scale_simple"]
extern fn __c_gdk_pixbuf_scale_simple(__recv: *u8, dest_width: i32, dest_height: i32, interp_type: i32) -> *u8;
#[link_name = "gdk_pixbuf_set_option"]
extern fn __c_gdk_pixbuf_set_option(__recv: *u8, key: *u8, value: *u8) -> i32;
#[link_name = "gdk_pixbuf_unref"]
extern fn __c_gdk_pixbuf_unref(__recv: *u8);
// `Pixbuf` — GObject wrapper (non-owning handle); parent `GObject.Object`.
struct Pixbuf {
    opaque _raw: *u8,
}

impl Pixbuf {
    fn raw(this) -> *u8 { return this._raw; }
    fn from_raw(ptr: *u8) -> Pixbuf { return Pixbuf { _raw: ptr }; }

    fn new(colorspace: i32, has_alpha: bool, bits_per_sample: i32, width: i32, height: i32) -> option::Option[Pixbuf] {
        let __r: *u8 = { __c_gdk_pixbuf_new(colorspace, (if has_alpha { 1 as i32 } else { 0 as i32 }), bits_per_sample, width, height) };
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn new_from_bytes(data: glib::Bytes, colorspace: i32, has_alpha: bool, bits_per_sample: i32, width: i32, height: i32, rowstride: i32) -> Pixbuf {
        let __r: *u8 = { __c_gdk_pixbuf_new_from_bytes(data.raw(), colorspace, (if has_alpha { 1 as i32 } else { 0 as i32 }), bits_per_sample, width, height, rowstride) };
        return Pixbuf::from_raw(__r);
    }

    fn new_from_file(filename: str) -> option::Option[Pixbuf] {
        let __cs_filename: *u8 = bridge::str_to_cstring(filename);
        let __r: *u8 = { __c_gdk_pixbuf_new_from_file(__cs_filename) };
        bridge::free_cstring(__cs_filename);
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn new_from_file_at_scale(filename: str, width: i32, height: i32, preserve_aspect_ratio: bool) -> option::Option[Pixbuf] {
        let __cs_filename: *u8 = bridge::str_to_cstring(filename);
        let __r: *u8 = { __c_gdk_pixbuf_new_from_file_at_scale(__cs_filename, width, height, (if preserve_aspect_ratio { 1 as i32 } else { 0 as i32 })) };
        bridge::free_cstring(__cs_filename);
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn new_from_file_at_size(filename: str, width: i32, height: i32) -> option::Option[Pixbuf] {
        let __cs_filename: *u8 = bridge::str_to_cstring(filename);
        let __r: *u8 = { __c_gdk_pixbuf_new_from_file_at_size(__cs_filename, width, height) };
        bridge::free_cstring(__cs_filename);
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn new_from_resource(resource_path: str) -> option::Option[Pixbuf] {
        let __cs_resource_path: *u8 = bridge::str_to_cstring(resource_path);
        let __r: *u8 = { __c_gdk_pixbuf_new_from_resource(__cs_resource_path) };
        bridge::free_cstring(__cs_resource_path);
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn new_from_resource_at_scale(resource_path: str, width: i32, height: i32, preserve_aspect_ratio: bool) -> option::Option[Pixbuf] {
        let __cs_resource_path: *u8 = bridge::str_to_cstring(resource_path);
        let __r: *u8 = { __c_gdk_pixbuf_new_from_resource_at_scale(__cs_resource_path, width, height, (if preserve_aspect_ratio { 1 as i32 } else { 0 as i32 })) };
        bridge::free_cstring(__cs_resource_path);
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn new_from_stream(stream: gio::InputStream, cancellable: gio::Cancellable) -> option::Option[Pixbuf] {
        let __r: *u8 = { __c_gdk_pixbuf_new_from_stream(stream.raw(), cancellable.raw()) };
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn new_from_stream_at_scale(stream: gio::InputStream, width: i32, height: i32, preserve_aspect_ratio: bool, cancellable: gio::Cancellable) -> option::Option[Pixbuf] {
        let __r: *u8 = { __c_gdk_pixbuf_new_from_stream_at_scale(stream.raw(), width, height, (if preserve_aspect_ratio { 1 as i32 } else { 0 as i32 }), cancellable.raw()) };
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn new_from_stream_finish(async_result: gio::AsyncResult) -> option::Option[Pixbuf] {
        let __r: *u8 = { __c_gdk_pixbuf_new_from_stream_finish(async_result.raw()) };
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn add_alpha(this, substitute_color: bool, r: u8, g: u8, b: u8) -> Pixbuf {
        let __r: *u8 = { __c_gdk_pixbuf_add_alpha(this._raw, (if substitute_color { 1 as i32 } else { 0 as i32 }), r, g, b) };
        return Pixbuf::from_raw(__r);
    }

    fn apply_embedded_orientation(this) -> option::Option[Pixbuf] {
        let __r: *u8 = { __c_gdk_pixbuf_apply_embedded_orientation(this._raw) };
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn composite(this, dest: Pixbuf, dest_x: i32, dest_y: i32, dest_width: i32, dest_height: i32, offset_x: f64, offset_y: f64, scale_x: f64, scale_y: f64, interp_type: i32, overall_alpha: i32) {
        { __c_gdk_pixbuf_composite(this._raw, dest.raw(), dest_x, dest_y, dest_width, dest_height, offset_x, offset_y, scale_x, scale_y, interp_type, overall_alpha) };
        return;
    }

    fn composite_color(this, dest: Pixbuf, dest_x: i32, dest_y: i32, dest_width: i32, dest_height: i32, offset_x: f64, offset_y: f64, scale_x: f64, scale_y: f64, interp_type: i32, overall_alpha: i32, check_x: i32, check_y: i32, check_size: i32, color1: u32, color2: u32) {
        { __c_gdk_pixbuf_composite_color(this._raw, dest.raw(), dest_x, dest_y, dest_width, dest_height, offset_x, offset_y, scale_x, scale_y, interp_type, overall_alpha, check_x, check_y, check_size, color1, color2) };
        return;
    }

    fn composite_color_simple(this, dest_width: i32, dest_height: i32, interp_type: i32, overall_alpha: i32, check_size: i32, color1: u32, color2: u32) -> option::Option[Pixbuf] {
        let __r: *u8 = { __c_gdk_pixbuf_composite_color_simple(this._raw, dest_width, dest_height, interp_type, overall_alpha, check_size, color1, color2) };
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn copy(this) -> option::Option[Pixbuf] {
        let __r: *u8 = { __c_gdk_pixbuf_copy(this._raw) };
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn copy_area(this, src_x: i32, src_y: i32, width: i32, height: i32, dest_pixbuf: Pixbuf, dest_x: i32, dest_y: i32) {
        { __c_gdk_pixbuf_copy_area(this._raw, src_x, src_y, width, height, dest_pixbuf.raw(), dest_x, dest_y) };
        return;
    }

    fn copy_options(this, dest_pixbuf: Pixbuf) -> bool {
        let __r: i32 = { __c_gdk_pixbuf_copy_options(this._raw, dest_pixbuf.raw()) };
        return __r != (0 as i32);
    }

    fn fill(this, pixel: u32) {
        { __c_gdk_pixbuf_fill(this._raw, pixel) };
        return;
    }

    fn flip(this, horizontal: bool) -> option::Option[Pixbuf] {
        let __r: *u8 = { __c_gdk_pixbuf_flip(this._raw, (if horizontal { 1 as i32 } else { 0 as i32 })) };
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn get_bits_per_sample(this) -> i32 {
        let __r: i32 = { __c_gdk_pixbuf_get_bits_per_sample(this._raw) };
        return __r;
    }

    fn get_byte_length(this) -> usize {
        let __r: usize = { __c_gdk_pixbuf_get_byte_length(this._raw) };
        return __r;
    }

    fn get_colorspace(this) -> i32 {
        let __r: i32 = { __c_gdk_pixbuf_get_colorspace(this._raw) };
        return __r;
    }

    fn get_has_alpha(this) -> bool {
        let __r: i32 = { __c_gdk_pixbuf_get_has_alpha(this._raw) };
        return __r != (0 as i32);
    }

    fn get_height(this) -> i32 {
        let __r: i32 = { __c_gdk_pixbuf_get_height(this._raw) };
        return __r;
    }

    fn get_n_channels(this) -> i32 {
        let __r: i32 = { __c_gdk_pixbuf_get_n_channels(this._raw) };
        return __r;
    }

    fn get_option(this, key: str) -> option::Option[text::Text] {
        let __cs_key: *u8 = bridge::str_to_cstring(key);
        let __r: *u8 = { __c_gdk_pixbuf_get_option(this._raw, __cs_key) };
        bridge::free_cstring(__cs_key);
        if __r == { 0 as *u8 } { return option::Option[text::Text]::None; }
        return option::some(bridge::cstr_to_text(__r));
    }

    fn get_options(this) -> glib::HashTable {
        let __r: *u8 = { __c_gdk_pixbuf_get_options(this._raw) };
        return glib::HashTable::from_raw(__r);
    }

    fn get_rowstride(this) -> i32 {
        let __r: i32 = { __c_gdk_pixbuf_get_rowstride(this._raw) };
        return __r;
    }

    fn get_width(this) -> i32 {
        let __r: i32 = { __c_gdk_pixbuf_get_width(this._raw) };
        return __r;
    }

    fn new_subpixbuf(this, src_x: i32, src_y: i32, width: i32, height: i32) -> Pixbuf {
        let __r: *u8 = { __c_gdk_pixbuf_new_subpixbuf(this._raw, src_x, src_y, width, height) };
        return Pixbuf::from_raw(__r);
    }

    fn read_pixel_bytes(this) -> glib::Bytes {
        let __r: *u8 = { __c_gdk_pixbuf_read_pixel_bytes(this._raw) };
        return glib::Bytes::from_raw(__r);
    }

    fn read_pixels(this) -> u8 {
        let __r: u8 = { __c_gdk_pixbuf_read_pixels(this._raw) };
        return __r;
    }

    fn ref(this) -> Pixbuf {
        let __r: *u8 = { __c_gdk_pixbuf_ref(this._raw) };
        return Pixbuf::from_raw(__r);
    }

    fn remove_option(this, key: str) -> bool {
        let __cs_key: *u8 = bridge::str_to_cstring(key);
        let __r: i32 = { __c_gdk_pixbuf_remove_option(this._raw, __cs_key) };
        bridge::free_cstring(__cs_key);
        return __r != (0 as i32);
    }

    fn rotate_simple(this, angle: i32) -> option::Option[Pixbuf] {
        let __r: *u8 = { __c_gdk_pixbuf_rotate_simple(this._raw, angle) };
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn saturate_and_pixelate(this, dest: Pixbuf, saturation: f32, pixelate: bool) {
        { __c_gdk_pixbuf_saturate_and_pixelate(this._raw, dest.raw(), saturation, (if pixelate { 1 as i32 } else { 0 as i32 })) };
        return;
    }

    fn scale(this, dest: Pixbuf, dest_x: i32, dest_y: i32, dest_width: i32, dest_height: i32, offset_x: f64, offset_y: f64, scale_x: f64, scale_y: f64, interp_type: i32) {
        { __c_gdk_pixbuf_scale(this._raw, dest.raw(), dest_x, dest_y, dest_width, dest_height, offset_x, offset_y, scale_x, scale_y, interp_type) };
        return;
    }

    fn scale_simple(this, dest_width: i32, dest_height: i32, interp_type: i32) -> option::Option[Pixbuf] {
        let __r: *u8 = { __c_gdk_pixbuf_scale_simple(this._raw, dest_width, dest_height, interp_type) };
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn set_option(this, key: str, value: str) -> bool {
        let __cs_key: *u8 = bridge::str_to_cstring(key);
        let __cs_value: *u8 = bridge::str_to_cstring(value);
        let __r: i32 = { __c_gdk_pixbuf_set_option(this._raw, __cs_key, __cs_value) };
        bridge::free_cstring(__cs_key);
        bridge::free_cstring(__cs_value);
        return __r != (0 as i32);
    }

    fn unref(this) {
        { __c_gdk_pixbuf_unref(this._raw) };
        return;
    }

    // upcast to parent `GObject.Object` (safe; the handle is-a GObject.Object).
    fn upcast(this) -> gobject_gir::Object { return gobject_gir::Object::from_raw(this._raw); }

    fn as_icon(this) -> gio::Icon { return gio::Icon::from_raw(this._raw); }

    fn as_loadable_icon(this) -> gio::LoadableIcon { return gio::LoadableIcon::from_raw(this._raw); }

}

#[link_name = "gdk_pixbuf_animation_new_from_file"]
extern fn __c_gdk_pixbuf_animation_new_from_file(filename: *u8) -> *u8;
#[link_name = "gdk_pixbuf_animation_new_from_resource"]
extern fn __c_gdk_pixbuf_animation_new_from_resource(resource_path: *u8) -> *u8;
#[link_name = "gdk_pixbuf_animation_new_from_stream"]
extern fn __c_gdk_pixbuf_animation_new_from_stream(stream: *u8, cancellable: *u8) -> *u8;
#[link_name = "gdk_pixbuf_animation_new_from_stream_finish"]
extern fn __c_gdk_pixbuf_animation_new_from_stream_finish(async_result: *u8) -> *u8;
#[link_name = "gdk_pixbuf_animation_get_height"]
extern fn __c_gdk_pixbuf_animation_get_height(__recv: *u8) -> i32;
// SKIPPED method `PixbufAnimation::get_iter`: param `start_time` — unmapped type `GLib.TimeVal`
#[link_name = "gdk_pixbuf_animation_get_static_image"]
extern fn __c_gdk_pixbuf_animation_get_static_image(__recv: *u8) -> *u8;
#[link_name = "gdk_pixbuf_animation_get_width"]
extern fn __c_gdk_pixbuf_animation_get_width(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_animation_is_static_image"]
extern fn __c_gdk_pixbuf_animation_is_static_image(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_animation_ref"]
extern fn __c_gdk_pixbuf_animation_ref(__recv: *u8) -> *u8;
#[link_name = "gdk_pixbuf_animation_unref"]
extern fn __c_gdk_pixbuf_animation_unref(__recv: *u8);
// `PixbufAnimation` — GObject wrapper (non-owning handle); parent `GObject.Object`.
struct PixbufAnimation {
    opaque _raw: *u8,
}

impl PixbufAnimation {
    fn raw(this) -> *u8 { return this._raw; }
    fn from_raw(ptr: *u8) -> PixbufAnimation { return PixbufAnimation { _raw: ptr }; }

    fn new_from_file(filename: str) -> option::Option[PixbufAnimation] {
        let __cs_filename: *u8 = bridge::str_to_cstring(filename);
        let __r: *u8 = { __c_gdk_pixbuf_animation_new_from_file(__cs_filename) };
        bridge::free_cstring(__cs_filename);
        if __r == { 0 as *u8 } { return option::Option[PixbufAnimation]::None; }
        return option::some(PixbufAnimation::from_raw(__r));
    }

    fn new_from_resource(resource_path: str) -> option::Option[PixbufAnimation] {
        let __cs_resource_path: *u8 = bridge::str_to_cstring(resource_path);
        let __r: *u8 = { __c_gdk_pixbuf_animation_new_from_resource(__cs_resource_path) };
        bridge::free_cstring(__cs_resource_path);
        if __r == { 0 as *u8 } { return option::Option[PixbufAnimation]::None; }
        return option::some(PixbufAnimation::from_raw(__r));
    }

    fn new_from_stream(stream: gio::InputStream, cancellable: gio::Cancellable) -> option::Option[PixbufAnimation] {
        let __r: *u8 = { __c_gdk_pixbuf_animation_new_from_stream(stream.raw(), cancellable.raw()) };
        if __r == { 0 as *u8 } { return option::Option[PixbufAnimation]::None; }
        return option::some(PixbufAnimation::from_raw(__r));
    }

    fn new_from_stream_finish(async_result: gio::AsyncResult) -> option::Option[PixbufAnimation] {
        let __r: *u8 = { __c_gdk_pixbuf_animation_new_from_stream_finish(async_result.raw()) };
        if __r == { 0 as *u8 } { return option::Option[PixbufAnimation]::None; }
        return option::some(PixbufAnimation::from_raw(__r));
    }

    fn get_height(this) -> i32 {
        let __r: i32 = { __c_gdk_pixbuf_animation_get_height(this._raw) };
        return __r;
    }

    fn get_static_image(this) -> Pixbuf {
        let __r: *u8 = { __c_gdk_pixbuf_animation_get_static_image(this._raw) };
        return Pixbuf::from_raw(__r);
    }

    fn get_width(this) -> i32 {
        let __r: i32 = { __c_gdk_pixbuf_animation_get_width(this._raw) };
        return __r;
    }

    fn is_static_image(this) -> bool {
        let __r: i32 = { __c_gdk_pixbuf_animation_is_static_image(this._raw) };
        return __r != (0 as i32);
    }

    fn ref(this) -> PixbufAnimation {
        let __r: *u8 = { __c_gdk_pixbuf_animation_ref(this._raw) };
        return PixbufAnimation::from_raw(__r);
    }

    fn unref(this) {
        { __c_gdk_pixbuf_animation_unref(this._raw) };
        return;
    }

    // upcast to parent `GObject.Object` (safe; the handle is-a GObject.Object).
    fn upcast(this) -> gobject_gir::Object { return gobject_gir::Object::from_raw(this._raw); }

}

// SKIPPED method `PixbufAnimationIter::advance`: param `current_time` — unmapped type `GLib.TimeVal`
#[link_name = "gdk_pixbuf_animation_iter_get_delay_time"]
extern fn __c_gdk_pixbuf_animation_iter_get_delay_time(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_animation_iter_get_pixbuf"]
extern fn __c_gdk_pixbuf_animation_iter_get_pixbuf(__recv: *u8) -> *u8;
#[link_name = "gdk_pixbuf_animation_iter_on_currently_loading_frame"]
extern fn __c_gdk_pixbuf_animation_iter_on_currently_loading_frame(__recv: *u8) -> i32;
// `PixbufAnimationIter` — GObject wrapper (non-owning handle); parent `GObject.Object`.
struct PixbufAnimationIter {
    opaque _raw: *u8,
}

impl PixbufAnimationIter {
    fn raw(this) -> *u8 { return this._raw; }
    fn from_raw(ptr: *u8) -> PixbufAnimationIter { return PixbufAnimationIter { _raw: ptr }; }

    fn get_delay_time(this) -> i32 {
        let __r: i32 = { __c_gdk_pixbuf_animation_iter_get_delay_time(this._raw) };
        return __r;
    }

    fn get_pixbuf(this) -> Pixbuf {
        let __r: *u8 = { __c_gdk_pixbuf_animation_iter_get_pixbuf(this._raw) };
        return Pixbuf::from_raw(__r);
    }

    fn on_currently_loading_frame(this) -> bool {
        let __r: i32 = { __c_gdk_pixbuf_animation_iter_on_currently_loading_frame(this._raw) };
        return __r != (0 as i32);
    }

    // upcast to parent `GObject.Object` (safe; the handle is-a GObject.Object).
    fn upcast(this) -> gobject_gir::Object { return gobject_gir::Object::from_raw(this._raw); }

}

#[link_name = "gdk_pixbuf_loader_new"]
extern fn __c_gdk_pixbuf_loader_new() -> *u8;
#[link_name = "gdk_pixbuf_loader_new_with_mime_type"]
extern fn __c_gdk_pixbuf_loader_new_with_mime_type(mime_type: *u8) -> *u8;
#[link_name = "gdk_pixbuf_loader_new_with_type"]
extern fn __c_gdk_pixbuf_loader_new_with_type(image_type: *u8) -> *u8;
#[link_name = "gdk_pixbuf_loader_close"]
extern fn __c_gdk_pixbuf_loader_close(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_loader_get_animation"]
extern fn __c_gdk_pixbuf_loader_get_animation(__recv: *u8) -> *u8;
#[link_name = "gdk_pixbuf_loader_get_format"]
extern fn __c_gdk_pixbuf_loader_get_format(__recv: *u8) -> *u8;
#[link_name = "gdk_pixbuf_loader_get_pixbuf"]
extern fn __c_gdk_pixbuf_loader_get_pixbuf(__recv: *u8) -> *u8;
#[link_name = "gdk_pixbuf_loader_set_size"]
extern fn __c_gdk_pixbuf_loader_set_size(__recv: *u8, width: i32, height: i32);
// SKIPPED method `PixbufLoader::write`: param `buf` — no <type> (array, callback, or varargs)
#[link_name = "gdk_pixbuf_loader_write_bytes"]
extern fn __c_gdk_pixbuf_loader_write_bytes(__recv: *u8, buffer: *u8) -> i32;
#[link_name = "g_signal_connect_data"]
extern fn __sigc_i32i32i32i32_v(instance: *u8, detailed: *u8, handler: fn(*u8, i32, i32, i32, i32, *u8), data: *u8, destroy: *u8, flags: i32) -> u64;
#[link_name = "g_signal_connect_data"]
extern fn __sigc_i32i32_v(instance: *u8, detailed: *u8, handler: fn(*u8, i32, i32, *u8), data: *u8, destroy: *u8, flags: i32) -> u64;
// `PixbufLoader` — GObject wrapper (non-owning handle); parent `GObject.Object`.
struct PixbufLoader {
    opaque _raw: *u8,
}

impl PixbufLoader {
    fn raw(this) -> *u8 { return this._raw; }
    fn from_raw(ptr: *u8) -> PixbufLoader { return PixbufLoader { _raw: ptr }; }

    fn new() -> PixbufLoader {
        let __r: *u8 = { __c_gdk_pixbuf_loader_new() };
        return PixbufLoader::from_raw(__r);
    }

    fn new_with_mime_type(mime_type: str) -> PixbufLoader {
        let __cs_mime_type: *u8 = bridge::str_to_cstring(mime_type);
        let __r: *u8 = { __c_gdk_pixbuf_loader_new_with_mime_type(__cs_mime_type) };
        bridge::free_cstring(__cs_mime_type);
        return PixbufLoader::from_raw(__r);
    }

    fn new_with_type(image_type: str) -> PixbufLoader {
        let __cs_image_type: *u8 = bridge::str_to_cstring(image_type);
        let __r: *u8 = { __c_gdk_pixbuf_loader_new_with_type(__cs_image_type) };
        bridge::free_cstring(__cs_image_type);
        return PixbufLoader::from_raw(__r);
    }

    fn close(this) -> bool {
        let __r: i32 = { __c_gdk_pixbuf_loader_close(this._raw) };
        return __r != (0 as i32);
    }

    fn get_animation(this) -> option::Option[PixbufAnimation] {
        let __r: *u8 = { __c_gdk_pixbuf_loader_get_animation(this._raw) };
        if __r == { 0 as *u8 } { return option::Option[PixbufAnimation]::None; }
        return option::some(PixbufAnimation::from_raw(__r));
    }

    fn get_format(this) -> option::Option[PixbufFormat] {
        let __r: *u8 = { __c_gdk_pixbuf_loader_get_format(this._raw) };
        if __r == { 0 as *u8 } { return option::Option[PixbufFormat]::None; }
        return option::some(PixbufFormat::from_raw(__r));
    }

    fn get_pixbuf(this) -> option::Option[Pixbuf] {
        let __r: *u8 = { __c_gdk_pixbuf_loader_get_pixbuf(this._raw) };
        if __r == { 0 as *u8 } { return option::Option[Pixbuf]::None; }
        return option::some(Pixbuf::from_raw(__r));
    }

    fn set_size(this, width: i32, height: i32) {
        { __c_gdk_pixbuf_loader_set_size(this._raw, width, height) };
        return;
    }

    fn write_bytes(this, buffer: glib::Bytes) -> bool {
        let __r: i32 = { __c_gdk_pixbuf_loader_write_bytes(this._raw, buffer.raw()) };
        return __r != (0 as i32);
    }

    fn connect_area_prepared(this, handler: fn(*u8, *u8), user: *u8) -> u64 {
        return sig::connect(this._raw, #str_ptr("area-prepared\0"), handler, user);
    }

    fn connect_area_updated(this, handler: fn(*u8, i32, i32, i32, i32, *u8), user: *u8) -> u64 {
        return { __sigc_i32i32i32i32_v(this._raw, #str_ptr("area-updated\0"), handler, user, { 0 as *u8 }, 0 as i32) };
    }

    fn connect_closed(this, handler: fn(*u8, *u8), user: *u8) -> u64 {
        return sig::connect(this._raw, #str_ptr("closed\0"), handler, user);
    }

    fn connect_size_prepared(this, handler: fn(*u8, i32, i32, *u8), user: *u8) -> u64 {
        return { __sigc_i32i32_v(this._raw, #str_ptr("size-prepared\0"), handler, user, { 0 as *u8 }, 0 as i32) };
    }

    // upcast to parent `GObject.Object` (safe; the handle is-a GObject.Object).
    fn upcast(this) -> gobject_gir::Object { return gobject_gir::Object::from_raw(this._raw); }

}

#[link_name = "gdk_pixbuf_non_anim_new"]
extern fn __c_gdk_pixbuf_non_anim_new(pixbuf: *u8) -> *u8;
// `PixbufNonAnim` — GObject wrapper (non-owning handle); parent `PixbufAnimation`.
struct PixbufNonAnim {
    opaque _raw: *u8,
}

impl PixbufNonAnim {
    fn raw(this) -> *u8 { return this._raw; }
    fn from_raw(ptr: *u8) -> PixbufNonAnim { return PixbufNonAnim { _raw: ptr }; }

    fn new(pixbuf: Pixbuf) -> PixbufNonAnim {
        let __r: *u8 = { __c_gdk_pixbuf_non_anim_new(pixbuf.raw()) };
        return PixbufNonAnim::from_raw(__r);
    }

    // upcast to parent `PixbufAnimation` (safe; the handle is-a PixbufAnimation).
    fn upcast(this) -> PixbufAnimation { return PixbufAnimation::from_raw(this._raw); }

}

#[link_name = "gdk_pixbuf_simple_anim_new"]
extern fn __c_gdk_pixbuf_simple_anim_new(width: i32, height: i32, rate: f32) -> *u8;
#[link_name = "gdk_pixbuf_simple_anim_add_frame"]
extern fn __c_gdk_pixbuf_simple_anim_add_frame(__recv: *u8, pixbuf: *u8);
#[link_name = "gdk_pixbuf_simple_anim_get_loop"]
extern fn __c_gdk_pixbuf_simple_anim_get_loop(__recv: *u8) -> i32;
#[link_name = "gdk_pixbuf_simple_anim_set_loop"]
extern fn __c_gdk_pixbuf_simple_anim_set_loop(__recv: *u8, loop_: i32);
// `PixbufSimpleAnim` — GObject wrapper (non-owning handle); parent `PixbufAnimation`.
struct PixbufSimpleAnim {
    opaque _raw: *u8,
}

impl PixbufSimpleAnim {
    fn raw(this) -> *u8 { return this._raw; }
    fn from_raw(ptr: *u8) -> PixbufSimpleAnim { return PixbufSimpleAnim { _raw: ptr }; }

    fn new(width: i32, height: i32, rate: f32) -> PixbufSimpleAnim {
        let __r: *u8 = { __c_gdk_pixbuf_simple_anim_new(width, height, rate) };
        return PixbufSimpleAnim::from_raw(__r);
    }

    fn add_frame(this, pixbuf: Pixbuf) {
        { __c_gdk_pixbuf_simple_anim_add_frame(this._raw, pixbuf.raw()) };
        return;
    }

    fn get_loop(this) -> bool {
        let __r: i32 = { __c_gdk_pixbuf_simple_anim_get_loop(this._raw) };
        return __r != (0 as i32);
    }

    fn set_loop(this, loop_: bool) {
        { __c_gdk_pixbuf_simple_anim_set_loop(this._raw, (if loop_ { 1 as i32 } else { 0 as i32 })) };
        return;
    }

    // upcast to parent `PixbufAnimation` (safe; the handle is-a PixbufAnimation).
    fn upcast(this) -> PixbufAnimation { return PixbufAnimation::from_raw(this._raw); }

}

// `PixbufSimpleAnimIter` — GObject wrapper (non-owning handle); parent `PixbufAnimationIter`.
struct PixbufSimpleAnimIter {
    opaque _raw: *u8,
}

impl PixbufSimpleAnimIter {
    fn raw(this) -> *u8 { return this._raw; }
    fn from_raw(ptr: *u8) -> PixbufSimpleAnimIter { return PixbufSimpleAnimIter { _raw: ptr }; }

    // upcast to parent `PixbufAnimationIter` (safe; the handle is-a PixbufAnimationIter).
    fn upcast(this) -> PixbufAnimationIter { return PixbufAnimationIter::from_raw(this._raw); }

}


// cpc-bindgen --gobject: 113 items emitted, 20 SKIPPED.
