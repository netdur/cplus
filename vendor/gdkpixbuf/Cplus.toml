[package]
name    = "gdkpixbuf"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/share/gir-1.0/GdkPixbuf-2.0.gir (namespace GdkPixbuf 2.0)
# Reproduce: cpc-bindgen --gobject GdkPixbuf --out vendor/gdkpixbuf --use GLib=glib --use GObject=gobject_gir --use GModule=gmodule --use Gio=gio --use cairo=cairo --use Graphene=graphene --use HarfBuzz=harfbuzz --use Pango=pango --use PangoCairo=pangocairo --use Gdk=gdk --use Gsk=gsk --use Gtk=gtk4
# Coverage:  113 items, 20 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"
gio         = "*"
glib        = "*"
gmodule     = "*"
gobject_gir = "*"

[link]
libs = ["gdk_pixbuf-2.0"]
