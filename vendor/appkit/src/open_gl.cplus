// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSOpenGL.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;

// `NSOpenGLContextParameter` (NS_ENUM)
enum OpenGLContextParameter {
    SwapInterval,
    SurfaceOrder,
    SurfaceOpacity,
    SurfaceBackingSize,
    ReclaimResources,
    CurrentRendererID,
    GPUVertexProcessing,
    GPUFragmentProcessing,
    HasDrawable,
    MPSwapsInFlight,
    SwapRectangle,
    SwapRectangleEnable,
    RasterizationEnable,
    StateValidation,
    SurfaceSurfaceVolatile,
}

fn open_g_l_context_parameter_raw(v: OpenGLContextParameter) -> i64 {
    return match v {
        OpenGLContextParameter::SwapInterval => { 222 as i64 },
        OpenGLContextParameter::SurfaceOrder => { 235 as i64 },
        OpenGLContextParameter::SurfaceOpacity => { 236 as i64 },
        OpenGLContextParameter::SurfaceBackingSize => { 304 as i64 },
        OpenGLContextParameter::ReclaimResources => { 308 as i64 },
        OpenGLContextParameter::CurrentRendererID => { 309 as i64 },
        OpenGLContextParameter::GPUVertexProcessing => { 310 as i64 },
        OpenGLContextParameter::GPUFragmentProcessing => { 311 as i64 },
        OpenGLContextParameter::HasDrawable => { 314 as i64 },
        OpenGLContextParameter::MPSwapsInFlight => { 315 as i64 },
        OpenGLContextParameter::SwapRectangle => { 200 as i64 },
        OpenGLContextParameter::SwapRectangleEnable => { 201 as i64 },
        OpenGLContextParameter::RasterizationEnable => { 221 as i64 },
        OpenGLContextParameter::StateValidation => { 301 as i64 },
        OpenGLContextParameter::SurfaceSurfaceVolatile => { 306 as i64 },
    };
}

fn open_g_l_context_parameter_from_raw(raw: i64) -> OpenGLContextParameter {
    if raw == (235 as i64) { return OpenGLContextParameter::SurfaceOrder; }
    if raw == (236 as i64) { return OpenGLContextParameter::SurfaceOpacity; }
    if raw == (304 as i64) { return OpenGLContextParameter::SurfaceBackingSize; }
    if raw == (308 as i64) { return OpenGLContextParameter::ReclaimResources; }
    if raw == (309 as i64) { return OpenGLContextParameter::CurrentRendererID; }
    if raw == (310 as i64) { return OpenGLContextParameter::GPUVertexProcessing; }
    if raw == (311 as i64) { return OpenGLContextParameter::GPUFragmentProcessing; }
    if raw == (314 as i64) { return OpenGLContextParameter::HasDrawable; }
    if raw == (315 as i64) { return OpenGLContextParameter::MPSwapsInFlight; }
    if raw == (200 as i64) { return OpenGLContextParameter::SwapRectangle; }
    if raw == (201 as i64) { return OpenGLContextParameter::SwapRectangleEnable; }
    if raw == (221 as i64) { return OpenGLContextParameter::RasterizationEnable; }
    if raw == (301 as i64) { return OpenGLContextParameter::StateValidation; }
    if raw == (306 as i64) { return OpenGLContextParameter::SurfaceSurfaceVolatile; }
    return OpenGLContextParameter::SwapInterval;
}

// `NSData` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Data {
    opaque _obj: *u8,
}

impl Data {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Data { return Data { _obj: ptr }; }

}

// `NSView` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct View {
    opaque _obj: *u8,
}

impl View {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> View { return View { _obj: ptr }; }

}

// `NSScreen` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Screen {
    opaque _obj: *u8,
}

impl Screen {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Screen { return Screen { _obj: ptr }; }

}

// `NSOpenGLPixelFormat` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct OpenGLPixelFormat {
    _obj: *u8,
}

impl OpenGLPixelFormat {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> OpenGLPixelFormat { return OpenGLPixelFormat { _obj: ptr }; }

    // SKIPPED `initWithCGLPixelFormatObj:`: param `CGLPixelFormatObj _Nonnull` — unmapped type `CGLPixelFormatObj`
    // SKIPPED `initWithAttributes:`: extra init variant (one `new` per type)
    // SKIPPED `initWithData:`: extra init variant (one `new` per type)
    fn attributes(this) -> option::Option[Data] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("attributes\0")));
        if obj == { 0 as *u8 } {
            return option::Option[Data]::None;
        }
        return option::some(Data::from_raw(obj));
    }

    fn set_attributes(this, attribs: Data) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setAttributes:\0")), attribs.raw());
    }

    // SKIPPED `getValues:forAttribute:forVirtualScreen:`: param `NSOpenGLPixelFormatAttribute` — unmapped type `NSOpenGLPixelFormatAttribute`
    // SKIPPED `numberOfVirtualScreens`: return `GLint` — unmapped type `GLint`
    // SKIPPED `CGLPixelFormatObj`: return `NS_RETURNS_INNER_POINTER CGLPixelFormatObj` — unmapped type `NS_RETURNS_INNER_POINTER CGLPixelFormatObj`
    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSOpenGLPixelBuffer` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct OpenGLPixelBuffer {
    _obj: *u8,
}

impl OpenGLPixelBuffer {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> OpenGLPixelBuffer { return OpenGLPixelBuffer { _obj: ptr }; }

    // SKIPPED `initWithTextureTarget:textureInternalFormat:textureMaxMipMapLevel:pixelsWide:pixelsHigh:`: param `GLenum` — unmapped type `GLenum`
    // SKIPPED `initWithCGLPBufferObj:`: extra init variant (one `new` per type)
    // SKIPPED `CGLPBufferObj`: return `NS_RETURNS_INNER_POINTER CGLPBufferObj` — unmapped type `NS_RETURNS_INNER_POINTER CGLPBufferObj`
    // SKIPPED `pixelsWide`: return `GLsizei` — unmapped type `GLsizei`
    // SKIPPED `pixelsHigh`: return `GLsizei` — unmapped type `GLsizei`
    // SKIPPED `textureTarget`: return `GLenum` — unmapped type `GLenum`
    // SKIPPED `textureInternalFormat`: return `GLenum` — unmapped type `GLenum`
    // SKIPPED `textureMaxMipMapLevel`: return `GLint` — unmapped type `GLint`
    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSOpenGLContext` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct OpenGLContext {
    _obj: *u8,
}

impl OpenGLContext {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> OpenGLContext { return OpenGLContext { _obj: ptr }; }

    // SKIPPED `initWithFormat:shareContext:`: init arg shape not yet modelled
    // SKIPPED `initWithCGLContextObj:`: extra init variant (one `new` per type)
    fn set_view(this, view: View) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setView:\0")), view.raw());
    }

    fn set_full_screen(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("setFullScreen\0")));
    }

    // SKIPPED `setOffScreen:width:height:rowbytes:`: param `GLsizei` — unmapped type `GLsizei`
    fn clear_drawable(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("clearDrawable\0")));
    }

    fn update(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("update\0")));
    }

    fn flush_buffer(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("flushBuffer\0")));
    }

    fn make_current_context(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("makeCurrentContext\0")));
    }

    fn clear_current_context() {
        rt::msg_void(rt::get_class(#str_ptr("NSOpenGLContext\0")), rt::sel(#str_ptr("clearCurrentContext\0")));
    }

    // SKIPPED `copyAttributesFromContext:withMask:`: param `GLbitfield` — unmapped type `GLbitfield`
    // SKIPPED `setValues:forParameter:`: (return, arg) shape not yet modelled
    // SKIPPED `getValues:forParameter:`: (return, arg) shape not yet modelled
    // SKIPPED `createTexture:fromView:internalFormat:`: param `GLenum` — unmapped type `GLenum`
    fn pixel_format(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("pixelFormat\0")));
    }

    fn view(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("view\0")));
    }

    fn current_context() -> option::Option[OpenGLContext] {
        let obj: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSOpenGLContext\0")), rt::sel(#str_ptr("currentContext\0")));
        if obj == { 0 as *u8 } {
            return option::Option[OpenGLContext]::None;
        }
        return option::some(OpenGLContext { _obj: rt::retain(obj) });
    }

    // SKIPPED `currentVirtualScreen`: return `GLint` — unmapped type `GLint`
    // SKIPPED `setCurrentVirtualScreen:`: param `GLint` — unmapped type `GLint`
    // SKIPPED `CGLContextObj`: return `NS_RETURNS_INNER_POINTER CGLContextObj` — unmapped type `NS_RETURNS_INNER_POINTER CGLContextObj`
    // SKIPPED `setPixelBuffer:cubeMapFace:mipMapLevel:currentVirtualScreen:`: param `GLenum` — unmapped type `GLenum`
    fn pixel_buffer(this) -> option::Option[OpenGLPixelBuffer] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("pixelBuffer\0")));
        if obj == { 0 as *u8 } {
            return option::Option[OpenGLPixelBuffer]::None;
        }
        return option::some(OpenGLPixelBuffer::from_raw(obj));
    }

    // SKIPPED `pixelBufferCubeMapFace`: return `GLenum` — unmapped type `GLenum`
    // SKIPPED `pixelBufferMipMapLevel`: return `GLint` — unmapped type `GLint`
    // SKIPPED `setTextureImageToPixelBuffer:colorBuffer:`: param `GLenum` — unmapped type `GLenum`
    fn drop(ref this) {
        rt::release(this._obj);
    }
}

