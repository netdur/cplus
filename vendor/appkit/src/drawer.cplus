// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSDrawer.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `NSRectEdge` (NS_ENUM)
enum RectEdge {
    MinX,
    MinY,
    MaxX,
    MaxY,
    NSMinXEdge,
    NSMinYEdge,
    NSMaxXEdge,
    NSMaxYEdge,
}

fn rect_edge_raw(v: RectEdge) -> i64 {
    return match v {
        RectEdge::MinX => { 0 as i64 },
        RectEdge::MinY => { 1 as i64 },
        RectEdge::MaxX => { 2 as i64 },
        RectEdge::MaxY => { 3 as i64 },
        RectEdge::NSMinXEdge => { 0 as i64 },
        RectEdge::NSMinYEdge => { 1 as i64 },
        RectEdge::NSMaxXEdge => { 2 as i64 },
        RectEdge::NSMaxYEdge => { 3 as i64 },
    };
}

fn rect_edge_from_raw(raw: i64) -> RectEdge {
    if raw == (1 as i64) { return RectEdge::MinY; }
    if raw == (2 as i64) { return RectEdge::MaxX; }
    if raw == (3 as i64) { return RectEdge::MaxY; }
    if raw == (0 as i64) { return RectEdge::NSMinXEdge; }
    if raw == (1 as i64) { return RectEdge::NSMinYEdge; }
    if raw == (2 as i64) { return RectEdge::NSMaxXEdge; }
    if raw == (3 as i64) { return RectEdge::NSMaxYEdge; }
    return RectEdge::MinX;
}

// `NSLock` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Lock {
    opaque _obj: *u8,
}

impl Lock {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSView` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct View {
    opaque _obj: *u8,
}

impl View {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSWindow` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Window {
    opaque _obj: *u8,
}

impl Window {
    fn raw(this) -> *u8 { return this._obj; }

    // SKIPPED `drawers`: return `API_DEPRECATED NSArray<NSDrawer *> *` — generic collection
}

// `NSNotification` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Notification {
    opaque _obj: *u8,
}

impl Notification {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSDrawer` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct Drawer {
    _obj: *u8,
}

impl Drawer {
    fn raw(this) -> *u8 { return this._obj; }

    // SKIPPED `initWithContentSize:preferredEdge:`: param `NSSize` — unmapped type `NSSize`
    fn open(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("open\0")));
    }

    // SKIPPED `openOnEdge:`: (return, arg) shape not yet modelled
    fn close(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("close\0")));
    }

    // SKIPPED `open:`: method `open` already defined
    // SKIPPED `close:`: method `close` already defined
    fn toggle(this, sender: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("toggle:\0")), sender);
    }

    fn parent_window(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("parentWindow\0")));
        return bridge::obj_option(obj);
    }

    fn set_parent_window(this, parent_window: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setParentWindow:\0")), parent_window);
    }

    fn content_view(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("contentView\0")));
        return bridge::obj_option(obj);
    }

    fn set_content_view(this, content_view: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setContentView:\0")), content_view);
    }

    fn preferred_edge(this) -> RectEdge {
        return rect_edge_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("preferredEdge\0"))));
    }

    // SKIPPED `setPreferredEdge:`: (return, arg) shape not yet modelled
    // SKIPPED `delegate`: return `id<NSDrawerDelegate> _Nullable` — generic collection
    // SKIPPED `setDelegate:`: param `id<NSDrawerDelegate> _Nullable` — generic collection
    fn state(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("state\0")));
    }

    fn edge(this) -> RectEdge {
        return rect_edge_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("edge\0"))));
    }

    // SKIPPED `contentSize`: return `NSSize` — unmapped type `NSSize`
    // SKIPPED `setContentSize:`: param `NSSize` — unmapped type `NSSize`
    // SKIPPED `minContentSize`: return `NSSize` — unmapped type `NSSize`
    // SKIPPED `setMinContentSize:`: param `NSSize` — unmapped type `NSSize`
    // SKIPPED `maxContentSize`: return `NSSize` — unmapped type `NSSize`
    // SKIPPED `setMaxContentSize:`: param `NSSize` — unmapped type `NSSize`
    fn leading_offset(this) -> f64 {
        return rt::msg_f64(this._obj, rt::sel(#str_ptr("leadingOffset\0")));
    }

    fn set_leading_offset(this, leading_offset: f64) {
        rt::msg_void_f64(this._obj, rt::sel(#str_ptr("setLeadingOffset:\0")), leading_offset);
    }

    fn trailing_offset(this) -> f64 {
        return rt::msg_f64(this._obj, rt::sel(#str_ptr("trailingOffset\0")));
    }

    fn set_trailing_offset(this, trailing_offset: f64) {
        rt::msg_void_f64(this._obj, rt::sel(#str_ptr("setTrailingOffset:\0")), trailing_offset);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSDrawerDelegate` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `drawerWillResizeContents:toSize:`: arg shape (only <=5 object args modelled)
fn DrawerDelegateProtocol_noop_i8_1(user: *u8, a0: *u8) -> i8 { return 0 as i8; }
fn DrawerDelegateProtocol_noop_v_1(user: *u8, a0: *u8) { return; }

struct DrawerDelegateProtocol {
    drawer_should_open: fn(*u8, *u8) -> i8,
    drawer_should_close: fn(*u8, *u8) -> i8,
    drawer_will_open: fn(*u8, *u8),
    drawer_did_open: fn(*u8, *u8),
    drawer_will_close: fn(*u8, *u8),
    drawer_did_close: fn(*u8, *u8),
    opaque user: *u8,
}

fn DrawerDelegateProtocol_new(user: *u8) -> DrawerDelegateProtocol {
    return DrawerDelegateProtocol { drawer_should_open: DrawerDelegateProtocol_noop_i8_1, drawer_should_close: DrawerDelegateProtocol_noop_i8_1, drawer_will_open: DrawerDelegateProtocol_noop_v_1, drawer_did_open: DrawerDelegateProtocol_noop_v_1, drawer_will_close: DrawerDelegateProtocol_noop_v_1, drawer_did_close: DrawerDelegateProtocol_noop_v_1, user: user };
}

impl DrawerDelegateProtocol {
    fn set_drawer_should_open(ref this, handler: fn(*u8, *u8) -> i8) {
        this.drawer_should_open = handler;
        return;
    }
    fn set_drawer_should_close(ref this, handler: fn(*u8, *u8) -> i8) {
        this.drawer_should_close = handler;
        return;
    }
    fn set_drawer_will_open(ref this, handler: fn(*u8, *u8)) {
        this.drawer_will_open = handler;
        return;
    }
    fn set_drawer_did_open(ref this, handler: fn(*u8, *u8)) {
        this.drawer_did_open = handler;
        return;
    }
    fn set_drawer_will_close(ref this, handler: fn(*u8, *u8)) {
        this.drawer_will_close = handler;
        return;
    }
    fn set_drawer_did_close(ref this, handler: fn(*u8, *u8)) {
        this.drawer_did_close = handler;
        return;
    }
}

fn DrawerDelegateProtocol_key() -> *u8 { return { 28672 as *u8 }; }

fn DrawerDelegateProtocol_drawer_should_open_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> i8 {
    let c: *DrawerDelegateProtocol = { synth::get_associated(self_obj, DrawerDelegateProtocol_key()) as *DrawerDelegateProtocol };
    let f: fn(*u8, *u8) -> i8 = { (*c).drawer_should_open };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn DrawerDelegateProtocol_drawer_should_close_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> i8 {
    let c: *DrawerDelegateProtocol = { synth::get_associated(self_obj, DrawerDelegateProtocol_key()) as *DrawerDelegateProtocol };
    let f: fn(*u8, *u8) -> i8 = { (*c).drawer_should_close };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn DrawerDelegateProtocol_drawer_will_open_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *DrawerDelegateProtocol = { synth::get_associated(self_obj, DrawerDelegateProtocol_key()) as *DrawerDelegateProtocol };
    let f: fn(*u8, *u8) = { (*c).drawer_will_open };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn DrawerDelegateProtocol_drawer_did_open_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *DrawerDelegateProtocol = { synth::get_associated(self_obj, DrawerDelegateProtocol_key()) as *DrawerDelegateProtocol };
    let f: fn(*u8, *u8) = { (*c).drawer_did_open };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn DrawerDelegateProtocol_drawer_will_close_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *DrawerDelegateProtocol = { synth::get_associated(self_obj, DrawerDelegateProtocol_key()) as *DrawerDelegateProtocol };
    let f: fn(*u8, *u8) = { (*c).drawer_will_close };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn DrawerDelegateProtocol_drawer_did_close_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *DrawerDelegateProtocol = { synth::get_associated(self_obj, DrawerDelegateProtocol_key()) as *DrawerDelegateProtocol };
    let f: fn(*u8, *u8) = { (*c).drawer_did_close };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn create_DrawerDelegateProtocol(ctx: *DrawerDelegateProtocol) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_DrawerDelegateProtocol\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_drawer_should_open: i8 = synth::add_method_i8_1id(cls, rt::sel(#str_ptr("drawerShouldOpen:\0")), DrawerDelegateProtocol_drawer_should_open_imp, #str_ptr("c@:@\0"));
        let add_drawer_should_close: i8 = synth::add_method_i8_1id(cls, rt::sel(#str_ptr("drawerShouldClose:\0")), DrawerDelegateProtocol_drawer_should_close_imp, #str_ptr("c@:@\0"));
        let add_drawer_will_open: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("drawerWillOpen:\0")), DrawerDelegateProtocol_drawer_will_open_imp, #str_ptr("v@:@\0"));
        let add_drawer_did_open: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("drawerDidOpen:\0")), DrawerDelegateProtocol_drawer_did_open_imp, #str_ptr("v@:@\0"));
        let add_drawer_will_close: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("drawerWillClose:\0")), DrawerDelegateProtocol_drawer_will_close_imp, #str_ptr("v@:@\0"));
        let add_drawer_did_close: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("drawerDidClose:\0")), DrawerDelegateProtocol_drawer_did_close_imp, #str_ptr("v@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, DrawerDelegateProtocol_key(), { ctx as *u8 });
    return d;
}

