// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSDrawer.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/vec" as vec;
import "objc/synthesis" as synth;

// `NSRectEdge` (NS_ENUM)
enum RectEdge {
    MinX,
    MinY,
    MaxX,
    MaxY,
    NSMinXEdge,
    NSMinYEdge,
    NSMaxXEdge,
    NSMaxYEdge,
}

fn rect_edge_raw(v: RectEdge) -> i64 {
    return match v {
        RectEdge::MinX => { 0 as i64 },
        RectEdge::MinY => { 1 as i64 },
        RectEdge::MaxX => { 2 as i64 },
        RectEdge::MaxY => { 3 as i64 },
        RectEdge::NSMinXEdge => { 0 as i64 },
        RectEdge::NSMinYEdge => { 1 as i64 },
        RectEdge::NSMaxXEdge => { 2 as i64 },
        RectEdge::NSMaxYEdge => { 3 as i64 },
    };
}

fn rect_edge_from_raw(raw: i64) -> RectEdge {
    if raw == (1 as i64) { return RectEdge::MinY; }
    if raw == (2 as i64) { return RectEdge::MaxX; }
    if raw == (3 as i64) { return RectEdge::MaxY; }
    if raw == (0 as i64) { return RectEdge::NSMinXEdge; }
    if raw == (1 as i64) { return RectEdge::NSMinYEdge; }
    if raw == (2 as i64) { return RectEdge::NSMaxXEdge; }
    if raw == (3 as i64) { return RectEdge::NSMaxYEdge; }
    return RectEdge::MinX;
}

// `NSLock` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Lock {
    opaque _obj: *u8,
}

impl Lock {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Lock { return Lock { _obj: ptr }; }

}

// `NSView` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct View {
    opaque _obj: *u8,
}

impl View {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> View { return View { _obj: ptr }; }

}

// `NSWindow` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Window {
    opaque _obj: *u8,
}

impl Window {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Window { return Window { _obj: ptr }; }

    fn drawers(this) -> vec::Vec[Drawer] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("drawers\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[Drawer] = vec::Vec[Drawer]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(Drawer::from_raw(rt::retain(rt::msg_id_u64(arr, at_sel, i))));
            i = i +% (1 as u64);
        }
        return out;
    }

}

// `NSNotification` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Notification {
    opaque _obj: *u8,
}

impl Notification {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Notification { return Notification { _obj: ptr }; }

}

// `NSDrawer` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct Drawer {
    _obj: *u8,
}

impl Drawer {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Drawer { return Drawer { _obj: ptr }; }

    // SKIPPED `initWithContentSize:preferredEdge:`: init arg shape not yet modelled
    fn open(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("open\0")));
    }

    fn open_on_edge(this, edge: RectEdge) {
        rt::msg_void_i64(this._obj, rt::sel(#str_ptr("openOnEdge:\0")), rect_edge_raw(edge));
    }

    fn close(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("close\0")));
    }

    // SKIPPED `open:`: method `open` already defined
    // SKIPPED `close:`: method `close` already defined
    fn toggle(this, sender: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("toggle:\0")), sender);
    }

    fn parent_window(this) -> option::Option[Window] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("parentWindow\0")));
        if obj == { 0 as *u8 } {
            return option::Option[Window]::None;
        }
        return option::some(Window::from_raw(obj));
    }

    fn set_parent_window(this, parent_window: Window) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setParentWindow:\0")), parent_window.raw());
    }

    fn content_view(this) -> option::Option[View] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("contentView\0")));
        if obj == { 0 as *u8 } {
            return option::Option[View]::None;
        }
        return option::some(View::from_raw(obj));
    }

    fn set_content_view(this, content_view: View) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setContentView:\0")), content_view.raw());
    }

    fn preferred_edge(this) -> RectEdge {
        return rect_edge_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("preferredEdge\0"))));
    }

    fn set_preferred_edge(this, preferred_edge: RectEdge) {
        rt::msg_void_i64(this._obj, rt::sel(#str_ptr("setPreferredEdge:\0")), rect_edge_raw(preferred_edge));
    }

    fn delegate(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("delegate\0")));
        return bridge::obj_option(obj);
    }

    fn set_delegate(this, delegate: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setDelegate:\0")), delegate);
    }

    fn state(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("state\0")));
    }

    fn edge(this) -> RectEdge {
        return rect_edge_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("edge\0"))));
    }

    fn content_size(this) -> rt::Size {
        return rt::msg_size(this._obj, rt::sel(#str_ptr("contentSize\0")));
    }

    fn set_content_size(this, content_size: rt::Size) {
        rt::msg_void_size(this._obj, rt::sel(#str_ptr("setContentSize:\0")), content_size);
    }

    fn min_content_size(this) -> rt::Size {
        return rt::msg_size(this._obj, rt::sel(#str_ptr("minContentSize\0")));
    }

    fn set_min_content_size(this, min_content_size: rt::Size) {
        rt::msg_void_size(this._obj, rt::sel(#str_ptr("setMinContentSize:\0")), min_content_size);
    }

    fn max_content_size(this) -> rt::Size {
        return rt::msg_size(this._obj, rt::sel(#str_ptr("maxContentSize\0")));
    }

    fn set_max_content_size(this, max_content_size: rt::Size) {
        rt::msg_void_size(this._obj, rt::sel(#str_ptr("setMaxContentSize:\0")), max_content_size);
    }

    fn leading_offset(this) -> f64 {
        return rt::msg_f64(this._obj, rt::sel(#str_ptr("leadingOffset\0")));
    }

    fn set_leading_offset(this, leading_offset: f64) {
        rt::msg_void_f64(this._obj, rt::sel(#str_ptr("setLeadingOffset:\0")), leading_offset);
    }

    fn trailing_offset(this) -> f64 {
        return rt::msg_f64(this._obj, rt::sel(#str_ptr("trailingOffset\0")));
    }

    fn set_trailing_offset(this, trailing_offset: f64) {
        rt::msg_void_f64(this._obj, rt::sel(#str_ptr("setTrailingOffset:\0")), trailing_offset);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSDrawerDelegate` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `drawerWillResizeContents:toSize:`: return `NSSize` not modelled
#[link_name = "class_addMethod"]
extern fn cplus_add_method_i8_id(cls: *u8, sel: *u8, imp: fn(*u8, *u8, *u8) -> i8, types: *u8) -> i8;
#[link_name = "class_addMethod"]
extern fn cplus_add_method_v_id(cls: *u8, sel: *u8, imp: fn(*u8, *u8, *u8), types: *u8) -> i8;

fn DrawerDelegate_noop_i8_id(user: *u8, a0: *u8) -> i8 { return 0 as i8; }
fn DrawerDelegate_noop_v_id(user: *u8, a0: *u8) { return; }

struct DrawerDelegate {
    drawer_should_open: fn(*u8, *u8) -> i8,
    drawer_should_close: fn(*u8, *u8) -> i8,
    drawer_will_open: fn(*u8, *u8),
    drawer_did_open: fn(*u8, *u8),
    drawer_will_close: fn(*u8, *u8),
    drawer_did_close: fn(*u8, *u8),
    opaque user: *u8,
}

fn DrawerDelegate_new(user: *u8) -> DrawerDelegate {
    return DrawerDelegate { drawer_should_open: DrawerDelegate_noop_i8_id, drawer_should_close: DrawerDelegate_noop_i8_id, drawer_will_open: DrawerDelegate_noop_v_id, drawer_did_open: DrawerDelegate_noop_v_id, drawer_will_close: DrawerDelegate_noop_v_id, drawer_did_close: DrawerDelegate_noop_v_id, user: user };
}

impl DrawerDelegate {
    fn set_drawer_should_open(ref this, handler: fn(*u8, *u8) -> i8) {
        this.drawer_should_open = handler;
        return;
    }
    fn set_drawer_should_close(ref this, handler: fn(*u8, *u8) -> i8) {
        this.drawer_should_close = handler;
        return;
    }
    fn set_drawer_will_open(ref this, handler: fn(*u8, *u8)) {
        this.drawer_will_open = handler;
        return;
    }
    fn set_drawer_did_open(ref this, handler: fn(*u8, *u8)) {
        this.drawer_did_open = handler;
        return;
    }
    fn set_drawer_will_close(ref this, handler: fn(*u8, *u8)) {
        this.drawer_will_close = handler;
        return;
    }
    fn set_drawer_did_close(ref this, handler: fn(*u8, *u8)) {
        this.drawer_did_close = handler;
        return;
    }
}

fn DrawerDelegate_key() -> *u8 { return { 28672 as *u8 }; }

fn DrawerDelegate_drawer_should_open_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> i8 {
    let c: *DrawerDelegate = { synth::get_associated(self_obj, DrawerDelegate_key()) as *DrawerDelegate };
    let f: fn(*u8, *u8) -> i8 = { (*c).drawer_should_open };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn DrawerDelegate_drawer_should_close_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> i8 {
    let c: *DrawerDelegate = { synth::get_associated(self_obj, DrawerDelegate_key()) as *DrawerDelegate };
    let f: fn(*u8, *u8) -> i8 = { (*c).drawer_should_close };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn DrawerDelegate_drawer_will_open_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *DrawerDelegate = { synth::get_associated(self_obj, DrawerDelegate_key()) as *DrawerDelegate };
    let f: fn(*u8, *u8) = { (*c).drawer_will_open };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn DrawerDelegate_drawer_did_open_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *DrawerDelegate = { synth::get_associated(self_obj, DrawerDelegate_key()) as *DrawerDelegate };
    let f: fn(*u8, *u8) = { (*c).drawer_did_open };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn DrawerDelegate_drawer_will_close_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *DrawerDelegate = { synth::get_associated(self_obj, DrawerDelegate_key()) as *DrawerDelegate };
    let f: fn(*u8, *u8) = { (*c).drawer_will_close };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn DrawerDelegate_drawer_did_close_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *DrawerDelegate = { synth::get_associated(self_obj, DrawerDelegate_key()) as *DrawerDelegate };
    let f: fn(*u8, *u8) = { (*c).drawer_did_close };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn create_DrawerDelegate(ctx: *DrawerDelegate) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_DrawerDelegate\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_drawer_should_open: i8 = cplus_add_method_i8_id(cls, rt::sel(#str_ptr("drawerShouldOpen:\0")), DrawerDelegate_drawer_should_open_imp, #str_ptr("c@:@\0"));
        let add_drawer_should_close: i8 = cplus_add_method_i8_id(cls, rt::sel(#str_ptr("drawerShouldClose:\0")), DrawerDelegate_drawer_should_close_imp, #str_ptr("c@:@\0"));
        let add_drawer_will_open: i8 = cplus_add_method_v_id(cls, rt::sel(#str_ptr("drawerWillOpen:\0")), DrawerDelegate_drawer_will_open_imp, #str_ptr("v@:@\0"));
        let add_drawer_did_open: i8 = cplus_add_method_v_id(cls, rt::sel(#str_ptr("drawerDidOpen:\0")), DrawerDelegate_drawer_did_open_imp, #str_ptr("v@:@\0"));
        let add_drawer_will_close: i8 = cplus_add_method_v_id(cls, rt::sel(#str_ptr("drawerWillClose:\0")), DrawerDelegate_drawer_will_close_imp, #str_ptr("v@:@\0"));
        let add_drawer_did_close: i8 = cplus_add_method_v_id(cls, rt::sel(#str_ptr("drawerDidClose:\0")), DrawerDelegate_drawer_did_close_imp, #str_ptr("v@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, DrawerDelegate_key(), { ctx as *u8 });
    return d;
}

