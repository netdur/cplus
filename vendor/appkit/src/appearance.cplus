// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSAppearance.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/vec" as vec;
import "objc/synthesis" as synth;

#[repr(C)]
struct Appearance_perform_as_current_drawing_appearance_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8),
    opaque ctx: *u8,
}

fn Appearance_perform_as_current_drawing_appearance_invoke(block: *u8) {
    let bl: *Appearance_perform_as_current_drawing_appearance_block = { block as *Appearance_perform_as_current_drawing_appearance_block };
    let f: fn(*u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx);
    return;
}

// `NSString` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct String {
    opaque _obj: *u8,
}

impl String {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSBundle` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Bundle {
    opaque _obj: *u8,
}

impl Bundle {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSAppearance` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct Appearance {
    _obj: *u8,
}

impl Appearance {
    fn raw(this) -> *u8 { return this._obj; }

    fn perform_as_current_drawing_appearance(this, cb: fn(*u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: Appearance_perform_as_current_drawing_appearance_block = Appearance_perform_as_current_drawing_appearance_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: Appearance_perform_as_current_drawing_appearance_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("performAsCurrentDrawingAppearance:\0")), bp);
        return;
    }

    fn appearance_named(name: str) -> option::Option[Appearance] {
        let obj: *u8 = rt::msg_id_id(rt::get_class(#str_ptr("NSAppearance\0")), rt::sel(#str_ptr("appearanceNamed:\0")), bridge::nsstring(name));
        if obj == { 0 as *u8 } {
            return option::Option[Appearance]::None;
        }
        return option::some(Appearance { _obj: rt::retain(obj) });
    }

    // SKIPPED `initWithAppearanceNamed:bundle:`: init arg shape not yet modelled
    // SKIPPED `initWithCoder:`: extra init variant (one `new` per type)
    fn best_match_from_appearances_with_names(this, appearances: vec::Vec[text::Text]) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("bestMatchFromAppearancesWithNames:\0")), bridge::nsarray_of_text(appearances));
        return bridge::to_text_option(ns);
    }

    // SKIPPED `name`: return `API_AVAILABLE NSAppearanceName` — unmapped type `API_AVAILABLE NSAppearanceName`
    fn current_appearance() -> Appearance {
        return Appearance { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("NSAppearance\0")), rt::sel(#str_ptr("currentAppearance\0")))) };
    }

    fn set_current_appearance(current_appearance: *u8) {
        rt::msg_void_id(rt::get_class(#str_ptr("NSAppearance\0")), rt::sel(#str_ptr("setCurrentAppearance:\0")), current_appearance);
    }

    fn current_drawing_appearance() -> *u8 {
        return rt::msg_id(rt::get_class(#str_ptr("NSAppearance\0")), rt::sel(#str_ptr("currentDrawingAppearance\0")));
    }

    fn allows_vibrancy(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("allowsVibrancy\0"))) != (0 as i8);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSAppearanceCustomization` delegate protocol -> runtime class-synthesis helper.
fn AppearanceCustomization_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn AppearanceCustomization_noop_v_1(user: *u8, a0: *u8) { return; }

struct AppearanceCustomization {
    appearance: fn(*u8) -> *u8,
    set_appearance: fn(*u8, *u8),
    effective_appearance: fn(*u8) -> *u8,
    opaque user: *u8,
}

fn AppearanceCustomization_new(user: *u8) -> AppearanceCustomization {
    return AppearanceCustomization { appearance: AppearanceCustomization_noop_id_0, set_appearance: AppearanceCustomization_noop_v_1, effective_appearance: AppearanceCustomization_noop_id_0, user: user };
}

impl AppearanceCustomization {
    fn set_appearance(ref this, handler: fn(*u8) -> *u8) {
        this.appearance = handler;
        return;
    }
    fn set_set_appearance(ref this, handler: fn(*u8, *u8)) {
        this.set_appearance = handler;
        return;
    }
    fn set_effective_appearance(ref this, handler: fn(*u8) -> *u8) {
        this.effective_appearance = handler;
        return;
    }
}

fn AppearanceCustomization_key() -> *u8 { return { 28672 as *u8 }; }

fn AppearanceCustomization_appearance_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *AppearanceCustomization = { synth::get_associated(self_obj, AppearanceCustomization_key()) as *AppearanceCustomization };
    let f: fn(*u8) -> *u8 = { (*c).appearance };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn AppearanceCustomization_set_appearance_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *AppearanceCustomization = { synth::get_associated(self_obj, AppearanceCustomization_key()) as *AppearanceCustomization };
    let f: fn(*u8, *u8) = { (*c).set_appearance };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn AppearanceCustomization_effective_appearance_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *AppearanceCustomization = { synth::get_associated(self_obj, AppearanceCustomization_key()) as *AppearanceCustomization };
    let f: fn(*u8) -> *u8 = { (*c).effective_appearance };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_AppearanceCustomization(ctx: *AppearanceCustomization) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_AppearanceCustomization\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_appearance: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("appearance\0")), AppearanceCustomization_appearance_imp, #str_ptr("@@:\0"));
        let add_set_appearance: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("setAppearance:\0")), AppearanceCustomization_set_appearance_imp, #str_ptr("v@:@\0"));
        let add_effective_appearance: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("effectiveAppearance\0")), AppearanceCustomization_effective_appearance_imp, #str_ptr("@@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, AppearanceCustomization_key(), { ctx as *u8 });
    return d;
}

