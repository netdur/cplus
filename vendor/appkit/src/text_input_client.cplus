// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSTextInputClient.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `NSAttributedString` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct AttributedString {
    opaque _obj: *u8,
}

impl AttributedString {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSAdaptiveImageGlyph` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct AdaptiveImageGlyph {
    opaque _obj: *u8,
}

impl AdaptiveImageGlyph {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSTextInputClient` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `insertText:replacementRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `doCommandBySelector:`: arg shape (only <=5 object args modelled)
// SKIPPED `setMarkedText:selectedRange:replacementRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `selectedRange`: return `NSRange` not modelled
// SKIPPED `markedRange`: return `NSRange` not modelled
// SKIPPED `hasMarkedText`: no `i8`-return / 0-arg IMP shape
// SKIPPED `attributedSubstringForProposedRange:actualRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `firstRectForCharacterRange:actualRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `characterIndexForPoint:`: arg shape (only <=5 object args modelled)
// SKIPPED `fractionOfDistanceThroughGlyphForPoint:`: arg shape (only <=5 object args modelled)
// SKIPPED `baselineDeltaForCharacterAtIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `drawsVerticallyForCharacterAtIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `insertAdaptiveImageGlyph:replacementRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `unionRectInVisibleSelectedRange`: return `NSRect` not modelled
// SKIPPED `documentVisibleRect`: return `NSRect` not modelled
// SKIPPED `supportsAdaptiveImageGlyph`: no `i8`-return / 0-arg IMP shape
fn TextInputClient_noop_i64_0(user: *u8) -> i64 { return 0 as i64; }
fn TextInputClient_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn TextInputClient_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }
fn TextInputClient_noop_v_0(user: *u8) { return; }

struct TextInputClient {
    unmark_text: fn(*u8),
    valid_attributes_for_marked_text: fn(*u8) -> *u8,
    attributed_string: fn(*u8) -> *u8,
    window_level: fn(*u8) -> i64,
    preferred_text_accessory_placement: fn(*u8) -> u64,
    opaque user: *u8,
}

fn TextInputClient_new(user: *u8) -> TextInputClient {
    return TextInputClient { unmark_text: TextInputClient_noop_v_0, valid_attributes_for_marked_text: TextInputClient_noop_id_0, attributed_string: TextInputClient_noop_id_0, window_level: TextInputClient_noop_i64_0, preferred_text_accessory_placement: TextInputClient_noop_u64_0, user: user };
}

impl TextInputClient {
    fn set_unmark_text(ref this, handler: fn(*u8)) {
        this.unmark_text = handler;
        return;
    }
    fn set_valid_attributes_for_marked_text(ref this, handler: fn(*u8) -> *u8) {
        this.valid_attributes_for_marked_text = handler;
        return;
    }
    fn set_attributed_string(ref this, handler: fn(*u8) -> *u8) {
        this.attributed_string = handler;
        return;
    }
    fn set_window_level(ref this, handler: fn(*u8) -> i64) {
        this.window_level = handler;
        return;
    }
    fn set_preferred_text_accessory_placement(ref this, handler: fn(*u8) -> u64) {
        this.preferred_text_accessory_placement = handler;
        return;
    }
}

fn TextInputClient_key() -> *u8 { return { 28672 as *u8 }; }

fn TextInputClient_unmark_text_imp(self_obj: *u8, cmd: *u8) {
    let c: *TextInputClient = { synth::get_associated(self_obj, TextInputClient_key()) as *TextInputClient };
    let f: fn(*u8) = { (*c).unmark_text };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn TextInputClient_valid_attributes_for_marked_text_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *TextInputClient = { synth::get_associated(self_obj, TextInputClient_key()) as *TextInputClient };
    let f: fn(*u8) -> *u8 = { (*c).valid_attributes_for_marked_text };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn TextInputClient_attributed_string_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *TextInputClient = { synth::get_associated(self_obj, TextInputClient_key()) as *TextInputClient };
    let f: fn(*u8) -> *u8 = { (*c).attributed_string };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn TextInputClient_window_level_imp(self_obj: *u8, cmd: *u8) -> i64 {
    let c: *TextInputClient = { synth::get_associated(self_obj, TextInputClient_key()) as *TextInputClient };
    let f: fn(*u8) -> i64 = { (*c).window_level };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn TextInputClient_preferred_text_accessory_placement_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *TextInputClient = { synth::get_associated(self_obj, TextInputClient_key()) as *TextInputClient };
    let f: fn(*u8) -> u64 = { (*c).preferred_text_accessory_placement };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_TextInputClient(ctx: *TextInputClient) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_TextInputClient\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_unmark_text: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("unmarkText\0")), TextInputClient_unmark_text_imp, #str_ptr("v@:\0"));
        let add_valid_attributes_for_marked_text: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("validAttributesForMarkedText\0")), TextInputClient_valid_attributes_for_marked_text_imp, #str_ptr("@@:\0"));
        let add_attributed_string: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("attributedString\0")), TextInputClient_attributed_string_imp, #str_ptr("@@:\0"));
        let add_window_level: i8 = synth::add_method_i64_0id(cls, rt::sel(#str_ptr("windowLevel\0")), TextInputClient_window_level_imp, #str_ptr("q@:\0"));
        let add_preferred_text_accessory_placement: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("preferredTextAccessoryPlacement\0")), TextInputClient_preferred_text_accessory_placement_imp, #str_ptr("Q@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, TextInputClient_key(), { ctx as *u8 });
    return d;
}

