// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSPopover.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `NSPopoverBehavior` (NS_ENUM)
enum PopoverBehavior {
    ApplicationDefined,
    Transient,
    Semitransient,
}

fn popover_behavior_raw(v: PopoverBehavior) -> i64 {
    return match v {
        PopoverBehavior::ApplicationDefined => { 0 as i64 },
        PopoverBehavior::Transient => { 1 as i64 },
        PopoverBehavior::Semitransient => { 2 as i64 },
    };
}

fn popover_behavior_from_raw(raw: i64) -> PopoverBehavior {
    if raw == (1 as i64) { return PopoverBehavior::Transient; }
    if raw == (2 as i64) { return PopoverBehavior::Semitransient; }
    return PopoverBehavior::ApplicationDefined;
}

// `NSView` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct View {
    opaque _obj: *u8,
}

impl View {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSViewController` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ViewController {
    opaque _obj: *u8,
}

impl ViewController {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSWindow` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Window {
    opaque _obj: *u8,
}

impl Window {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSNotification` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Notification {
    opaque _obj: *u8,
}

impl Notification {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSString` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct String {
    opaque _obj: *u8,
}

impl String {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSToolbarItem` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ToolbarItem {
    opaque _obj: *u8,
}

impl ToolbarItem {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSPopover` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct Popover {
    _obj: *u8,
}

impl Popover {
    fn raw(this) -> *u8 { return this._obj; }

    fn new() -> Popover {
        let cls: *u8 = rt::get_class(#str_ptr("NSPopover\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        return Popover { _obj: rt::msg_id(alloced, rt::sel(#str_ptr("init\0"))) };
    }

    // SKIPPED `initWithCoder:`: extra init variant (one `new` per type)
    // SKIPPED `showRelativeToRect:ofView:preferredEdge:`: param `NSRect` — unmapped type `NSRect`
    fn show_relative_to_toolbar_item(this, toolbar_item: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("showRelativeToToolbarItem:\0")), toolbar_item);
    }

    fn perform_close(this, sender: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("performClose:\0")), sender);
    }

    fn close(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("close\0")));
    }

    // SKIPPED `delegate`: return `id<NSPopoverDelegate> _Nullable` — generic collection
    // SKIPPED `setDelegate:`: param `id<NSPopoverDelegate> _Nullable` — generic collection
    fn appearance(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("appearance\0")));
    }

    fn set_appearance(this, appearance: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setAppearance:\0")), appearance);
    }

    fn effective_appearance(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("effectiveAppearance\0")));
    }

    fn behavior(this) -> PopoverBehavior {
        return popover_behavior_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("behavior\0"))));
    }

    // SKIPPED `setBehavior:`: (return, arg) shape not yet modelled
    fn animates(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("animates\0"))) != (0 as i8);
    }

    fn set_animates(this, animates: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setAnimates:\0")), animates as i8);
    }

    fn content_view_controller(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("contentViewController\0")));
        return bridge::obj_option(obj);
    }

    fn set_content_view_controller(this, content_view_controller: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setContentViewController:\0")), content_view_controller);
    }

    // SKIPPED `contentSize`: return `NSSize` — unmapped type `NSSize`
    // SKIPPED `setContentSize:`: param `NSSize` — unmapped type `NSSize`
    fn is_shown(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("isShown\0"))) != (0 as i8);
    }

    fn is_detached(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("isDetached\0"))) != (0 as i8);
    }

    // SKIPPED `positioningRect`: return `NSRect` — unmapped type `NSRect`
    // SKIPPED `setPositioningRect:`: param `NSRect` — unmapped type `NSRect`
    fn has_full_size_content(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("hasFullSizeContent\0"))) != (0 as i8);
    }

    fn set_has_full_size_content(this, has_full_size_content: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setHasFullSizeContent:\0")), has_full_size_content as i8);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSPopoverDelegate` delegate protocol -> runtime class-synthesis helper.
fn PopoverDelegateProtocol_noop_i8_1(user: *u8, a0: *u8) -> i8 { return 0 as i8; }
fn PopoverDelegateProtocol_noop_id_1(user: *u8, a0: *u8) -> *u8 { return { 0 as *u8 }; }
fn PopoverDelegateProtocol_noop_v_1(user: *u8, a0: *u8) { return; }

struct PopoverDelegateProtocol {
    popover_should_close: fn(*u8, *u8) -> i8,
    popover_should_detach: fn(*u8, *u8) -> i8,
    popover_did_detach: fn(*u8, *u8),
    detachable_window_for_popover: fn(*u8, *u8) -> *u8,
    popover_will_show: fn(*u8, *u8),
    popover_did_show: fn(*u8, *u8),
    popover_will_close: fn(*u8, *u8),
    popover_did_close: fn(*u8, *u8),
    opaque user: *u8,
}

fn PopoverDelegateProtocol_new(user: *u8) -> PopoverDelegateProtocol {
    return PopoverDelegateProtocol { popover_should_close: PopoverDelegateProtocol_noop_i8_1, popover_should_detach: PopoverDelegateProtocol_noop_i8_1, popover_did_detach: PopoverDelegateProtocol_noop_v_1, detachable_window_for_popover: PopoverDelegateProtocol_noop_id_1, popover_will_show: PopoverDelegateProtocol_noop_v_1, popover_did_show: PopoverDelegateProtocol_noop_v_1, popover_will_close: PopoverDelegateProtocol_noop_v_1, popover_did_close: PopoverDelegateProtocol_noop_v_1, user: user };
}

impl PopoverDelegateProtocol {
    fn set_popover_should_close(ref this, handler: fn(*u8, *u8) -> i8) {
        this.popover_should_close = handler;
        return;
    }
    fn set_popover_should_detach(ref this, handler: fn(*u8, *u8) -> i8) {
        this.popover_should_detach = handler;
        return;
    }
    fn set_popover_did_detach(ref this, handler: fn(*u8, *u8)) {
        this.popover_did_detach = handler;
        return;
    }
    fn set_detachable_window_for_popover(ref this, handler: fn(*u8, *u8) -> *u8) {
        this.detachable_window_for_popover = handler;
        return;
    }
    fn set_popover_will_show(ref this, handler: fn(*u8, *u8)) {
        this.popover_will_show = handler;
        return;
    }
    fn set_popover_did_show(ref this, handler: fn(*u8, *u8)) {
        this.popover_did_show = handler;
        return;
    }
    fn set_popover_will_close(ref this, handler: fn(*u8, *u8)) {
        this.popover_will_close = handler;
        return;
    }
    fn set_popover_did_close(ref this, handler: fn(*u8, *u8)) {
        this.popover_did_close = handler;
        return;
    }
}

fn PopoverDelegateProtocol_key() -> *u8 { return { 28672 as *u8 }; }

fn PopoverDelegateProtocol_popover_should_close_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> i8 {
    let c: *PopoverDelegateProtocol = { synth::get_associated(self_obj, PopoverDelegateProtocol_key()) as *PopoverDelegateProtocol };
    let f: fn(*u8, *u8) -> i8 = { (*c).popover_should_close };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn PopoverDelegateProtocol_popover_should_detach_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> i8 {
    let c: *PopoverDelegateProtocol = { synth::get_associated(self_obj, PopoverDelegateProtocol_key()) as *PopoverDelegateProtocol };
    let f: fn(*u8, *u8) -> i8 = { (*c).popover_should_detach };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn PopoverDelegateProtocol_popover_did_detach_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *PopoverDelegateProtocol = { synth::get_associated(self_obj, PopoverDelegateProtocol_key()) as *PopoverDelegateProtocol };
    let f: fn(*u8, *u8) = { (*c).popover_did_detach };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn PopoverDelegateProtocol_detachable_window_for_popover_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> *u8 {
    let c: *PopoverDelegateProtocol = { synth::get_associated(self_obj, PopoverDelegateProtocol_key()) as *PopoverDelegateProtocol };
    let f: fn(*u8, *u8) -> *u8 = { (*c).detachable_window_for_popover };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn PopoverDelegateProtocol_popover_will_show_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *PopoverDelegateProtocol = { synth::get_associated(self_obj, PopoverDelegateProtocol_key()) as *PopoverDelegateProtocol };
    let f: fn(*u8, *u8) = { (*c).popover_will_show };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn PopoverDelegateProtocol_popover_did_show_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *PopoverDelegateProtocol = { synth::get_associated(self_obj, PopoverDelegateProtocol_key()) as *PopoverDelegateProtocol };
    let f: fn(*u8, *u8) = { (*c).popover_did_show };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn PopoverDelegateProtocol_popover_will_close_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *PopoverDelegateProtocol = { synth::get_associated(self_obj, PopoverDelegateProtocol_key()) as *PopoverDelegateProtocol };
    let f: fn(*u8, *u8) = { (*c).popover_will_close };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn PopoverDelegateProtocol_popover_did_close_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *PopoverDelegateProtocol = { synth::get_associated(self_obj, PopoverDelegateProtocol_key()) as *PopoverDelegateProtocol };
    let f: fn(*u8, *u8) = { (*c).popover_did_close };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn create_PopoverDelegateProtocol(ctx: *PopoverDelegateProtocol) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_PopoverDelegateProtocol\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_popover_should_close: i8 = synth::add_method_i8_1id(cls, rt::sel(#str_ptr("popoverShouldClose:\0")), PopoverDelegateProtocol_popover_should_close_imp, #str_ptr("c@:@\0"));
        let add_popover_should_detach: i8 = synth::add_method_i8_1id(cls, rt::sel(#str_ptr("popoverShouldDetach:\0")), PopoverDelegateProtocol_popover_should_detach_imp, #str_ptr("c@:@\0"));
        let add_popover_did_detach: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("popoverDidDetach:\0")), PopoverDelegateProtocol_popover_did_detach_imp, #str_ptr("v@:@\0"));
        let add_detachable_window_for_popover: i8 = synth::add_method_id_1id(cls, rt::sel(#str_ptr("detachableWindowForPopover:\0")), PopoverDelegateProtocol_detachable_window_for_popover_imp, #str_ptr("@@:@\0"));
        let add_popover_will_show: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("popoverWillShow:\0")), PopoverDelegateProtocol_popover_will_show_imp, #str_ptr("v@:@\0"));
        let add_popover_did_show: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("popoverDidShow:\0")), PopoverDelegateProtocol_popover_did_show_imp, #str_ptr("v@:@\0"));
        let add_popover_will_close: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("popoverWillClose:\0")), PopoverDelegateProtocol_popover_will_close_imp, #str_ptr("v@:@\0"));
        let add_popover_did_close: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("popoverDidClose:\0")), PopoverDelegateProtocol_popover_did_close_imp, #str_ptr("v@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, PopoverDelegateProtocol_key(), { ctx as *u8 });
    return d;
}

