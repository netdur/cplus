// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSPopover.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `NSRectEdge` (NS_ENUM)
enum RectEdge {
    MinX,
    MinY,
    MaxX,
    MaxY,
    NSMinXEdge,
    NSMinYEdge,
    NSMaxXEdge,
    NSMaxYEdge,
}

fn rect_edge_raw(v: RectEdge) -> i64 {
    return match v {
        RectEdge::MinX => { 0 as i64 },
        RectEdge::MinY => { 1 as i64 },
        RectEdge::MaxX => { 2 as i64 },
        RectEdge::MaxY => { 3 as i64 },
        RectEdge::NSMinXEdge => { 0 as i64 },
        RectEdge::NSMinYEdge => { 1 as i64 },
        RectEdge::NSMaxXEdge => { 2 as i64 },
        RectEdge::NSMaxYEdge => { 3 as i64 },
    };
}

fn rect_edge_from_raw(raw: i64) -> RectEdge {
    if raw == (1 as i64) { return RectEdge::MinY; }
    if raw == (2 as i64) { return RectEdge::MaxX; }
    if raw == (3 as i64) { return RectEdge::MaxY; }
    if raw == (0 as i64) { return RectEdge::NSMinXEdge; }
    if raw == (1 as i64) { return RectEdge::NSMinYEdge; }
    if raw == (2 as i64) { return RectEdge::NSMaxXEdge; }
    if raw == (3 as i64) { return RectEdge::NSMaxYEdge; }
    return RectEdge::MinX;
}

// `NSPopoverBehavior` (NS_ENUM)
enum PopoverBehavior {
    ApplicationDefined,
    Transient,
    Semitransient,
}

fn popover_behavior_raw(v: PopoverBehavior) -> i64 {
    return match v {
        PopoverBehavior::ApplicationDefined => { 0 as i64 },
        PopoverBehavior::Transient => { 1 as i64 },
        PopoverBehavior::Semitransient => { 2 as i64 },
    };
}

fn popover_behavior_from_raw(raw: i64) -> PopoverBehavior {
    if raw == (1 as i64) { return PopoverBehavior::Transient; }
    if raw == (2 as i64) { return PopoverBehavior::Semitransient; }
    return PopoverBehavior::ApplicationDefined;
}

// `NSView` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct View {
    opaque _obj: *u8,
}

impl View {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> View { return View { _obj: ptr }; }

}

// `NSViewController` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ViewController {
    opaque _obj: *u8,
}

impl ViewController {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ViewController { return ViewController { _obj: ptr }; }

}

// `NSWindow` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Window {
    opaque _obj: *u8,
}

impl Window {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Window { return Window { _obj: ptr }; }

}

// `NSNotification` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Notification {
    opaque _obj: *u8,
}

impl Notification {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Notification { return Notification { _obj: ptr }; }

}

// `NSString` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct String {
    opaque _obj: *u8,
}

impl String {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> String { return String { _obj: ptr }; }

}

// `NSToolbarItem` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ToolbarItem {
    opaque _obj: *u8,
}

impl ToolbarItem {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ToolbarItem { return ToolbarItem { _obj: ptr }; }

}

// `NSPopover` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct Popover {
    _obj: *u8,
}

impl Popover {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Popover { return Popover { _obj: ptr }; }

    fn new() -> Popover {
        let cls: *u8 = rt::get_class(#str_ptr("NSPopover\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        return Popover { _obj: rt::msg_id(alloced, rt::sel(#str_ptr("init\0"))) };
    }

    fn new_with_coder(coder: *u8) -> Popover {
        let cls: *u8 = rt::get_class(#str_ptr("NSPopover\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        return Popover { _obj: rt::msg_id_id(alloced, rt::sel(#str_ptr("initWithCoder:\0")), coder) };
    }

    fn show_relative_to_rect(this, positioning_rect: rt::Rect, of_view: View, preferred_edge: RectEdge) {
        rt::msg_void_rect_id_i64(this._obj, rt::sel(#str_ptr("showRelativeToRect:ofView:preferredEdge:\0")), positioning_rect, of_view.raw(), rect_edge_raw(preferred_edge));
    }

    fn show_relative_to_toolbar_item(this, toolbar_item: ToolbarItem) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("showRelativeToToolbarItem:\0")), toolbar_item.raw());
    }

    fn perform_close(this, sender: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("performClose:\0")), sender);
    }

    fn close(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("close\0")));
    }

    fn delegate(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("delegate\0")));
        return bridge::obj_option(obj);
    }

    fn set_delegate(this, delegate: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setDelegate:\0")), delegate);
    }

    fn appearance(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("appearance\0")));
    }

    fn set_appearance(this, appearance: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setAppearance:\0")), appearance);
    }

    fn effective_appearance(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("effectiveAppearance\0")));
    }

    fn behavior(this) -> PopoverBehavior {
        return popover_behavior_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("behavior\0"))));
    }

    fn set_behavior(this, behavior: PopoverBehavior) {
        rt::msg_void_i64(this._obj, rt::sel(#str_ptr("setBehavior:\0")), popover_behavior_raw(behavior));
    }

    fn animates(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("animates\0"))) != (0 as i8);
    }

    fn set_animates(this, animates: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setAnimates:\0")), animates as i8);
    }

    fn content_view_controller(this) -> option::Option[ViewController] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("contentViewController\0")));
        if obj == { 0 as *u8 } {
            return option::Option[ViewController]::None;
        }
        return option::some(ViewController::from_raw(obj));
    }

    fn set_content_view_controller(this, content_view_controller: ViewController) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setContentViewController:\0")), content_view_controller.raw());
    }

    fn content_size(this) -> rt::Size {
        return rt::msg_size(this._obj, rt::sel(#str_ptr("contentSize\0")));
    }

    fn set_content_size(this, content_size: rt::Size) {
        rt::msg_void_size(this._obj, rt::sel(#str_ptr("setContentSize:\0")), content_size);
    }

    fn is_shown(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("isShown\0"))) != (0 as i8);
    }

    fn is_detached(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("isDetached\0"))) != (0 as i8);
    }

    fn positioning_rect(this) -> rt::Rect {
        return rt::msg_rect(this._obj, rt::sel(#str_ptr("positioningRect\0")));
    }

    fn set_positioning_rect(this, positioning_rect: rt::Rect) {
        rt::msg_void_rect(this._obj, rt::sel(#str_ptr("setPositioningRect:\0")), positioning_rect);
    }

    fn has_full_size_content(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("hasFullSizeContent\0"))) != (0 as i8);
    }

    fn set_has_full_size_content(this, has_full_size_content: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setHasFullSizeContent:\0")), has_full_size_content as i8);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSPopoverDelegate` delegate protocol -> runtime class-synthesis helper.
#[link_name = "class_addMethod"]
extern fn cplus_add_method_i8_id(cls: *u8, sel: *u8, imp: fn(*u8, *u8, *u8) -> i8, types: *u8) -> i8;
#[link_name = "class_addMethod"]
extern fn cplus_add_method_v_id(cls: *u8, sel: *u8, imp: fn(*u8, *u8, *u8), types: *u8) -> i8;
#[link_name = "class_addMethod"]
extern fn cplus_add_method_id_id(cls: *u8, sel: *u8, imp: fn(*u8, *u8, *u8) -> *u8, types: *u8) -> i8;

fn PopoverDelegate_noop_i8_id(user: *u8, a0: *u8) -> i8 { return 0 as i8; }
fn PopoverDelegate_noop_v_id(user: *u8, a0: *u8) { return; }
fn PopoverDelegate_noop_id_id(user: *u8, a0: *u8) -> *u8 { return { 0 as *u8 }; }

struct PopoverDelegate {
    popover_should_close: fn(*u8, *u8) -> i8,
    popover_should_detach: fn(*u8, *u8) -> i8,
    popover_did_detach: fn(*u8, *u8),
    detachable_window_for_popover: fn(*u8, *u8) -> *u8,
    popover_will_show: fn(*u8, *u8),
    popover_did_show: fn(*u8, *u8),
    popover_will_close: fn(*u8, *u8),
    popover_did_close: fn(*u8, *u8),
    opaque user: *u8,
}

fn PopoverDelegate_new(user: *u8) -> PopoverDelegate {
    return PopoverDelegate { popover_should_close: PopoverDelegate_noop_i8_id, popover_should_detach: PopoverDelegate_noop_i8_id, popover_did_detach: PopoverDelegate_noop_v_id, detachable_window_for_popover: PopoverDelegate_noop_id_id, popover_will_show: PopoverDelegate_noop_v_id, popover_did_show: PopoverDelegate_noop_v_id, popover_will_close: PopoverDelegate_noop_v_id, popover_did_close: PopoverDelegate_noop_v_id, user: user };
}

impl PopoverDelegate {
    fn set_popover_should_close(ref this, handler: fn(*u8, *u8) -> i8) {
        this.popover_should_close = handler;
        return;
    }
    fn set_popover_should_detach(ref this, handler: fn(*u8, *u8) -> i8) {
        this.popover_should_detach = handler;
        return;
    }
    fn set_popover_did_detach(ref this, handler: fn(*u8, *u8)) {
        this.popover_did_detach = handler;
        return;
    }
    fn set_detachable_window_for_popover(ref this, handler: fn(*u8, *u8) -> *u8) {
        this.detachable_window_for_popover = handler;
        return;
    }
    fn set_popover_will_show(ref this, handler: fn(*u8, *u8)) {
        this.popover_will_show = handler;
        return;
    }
    fn set_popover_did_show(ref this, handler: fn(*u8, *u8)) {
        this.popover_did_show = handler;
        return;
    }
    fn set_popover_will_close(ref this, handler: fn(*u8, *u8)) {
        this.popover_will_close = handler;
        return;
    }
    fn set_popover_did_close(ref this, handler: fn(*u8, *u8)) {
        this.popover_did_close = handler;
        return;
    }
}

fn PopoverDelegate_key() -> *u8 { return { 28672 as *u8 }; }

fn PopoverDelegate_popover_should_close_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> i8 {
    let c: *PopoverDelegate = { synth::get_associated(self_obj, PopoverDelegate_key()) as *PopoverDelegate };
    let f: fn(*u8, *u8) -> i8 = { (*c).popover_should_close };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn PopoverDelegate_popover_should_detach_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> i8 {
    let c: *PopoverDelegate = { synth::get_associated(self_obj, PopoverDelegate_key()) as *PopoverDelegate };
    let f: fn(*u8, *u8) -> i8 = { (*c).popover_should_detach };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn PopoverDelegate_popover_did_detach_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *PopoverDelegate = { synth::get_associated(self_obj, PopoverDelegate_key()) as *PopoverDelegate };
    let f: fn(*u8, *u8) = { (*c).popover_did_detach };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn PopoverDelegate_detachable_window_for_popover_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> *u8 {
    let c: *PopoverDelegate = { synth::get_associated(self_obj, PopoverDelegate_key()) as *PopoverDelegate };
    let f: fn(*u8, *u8) -> *u8 = { (*c).detachable_window_for_popover };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn PopoverDelegate_popover_will_show_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *PopoverDelegate = { synth::get_associated(self_obj, PopoverDelegate_key()) as *PopoverDelegate };
    let f: fn(*u8, *u8) = { (*c).popover_will_show };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn PopoverDelegate_popover_did_show_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *PopoverDelegate = { synth::get_associated(self_obj, PopoverDelegate_key()) as *PopoverDelegate };
    let f: fn(*u8, *u8) = { (*c).popover_did_show };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn PopoverDelegate_popover_will_close_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *PopoverDelegate = { synth::get_associated(self_obj, PopoverDelegate_key()) as *PopoverDelegate };
    let f: fn(*u8, *u8) = { (*c).popover_will_close };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn PopoverDelegate_popover_did_close_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *PopoverDelegate = { synth::get_associated(self_obj, PopoverDelegate_key()) as *PopoverDelegate };
    let f: fn(*u8, *u8) = { (*c).popover_did_close };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn create_PopoverDelegate(ctx: *PopoverDelegate) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_PopoverDelegate\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_popover_should_close: i8 = cplus_add_method_i8_id(cls, rt::sel(#str_ptr("popoverShouldClose:\0")), PopoverDelegate_popover_should_close_imp, #str_ptr("c@:@\0"));
        let add_popover_should_detach: i8 = cplus_add_method_i8_id(cls, rt::sel(#str_ptr("popoverShouldDetach:\0")), PopoverDelegate_popover_should_detach_imp, #str_ptr("c@:@\0"));
        let add_popover_did_detach: i8 = cplus_add_method_v_id(cls, rt::sel(#str_ptr("popoverDidDetach:\0")), PopoverDelegate_popover_did_detach_imp, #str_ptr("v@:@\0"));
        let add_detachable_window_for_popover: i8 = cplus_add_method_id_id(cls, rt::sel(#str_ptr("detachableWindowForPopover:\0")), PopoverDelegate_detachable_window_for_popover_imp, #str_ptr("@@:@\0"));
        let add_popover_will_show: i8 = cplus_add_method_v_id(cls, rt::sel(#str_ptr("popoverWillShow:\0")), PopoverDelegate_popover_will_show_imp, #str_ptr("v@:@\0"));
        let add_popover_did_show: i8 = cplus_add_method_v_id(cls, rt::sel(#str_ptr("popoverDidShow:\0")), PopoverDelegate_popover_did_show_imp, #str_ptr("v@:@\0"));
        let add_popover_will_close: i8 = cplus_add_method_v_id(cls, rt::sel(#str_ptr("popoverWillClose:\0")), PopoverDelegate_popover_will_close_imp, #str_ptr("v@:@\0"));
        let add_popover_did_close: i8 = cplus_add_method_v_id(cls, rt::sel(#str_ptr("popoverDidClose:\0")), PopoverDelegate_popover_did_close_imp, #str_ptr("v@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, PopoverDelegate_key(), { ctx as *u8 });
    return d;
}

