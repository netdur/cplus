import "./runtime" as rt;
import "./convert" as bridge;
import "stdlib/option" as option;

struct Toolbar {
    _obj: *u8,
}

impl Toolbar {
    fn raw(this) -> *u8 { return this._obj; }

    fn drop(ref this) {
        rt::release(this._obj);
    }

    fn new(identifier: str) -> Toolbar {
        let cls: *u8 = rt::get_class(#str_ptr("NSToolbar\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        let ns_id: *u8 = bridge::cplus_str_to_nsstring(identifier);
        return Toolbar { _obj: rt::msg_id_id(alloced, rt::sel(#str_ptr("initWithIdentifier:\0")), ns_id) };
    }

    fn set_delegate(this, delegate: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setDelegate:\0")), delegate);
    }

    fn set_display_mode(this, mode: i64) {
        rt::msg_void_i64(this._obj, rt::sel(#str_ptr("setDisplayMode:\0")), mode);
    }

    fn set_allows_user_customization(this, allows: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setAllowsUserCustomization:\0")), allows as i8);
    }

    // GAP 4 (v0.0.19): center a single toolbar item by identifier. Uses the
    // modern `centeredItemIdentifiers` (NSSet, macOS 13+) — wraps the one
    // identifier in an `NSSet` — rather than the deprecated singular
    // `centeredItemIdentifier`.
    fn set_centered_item(this, identifier: str) {
        let ns_id: *u8 = bridge::cplus_str_to_nsstring(identifier);
        let set_cls: *u8 = rt::get_class(#str_ptr("NSSet\0"));
        let set: *u8 = rt::msg_id_id(set_cls, rt::sel(#str_ptr("setWithObject:\0")), ns_id);
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setCenteredItemIdentifiers:\0")), set);
    }
}

fn create_toolbar_delegate(
    item_for_id: fn(*u8, *u8, *u8, *u8, i8) -> *u8,
    default_ids: fn(*u8, *u8, *u8) -> *u8,
    allowed_ids: fn(*u8, *u8, *u8) -> *u8
) -> *u8 {
    let existing: *u8 = rt::get_class(#str_ptr("CToolbarDelegate\0"));
    var cls: *u8 = existing;
    if existing == { 0 as *u8 } {
        let nsobject: *u8 = rt::get_class(#str_ptr("NSObject\0"));
        cls = rt::allocate_class_pair(nsobject, #str_ptr("CToolbarDelegate\0"), 0 as usize);

        let _a: i8 = rt::add_method_id_toolbar_item(
            cls,
            rt::sel(#str_ptr("toolbar:itemForItemIdentifier:willBeInsertedIntoToolbar:\0")),
            item_for_id,
            #str_ptr("@@:@@c\0")
        );
        let _b: i8 = rt::add_method_id_toolbar_array(
            cls,
            rt::sel(#str_ptr("toolbarDefaultItemIdentifiers:\0")),
            default_ids,
            #str_ptr("@@:@\0")
        );
        let _c: i8 = rt::add_method_id_toolbar_array(
            cls,
            rt::sel(#str_ptr("toolbarAllowedItemIdentifiers:\0")),
            allowed_ids,
            #str_ptr("@@:@\0")
        );
        rt::register_class_pair(cls);
    }
    return rt::msg_id(cls, rt::sel(#str_ptr("new\0")));
}

struct ToolbarItem {
    _obj: *u8,
}

impl ToolbarItem {
    fn raw(this) -> *u8 { return this._obj; }

    fn drop(ref this) {
        rt::release(this._obj);
    }

    fn new(identifier: str) -> ToolbarItem {
        let cls = rt::get_class(#str_ptr("NSToolbarItem\0"));
        let alloced = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        let item = rt::msg_id_id(alloced, rt::sel(#str_ptr("initWithItemIdentifier:\0")), bridge::cplus_str_to_nsstring(identifier));
        return ToolbarItem { _obj: item };
    }

    fn set_label(this, label: str) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setLabel:\0")), bridge::cplus_str_to_nsstring(label));
    }

    fn set_palette_label(this, label: str) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setPaletteLabel:\0")), bridge::cplus_str_to_nsstring(label));
    }

    fn set_tool_tip(this, tip: str) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setToolTip:\0")), bridge::cplus_str_to_nsstring(tip));
    }

    fn set_image(this, image: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setImage:\0")), image);
    }

    fn set_view(this, view: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setView:\0")), view);
    }

    fn set_target(this, target: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setTarget:\0")), target);
    }

    fn set_action(this, action: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setAction:\0")), action);
    }

    // GAP 4 (v0.0.19): pin a view-backed item's size. NOTE: `minSize`/`maxSize`
    // are deprecated (macOS 10–12) — Apple recommends letting the system measure
    // a `view`-backed item via Auto Layout constraints instead. They still
    // function; provided for parity, but prefer constraints on the item's view.
    fn set_min_size(this, size: rt::Size) {
        rt::msg_void_size(this._obj, rt::sel(#str_ptr("setMinSize:\0")), size);
    }

    fn set_max_size(this, size: rt::Size) {
        rt::msg_void_size(this._obj, rt::sel(#str_ptr("setMaxSize:\0")), size);
    }
}

struct ToolbarItemGroup {
    _obj: *u8,
}

impl ToolbarItemGroup {
    fn raw(this) -> *u8 { return this._obj; }

    fn drop(ref this) {
        rt::release(this._obj);
    }

    fn new(identifier: str) -> ToolbarItemGroup {
        let cls = rt::get_class(#str_ptr("NSToolbarItemGroup\0"));
        let alloced = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        let item = rt::msg_id_id(alloced, rt::sel(#str_ptr("initWithItemIdentifier:\0")), bridge::cplus_str_to_nsstring(identifier));
        return ToolbarItemGroup { _obj: item };
    }

    fn set_subitems(this, items: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setSubitems:\0")), items);
    }
}

struct StatusBar {
    opaque _obj: *u8,
}

impl StatusBar {
    fn raw(this) -> *u8 { return this._obj; }

    fn system() -> StatusBar {
        return StatusBar { _obj: rt::msg_id(rt::get_class(#str_ptr("NSStatusBar\0")), rt::sel(#str_ptr("systemStatusBar\0"))) };
    }

    fn status_item(this, length: f64) -> *u8 {
        return rt::msg_id_f64(this._obj, rt::sel(#str_ptr("statusItemWithLength:\0")), length);
    }
}

struct StatusItem {
    opaque _obj: *u8,
}

impl StatusItem {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_obj(obj: *u8) -> StatusItem {
        return StatusItem { _obj: obj };
    }

    // The status item's button (an `NSStatusBarButton*`), or `None` when the
    // item uses a custom view instead.
    fn button(this) -> option::Option[*u8] {
        let btn: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("button\0")));
        if btn == { 0 as *u8 } {
            return option::Option[*u8]::None;
        }
        return option::some(btn);
    }

    fn set_menu(this, menu: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setMenu:\0")), menu);
    }

    fn set_length(this, length: f64) {
        rt::msg_void_f64(this._obj, rt::sel(#str_ptr("setLength:\0")), length);
    }
}

struct StatusBarButton {
    opaque _obj: *u8,
}

impl StatusBarButton {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_obj(obj: *u8) -> StatusBarButton {
        return StatusBarButton { _obj: obj };
    }

    fn set_title(this, title: str) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setTitle:\0")), bridge::cplus_str_to_nsstring(title));
    }

    fn set_image(this, image: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setImage:\0")), image);
    }

    fn set_on_click(this, cb: fn(*u8)) {
        rt::attach_callback(this._obj, cb);
    }
}

struct TouchBar {
    _obj: *u8,
}

impl TouchBar {
    fn raw(this) -> *u8 { return this._obj; }

    fn drop(ref this) {
        rt::release(this._obj);
    }

    fn new() -> TouchBar {
        return TouchBar { _obj: rt::alloc_init(#str_ptr("NSTouchBar\0")) };
    }

    fn set_delegate(this, delegate: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setDelegate:\0")), delegate);
    }
}

struct TouchBarItem {
    _obj: *u8,
}

impl TouchBarItem {
    fn raw(this) -> *u8 { return this._obj; }

    fn drop(ref this) {
        rt::release(this._obj);
    }

    fn new(identifier: str) -> TouchBarItem {
        let cls: *u8 = rt::get_class(#str_ptr("NSTouchBarItem\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        let ns_id: *u8 = bridge::cplus_str_to_nsstring(identifier);
        return TouchBarItem { _obj: rt::msg_id_id(alloced, rt::sel(#str_ptr("initWithIdentifier:\0")), ns_id) };
    }
}
