// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSColorPicking.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `NSButtonCell` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ButtonCell {
    opaque _obj: *u8,
}

impl ButtonCell {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSColor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Color {
    opaque _obj: *u8,
}

impl Color {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSColorPanel` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ColorPanel {
    opaque _obj: *u8,
}

impl ColorPanel {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSColorList` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ColorList {
    opaque _obj: *u8,
}

impl ColorList {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSImage` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Image {
    opaque _obj: *u8,
}

impl Image {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSView` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct View {
    opaque _obj: *u8,
}

impl View {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSColorPickingDefault` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `initWithPickerMask:colorPanel:`: arg shape (only <=5 object args modelled)
// SKIPPED `setMode:`: arg shape (only <=5 object args modelled)
// SKIPPED `minContentSize`: return `NSSize` not modelled
fn ColorPickingDefault_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn ColorPickingDefault_noop_v_1(user: *u8, a0: *u8) { return; }
fn ColorPickingDefault_noop_v_2(user: *u8, a0: *u8, a1: *u8) { return; }

struct ColorPickingDefault {
    provide_new_button_image: fn(*u8) -> *u8,
    insert_new_button_image_in: fn(*u8, *u8, *u8),
    view_size_changed: fn(*u8, *u8),
    alpha_control_added_or_removed: fn(*u8, *u8),
    attach_color_list: fn(*u8, *u8),
    detach_color_list: fn(*u8, *u8),
    button_tool_tip: fn(*u8) -> *u8,
    opaque user: *u8,
}

fn ColorPickingDefault_new(user: *u8) -> ColorPickingDefault {
    return ColorPickingDefault { provide_new_button_image: ColorPickingDefault_noop_id_0, insert_new_button_image_in: ColorPickingDefault_noop_v_2, view_size_changed: ColorPickingDefault_noop_v_1, alpha_control_added_or_removed: ColorPickingDefault_noop_v_1, attach_color_list: ColorPickingDefault_noop_v_1, detach_color_list: ColorPickingDefault_noop_v_1, button_tool_tip: ColorPickingDefault_noop_id_0, user: user };
}

impl ColorPickingDefault {
    fn set_provide_new_button_image(ref this, handler: fn(*u8) -> *u8) {
        this.provide_new_button_image = handler;
        return;
    }
    fn set_insert_new_button_image_in(ref this, handler: fn(*u8, *u8, *u8)) {
        this.insert_new_button_image_in = handler;
        return;
    }
    fn set_view_size_changed(ref this, handler: fn(*u8, *u8)) {
        this.view_size_changed = handler;
        return;
    }
    fn set_alpha_control_added_or_removed(ref this, handler: fn(*u8, *u8)) {
        this.alpha_control_added_or_removed = handler;
        return;
    }
    fn set_attach_color_list(ref this, handler: fn(*u8, *u8)) {
        this.attach_color_list = handler;
        return;
    }
    fn set_detach_color_list(ref this, handler: fn(*u8, *u8)) {
        this.detach_color_list = handler;
        return;
    }
    fn set_button_tool_tip(ref this, handler: fn(*u8) -> *u8) {
        this.button_tool_tip = handler;
        return;
    }
}

fn ColorPickingDefault_key() -> *u8 { return { 28672 as *u8 }; }

fn ColorPickingDefault_provide_new_button_image_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *ColorPickingDefault = { synth::get_associated(self_obj, ColorPickingDefault_key()) as *ColorPickingDefault };
    let f: fn(*u8) -> *u8 = { (*c).provide_new_button_image };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn ColorPickingDefault_insert_new_button_image_in_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8) {
    let c: *ColorPickingDefault = { synth::get_associated(self_obj, ColorPickingDefault_key()) as *ColorPickingDefault };
    let f: fn(*u8, *u8, *u8) = { (*c).insert_new_button_image_in };
    let u: *u8 = { (*c).user };
    f(u, a0, a1);
    return;
}

fn ColorPickingDefault_view_size_changed_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *ColorPickingDefault = { synth::get_associated(self_obj, ColorPickingDefault_key()) as *ColorPickingDefault };
    let f: fn(*u8, *u8) = { (*c).view_size_changed };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn ColorPickingDefault_alpha_control_added_or_removed_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *ColorPickingDefault = { synth::get_associated(self_obj, ColorPickingDefault_key()) as *ColorPickingDefault };
    let f: fn(*u8, *u8) = { (*c).alpha_control_added_or_removed };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn ColorPickingDefault_attach_color_list_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *ColorPickingDefault = { synth::get_associated(self_obj, ColorPickingDefault_key()) as *ColorPickingDefault };
    let f: fn(*u8, *u8) = { (*c).attach_color_list };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn ColorPickingDefault_detach_color_list_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *ColorPickingDefault = { synth::get_associated(self_obj, ColorPickingDefault_key()) as *ColorPickingDefault };
    let f: fn(*u8, *u8) = { (*c).detach_color_list };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn ColorPickingDefault_button_tool_tip_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *ColorPickingDefault = { synth::get_associated(self_obj, ColorPickingDefault_key()) as *ColorPickingDefault };
    let f: fn(*u8) -> *u8 = { (*c).button_tool_tip };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_ColorPickingDefault(ctx: *ColorPickingDefault) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_ColorPickingDefault\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_provide_new_button_image: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("provideNewButtonImage\0")), ColorPickingDefault_provide_new_button_image_imp, #str_ptr("@@:\0"));
        let add_insert_new_button_image_in: i8 = synth::add_method_v_2id(cls, rt::sel(#str_ptr("insertNewButtonImage:in:\0")), ColorPickingDefault_insert_new_button_image_in_imp, #str_ptr("v@:@@\0"));
        let add_view_size_changed: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("viewSizeChanged:\0")), ColorPickingDefault_view_size_changed_imp, #str_ptr("v@:@\0"));
        let add_alpha_control_added_or_removed: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("alphaControlAddedOrRemoved:\0")), ColorPickingDefault_alpha_control_added_or_removed_imp, #str_ptr("v@:@\0"));
        let add_attach_color_list: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("attachColorList:\0")), ColorPickingDefault_attach_color_list_imp, #str_ptr("v@:@\0"));
        let add_detach_color_list: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("detachColorList:\0")), ColorPickingDefault_detach_color_list_imp, #str_ptr("v@:@\0"));
        let add_button_tool_tip: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("buttonToolTip\0")), ColorPickingDefault_button_tool_tip_imp, #str_ptr("@@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, ColorPickingDefault_key(), { ctx as *u8 });
    return d;
}

// `NSColorPickingCustom` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `supportsMode:`: arg shape (only <=5 object args modelled)
// SKIPPED `provideNewView:`: arg shape (only <=5 object args modelled)
fn ColorPickingCustom_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }
fn ColorPickingCustom_noop_v_1(user: *u8, a0: *u8) { return; }

struct ColorPickingCustom {
    current_mode: fn(*u8) -> u64,
    set_color: fn(*u8, *u8),
    opaque user: *u8,
}

fn ColorPickingCustom_new(user: *u8) -> ColorPickingCustom {
    return ColorPickingCustom { current_mode: ColorPickingCustom_noop_u64_0, set_color: ColorPickingCustom_noop_v_1, user: user };
}

impl ColorPickingCustom {
    fn set_current_mode(ref this, handler: fn(*u8) -> u64) {
        this.current_mode = handler;
        return;
    }
    fn set_set_color(ref this, handler: fn(*u8, *u8)) {
        this.set_color = handler;
        return;
    }
}

fn ColorPickingCustom_key() -> *u8 { return { 28673 as *u8 }; }

fn ColorPickingCustom_current_mode_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *ColorPickingCustom = { synth::get_associated(self_obj, ColorPickingCustom_key()) as *ColorPickingCustom };
    let f: fn(*u8) -> u64 = { (*c).current_mode };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn ColorPickingCustom_set_color_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *ColorPickingCustom = { synth::get_associated(self_obj, ColorPickingCustom_key()) as *ColorPickingCustom };
    let f: fn(*u8, *u8) = { (*c).set_color };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn create_ColorPickingCustom(ctx: *ColorPickingCustom) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_ColorPickingCustom\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_current_mode: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("currentMode\0")), ColorPickingCustom_current_mode_imp, #str_ptr("Q@:\0"));
        let add_set_color: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("setColor:\0")), ColorPickingCustom_set_color_imp, #str_ptr("v@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, ColorPickingCustom_key(), { ctx as *u8 });
    return d;
}

