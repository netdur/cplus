// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSTextSelection.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/vec" as vec;

// `NSTextSelectionAffinity` (NS_ENUM)
enum TextSelectionAffinity {
    Upstream,
    Downstream,
}

fn text_selection_affinity_raw(v: TextSelectionAffinity) -> i64 {
    return match v {
        TextSelectionAffinity::Upstream => { 0 as i64 },
        TextSelectionAffinity::Downstream => { 1 as i64 },
    };
}

fn text_selection_affinity_from_raw(raw: i64) -> TextSelectionAffinity {
    if raw == (1 as i64) { return TextSelectionAffinity::Downstream; }
    return TextSelectionAffinity::Upstream;
}

// `NSTextSelectionGranularity` (NS_ENUM)
enum TextSelectionGranularity {
    Character,
    Word,
    Paragraph,
    Line,
    Sentence,
}

fn text_selection_granularity_raw(v: TextSelectionGranularity) -> i64 {
    return match v {
        TextSelectionGranularity::Character => { 0 as i64 },
        TextSelectionGranularity::Word => { 1 as i64 },
        TextSelectionGranularity::Paragraph => { 2 as i64 },
        TextSelectionGranularity::Line => { 3 as i64 },
        TextSelectionGranularity::Sentence => { 4 as i64 },
    };
}

fn text_selection_granularity_from_raw(raw: i64) -> TextSelectionGranularity {
    if raw == (1 as i64) { return TextSelectionGranularity::Word; }
    if raw == (2 as i64) { return TextSelectionGranularity::Paragraph; }
    if raw == (3 as i64) { return TextSelectionGranularity::Line; }
    if raw == (4 as i64) { return TextSelectionGranularity::Sentence; }
    return TextSelectionGranularity::Character;
}

// `NSTextRange` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextRange {
    opaque _obj: *u8,
}

impl TextRange {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TextRange { return TextRange { _obj: ptr }; }

}

// `NSTextSelection` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct TextSelection {
    _obj: *u8,
}

impl TextSelection {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TextSelection { return TextSelection { _obj: ptr }; }

    // SKIPPED `initWithRanges:affinity:granularity:`: init with an NSArray<id> param not modelled
    // SKIPPED `initWithCoder:`: extra init variant (one `new` per type)
    // SKIPPED `initWithRange:affinity:granularity:`: extra init variant (one `new` per type)
    // SKIPPED `initWithLocation:affinity:`: extra init variant (one `new` per type)
    // SKIPPED `init`: extra init variant (one `new` per type)
    fn text_selection_with_text_ranges(this, text_ranges: vec::Vec[TextRange]) -> TextSelection {
        let arr_text_ranges: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_text_ranges: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let n_text_ranges: usize = text_ranges.count();
        var i_text_ranges: usize = 0 as usize;
        while i_text_ranges < n_text_ranges {
            match text_ranges.at(i_text_ranges) {
                option::Option[TextRange]::Some(e_text_ranges) => { rt::msg_void_id(arr_text_ranges, add_sel_text_ranges, e_text_ranges.raw()); }
                option::Option[TextRange]::None => {}
            }
            i_text_ranges = i_text_ranges +% (1 as usize);
        }
        return TextSelection::from_raw(rt::msg_id_id(this._obj, rt::sel(#str_ptr("textSelectionWithTextRanges:\0")), arr_text_ranges));
    }

    fn text_ranges(this) -> vec::Vec[TextRange] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("textRanges\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[TextRange] = vec::Vec[TextRange]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(TextRange::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn granularity(this) -> TextSelectionGranularity {
        return text_selection_granularity_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("granularity\0"))));
    }

    fn affinity(this) -> TextSelectionAffinity {
        return text_selection_affinity_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("affinity\0"))));
    }

    fn is_transient(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("isTransient\0"))) != (0 as i8);
    }

    fn anchor_position_offset(this) -> f64 {
        return rt::msg_f64(this._obj, rt::sel(#str_ptr("anchorPositionOffset\0")));
    }

    fn set_anchor_position_offset(this, anchor_position_offset: f64) {
        rt::msg_void_f64(this._obj, rt::sel(#str_ptr("setAnchorPositionOffset:\0")), anchor_position_offset);
    }

    fn is_logical(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("isLogical\0"))) != (0 as i8);
    }

    fn set_logical(this, logical: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setLogical:\0")), logical as i8);
    }

    fn secondary_selection_location(this) -> option::Option[TextLocation] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("secondarySelectionLocation\0")));
        if obj == { 0 as *u8 } {
            return option::Option[TextLocation]::None;
        }
        return option::some(TextLocation::from_raw(obj));
    }

    fn set_secondary_selection_location(this, secondary_selection_location: TextLocation) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setSecondarySelectionLocation:\0")), secondary_selection_location.raw());
    }

    // SKIPPED `typingAttributes`: return `NSDictionary<NSAttributedStringKey,id> * _Nonnull` — NSDictionary value `id` not modelled (NSString / NSNumber only)
    // SKIPPED `setTypingAttributes:`: param `NSDictionary<NSAttributedStringKey,id> * _Nonnull` — generic collection
    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSTextLocation` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct TextLocation {
    opaque _obj: *u8,
}

impl TextLocation {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TextLocation { return TextLocation { _obj: ptr }; }

}

