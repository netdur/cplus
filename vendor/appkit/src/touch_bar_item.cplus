// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSTouchBarItem.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;

// `NSTouchBar` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TouchBar {
    opaque _obj: *u8,
}

impl TouchBar {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TouchBar { return TouchBar { _obj: ptr }; }

}

// `NSViewController` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ViewController {
    opaque _obj: *u8,
}

impl ViewController {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> ViewController { return ViewController { _obj: ptr }; }

}

// `NSView` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct View {
    opaque _obj: *u8,
}

impl View {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> View { return View { _obj: ptr }; }

}

// `NSImage` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Image {
    opaque _obj: *u8,
}

impl Image {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Image { return Image { _obj: ptr }; }

}

// `NSGestureRecognizer` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct GestureRecognizer {
    opaque _obj: *u8,
}

impl GestureRecognizer {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> GestureRecognizer { return GestureRecognizer { _obj: ptr }; }

}

// `NSString` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct String {
    opaque _obj: *u8,
}

impl String {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> String { return String { _obj: ptr }; }

}

// `NSTouchBarItem` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct TouchBarItem {
    _obj: *u8,
}

impl TouchBarItem {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TouchBarItem { return TouchBarItem { _obj: ptr }; }

    fn new(identifier: str) -> TouchBarItem {
        let cls: *u8 = rt::get_class(#str_ptr("NSTouchBarItem\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        return TouchBarItem { _obj: rt::msg_id_id(alloced, rt::sel(#str_ptr("initWithIdentifier:\0")), bridge::nsstring(identifier)) };
    }

    // SKIPPED `initWithCoder:`: extra init variant (one `new` per type)
    // SKIPPED `init`: extra init variant (one `new` per type)
    // SKIPPED `identifier`: return `API_AVAILABLE NSTouchBarItemIdentifier` — unmapped type `API_AVAILABLE NSTouchBarItemIdentifier`
    // SKIPPED `visibilityPriority`: return `NSTouchBarItemPriority` — unmapped type `NSTouchBarItemPriority`
    // SKIPPED `setVisibilityPriority:`: param `NSTouchBarItemPriority` — unmapped type `NSTouchBarItemPriority`
    fn view(this) -> option::Option[View] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("view\0")));
        if obj == { 0 as *u8 } {
            return option::Option[View]::None;
        }
        return option::some(View::from_raw(obj));
    }

    fn view_controller(this) -> option::Option[ViewController] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("viewController\0")));
        if obj == { 0 as *u8 } {
            return option::Option[ViewController]::None;
        }
        return option::some(ViewController::from_raw(obj));
    }

    fn customization_label(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("customizationLabel\0")));
    }

    fn is_visible(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("isVisible\0"))) != (0 as i8);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

