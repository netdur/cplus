// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSInputManager.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `NSArray` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Array {
    opaque _obj: *u8,
}

impl Array {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSAttributedString` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct AttributedString {
    opaque _obj: *u8,
}

impl AttributedString {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSEvent` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Event {
    opaque _obj: *u8,
}

impl Event {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSInputServer` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct InputServer {
    opaque _obj: *u8,
}

impl InputServer {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSImage` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Image {
    opaque _obj: *u8,
}

impl Image {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSInputManager` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct InputManager {
    _obj: *u8,
}

impl InputManager {
    fn raw(this) -> *u8 { return this._obj; }

    fn current_input_manager() -> option::Option[InputManager] {
        let obj: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSInputManager\0")), rt::sel(#str_ptr("currentInputManager\0")));
        if obj == { 0 as *u8 } {
            return option::Option[InputManager]::None;
        }
        return option::some(InputManager { _obj: rt::retain(obj) });
    }

    fn cycle_to_next_input_language(sender: *u8) {
        rt::msg_void_id(rt::get_class(#str_ptr("NSInputManager\0")), rt::sel(#str_ptr("cycleToNextInputLanguage:\0")), sender);
    }

    fn cycle_to_next_input_server_in_language(sender: *u8) {
        rt::msg_void_id(rt::get_class(#str_ptr("NSInputManager\0")), rt::sel(#str_ptr("cycleToNextInputServerInLanguage:\0")), sender);
    }

    // SKIPPED `initWithName:host:`: init arg shape not yet modelled
    fn localized_input_manager_name(this) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("localizedInputManagerName\0")));
        return bridge::to_text_option(ns);
    }

    fn marked_text_abandoned(this, cli: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("markedTextAbandoned:\0")), cli);
    }

    fn marked_text_selection_changed_client(this, new_sel: rt::Range, cli: *u8) {
        rt::msg_void_range_id(this._obj, rt::sel(#str_ptr("markedTextSelectionChanged:client:\0")), new_sel, cli);
    }

    fn wants_to_interpret_all_keystrokes(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("wantsToInterpretAllKeystrokes\0"))) != (0 as i8);
    }

    fn language(this) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("language\0")));
        return bridge::to_text_option(ns);
    }

    fn image(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("image\0")));
        return bridge::obj_option(obj);
    }

    fn server(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("server\0")));
        return bridge::obj_option(obj);
    }

    fn wants_to_handle_mouse_events(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("wantsToHandleMouseEvents\0"))) != (0 as i8);
    }

    // SKIPPED `handleMouseEvent:`: (return, arg) shape not yet modelled
    fn wants_to_delay_text_change_notifications(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("wantsToDelayTextChangeNotifications\0"))) != (0 as i8);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSTextInput` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `doCommandBySelector:`: arg shape (only <=5 object args modelled)
// SKIPPED `setMarkedText:selectedRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `hasMarkedText`: no `i8`-return / 0-arg IMP shape
// SKIPPED `attributedSubstringFromRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `markedRange`: return `NSRange` not modelled
// SKIPPED `selectedRange`: return `NSRange` not modelled
// SKIPPED `firstRectForCharacterRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `characterIndexForPoint:`: arg shape (only <=5 object args modelled)
fn TextInput_noop_i64_0(user: *u8) -> i64 { return 0 as i64; }
fn TextInput_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn TextInput_noop_v_0(user: *u8) { return; }
fn TextInput_noop_v_1(user: *u8, a0: *u8) { return; }

struct TextInput {
    insert_text: fn(*u8, *u8),
    unmark_text: fn(*u8),
    conversation_identifier: fn(*u8) -> i64,
    valid_attributes_for_marked_text: fn(*u8) -> *u8,
    opaque user: *u8,
}

fn TextInput_new(user: *u8) -> TextInput {
    return TextInput { insert_text: TextInput_noop_v_1, unmark_text: TextInput_noop_v_0, conversation_identifier: TextInput_noop_i64_0, valid_attributes_for_marked_text: TextInput_noop_id_0, user: user };
}

impl TextInput {
    fn set_insert_text(ref this, handler: fn(*u8, *u8)) {
        this.insert_text = handler;
        return;
    }
    fn set_unmark_text(ref this, handler: fn(*u8)) {
        this.unmark_text = handler;
        return;
    }
    fn set_conversation_identifier(ref this, handler: fn(*u8) -> i64) {
        this.conversation_identifier = handler;
        return;
    }
    fn set_valid_attributes_for_marked_text(ref this, handler: fn(*u8) -> *u8) {
        this.valid_attributes_for_marked_text = handler;
        return;
    }
}

fn TextInput_key() -> *u8 { return { 28672 as *u8 }; }

fn TextInput_insert_text_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *TextInput = { synth::get_associated(self_obj, TextInput_key()) as *TextInput };
    let f: fn(*u8, *u8) = { (*c).insert_text };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn TextInput_unmark_text_imp(self_obj: *u8, cmd: *u8) {
    let c: *TextInput = { synth::get_associated(self_obj, TextInput_key()) as *TextInput };
    let f: fn(*u8) = { (*c).unmark_text };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn TextInput_conversation_identifier_imp(self_obj: *u8, cmd: *u8) -> i64 {
    let c: *TextInput = { synth::get_associated(self_obj, TextInput_key()) as *TextInput };
    let f: fn(*u8) -> i64 = { (*c).conversation_identifier };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn TextInput_valid_attributes_for_marked_text_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *TextInput = { synth::get_associated(self_obj, TextInput_key()) as *TextInput };
    let f: fn(*u8) -> *u8 = { (*c).valid_attributes_for_marked_text };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_TextInput(ctx: *TextInput) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_TextInput\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_insert_text: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("insertText:\0")), TextInput_insert_text_imp, #str_ptr("v@:@\0"));
        let add_unmark_text: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("unmarkText\0")), TextInput_unmark_text_imp, #str_ptr("v@:\0"));
        let add_conversation_identifier: i8 = synth::add_method_i64_0id(cls, rt::sel(#str_ptr("conversationIdentifier\0")), TextInput_conversation_identifier_imp, #str_ptr("q@:\0"));
        let add_valid_attributes_for_marked_text: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("validAttributesForMarkedText\0")), TextInput_valid_attributes_for_marked_text_imp, #str_ptr("@@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, TextInput_key(), { ctx as *u8 });
    return d;
}

