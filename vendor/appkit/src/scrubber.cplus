// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSScrubber.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `NSScrubberAlignment` (NS_ENUM)
enum ScrubberAlignment {
    None,
    Leading,
    Trailing,
    Center,
}

fn scrubber_alignment_raw(v: ScrubberAlignment) -> i64 {
    return match v {
        ScrubberAlignment::None => { 0 as i64 },
        ScrubberAlignment::Leading => { 1 as i64 },
        ScrubberAlignment::Trailing => { 2 as i64 },
        ScrubberAlignment::Center => { 3 as i64 },
    };
}

fn scrubber_alignment_from_raw(raw: i64) -> ScrubberAlignment {
    if raw == (1 as i64) { return ScrubberAlignment::Leading; }
    if raw == (2 as i64) { return ScrubberAlignment::Trailing; }
    if raw == (3 as i64) { return ScrubberAlignment::Center; }
    return ScrubberAlignment::None;
}

// `NSScrubberMode` (NS_ENUM)
enum ScrubberMode {
    Fixed,
    Free,
}

fn scrubber_mode_raw(v: ScrubberMode) -> i64 {
    return match v {
        ScrubberMode::Fixed => { 0 as i64 },
        ScrubberMode::Free => { 1 as i64 },
    };
}

fn scrubber_mode_from_raw(raw: i64) -> ScrubberMode {
    if raw == (1 as i64) { return ScrubberMode::Free; }
    return ScrubberMode::Fixed;
}

#[repr(C)]
struct Scrubber_perform_sequential_batch_updates_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8),
    opaque ctx: *u8,
}

fn Scrubber_perform_sequential_batch_updates_invoke(block: *u8) {
    let bl: *Scrubber_perform_sequential_batch_updates_block = { block as *Scrubber_perform_sequential_batch_updates_block };
    let f: fn(*u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx);
    return;
}

// `NSScrubber` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct Scrubber {
    _obj: *u8,
}

impl Scrubber {
    fn raw(this) -> *u8 { return this._obj; }

    // SKIPPED `initWithFrame:`: param `NSRect` — unmapped type `NSRect`
    // SKIPPED `initWithCoder:`: extra init variant (one `new` per type)
    fn reload_data(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("reloadData\0")));
    }

    fn perform_sequential_batch_updates(this, cb: fn(*u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: Scrubber_perform_sequential_batch_updates_block = Scrubber_perform_sequential_batch_updates_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: Scrubber_perform_sequential_batch_updates_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("performSequentialBatchUpdates:\0")), bp);
        return;
    }

    fn insert_items_at_indexes(this, indexes: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("insertItemsAtIndexes:\0")), indexes);
    }

    fn remove_items_at_indexes(this, indexes: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("removeItemsAtIndexes:\0")), indexes);
    }

    fn reload_items_at_indexes(this, indexes: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("reloadItemsAtIndexes:\0")), indexes);
    }

    // SKIPPED `moveItemAtIndex:toIndex:`: (return, arg) shape not yet modelled
    // SKIPPED `scrollItemAtIndex:toAlignment:`: (return, arg) shape not yet modelled
    fn item_view_for_item_at_index(this, index: i64) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id_i64(this._obj, rt::sel(#str_ptr("itemViewForItemAtIndex:\0")), index);
        return bridge::obj_option(obj);
    }

    // SKIPPED `registerClass:forItemIdentifier:`: param `Class _Nullable` — unmapped type `Class`
    // SKIPPED `registerNib:forItemIdentifier:`: (return, arg) shape not yet modelled
    // SKIPPED `makeItemWithIdentifier:owner:`: (return, arg) shape not yet modelled
    // SKIPPED `dataSource`: return `id<NSScrubberDataSource> _Nullable` — generic collection
    // SKIPPED `setDataSource:`: param `id<NSScrubberDataSource> _Nullable` — generic collection
    // SKIPPED `delegate`: return `id<NSScrubberDelegate> _Nullable` — generic collection
    // SKIPPED `setDelegate:`: param `id<NSScrubberDelegate> _Nullable` — generic collection
    fn scrubber_layout(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("scrubberLayout\0")));
    }

    fn set_scrubber_layout(this, scrubber_layout: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setScrubberLayout:\0")), scrubber_layout);
    }

    fn number_of_items(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("numberOfItems\0")));
    }

    fn highlighted_index(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("highlightedIndex\0")));
    }

    fn selected_index(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("selectedIndex\0")));
    }

    // SKIPPED `setSelectedIndex:`: (return, arg) shape not yet modelled
    fn mode(this) -> ScrubberMode {
        return scrubber_mode_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("mode\0"))));
    }

    // SKIPPED `setMode:`: (return, arg) shape not yet modelled
    fn item_alignment(this) -> ScrubberAlignment {
        return scrubber_alignment_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("itemAlignment\0"))));
    }

    // SKIPPED `setItemAlignment:`: (return, arg) shape not yet modelled
    fn is_continuous(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("isContinuous\0"))) != (0 as i8);
    }

    fn set_continuous(this, continuous: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setContinuous:\0")), continuous as i8);
    }

    fn floats_selection_views(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("floatsSelectionViews\0"))) != (0 as i8);
    }

    fn set_floats_selection_views(this, floats_selection_views: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setFloatsSelectionViews:\0")), floats_selection_views as i8);
    }

    fn selection_background_style(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("selectionBackgroundStyle\0")));
        return bridge::obj_option(obj);
    }

    fn set_selection_background_style(this, selection_background_style: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setSelectionBackgroundStyle:\0")), selection_background_style);
    }

    fn selection_overlay_style(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("selectionOverlayStyle\0")));
        return bridge::obj_option(obj);
    }

    fn set_selection_overlay_style(this, selection_overlay_style: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setSelectionOverlayStyle:\0")), selection_overlay_style);
    }

    fn shows_arrow_buttons(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("showsArrowButtons\0"))) != (0 as i8);
    }

    fn set_shows_arrow_buttons(this, shows_arrow_buttons: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setShowsArrowButtons:\0")), shows_arrow_buttons as i8);
    }

    fn shows_additional_content_indicators(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("showsAdditionalContentIndicators\0"))) != (0 as i8);
    }

    fn set_shows_additional_content_indicators(this, shows_additional_content_indicators: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setShowsAdditionalContentIndicators:\0")), shows_additional_content_indicators as i8);
    }

    fn background_color(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("backgroundColor\0")));
        return bridge::obj_option(obj);
    }

    fn set_background_color(this, background_color: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setBackgroundColor:\0")), background_color);
    }

    fn background_view(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("backgroundView\0")));
        return bridge::obj_option(obj);
    }

    fn set_background_view(this, background_view: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setBackgroundView:\0")), background_view);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSScrubberItemView` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ScrubberItemView {
    opaque _obj: *u8,
}

impl ScrubberItemView {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSScrubberSelectionView` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ScrubberSelectionView {
    opaque _obj: *u8,
}

impl ScrubberSelectionView {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSScrubberLayout` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ScrubberLayout {
    opaque _obj: *u8,
}

impl ScrubberLayout {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSPanGestureRecognizer` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct PanGestureRecognizer {
    opaque _obj: *u8,
}

impl PanGestureRecognizer {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSPressGestureRecognizer` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct PressGestureRecognizer {
    opaque _obj: *u8,
}

impl PressGestureRecognizer {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSButton` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Button {
    opaque _obj: *u8,
}

impl Button {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSNib` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Nib {
    opaque _obj: *u8,
}

impl Nib {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSScrubberSelectionStyle` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct ScrubberSelectionStyle {
    _obj: *u8,
}

impl ScrubberSelectionStyle {
    fn raw(this) -> *u8 { return this._obj; }

    fn new() -> ScrubberSelectionStyle {
        let cls: *u8 = rt::get_class(#str_ptr("NSScrubberSelectionStyle\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        return ScrubberSelectionStyle { _obj: rt::msg_id(alloced, rt::sel(#str_ptr("init\0"))) };
    }

    // SKIPPED `initWithCoder:`: extra init variant (one `new` per type)
    fn make_selection_view(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("makeSelectionView\0")));
        return bridge::obj_option(obj);
    }

    fn outline_overlay_style() -> ScrubberSelectionStyle {
        return ScrubberSelectionStyle { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("NSScrubberSelectionStyle\0")), rt::sel(#str_ptr("outlineOverlayStyle\0")))) };
    }

    fn rounded_background_style() -> ScrubberSelectionStyle {
        return ScrubberSelectionStyle { _obj: rt::retain(rt::msg_id(rt::get_class(#str_ptr("NSScrubberSelectionStyle\0")), rt::sel(#str_ptr("roundedBackgroundStyle\0")))) };
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSScrubberDataSource` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `scrubber:viewForItemAtIndex:`: arg shape (only <=5 object args modelled)
fn ScrubberDataSource_noop_i64_1(user: *u8, a0: *u8) -> i64 { return 0 as i64; }

struct ScrubberDataSource {
    number_of_items_for_scrubber: fn(*u8, *u8) -> i64,
    opaque user: *u8,
}

fn ScrubberDataSource_new(user: *u8) -> ScrubberDataSource {
    return ScrubberDataSource { number_of_items_for_scrubber: ScrubberDataSource_noop_i64_1, user: user };
}

impl ScrubberDataSource {
    fn set_number_of_items_for_scrubber(ref this, handler: fn(*u8, *u8) -> i64) {
        this.number_of_items_for_scrubber = handler;
        return;
    }
}

fn ScrubberDataSource_key() -> *u8 { return { 28672 as *u8 }; }

fn ScrubberDataSource_number_of_items_for_scrubber_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> i64 {
    let c: *ScrubberDataSource = { synth::get_associated(self_obj, ScrubberDataSource_key()) as *ScrubberDataSource };
    let f: fn(*u8, *u8) -> i64 = { (*c).number_of_items_for_scrubber };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn create_ScrubberDataSource(ctx: *ScrubberDataSource) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_ScrubberDataSource\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_number_of_items_for_scrubber: i8 = synth::add_method_i64_1id(cls, rt::sel(#str_ptr("numberOfItemsForScrubber:\0")), ScrubberDataSource_number_of_items_for_scrubber_imp, #str_ptr("q@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, ScrubberDataSource_key(), { ctx as *u8 });
    return d;
}

// `NSScrubberDelegate` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `scrubber:didSelectItemAtIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `scrubber:didHighlightItemAtIndex:`: arg shape (only <=5 object args modelled)
// SKIPPED `scrubber:didChangeVisibleRange:`: arg shape (only <=5 object args modelled)
fn ScrubberDelegate_noop_v_1(user: *u8, a0: *u8) { return; }

struct ScrubberDelegate {
    did_begin_interacting_with_scrubber: fn(*u8, *u8),
    did_finish_interacting_with_scrubber: fn(*u8, *u8),
    did_cancel_interacting_with_scrubber: fn(*u8, *u8),
    opaque user: *u8,
}

fn ScrubberDelegate_new(user: *u8) -> ScrubberDelegate {
    return ScrubberDelegate { did_begin_interacting_with_scrubber: ScrubberDelegate_noop_v_1, did_finish_interacting_with_scrubber: ScrubberDelegate_noop_v_1, did_cancel_interacting_with_scrubber: ScrubberDelegate_noop_v_1, user: user };
}

impl ScrubberDelegate {
    fn set_did_begin_interacting_with_scrubber(ref this, handler: fn(*u8, *u8)) {
        this.did_begin_interacting_with_scrubber = handler;
        return;
    }
    fn set_did_finish_interacting_with_scrubber(ref this, handler: fn(*u8, *u8)) {
        this.did_finish_interacting_with_scrubber = handler;
        return;
    }
    fn set_did_cancel_interacting_with_scrubber(ref this, handler: fn(*u8, *u8)) {
        this.did_cancel_interacting_with_scrubber = handler;
        return;
    }
}

fn ScrubberDelegate_key() -> *u8 { return { 28673 as *u8 }; }

fn ScrubberDelegate_did_begin_interacting_with_scrubber_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *ScrubberDelegate = { synth::get_associated(self_obj, ScrubberDelegate_key()) as *ScrubberDelegate };
    let f: fn(*u8, *u8) = { (*c).did_begin_interacting_with_scrubber };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn ScrubberDelegate_did_finish_interacting_with_scrubber_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *ScrubberDelegate = { synth::get_associated(self_obj, ScrubberDelegate_key()) as *ScrubberDelegate };
    let f: fn(*u8, *u8) = { (*c).did_finish_interacting_with_scrubber };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn ScrubberDelegate_did_cancel_interacting_with_scrubber_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *ScrubberDelegate = { synth::get_associated(self_obj, ScrubberDelegate_key()) as *ScrubberDelegate };
    let f: fn(*u8, *u8) = { (*c).did_cancel_interacting_with_scrubber };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn create_ScrubberDelegate(ctx: *ScrubberDelegate) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_ScrubberDelegate\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_did_begin_interacting_with_scrubber: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("didBeginInteractingWithScrubber:\0")), ScrubberDelegate_did_begin_interacting_with_scrubber_imp, #str_ptr("v@:@\0"));
        let add_did_finish_interacting_with_scrubber: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("didFinishInteractingWithScrubber:\0")), ScrubberDelegate_did_finish_interacting_with_scrubber_imp, #str_ptr("v@:@\0"));
        let add_did_cancel_interacting_with_scrubber: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("didCancelInteractingWithScrubber:\0")), ScrubberDelegate_did_cancel_interacting_with_scrubber_imp, #str_ptr("v@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, ScrubberDelegate_key(), { ctx as *u8 });
    return d;
}

