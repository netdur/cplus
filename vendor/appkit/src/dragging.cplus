// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSDragging.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `NSDraggingItem` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct DraggingItem {
    opaque _obj: *u8,
}

impl DraggingItem {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSDraggingSession` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct DraggingSession {
    opaque _obj: *u8,
}

impl DraggingSession {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSImage` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Image {
    opaque _obj: *u8,
}

impl Image {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSPasteboard` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Pasteboard {
    opaque _obj: *u8,
}

impl Pasteboard {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSView` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct View {
    opaque _obj: *u8,
}

impl View {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSWindow` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Window {
    opaque _obj: *u8,
}

impl Window {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSURL` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct URL {
    opaque _obj: *u8,
}

impl URL {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSDraggingInfo` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `slideDraggedImageTo:`: arg shape (only <=5 object args modelled)
// SKIPPED `enumerateDraggingItemsWithOptions:forView:classes:searchOptions:usingBlock:`: arg shape (only <=5 object args modelled)
// SKIPPED `draggingLocation`: return `NSPoint` not modelled
// SKIPPED `draggedImageLocation`: return `NSPoint` not modelled
// SKIPPED `draggingSource`: return `NS_SWIFT_UI_ACTOR id` not modelled
// SKIPPED `setDraggingFormation:`: arg shape (only <=5 object args modelled)
// SKIPPED `animatesToDestination`: no `i8`-return / 0-arg IMP shape
// SKIPPED `setAnimatesToDestination:`: arg shape (only <=5 object args modelled)
// SKIPPED `setNumberOfValidItemsForDrop:`: arg shape (only <=5 object args modelled)
fn DraggingInfo_noop_i64_0(user: *u8) -> i64 { return 0 as i64; }
fn DraggingInfo_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn DraggingInfo_noop_id_1(user: *u8, a0: *u8) -> *u8 { return { 0 as *u8 }; }
fn DraggingInfo_noop_u64_0(user: *u8) -> u64 { return 0 as u64; }
fn DraggingInfo_noop_v_0(user: *u8) { return; }

struct DraggingInfo {
    names_of_promised_files_dropped_at_destination: fn(*u8, *u8) -> *u8,
    reset_spring_loading: fn(*u8),
    dragging_destination_window: fn(*u8) -> *u8,
    dragging_source_operation_mask: fn(*u8) -> u64,
    dragged_image: fn(*u8) -> *u8,
    dragging_pasteboard: fn(*u8) -> *u8,
    dragging_sequence_number: fn(*u8) -> i64,
    dragging_formation: fn(*u8) -> u64,
    number_of_valid_items_for_drop: fn(*u8) -> i64,
    spring_loading_highlight: fn(*u8) -> u64,
    opaque user: *u8,
}

fn DraggingInfo_new(user: *u8) -> DraggingInfo {
    return DraggingInfo { names_of_promised_files_dropped_at_destination: DraggingInfo_noop_id_1, reset_spring_loading: DraggingInfo_noop_v_0, dragging_destination_window: DraggingInfo_noop_id_0, dragging_source_operation_mask: DraggingInfo_noop_u64_0, dragged_image: DraggingInfo_noop_id_0, dragging_pasteboard: DraggingInfo_noop_id_0, dragging_sequence_number: DraggingInfo_noop_i64_0, dragging_formation: DraggingInfo_noop_u64_0, number_of_valid_items_for_drop: DraggingInfo_noop_i64_0, spring_loading_highlight: DraggingInfo_noop_u64_0, user: user };
}

impl DraggingInfo {
    fn set_names_of_promised_files_dropped_at_destination(ref this, handler: fn(*u8, *u8) -> *u8) {
        this.names_of_promised_files_dropped_at_destination = handler;
        return;
    }
    fn set_reset_spring_loading(ref this, handler: fn(*u8)) {
        this.reset_spring_loading = handler;
        return;
    }
    fn set_dragging_destination_window(ref this, handler: fn(*u8) -> *u8) {
        this.dragging_destination_window = handler;
        return;
    }
    fn set_dragging_source_operation_mask(ref this, handler: fn(*u8) -> u64) {
        this.dragging_source_operation_mask = handler;
        return;
    }
    fn set_dragged_image(ref this, handler: fn(*u8) -> *u8) {
        this.dragged_image = handler;
        return;
    }
    fn set_dragging_pasteboard(ref this, handler: fn(*u8) -> *u8) {
        this.dragging_pasteboard = handler;
        return;
    }
    fn set_dragging_sequence_number(ref this, handler: fn(*u8) -> i64) {
        this.dragging_sequence_number = handler;
        return;
    }
    fn set_dragging_formation(ref this, handler: fn(*u8) -> u64) {
        this.dragging_formation = handler;
        return;
    }
    fn set_number_of_valid_items_for_drop(ref this, handler: fn(*u8) -> i64) {
        this.number_of_valid_items_for_drop = handler;
        return;
    }
    fn set_spring_loading_highlight(ref this, handler: fn(*u8) -> u64) {
        this.spring_loading_highlight = handler;
        return;
    }
}

fn DraggingInfo_key() -> *u8 { return { 28672 as *u8 }; }

fn DraggingInfo_names_of_promised_files_dropped_at_destination_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> *u8 {
    let c: *DraggingInfo = { synth::get_associated(self_obj, DraggingInfo_key()) as *DraggingInfo };
    let f: fn(*u8, *u8) -> *u8 = { (*c).names_of_promised_files_dropped_at_destination };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn DraggingInfo_reset_spring_loading_imp(self_obj: *u8, cmd: *u8) {
    let c: *DraggingInfo = { synth::get_associated(self_obj, DraggingInfo_key()) as *DraggingInfo };
    let f: fn(*u8) = { (*c).reset_spring_loading };
    let u: *u8 = { (*c).user };
    f(u);
    return;
}

fn DraggingInfo_dragging_destination_window_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *DraggingInfo = { synth::get_associated(self_obj, DraggingInfo_key()) as *DraggingInfo };
    let f: fn(*u8) -> *u8 = { (*c).dragging_destination_window };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn DraggingInfo_dragging_source_operation_mask_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *DraggingInfo = { synth::get_associated(self_obj, DraggingInfo_key()) as *DraggingInfo };
    let f: fn(*u8) -> u64 = { (*c).dragging_source_operation_mask };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn DraggingInfo_dragged_image_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *DraggingInfo = { synth::get_associated(self_obj, DraggingInfo_key()) as *DraggingInfo };
    let f: fn(*u8) -> *u8 = { (*c).dragged_image };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn DraggingInfo_dragging_pasteboard_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *DraggingInfo = { synth::get_associated(self_obj, DraggingInfo_key()) as *DraggingInfo };
    let f: fn(*u8) -> *u8 = { (*c).dragging_pasteboard };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn DraggingInfo_dragging_sequence_number_imp(self_obj: *u8, cmd: *u8) -> i64 {
    let c: *DraggingInfo = { synth::get_associated(self_obj, DraggingInfo_key()) as *DraggingInfo };
    let f: fn(*u8) -> i64 = { (*c).dragging_sequence_number };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn DraggingInfo_dragging_formation_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *DraggingInfo = { synth::get_associated(self_obj, DraggingInfo_key()) as *DraggingInfo };
    let f: fn(*u8) -> u64 = { (*c).dragging_formation };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn DraggingInfo_number_of_valid_items_for_drop_imp(self_obj: *u8, cmd: *u8) -> i64 {
    let c: *DraggingInfo = { synth::get_associated(self_obj, DraggingInfo_key()) as *DraggingInfo };
    let f: fn(*u8) -> i64 = { (*c).number_of_valid_items_for_drop };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn DraggingInfo_spring_loading_highlight_imp(self_obj: *u8, cmd: *u8) -> u64 {
    let c: *DraggingInfo = { synth::get_associated(self_obj, DraggingInfo_key()) as *DraggingInfo };
    let f: fn(*u8) -> u64 = { (*c).spring_loading_highlight };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_DraggingInfo(ctx: *DraggingInfo) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_DraggingInfo\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_names_of_promised_files_dropped_at_destination: i8 = synth::add_method_id_1id(cls, rt::sel(#str_ptr("namesOfPromisedFilesDroppedAtDestination:\0")), DraggingInfo_names_of_promised_files_dropped_at_destination_imp, #str_ptr("@@:@\0"));
        let add_reset_spring_loading: i8 = synth::add_method_v_0id(cls, rt::sel(#str_ptr("resetSpringLoading\0")), DraggingInfo_reset_spring_loading_imp, #str_ptr("v@:\0"));
        let add_dragging_destination_window: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("draggingDestinationWindow\0")), DraggingInfo_dragging_destination_window_imp, #str_ptr("@@:\0"));
        let add_dragging_source_operation_mask: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("draggingSourceOperationMask\0")), DraggingInfo_dragging_source_operation_mask_imp, #str_ptr("Q@:\0"));
        let add_dragged_image: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("draggedImage\0")), DraggingInfo_dragged_image_imp, #str_ptr("@@:\0"));
        let add_dragging_pasteboard: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("draggingPasteboard\0")), DraggingInfo_dragging_pasteboard_imp, #str_ptr("@@:\0"));
        let add_dragging_sequence_number: i8 = synth::add_method_i64_0id(cls, rt::sel(#str_ptr("draggingSequenceNumber\0")), DraggingInfo_dragging_sequence_number_imp, #str_ptr("q@:\0"));
        let add_dragging_formation: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("draggingFormation\0")), DraggingInfo_dragging_formation_imp, #str_ptr("Q@:\0"));
        let add_number_of_valid_items_for_drop: i8 = synth::add_method_i64_0id(cls, rt::sel(#str_ptr("numberOfValidItemsForDrop\0")), DraggingInfo_number_of_valid_items_for_drop_imp, #str_ptr("q@:\0"));
        let add_spring_loading_highlight: i8 = synth::add_method_u64_0id(cls, rt::sel(#str_ptr("springLoadingHighlight\0")), DraggingInfo_spring_loading_highlight_imp, #str_ptr("Q@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, DraggingInfo_key(), { ctx as *u8 });
    return d;
}

// `NSDraggingSource` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `draggingSession:sourceOperationMaskForDraggingContext:`: arg shape (only <=5 object args modelled)
// SKIPPED `draggingSession:willBeginAtPoint:`: arg shape (only <=5 object args modelled)
// SKIPPED `draggingSession:movedToPoint:`: arg shape (only <=5 object args modelled)
// SKIPPED `draggingSession:endedAtPoint:operation:`: arg shape (only <=5 object args modelled)
fn DraggingSource_noop_i8_1(user: *u8, a0: *u8) -> i8 { return 0 as i8; }

struct DraggingSource {
    ignore_modifier_keys_for_dragging_session: fn(*u8, *u8) -> i8,
    opaque user: *u8,
}

fn DraggingSource_new(user: *u8) -> DraggingSource {
    return DraggingSource { ignore_modifier_keys_for_dragging_session: DraggingSource_noop_i8_1, user: user };
}

impl DraggingSource {
    fn set_ignore_modifier_keys_for_dragging_session(ref this, handler: fn(*u8, *u8) -> i8) {
        this.ignore_modifier_keys_for_dragging_session = handler;
        return;
    }
}

fn DraggingSource_key() -> *u8 { return { 28673 as *u8 }; }

fn DraggingSource_ignore_modifier_keys_for_dragging_session_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> i8 {
    let c: *DraggingSource = { synth::get_associated(self_obj, DraggingSource_key()) as *DraggingSource };
    let f: fn(*u8, *u8) -> i8 = { (*c).ignore_modifier_keys_for_dragging_session };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn create_DraggingSource(ctx: *DraggingSource) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_DraggingSource\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_ignore_modifier_keys_for_dragging_session: i8 = synth::add_method_i8_1id(cls, rt::sel(#str_ptr("ignoreModifierKeysForDraggingSession:\0")), DraggingSource_ignore_modifier_keys_for_dragging_session_imp, #str_ptr("c@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, DraggingSource_key(), { ctx as *u8 });
    return d;
}

