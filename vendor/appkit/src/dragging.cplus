// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSDragging.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/vec" as vec;

// `NSDraggingItemEnumerationOptions` (NS_OPTIONS) — bitfield; combine with `|`.
const dragging_item_enumeration_options_n_s_dragging_item_enumeration_concurrent: u64 = 1 as u64;
const dragging_item_enumeration_options_n_s_dragging_item_enumeration_clear_nonenumerated_images: u64 = 65536 as u64;

// `NSDragOperation` (NS_OPTIONS) — bitfield; combine with `|`.
const drag_operation_none: u64 = 0 as u64;
const drag_operation_copy: u64 = 1 as u64;
const drag_operation_link: u64 = 2 as u64;
const drag_operation_generic: u64 = 4 as u64;
const drag_operation_private: u64 = 8 as u64;
const drag_operation_move: u64 = 16 as u64;
const drag_operation_delete: u64 = 32 as u64;
const drag_operation_every: u64 = 9223372036854775807 as u64;
const drag_operation_all__obsolete: u64 = 15 as u64;
const drag_operation_all: u64 = 15 as u64;

// `NSDraggingFormation` (NS_ENUM)
enum DraggingFormation {
    Default,
    None,
    Pile,
    List,
    Stack,
}

fn dragging_formation_raw(v: DraggingFormation) -> i64 {
    return match v {
        DraggingFormation::Default => { 0 as i64 },
        DraggingFormation::None => { 1 as i64 },
        DraggingFormation::Pile => { 2 as i64 },
        DraggingFormation::List => { 3 as i64 },
        DraggingFormation::Stack => { 4 as i64 },
    };
}

fn dragging_formation_from_raw(raw: i64) -> DraggingFormation {
    if raw == (1 as i64) { return DraggingFormation::None; }
    if raw == (2 as i64) { return DraggingFormation::Pile; }
    if raw == (3 as i64) { return DraggingFormation::List; }
    if raw == (4 as i64) { return DraggingFormation::Stack; }
    return DraggingFormation::Default;
}

// `NSSpringLoadingHighlight` (NS_ENUM)
enum SpringLoadingHighlight {
    None,
    Standard,
    Emphasized,
}

fn spring_loading_highlight_raw(v: SpringLoadingHighlight) -> i64 {
    return match v {
        SpringLoadingHighlight::None => { 0 as i64 },
        SpringLoadingHighlight::Standard => { 1 as i64 },
        SpringLoadingHighlight::Emphasized => { 2 as i64 },
    };
}

fn spring_loading_highlight_from_raw(raw: i64) -> SpringLoadingHighlight {
    if raw == (1 as i64) { return SpringLoadingHighlight::Standard; }
    if raw == (2 as i64) { return SpringLoadingHighlight::Emphasized; }
    return SpringLoadingHighlight::None;
}

// `NSDraggingContext` (NS_ENUM)
enum DraggingContext {
    OutsideApplication,
    WithinApplication,
}

fn dragging_context_raw(v: DraggingContext) -> i64 {
    return match v {
        DraggingContext::OutsideApplication => { 0 as i64 },
        DraggingContext::WithinApplication => { 1 as i64 },
    };
}

fn dragging_context_from_raw(raw: i64) -> DraggingContext {
    if raw == (1 as i64) { return DraggingContext::WithinApplication; }
    return DraggingContext::OutsideApplication;
}

// `NSSpringLoadingOptions` (NS_OPTIONS) — bitfield; combine with `|`.
const spring_loading_options_n_s_spring_loading_disabled: u64 = 0 as u64;
const spring_loading_options_n_s_spring_loading_enabled: u64 = 1 as u64;
const spring_loading_options_n_s_spring_loading_continuous_activation: u64 = 2 as u64;
const spring_loading_options_n_s_spring_loading_no_hover: u64 = 8 as u64;

// `NSDraggingItem` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct DraggingItem {
    opaque _obj: *u8,
}

impl DraggingItem {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> DraggingItem { return DraggingItem { _obj: ptr }; }

}

// `NSDraggingSession` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct DraggingSession {
    opaque _obj: *u8,
}

impl DraggingSession {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> DraggingSession { return DraggingSession { _obj: ptr }; }

}

// `NSImage` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Image {
    opaque _obj: *u8,
}

impl Image {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Image { return Image { _obj: ptr }; }

}

// `NSPasteboard` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Pasteboard {
    opaque _obj: *u8,
}

impl Pasteboard {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Pasteboard { return Pasteboard { _obj: ptr }; }

}

// `NSView` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct View {
    opaque _obj: *u8,
}

impl View {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> View { return View { _obj: ptr }; }

}

// `NSWindow` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Window {
    opaque _obj: *u8,
}

impl Window {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> Window { return Window { _obj: ptr }; }

}

// `NSURL` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct URL {
    opaque _obj: *u8,
}

impl URL {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> URL { return URL { _obj: ptr }; }

}

// `NSPasteboardWriting` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct PasteboardWriting {
    opaque _obj: *u8,
}

impl PasteboardWriting {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> PasteboardWriting { return PasteboardWriting { _obj: ptr }; }

}

// `NSDraggingInfo` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct DraggingInfo {
    opaque _obj: *u8,
}

impl DraggingInfo {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> DraggingInfo { return DraggingInfo { _obj: ptr }; }

    fn slide_dragged_image_to(this, screen_point: rt::Point) {
        rt::msg_void_point(this._obj, rt::sel(#str_ptr("slideDraggedImageTo:\0")), screen_point);
    }

    fn names_of_promised_files_dropped_at_destination(this, drop_destination: URL) -> vec::Vec[text::Text] {
        let arr: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("namesOfPromisedFilesDroppedAtDestination:\0")), drop_destination.raw());
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[text::Text] = vec::Vec[text::Text]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            let value: *u8 = rt::msg_id_u64(arr, at_sel, i);
            out.append(bridge::to_text(value));
            i = i +% (1 as u64);
        }
        return out;
    }

    // SKIPPED `enumerateDraggingItemsWithOptions:forView:classes:searchOptions:usingBlock:`: leading param `NSArray<Class> * _Nonnull` — generic collection
    fn reset_spring_loading(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("resetSpringLoading\0")));
    }

    fn dragging_destination_window(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("draggingDestinationWindow\0")));
    }

    fn dragging_source_operation_mask(this) -> u64 {
        return rt::msg_u64(this._obj, rt::sel(#str_ptr("draggingSourceOperationMask\0")));
    }

    fn dragging_location(this) -> rt::Point {
        return rt::msg_point(this._obj, rt::sel(#str_ptr("draggingLocation\0")));
    }

    fn dragged_image_location(this) -> rt::Point {
        return rt::msg_point(this._obj, rt::sel(#str_ptr("draggedImageLocation\0")));
    }

    fn dragged_image(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("draggedImage\0")));
    }

    fn dragging_pasteboard(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("draggingPasteboard\0")));
    }

    // SKIPPED `draggingSource`: return `NS_SWIFT_UI_ACTOR id` — unmapped type `NS_SWIFT_UI_ACTOR id`
    fn dragging_sequence_number(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("draggingSequenceNumber\0")));
    }

    fn dragging_formation(this) -> DraggingFormation {
        return dragging_formation_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("draggingFormation\0"))));
    }

    // SKIPPED `setDraggingFormation:`: (return, arg) shape not yet modelled
    fn animates_to_destination(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("animatesToDestination\0"))) != (0 as i8);
    }

    fn set_animates_to_destination(this, animates_to_destination: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setAnimatesToDestination:\0")), animates_to_destination as i8);
    }

    fn number_of_valid_items_for_drop(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("numberOfValidItemsForDrop\0")));
    }

    // SKIPPED `setNumberOfValidItemsForDrop:`: (return, arg) shape not yet modelled
    fn spring_loading_highlight(this) -> SpringLoadingHighlight {
        return spring_loading_highlight_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("springLoadingHighlight\0"))));
    }

}

// `NSDraggingDestination` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct DraggingDestination {
    opaque _obj: *u8,
}

impl DraggingDestination {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> DraggingDestination { return DraggingDestination { _obj: ptr }; }

    // SKIPPED `draggingEntered:`: (return, arg) shape not yet modelled
    // SKIPPED `draggingUpdated:`: (return, arg) shape not yet modelled
    fn dragging_exited(this, sender: DraggingInfo) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("draggingExited:\0")), sender.raw());
    }

    // SKIPPED `prepareForDragOperation:`: (return, arg) shape not yet modelled
    // SKIPPED `performDragOperation:`: (return, arg) shape not yet modelled
    fn conclude_drag_operation(this, sender: DraggingInfo) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("concludeDragOperation:\0")), sender.raw());
    }

    fn dragging_ended(this, sender: DraggingInfo) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("draggingEnded:\0")), sender.raw());
    }

    fn wants_periodic_dragging_updates(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("wantsPeriodicDraggingUpdates\0"))) != (0 as i8);
    }

    fn update_dragging_items_for_drag(this, sender: DraggingInfo) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("updateDraggingItemsForDrag:\0")), sender.raw());
    }

}

// `NSDraggingSource` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct DraggingSource {
    opaque _obj: *u8,
}

impl DraggingSource {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> DraggingSource { return DraggingSource { _obj: ptr }; }

    // SKIPPED `draggingSession:sourceOperationMaskForDraggingContext:`: (return, arg) shape not yet modelled
    // SKIPPED `draggingSession:willBeginAtPoint:`: (return, arg) shape not yet modelled
    // SKIPPED `draggingSession:movedToPoint:`: (return, arg) shape not yet modelled
    // SKIPPED `draggingSession:endedAtPoint:operation:`: (return, arg) shape not yet modelled
    // SKIPPED `ignoreModifierKeysForDraggingSession:`: (return, arg) shape not yet modelled
}

// `NSSpringLoadingDestination` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct SpringLoadingDestination {
    opaque _obj: *u8,
}

impl SpringLoadingDestination {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> SpringLoadingDestination { return SpringLoadingDestination { _obj: ptr }; }

    // SKIPPED `springLoadingActivated:draggingInfo:`: (return, arg) shape not yet modelled
    fn spring_loading_highlight_changed(this, dragging_info: DraggingInfo) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("springLoadingHighlightChanged:\0")), dragging_info.raw());
    }

    // SKIPPED `springLoadingEntered:`: (return, arg) shape not yet modelled
    // SKIPPED `springLoadingUpdated:`: (return, arg) shape not yet modelled
    fn spring_loading_exited(this, dragging_info: DraggingInfo) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("springLoadingExited:\0")), dragging_info.raw());
    }

    fn dragging_ended(this, dragging_info: DraggingInfo) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("draggingEnded:\0")), dragging_info.raw());
    }

}

