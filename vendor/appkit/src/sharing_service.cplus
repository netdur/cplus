// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSSharingService.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `NSString` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct String {
    opaque _obj: *u8,
}

impl String {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSImage` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Image {
    opaque _obj: *u8,
}

impl Image {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSMenuItem` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct MenuItem {
    opaque _obj: *u8,
}

impl MenuItem {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSView` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct View {
    opaque _obj: *u8,
}

impl View {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSError` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Error {
    opaque _obj: *u8,
}

impl Error {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSWindow` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Window {
    opaque _obj: *u8,
}

impl Window {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSSharingCollaborationModeRestriction` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct SharingCollaborationModeRestriction {
    opaque _obj: *u8,
}

impl SharingCollaborationModeRestriction {
    fn raw(this) -> *u8 { return this._obj; }

}

// `CKShare` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct CKShare {
    opaque _obj: *u8,
}

impl CKShare {
    fn raw(this) -> *u8 { return this._obj; }

}

// `CKContainer` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct CKContainer {
    opaque _obj: *u8,
}

impl CKContainer {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSSharingService` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct SharingService {
    _obj: *u8,
}

impl SharingService {
    fn raw(this) -> *u8 { return this._obj; }

    // SKIPPED `sharingServicesForItems:`: return `NSArray<NSSharingService *> * _Nonnull` — generic collection
    fn sharing_service_named(service_name: str) -> option::Option[SharingService] {
        let obj: *u8 = rt::msg_id_id(rt::get_class(#str_ptr("NSSharingService\0")), rt::sel(#str_ptr("sharingServiceNamed:\0")), bridge::nsstring(service_name));
        if obj == { 0 as *u8 } {
            return option::Option[SharingService]::None;
        }
        return option::some(SharingService { _obj: rt::retain(obj) });
    }

    // SKIPPED `initWithTitle:image:alternateImage:handler:`: block method with non-void return
    // SKIPPED `init`: extra init variant (one `new` per type)
    // SKIPPED `canPerformWithItems:`: (return, arg) shape not yet modelled
    fn perform_with_items(this, items: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("performWithItems:\0")), items);
    }

    // SKIPPED `delegate`: return `id<NSSharingServiceDelegate> _Nullable` — generic collection
    // SKIPPED `setDelegate:`: param `id<NSSharingServiceDelegate> _Nullable` — generic collection
    fn title(this) -> text::Text {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("title\0")));
        return bridge::to_text(ns);
    }

    fn image(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("image\0")));
    }

    fn alternate_image(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("alternateImage\0")));
        return bridge::obj_option(obj);
    }

    fn menu_item_title(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("menuItemTitle\0")));
    }

    fn set_menu_item_title(this, menu_item_title: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setMenuItemTitle:\0")), menu_item_title);
    }

    // SKIPPED `recipients`: return `API_AVAILABLE NSArray<NSString *> *` — generic collection
    // SKIPPED `setRecipients:`: param `API_AVAILABLE NSArray<NSString *> *` — generic collection
    fn subject(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("subject\0")));
    }

    fn set_subject(this, subject: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setSubject:\0")), subject);
    }

    fn message_body(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("messageBody\0")));
    }

    fn permanent_link(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("permanentLink\0")));
    }

    fn account_name(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("accountName\0")));
    }

    // SKIPPED `attachmentFileURLs`: return `API_AVAILABLE NSArray<NSURL *> *` — generic collection
    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSSharingServicePicker` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct SharingServicePicker {
    _obj: *u8,
}

impl SharingServicePicker {
    fn raw(this) -> *u8 { return this._obj; }

    fn new(items: *u8) -> SharingServicePicker {
        let cls: *u8 = rt::get_class(#str_ptr("NSSharingServicePicker\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        return SharingServicePicker { _obj: rt::msg_id_id(alloced, rt::sel(#str_ptr("initWithItems:\0")), items) };
    }

    // SKIPPED `init`: extra init variant (one `new` per type)
    // SKIPPED `showRelativeToRect:ofView:preferredEdge:`: param `NSRect` — unmapped type `NSRect`
    fn close(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("close\0")));
    }

    // SKIPPED `delegate`: return `id<NSSharingServicePickerDelegate> _Nullable` — generic collection
    // SKIPPED `setDelegate:`: param `id<NSSharingServicePickerDelegate> _Nullable` — generic collection
    fn standard_share_menu_item(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("standardShareMenuItem\0")));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSSharingServiceDelegate` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `sharingService:sourceFrameOnScreenForShareItem:`: return `NSRect` not modelled
fn SharingServiceDelegateProtocol_noop_id_3(user: *u8, a0: *u8, a1: *u8, a2: *u8) -> *u8 { return { 0 as *u8 }; }
fn SharingServiceDelegateProtocol_noop_v_2(user: *u8, a0: *u8, a1: *u8) { return; }
fn SharingServiceDelegateProtocol_noop_v_3(user: *u8, a0: *u8, a1: *u8, a2: *u8) { return; }

struct SharingServiceDelegateProtocol {
    sharing_service_will_share_items: fn(*u8, *u8, *u8),
    sharing_service_did_fail_to_share_items_error: fn(*u8, *u8, *u8, *u8),
    sharing_service_did_share_items: fn(*u8, *u8, *u8),
    sharing_service_transition_image_for_share_item_content_rect: fn(*u8, *u8, *u8, *u8) -> *u8,
    sharing_service_source_window_for_share_items_sharing_content_scope: fn(*u8, *u8, *u8, *u8) -> *u8,
    anchoring_view_for_sharing_service_show_relative_to_rect_preferred_edge: fn(*u8, *u8, *u8, *u8) -> *u8,
    opaque user: *u8,
}

fn SharingServiceDelegateProtocol_new(user: *u8) -> SharingServiceDelegateProtocol {
    return SharingServiceDelegateProtocol { sharing_service_will_share_items: SharingServiceDelegateProtocol_noop_v_2, sharing_service_did_fail_to_share_items_error: SharingServiceDelegateProtocol_noop_v_3, sharing_service_did_share_items: SharingServiceDelegateProtocol_noop_v_2, sharing_service_transition_image_for_share_item_content_rect: SharingServiceDelegateProtocol_noop_id_3, sharing_service_source_window_for_share_items_sharing_content_scope: SharingServiceDelegateProtocol_noop_id_3, anchoring_view_for_sharing_service_show_relative_to_rect_preferred_edge: SharingServiceDelegateProtocol_noop_id_3, user: user };
}

impl SharingServiceDelegateProtocol {
    fn set_sharing_service_will_share_items(ref this, handler: fn(*u8, *u8, *u8)) {
        this.sharing_service_will_share_items = handler;
        return;
    }
    fn set_sharing_service_did_fail_to_share_items_error(ref this, handler: fn(*u8, *u8, *u8, *u8)) {
        this.sharing_service_did_fail_to_share_items_error = handler;
        return;
    }
    fn set_sharing_service_did_share_items(ref this, handler: fn(*u8, *u8, *u8)) {
        this.sharing_service_did_share_items = handler;
        return;
    }
    fn set_sharing_service_transition_image_for_share_item_content_rect(ref this, handler: fn(*u8, *u8, *u8, *u8) -> *u8) {
        this.sharing_service_transition_image_for_share_item_content_rect = handler;
        return;
    }
    fn set_sharing_service_source_window_for_share_items_sharing_content_scope(ref this, handler: fn(*u8, *u8, *u8, *u8) -> *u8) {
        this.sharing_service_source_window_for_share_items_sharing_content_scope = handler;
        return;
    }
    fn set_anchoring_view_for_sharing_service_show_relative_to_rect_preferred_edge(ref this, handler: fn(*u8, *u8, *u8, *u8) -> *u8) {
        this.anchoring_view_for_sharing_service_show_relative_to_rect_preferred_edge = handler;
        return;
    }
}

fn SharingServiceDelegateProtocol_key() -> *u8 { return { 28672 as *u8 }; }

fn SharingServiceDelegateProtocol_sharing_service_will_share_items_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8) {
    let c: *SharingServiceDelegateProtocol = { synth::get_associated(self_obj, SharingServiceDelegateProtocol_key()) as *SharingServiceDelegateProtocol };
    let f: fn(*u8, *u8, *u8) = { (*c).sharing_service_will_share_items };
    let u: *u8 = { (*c).user };
    f(u, a0, a1);
    return;
}

fn SharingServiceDelegateProtocol_sharing_service_did_fail_to_share_items_error_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8, a2: *u8) {
    let c: *SharingServiceDelegateProtocol = { synth::get_associated(self_obj, SharingServiceDelegateProtocol_key()) as *SharingServiceDelegateProtocol };
    let f: fn(*u8, *u8, *u8, *u8) = { (*c).sharing_service_did_fail_to_share_items_error };
    let u: *u8 = { (*c).user };
    f(u, a0, a1, a2);
    return;
}

fn SharingServiceDelegateProtocol_sharing_service_did_share_items_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8) {
    let c: *SharingServiceDelegateProtocol = { synth::get_associated(self_obj, SharingServiceDelegateProtocol_key()) as *SharingServiceDelegateProtocol };
    let f: fn(*u8, *u8, *u8) = { (*c).sharing_service_did_share_items };
    let u: *u8 = { (*c).user };
    f(u, a0, a1);
    return;
}

fn SharingServiceDelegateProtocol_sharing_service_transition_image_for_share_item_content_rect_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8, a2: *u8) -> *u8 {
    let c: *SharingServiceDelegateProtocol = { synth::get_associated(self_obj, SharingServiceDelegateProtocol_key()) as *SharingServiceDelegateProtocol };
    let f: fn(*u8, *u8, *u8, *u8) -> *u8 = { (*c).sharing_service_transition_image_for_share_item_content_rect };
    let u: *u8 = { (*c).user };
    return f(u, a0, a1, a2);
}

fn SharingServiceDelegateProtocol_sharing_service_source_window_for_share_items_sharing_content_scope_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8, a2: *u8) -> *u8 {
    let c: *SharingServiceDelegateProtocol = { synth::get_associated(self_obj, SharingServiceDelegateProtocol_key()) as *SharingServiceDelegateProtocol };
    let f: fn(*u8, *u8, *u8, *u8) -> *u8 = { (*c).sharing_service_source_window_for_share_items_sharing_content_scope };
    let u: *u8 = { (*c).user };
    return f(u, a0, a1, a2);
}

fn SharingServiceDelegateProtocol_anchoring_view_for_sharing_service_show_relative_to_rect_preferred_edge_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8, a2: *u8) -> *u8 {
    let c: *SharingServiceDelegateProtocol = { synth::get_associated(self_obj, SharingServiceDelegateProtocol_key()) as *SharingServiceDelegateProtocol };
    let f: fn(*u8, *u8, *u8, *u8) -> *u8 = { (*c).anchoring_view_for_sharing_service_show_relative_to_rect_preferred_edge };
    let u: *u8 = { (*c).user };
    return f(u, a0, a1, a2);
}

fn create_SharingServiceDelegateProtocol(ctx: *SharingServiceDelegateProtocol) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_SharingServiceDelegateProtocol\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_sharing_service_will_share_items: i8 = synth::add_method_v_2id(cls, rt::sel(#str_ptr("sharingService:willShareItems:\0")), SharingServiceDelegateProtocol_sharing_service_will_share_items_imp, #str_ptr("v@:@@\0"));
        let add_sharing_service_did_fail_to_share_items_error: i8 = synth::add_method_v_3id(cls, rt::sel(#str_ptr("sharingService:didFailToShareItems:error:\0")), SharingServiceDelegateProtocol_sharing_service_did_fail_to_share_items_error_imp, #str_ptr("v@:@@@\0"));
        let add_sharing_service_did_share_items: i8 = synth::add_method_v_2id(cls, rt::sel(#str_ptr("sharingService:didShareItems:\0")), SharingServiceDelegateProtocol_sharing_service_did_share_items_imp, #str_ptr("v@:@@\0"));
        let add_sharing_service_transition_image_for_share_item_content_rect: i8 = synth::add_method_id_3id(cls, rt::sel(#str_ptr("sharingService:transitionImageForShareItem:contentRect:\0")), SharingServiceDelegateProtocol_sharing_service_transition_image_for_share_item_content_rect_imp, #str_ptr("@@:@@@\0"));
        let add_sharing_service_source_window_for_share_items_sharing_content_scope: i8 = synth::add_method_id_3id(cls, rt::sel(#str_ptr("sharingService:sourceWindowForShareItems:sharingContentScope:\0")), SharingServiceDelegateProtocol_sharing_service_source_window_for_share_items_sharing_content_scope_imp, #str_ptr("@@:@@@\0"));
        let add_anchoring_view_for_sharing_service_show_relative_to_rect_preferred_edge: i8 = synth::add_method_id_3id(cls, rt::sel(#str_ptr("anchoringViewForSharingService:showRelativeToRect:preferredEdge:\0")), SharingServiceDelegateProtocol_anchoring_view_for_sharing_service_show_relative_to_rect_preferred_edge_imp, #str_ptr("@@:@@@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, SharingServiceDelegateProtocol_key(), { ctx as *u8 });
    return d;
}

// `NSCloudSharingServiceDelegate` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `optionsForSharingService:shareProvider:`: no `u64`-return / 2-arg IMP shape
fn CloudSharingServiceDelegate_noop_v_2(user: *u8, a0: *u8, a1: *u8) { return; }
fn CloudSharingServiceDelegate_noop_v_3(user: *u8, a0: *u8, a1: *u8, a2: *u8) { return; }

struct CloudSharingServiceDelegate {
    sharing_service_did_complete_for_items_error: fn(*u8, *u8, *u8, *u8),
    sharing_service_did_save_share: fn(*u8, *u8, *u8),
    sharing_service_did_stop_sharing: fn(*u8, *u8, *u8),
    opaque user: *u8,
}

fn CloudSharingServiceDelegate_new(user: *u8) -> CloudSharingServiceDelegate {
    return CloudSharingServiceDelegate { sharing_service_did_complete_for_items_error: CloudSharingServiceDelegate_noop_v_3, sharing_service_did_save_share: CloudSharingServiceDelegate_noop_v_2, sharing_service_did_stop_sharing: CloudSharingServiceDelegate_noop_v_2, user: user };
}

impl CloudSharingServiceDelegate {
    fn set_sharing_service_did_complete_for_items_error(ref this, handler: fn(*u8, *u8, *u8, *u8)) {
        this.sharing_service_did_complete_for_items_error = handler;
        return;
    }
    fn set_sharing_service_did_save_share(ref this, handler: fn(*u8, *u8, *u8)) {
        this.sharing_service_did_save_share = handler;
        return;
    }
    fn set_sharing_service_did_stop_sharing(ref this, handler: fn(*u8, *u8, *u8)) {
        this.sharing_service_did_stop_sharing = handler;
        return;
    }
}

fn CloudSharingServiceDelegate_key() -> *u8 { return { 28673 as *u8 }; }

fn CloudSharingServiceDelegate_sharing_service_did_complete_for_items_error_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8, a2: *u8) {
    let c: *CloudSharingServiceDelegate = { synth::get_associated(self_obj, CloudSharingServiceDelegate_key()) as *CloudSharingServiceDelegate };
    let f: fn(*u8, *u8, *u8, *u8) = { (*c).sharing_service_did_complete_for_items_error };
    let u: *u8 = { (*c).user };
    f(u, a0, a1, a2);
    return;
}

fn CloudSharingServiceDelegate_sharing_service_did_save_share_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8) {
    let c: *CloudSharingServiceDelegate = { synth::get_associated(self_obj, CloudSharingServiceDelegate_key()) as *CloudSharingServiceDelegate };
    let f: fn(*u8, *u8, *u8) = { (*c).sharing_service_did_save_share };
    let u: *u8 = { (*c).user };
    f(u, a0, a1);
    return;
}

fn CloudSharingServiceDelegate_sharing_service_did_stop_sharing_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8) {
    let c: *CloudSharingServiceDelegate = { synth::get_associated(self_obj, CloudSharingServiceDelegate_key()) as *CloudSharingServiceDelegate };
    let f: fn(*u8, *u8, *u8) = { (*c).sharing_service_did_stop_sharing };
    let u: *u8 = { (*c).user };
    f(u, a0, a1);
    return;
}

fn create_CloudSharingServiceDelegate(ctx: *CloudSharingServiceDelegate) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_CloudSharingServiceDelegate\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_sharing_service_did_complete_for_items_error: i8 = synth::add_method_v_3id(cls, rt::sel(#str_ptr("sharingService:didCompleteForItems:error:\0")), CloudSharingServiceDelegate_sharing_service_did_complete_for_items_error_imp, #str_ptr("v@:@@@\0"));
        let add_sharing_service_did_save_share: i8 = synth::add_method_v_2id(cls, rt::sel(#str_ptr("sharingService:didSaveShare:\0")), CloudSharingServiceDelegate_sharing_service_did_save_share_imp, #str_ptr("v@:@@\0"));
        let add_sharing_service_did_stop_sharing: i8 = synth::add_method_v_2id(cls, rt::sel(#str_ptr("sharingService:didStopSharing:\0")), CloudSharingServiceDelegate_sharing_service_did_stop_sharing_imp, #str_ptr("v@:@@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, CloudSharingServiceDelegate_key(), { ctx as *u8 });
    return d;
}

// `NSSharingServicePickerDelegate` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `sharingServicePicker:delegateForSharingService:`: return `id<NSSharingServiceDelegate> _Nullable` not modelled
fn SharingServicePickerDelegateProtocol_noop_id_1(user: *u8, a0: *u8) -> *u8 { return { 0 as *u8 }; }
fn SharingServicePickerDelegateProtocol_noop_id_3(user: *u8, a0: *u8, a1: *u8, a2: *u8) -> *u8 { return { 0 as *u8 }; }
fn SharingServicePickerDelegateProtocol_noop_v_2(user: *u8, a0: *u8, a1: *u8) { return; }

struct SharingServicePickerDelegateProtocol {
    sharing_service_picker_sharing_services_for_items_proposed_sharing_services: fn(*u8, *u8, *u8, *u8) -> *u8,
    sharing_service_picker_did_choose_sharing_service: fn(*u8, *u8, *u8),
    sharing_service_picker_collaboration_mode_restrictions: fn(*u8, *u8) -> *u8,
    opaque user: *u8,
}

fn SharingServicePickerDelegateProtocol_new(user: *u8) -> SharingServicePickerDelegateProtocol {
    return SharingServicePickerDelegateProtocol { sharing_service_picker_sharing_services_for_items_proposed_sharing_services: SharingServicePickerDelegateProtocol_noop_id_3, sharing_service_picker_did_choose_sharing_service: SharingServicePickerDelegateProtocol_noop_v_2, sharing_service_picker_collaboration_mode_restrictions: SharingServicePickerDelegateProtocol_noop_id_1, user: user };
}

impl SharingServicePickerDelegateProtocol {
    fn set_sharing_service_picker_sharing_services_for_items_proposed_sharing_services(ref this, handler: fn(*u8, *u8, *u8, *u8) -> *u8) {
        this.sharing_service_picker_sharing_services_for_items_proposed_sharing_services = handler;
        return;
    }
    fn set_sharing_service_picker_did_choose_sharing_service(ref this, handler: fn(*u8, *u8, *u8)) {
        this.sharing_service_picker_did_choose_sharing_service = handler;
        return;
    }
    fn set_sharing_service_picker_collaboration_mode_restrictions(ref this, handler: fn(*u8, *u8) -> *u8) {
        this.sharing_service_picker_collaboration_mode_restrictions = handler;
        return;
    }
}

fn SharingServicePickerDelegateProtocol_key() -> *u8 { return { 28674 as *u8 }; }

fn SharingServicePickerDelegateProtocol_sharing_service_picker_sharing_services_for_items_proposed_sharing_services_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8, a2: *u8) -> *u8 {
    let c: *SharingServicePickerDelegateProtocol = { synth::get_associated(self_obj, SharingServicePickerDelegateProtocol_key()) as *SharingServicePickerDelegateProtocol };
    let f: fn(*u8, *u8, *u8, *u8) -> *u8 = { (*c).sharing_service_picker_sharing_services_for_items_proposed_sharing_services };
    let u: *u8 = { (*c).user };
    return f(u, a0, a1, a2);
}

fn SharingServicePickerDelegateProtocol_sharing_service_picker_did_choose_sharing_service_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8) {
    let c: *SharingServicePickerDelegateProtocol = { synth::get_associated(self_obj, SharingServicePickerDelegateProtocol_key()) as *SharingServicePickerDelegateProtocol };
    let f: fn(*u8, *u8, *u8) = { (*c).sharing_service_picker_did_choose_sharing_service };
    let u: *u8 = { (*c).user };
    f(u, a0, a1);
    return;
}

fn SharingServicePickerDelegateProtocol_sharing_service_picker_collaboration_mode_restrictions_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> *u8 {
    let c: *SharingServicePickerDelegateProtocol = { synth::get_associated(self_obj, SharingServicePickerDelegateProtocol_key()) as *SharingServicePickerDelegateProtocol };
    let f: fn(*u8, *u8) -> *u8 = { (*c).sharing_service_picker_collaboration_mode_restrictions };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn create_SharingServicePickerDelegateProtocol(ctx: *SharingServicePickerDelegateProtocol) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_SharingServicePickerDelegateProtocol\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_sharing_service_picker_sharing_services_for_items_proposed_sharing_services: i8 = synth::add_method_id_3id(cls, rt::sel(#str_ptr("sharingServicePicker:sharingServicesForItems:proposedSharingServices:\0")), SharingServicePickerDelegateProtocol_sharing_service_picker_sharing_services_for_items_proposed_sharing_services_imp, #str_ptr("@@:@@@\0"));
        let add_sharing_service_picker_did_choose_sharing_service: i8 = synth::add_method_v_2id(cls, rt::sel(#str_ptr("sharingServicePicker:didChooseSharingService:\0")), SharingServicePickerDelegateProtocol_sharing_service_picker_did_choose_sharing_service_imp, #str_ptr("v@:@@\0"));
        let add_sharing_service_picker_collaboration_mode_restrictions: i8 = synth::add_method_id_1id(cls, rt::sel(#str_ptr("sharingServicePickerCollaborationModeRestrictions:\0")), SharingServicePickerDelegateProtocol_sharing_service_picker_collaboration_mode_restrictions_imp, #str_ptr("@@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, SharingServicePickerDelegateProtocol_key(), { ctx as *u8 });
    return d;
}

