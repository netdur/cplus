// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSTextContentManager.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

#[repr(C)]
struct TextContentManager_synchronize_text_layout_managers_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8),
    opaque ctx: *u8,
}

fn TextContentManager_synchronize_text_layout_managers_invoke(block: *u8, a0: *u8) {
    let bl: *TextContentManager_synchronize_text_layout_managers_block = { block as *TextContentManager_synchronize_text_layout_managers_block };
    let f: fn(*u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0);
    return;
}

#[repr(C)]
struct TextContentManager_perform_editing_transaction_using_block_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8),
    opaque ctx: *u8,
}

fn TextContentManager_perform_editing_transaction_using_block_invoke(block: *u8) {
    let bl: *TextContentManager_perform_editing_transaction_using_block_block = { block as *TextContentManager_perform_editing_transaction_using_block_block };
    let f: fn(*u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx);
    return;
}

// `NSTextRange` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextRange {
    opaque _obj: *u8,
}

impl TextRange {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSTextElement` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextElement {
    opaque _obj: *u8,
}

impl TextElement {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSTextParagraph` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextParagraph {
    opaque _obj: *u8,
}

impl TextParagraph {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSTextLayoutManager` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextLayoutManager {
    opaque _obj: *u8,
}

impl TextLayoutManager {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSTextStorage` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextStorage {
    opaque _obj: *u8,
}

impl TextStorage {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSTextContentStorage` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextContentStorage {
    opaque _obj: *u8,
}

impl TextContentStorage {
    fn raw(this) -> *u8 { return this._obj; }

    fn attributed_string_for_text_element(this, text_element: *u8) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("attributedStringForTextElement:\0")), text_element);
        return bridge::obj_option(obj);
    }

    fn text_element_for_attributed_string(this, attributed_string: *u8) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("textElementForAttributedString:\0")), attributed_string);
        return bridge::obj_option(obj);
    }

    // SKIPPED `locationFromLocation:withOffset:`: param `id<NSTextLocation> _Nonnull` — generic collection
    // SKIPPED `offsetFromLocation:toLocation:`: param `id<NSTextLocation> _Nonnull` — generic collection
    // SKIPPED `adjustedRangeFromRange:forEditingTextSelection:`: (return, arg) shape not yet modelled
    // SKIPPED `delegate`: return `id<NSTextContentStorageDelegate> _Nullable` — generic collection
    // SKIPPED `setDelegate:`: param `id<NSTextContentStorageDelegate> _Nullable` — generic collection
    fn includes_text_list_markers(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("includesTextListMarkers\0"))) != (0 as i8);
    }

    fn set_includes_text_list_markers(this, includes_text_list_markers: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setIncludesTextListMarkers:\0")), includes_text_list_markers as i8);
    }

    fn attributed_string(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("attributedString\0")));
        return bridge::obj_option(obj);
    }

    fn set_attributed_string(this, attributed_string: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setAttributedString:\0")), attributed_string);
    }

}

// `NSTextContentManager` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct TextContentManager {
    _obj: *u8,
}

impl TextContentManager {
    fn raw(this) -> *u8 { return this._obj; }

    fn new() -> TextContentManager {
        let cls: *u8 = rt::get_class(#str_ptr("NSTextContentManager\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        return TextContentManager { _obj: rt::msg_id(alloced, rt::sel(#str_ptr("init\0"))) };
    }

    // SKIPPED `initWithCoder:`: extra init variant (one `new` per type)
    fn add_text_layout_manager(this, text_layout_manager: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("addTextLayoutManager:\0")), text_layout_manager);
    }

    fn remove_text_layout_manager(this, text_layout_manager: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("removeTextLayoutManager:\0")), text_layout_manager);
    }

    fn synchronize_text_layout_managers(this, cb: fn(*u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: TextContentManager_synchronize_text_layout_managers_block = TextContentManager_synchronize_text_layout_managers_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: TextContentManager_synchronize_text_layout_managers_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("synchronizeTextLayoutManagers:\0")), bp);
        return;
    }

    // SKIPPED `textElementsForRange:`: return `NSArray<NSTextElement *> * _Nonnull` — generic collection
    fn perform_editing_transaction_using_block(this, cb: fn(*u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: TextContentManager_perform_editing_transaction_using_block_block = TextContentManager_perform_editing_transaction_using_block_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: TextContentManager_perform_editing_transaction_using_block_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("performEditingTransactionUsingBlock:\0")), bp);
        return;
    }

    // SKIPPED `recordEditActionInRange:newTextRange:`: (return, arg) shape not yet modelled
    // SKIPPED `delegate`: return `id<NSTextContentManagerDelegate> _Nullable` — generic collection
    // SKIPPED `setDelegate:`: param `id<NSTextContentManagerDelegate> _Nullable` — generic collection
    // SKIPPED `textLayoutManagers`: return `NSArray<NSTextLayoutManager *> * _Nonnull` — generic collection
    fn primary_text_layout_manager(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("primaryTextLayoutManager\0")));
        return bridge::obj_option(obj);
    }

    fn set_primary_text_layout_manager(this, primary_text_layout_manager: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setPrimaryTextLayoutManager:\0")), primary_text_layout_manager);
    }

    fn has_editing_transaction(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("hasEditingTransaction\0"))) != (0 as i8);
    }

    fn automatically_synchronizes_text_layout_managers(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("automaticallySynchronizesTextLayoutManagers\0"))) != (0 as i8);
    }

    fn set_automatically_synchronizes_text_layout_managers(this, automatically_synchronizes_text_layout_managers: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setAutomaticallySynchronizesTextLayoutManagers:\0")), automatically_synchronizes_text_layout_managers as i8);
    }

    fn automatically_synchronizes_to_backing_store(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("automaticallySynchronizesToBackingStore\0"))) != (0 as i8);
    }

    fn set_automatically_synchronizes_to_backing_store(this, automatically_synchronizes_to_backing_store: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setAutomaticallySynchronizesToBackingStore:\0")), automatically_synchronizes_to_backing_store as i8);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSTextElementProvider` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `enumerateTextElementsFromLocation:options:usingBlock:`: arg shape (only <=5 object args modelled)
// SKIPPED `synchronizeToBackingStore:`: arg shape (only <=5 object args modelled)
// SKIPPED `locationFromLocation:withOffset:`: arg shape (only <=5 object args modelled)
// SKIPPED `offsetFromLocation:toLocation:`: arg shape (only <=5 object args modelled)
// SKIPPED `adjustedRangeFromRange:forEditingTextSelection:`: arg shape (only <=5 object args modelled)
fn TextElementProvider_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn TextElementProvider_noop_v_2(user: *u8, a0: *u8, a1: *u8) { return; }

struct TextElementProvider {
    replace_contents_in_range_with_text_elements: fn(*u8, *u8, *u8),
    document_range: fn(*u8) -> *u8,
    opaque user: *u8,
}

fn TextElementProvider_new(user: *u8) -> TextElementProvider {
    return TextElementProvider { replace_contents_in_range_with_text_elements: TextElementProvider_noop_v_2, document_range: TextElementProvider_noop_id_0, user: user };
}

impl TextElementProvider {
    fn set_replace_contents_in_range_with_text_elements(ref this, handler: fn(*u8, *u8, *u8)) {
        this.replace_contents_in_range_with_text_elements = handler;
        return;
    }
    fn set_document_range(ref this, handler: fn(*u8) -> *u8) {
        this.document_range = handler;
        return;
    }
}

fn TextElementProvider_key() -> *u8 { return { 28672 as *u8 }; }

fn TextElementProvider_replace_contents_in_range_with_text_elements_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8) {
    let c: *TextElementProvider = { synth::get_associated(self_obj, TextElementProvider_key()) as *TextElementProvider };
    let f: fn(*u8, *u8, *u8) = { (*c).replace_contents_in_range_with_text_elements };
    let u: *u8 = { (*c).user };
    f(u, a0, a1);
    return;
}

fn TextElementProvider_document_range_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *TextElementProvider = { synth::get_associated(self_obj, TextElementProvider_key()) as *TextElementProvider };
    let f: fn(*u8) -> *u8 = { (*c).document_range };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn create_TextElementProvider(ctx: *TextElementProvider) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_TextElementProvider\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_replace_contents_in_range_with_text_elements: i8 = synth::add_method_v_2id(cls, rt::sel(#str_ptr("replaceContentsInRange:withTextElements:\0")), TextElementProvider_replace_contents_in_range_with_text_elements_imp, #str_ptr("v@:@@\0"));
        let add_document_range: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("documentRange\0")), TextElementProvider_document_range_imp, #str_ptr("@@:\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, TextElementProvider_key(), { ctx as *u8 });
    return d;
}

