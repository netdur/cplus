// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSTextContentManager.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "stdlib/vec" as vec;
import "objc/synthesis" as synth;

// `NSTextContentManagerEnumerationOptions` (NS_OPTIONS) — bitfield; combine with `|`.
const text_content_manager_enumeration_options_none: u64 = 0 as u64;
const text_content_manager_enumeration_options_reverse: u64 = 1 as u64;

#[repr(C)]
struct TextContentManager_synchronize_text_layout_managers_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8),
    opaque ctx: *u8,
}

fn TextContentManager_synchronize_text_layout_managers_invoke(block: *u8, a0: *u8) {
    let bl: *TextContentManager_synchronize_text_layout_managers_block = { block as *TextContentManager_synchronize_text_layout_managers_block };
    let f: fn(*u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0);
    return;
}

#[repr(C)]
struct TextContentManager_perform_editing_transaction_using_block_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8),
    opaque ctx: *u8,
}

fn TextContentManager_perform_editing_transaction_using_block_invoke(block: *u8) {
    let bl: *TextContentManager_perform_editing_transaction_using_block_block = { block as *TextContentManager_perform_editing_transaction_using_block_block };
    let f: fn(*u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx);
    return;
}

#[repr(C)]
struct TextElementProvider_enumerate_text_elements_from_location_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8),
    opaque ctx: *u8,
}

fn TextElementProvider_enumerate_text_elements_from_location_invoke(block: *u8, a0: *u8) {
    let bl: *TextElementProvider_enumerate_text_elements_from_location_block = { block as *TextElementProvider_enumerate_text_elements_from_location_block };
    let f: fn(*u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0);
    return;
}

#[repr(C)]
struct TextElementProvider_synchronize_to_backing_store_block {
    opaque isa: *u8,
    flags: i32,
    reserved: i32,
    invoke: fn(*u8, *u8),
    opaque descriptor: *u8,
    user_fn: fn(*u8, *u8),
    opaque ctx: *u8,
}

fn TextElementProvider_synchronize_to_backing_store_invoke(block: *u8, a0: *u8) {
    let bl: *TextElementProvider_synchronize_to_backing_store_block = { block as *TextElementProvider_synchronize_to_backing_store_block };
    let f: fn(*u8, *u8) = { (*bl).user_fn };
    let ctx: *u8 = { (*bl).ctx };
    f(ctx, a0);
    return;
}

// `NSTextRange` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextRange {
    opaque _obj: *u8,
}

impl TextRange {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TextRange { return TextRange { _obj: ptr }; }

}

// `NSTextElement` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextElement {
    opaque _obj: *u8,
}

impl TextElement {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TextElement { return TextElement { _obj: ptr }; }

}

// `NSTextParagraph` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextParagraph {
    opaque _obj: *u8,
}

impl TextParagraph {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TextParagraph { return TextParagraph { _obj: ptr }; }

}

// `NSTextLayoutManager` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextLayoutManager {
    opaque _obj: *u8,
}

impl TextLayoutManager {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TextLayoutManager { return TextLayoutManager { _obj: ptr }; }

}

// `NSTextStorage` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextStorage {
    opaque _obj: *u8,
}

impl TextStorage {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TextStorage { return TextStorage { _obj: ptr }; }

}

// `NSTextContentStorage` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextContentStorage {
    opaque _obj: *u8,
}

impl TextContentStorage {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TextContentStorage { return TextContentStorage { _obj: ptr }; }

    fn attributed_string_for_text_element(this, text_element: TextElement) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("attributedStringForTextElement:\0")), text_element.raw());
        return bridge::obj_option(obj);
    }

    fn text_element_for_attributed_string(this, attributed_string: *u8) -> option::Option[TextElement] {
        let obj: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("textElementForAttributedString:\0")), attributed_string);
        if obj == { 0 as *u8 } {
            return option::Option[TextElement]::None;
        }
        return option::some(TextElement::from_raw(obj));
    }

    fn location_from_location(this, location: TextLocation, with_offset: i64) -> option::Option[TextLocation] {
        let obj: *u8 = rt::msg_id_id_i64(this._obj, rt::sel(#str_ptr("locationFromLocation:withOffset:\0")), location.raw(), with_offset);
        if obj == { 0 as *u8 } {
            return option::Option[TextLocation]::None;
        }
        return option::some(TextLocation::from_raw(obj));
    }

    fn offset_from_location(this, from: TextLocation, to_location: TextLocation) -> i64 {
        return rt::msg_i64_id_id(this._obj, rt::sel(#str_ptr("offsetFromLocation:toLocation:\0")), from.raw(), to_location.raw());
    }

    fn adjusted_range_from_range(this, text_range: TextRange, for_editing_text_selection: bool) -> option::Option[TextRange] {
        let obj: *u8 = rt::msg_id_id_i8(this._obj, rt::sel(#str_ptr("adjustedRangeFromRange:forEditingTextSelection:\0")), text_range.raw(), for_editing_text_selection as i8);
        if obj == { 0 as *u8 } {
            return option::Option[TextRange]::None;
        }
        return option::some(TextRange::from_raw(obj));
    }

    fn delegate(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("delegate\0")));
        return bridge::obj_option(obj);
    }

    fn set_delegate(this, delegate: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setDelegate:\0")), delegate);
    }

    fn includes_text_list_markers(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("includesTextListMarkers\0"))) != (0 as i8);
    }

    fn set_includes_text_list_markers(this, includes_text_list_markers: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setIncludesTextListMarkers:\0")), includes_text_list_markers as i8);
    }

    fn attributed_string(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("attributedString\0")));
        return bridge::obj_option(obj);
    }

    fn set_attributed_string(this, attributed_string: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setAttributedString:\0")), attributed_string);
    }

}

// `NSTextContentManager` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct TextContentManager {
    _obj: *u8,
}

impl TextContentManager {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TextContentManager { return TextContentManager { _obj: ptr }; }

    fn new() -> TextContentManager {
        let cls: *u8 = rt::get_class(#str_ptr("NSTextContentManager\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        return TextContentManager { _obj: rt::msg_id(alloced, rt::sel(#str_ptr("init\0"))) };
    }

    fn new_with_coder(coder: *u8) -> TextContentManager {
        let cls: *u8 = rt::get_class(#str_ptr("NSTextContentManager\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        return TextContentManager { _obj: rt::msg_id_id(alloced, rt::sel(#str_ptr("initWithCoder:\0")), coder) };
    }

    fn add_text_layout_manager(this, text_layout_manager: TextLayoutManager) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("addTextLayoutManager:\0")), text_layout_manager.raw());
    }

    fn remove_text_layout_manager(this, text_layout_manager: TextLayoutManager) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("removeTextLayoutManager:\0")), text_layout_manager.raw());
    }

    fn synchronize_text_layout_managers(this, cb: fn(*u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: TextContentManager_synchronize_text_layout_managers_block = TextContentManager_synchronize_text_layout_managers_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: TextContentManager_synchronize_text_layout_managers_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("synchronizeTextLayoutManagers:\0")), bp);
        return;
    }

    fn text_elements_for_range(this, range: TextRange) -> vec::Vec[TextElement] {
        let arr: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("textElementsForRange:\0")), range.raw());
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[TextElement] = vec::Vec[TextElement]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(TextElement::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn perform_editing_transaction_using_block(this, cb: fn(*u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: TextContentManager_perform_editing_transaction_using_block_block = TextContentManager_perform_editing_transaction_using_block_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: TextContentManager_perform_editing_transaction_using_block_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("performEditingTransactionUsingBlock:\0")), bp);
        return;
    }

    fn record_edit_action_in_range(this, original_text_range: TextRange, new_text_range: TextRange) {
        rt::msg_void_id_id(this._obj, rt::sel(#str_ptr("recordEditActionInRange:newTextRange:\0")), original_text_range.raw(), new_text_range.raw());
    }

    fn delegate(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("delegate\0")));
        return bridge::obj_option(obj);
    }

    fn set_delegate(this, delegate: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setDelegate:\0")), delegate);
    }

    fn text_layout_managers(this) -> vec::Vec[TextLayoutManager] {
        let arr: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("textLayoutManagers\0")));
        let n: u64 = rt::msg_u64(arr, rt::sel(#str_ptr("count\0")));
        var out: vec::Vec[TextLayoutManager] = vec::Vec[TextLayoutManager]::with_capacity(n as usize);
        let at_sel: *u8 = rt::sel(#str_ptr("objectAtIndex:\0"));
        var i: u64 = 0 as u64;
        while i < n {
            out.append(TextLayoutManager::from_raw(rt::msg_id_u64(arr, at_sel, i)));
            i = i +% (1 as u64);
        }
        return out;
    }

    fn primary_text_layout_manager(this) -> option::Option[TextLayoutManager] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("primaryTextLayoutManager\0")));
        if obj == { 0 as *u8 } {
            return option::Option[TextLayoutManager]::None;
        }
        return option::some(TextLayoutManager::from_raw(obj));
    }

    fn set_primary_text_layout_manager(this, primary_text_layout_manager: TextLayoutManager) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setPrimaryTextLayoutManager:\0")), primary_text_layout_manager.raw());
    }

    fn has_editing_transaction(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("hasEditingTransaction\0"))) != (0 as i8);
    }

    fn automatically_synchronizes_text_layout_managers(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("automaticallySynchronizesTextLayoutManagers\0"))) != (0 as i8);
    }

    fn set_automatically_synchronizes_text_layout_managers(this, automatically_synchronizes_text_layout_managers: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setAutomaticallySynchronizesTextLayoutManagers:\0")), automatically_synchronizes_text_layout_managers as i8);
    }

    fn automatically_synchronizes_to_backing_store(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("automaticallySynchronizesToBackingStore\0"))) != (0 as i8);
    }

    fn set_automatically_synchronizes_to_backing_store(this, automatically_synchronizes_to_backing_store: bool) {
        rt::msg_void_i8(this._obj, rt::sel(#str_ptr("setAutomaticallySynchronizesToBackingStore:\0")), automatically_synchronizes_to_backing_store as i8);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSTextLocation` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct TextLocation {
    opaque _obj: *u8,
}

impl TextLocation {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TextLocation { return TextLocation { _obj: ptr }; }

}

// `NSTextContentManagerDelegate` delegate protocol -> runtime class-synthesis helper.
#[link_name = "class_addMethod"]
extern fn cplus_add_method_id_id_id(cls: *u8, sel: *u8, imp: fn(*u8, *u8, *u8, *u8) -> *u8, types: *u8) -> i8;
#[link_name = "class_addMethod"]
extern fn cplus_add_method_i8_id_id_u64(cls: *u8, sel: *u8, imp: fn(*u8, *u8, *u8, *u8, u64) -> i8, types: *u8) -> i8;

fn TextContentManagerDelegate_noop_id_id_id(user: *u8, a0: *u8, a1: *u8) -> *u8 { return { 0 as *u8 }; }
fn TextContentManagerDelegate_noop_i8_id_id_u64(user: *u8, a0: *u8, a1: *u8, a2: u64) -> i8 { return 0 as i8; }

struct TextContentManagerDelegate {
    text_content_manager_text_element_at_location: fn(*u8, *u8, *u8) -> *u8,
    text_content_manager_should_enumerate_text_element_options: fn(*u8, *u8, *u8, u64) -> i8,
    opaque user: *u8,
}

fn TextContentManagerDelegate_new(user: *u8) -> TextContentManagerDelegate {
    return TextContentManagerDelegate { text_content_manager_text_element_at_location: TextContentManagerDelegate_noop_id_id_id, text_content_manager_should_enumerate_text_element_options: TextContentManagerDelegate_noop_i8_id_id_u64, user: user };
}

impl TextContentManagerDelegate {
    fn set_text_content_manager_text_element_at_location(ref this, handler: fn(*u8, *u8, *u8) -> *u8) {
        this.text_content_manager_text_element_at_location = handler;
        return;
    }
    fn set_text_content_manager_should_enumerate_text_element_options(ref this, handler: fn(*u8, *u8, *u8, u64) -> i8) {
        this.text_content_manager_should_enumerate_text_element_options = handler;
        return;
    }
}

fn TextContentManagerDelegate_key() -> *u8 { return { 28672 as *u8 }; }

fn TextContentManagerDelegate_text_content_manager_text_element_at_location_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8) -> *u8 {
    let c: *TextContentManagerDelegate = { synth::get_associated(self_obj, TextContentManagerDelegate_key()) as *TextContentManagerDelegate };
    let f: fn(*u8, *u8, *u8) -> *u8 = { (*c).text_content_manager_text_element_at_location };
    let u: *u8 = { (*c).user };
    return f(u, a0, a1);
}

fn TextContentManagerDelegate_text_content_manager_should_enumerate_text_element_options_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8, a2: u64) -> i8 {
    let c: *TextContentManagerDelegate = { synth::get_associated(self_obj, TextContentManagerDelegate_key()) as *TextContentManagerDelegate };
    let f: fn(*u8, *u8, *u8, u64) -> i8 = { (*c).text_content_manager_should_enumerate_text_element_options };
    let u: *u8 = { (*c).user };
    return f(u, a0, a1, a2);
}

fn create_TextContentManagerDelegate(ctx: *TextContentManagerDelegate) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_TextContentManagerDelegate\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_text_content_manager_text_element_at_location: i8 = cplus_add_method_id_id_id(cls, rt::sel(#str_ptr("textContentManager:textElementAtLocation:\0")), TextContentManagerDelegate_text_content_manager_text_element_at_location_imp, #str_ptr("@@:@@\0"));
        let add_text_content_manager_should_enumerate_text_element_options: i8 = cplus_add_method_i8_id_id_u64(cls, rt::sel(#str_ptr("textContentManager:shouldEnumerateTextElement:options:\0")), TextContentManagerDelegate_text_content_manager_should_enumerate_text_element_options_imp, #str_ptr("c@:@@Q\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, TextContentManagerDelegate_key(), { ctx as *u8 });
    return d;
}

// `NSTextStorageObserving` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct TextStorageObserving {
    opaque _obj: *u8,
}

impl TextStorageObserving {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TextStorageObserving { return TextStorageObserving { _obj: ptr }; }

}

// `NSTextElementProvider` (ObjC protocol). Non-owning handle; `opaque`, no drop.
struct TextElementProvider {
    opaque _obj: *u8,
}

impl TextElementProvider {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> TextElementProvider { return TextElementProvider { _obj: ptr }; }

    fn enumerate_text_elements_from_location(this, text_location: TextLocation, options: u64, cb: fn(*u8, *u8), ctx: *u8) -> option::Option[TextLocation] {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: TextElementProvider_enumerate_text_elements_from_location_block = TextElementProvider_enumerate_text_elements_from_location_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: TextElementProvider_enumerate_text_elements_from_location_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        let obj: *u8 = rt::msg_id_id_u64_id(this._obj, rt::sel(#str_ptr("enumerateTextElementsFromLocation:options:usingBlock:\0")), text_location.raw(), options, bp);
        if obj == { 0 as *u8 } {
            return option::Option[TextLocation]::None;
        }
        return option::some(TextLocation::from_raw(obj));
    }

    fn replace_contents_in_range(this, range: TextRange, with_text_elements: vec::Vec[TextElement]) {
        let arr_with_text_elements: *u8 = rt::msg_id(rt::get_class(#str_ptr("NSMutableArray\0")), rt::sel(#str_ptr("array\0")));
        let add_sel_with_text_elements: *u8 = rt::sel(#str_ptr("addObject:\0"));
        let n_with_text_elements: usize = with_text_elements.count();
        var i_with_text_elements: usize = 0 as usize;
        while i_with_text_elements < n_with_text_elements {
            match with_text_elements.at_ptr(i_with_text_elements) {
                option::Option[*TextElement]::Some(e_with_text_elements) => { rt::msg_void_id(arr_with_text_elements, add_sel_with_text_elements, (*e_with_text_elements).raw()); }
                option::Option[*TextElement]::None => {}
            }
            i_with_text_elements = i_with_text_elements +% (1 as usize);
        }
        rt::msg_void_id_id(this._obj, rt::sel(#str_ptr("replaceContentsInRange:withTextElements:\0")), range.raw(), arr_with_text_elements);
    }

    fn synchronize_to_backing_store(this, cb: fn(*u8, *u8), ctx: *u8) {
        var desc: rt::BlockDescriptor = rt::BlockDescriptor { reserved: 0 as u64, size: 48 as u64 };
        var blk: TextElementProvider_synchronize_to_backing_store_block = TextElementProvider_synchronize_to_backing_store_block { isa: rt::stack_block_isa(), flags: 0 as i32, reserved: 0 as i32, invoke: TextElementProvider_synchronize_to_backing_store_invoke, descriptor: { #addr_of(desc) as *u8 }, user_fn: cb, ctx: ctx };
        let bp: *u8 = { #addr_of(blk) as *u8 };
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("synchronizeToBackingStore:\0")), bp);
        return;
    }

    fn location_from_location(this, location: TextLocation, with_offset: i64) -> option::Option[TextLocation] {
        let obj: *u8 = rt::msg_id_id_i64(this._obj, rt::sel(#str_ptr("locationFromLocation:withOffset:\0")), location.raw(), with_offset);
        if obj == { 0 as *u8 } {
            return option::Option[TextLocation]::None;
        }
        return option::some(TextLocation::from_raw(obj));
    }

    fn offset_from_location(this, from: TextLocation, to_location: TextLocation) -> i64 {
        return rt::msg_i64_id_id(this._obj, rt::sel(#str_ptr("offsetFromLocation:toLocation:\0")), from.raw(), to_location.raw());
    }

    fn adjusted_range_from_range(this, text_range: TextRange, for_editing_text_selection: bool) -> option::Option[TextRange] {
        let obj: *u8 = rt::msg_id_id_i8(this._obj, rt::sel(#str_ptr("adjustedRangeFromRange:forEditingTextSelection:\0")), text_range.raw(), for_editing_text_selection as i8);
        if obj == { 0 as *u8 } {
            return option::Option[TextRange]::None;
        }
        return option::some(TextRange::from_raw(obj));
    }

    fn document_range(this) -> TextRange {
        return TextRange::from_raw(rt::msg_id(this._obj, rt::sel(#str_ptr("documentRange\0"))));
    }

}

