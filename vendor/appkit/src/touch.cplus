// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSTouch.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;

// `NSTouchPhase` (NS_ENUM)
enum TouchPhase {
    Began,
    Moved,
    Stationary,
    Ended,
    Cancelled,
    Touching,
    Any,
}

fn touch_phase_raw(v: TouchPhase) -> i64 {
    return match v {
        TouchPhase::Began => { 1 as i64 },
        TouchPhase::Moved => { 2 as i64 },
        TouchPhase::Stationary => { 4 as i64 },
        TouchPhase::Ended => { 8 as i64 },
        TouchPhase::Cancelled => { 16 as i64 },
        TouchPhase::Touching => { 7 as i64 },
        TouchPhase::Any => { 9223372036854775807 as i64 },
    };
}

fn touch_phase_from_raw(raw: i64) -> TouchPhase {
    if raw == (2 as i64) { return TouchPhase::Moved; }
    if raw == (4 as i64) { return TouchPhase::Stationary; }
    if raw == (8 as i64) { return TouchPhase::Ended; }
    if raw == (16 as i64) { return TouchPhase::Cancelled; }
    if raw == (7 as i64) { return TouchPhase::Touching; }
    if raw == (9223372036854775807 as i64) { return TouchPhase::Any; }
    return TouchPhase::Began;
}

// `NSTouchType` (NS_ENUM)
enum TouchType {
    Direct,
    Indirect,
}

fn touch_type_raw(v: TouchType) -> i64 {
    return match v {
        TouchType::Direct => { 0 as i64 },
        TouchType::Indirect => { 1 as i64 },
    };
}

fn touch_type_from_raw(raw: i64) -> TouchType {
    if raw == (1 as i64) { return TouchType::Indirect; }
    return TouchType::Direct;
}

// `NSView` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct View {
    opaque _obj: *u8,
}

impl View {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSTouch` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Touch {
    opaque _obj: *u8,
}

impl Touch {
    fn raw(this) -> *u8 { return this._obj; }

    // SKIPPED `identity`: return `id<NSObject,NSCopying> _Nonnull` — generic collection
    fn phase(this) -> TouchPhase {
        return touch_phase_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("phase\0"))));
    }

    // SKIPPED `normalizedPosition`: return `NSPoint` — unmapped type `NSPoint`
    fn is_resting(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("isResting\0"))) != (0 as i8);
    }

    fn device(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("device\0")));
        return bridge::obj_option(obj);
    }

    // SKIPPED `deviceSize`: return `NSSize` — unmapped type `NSSize`
    // SKIPPED `locationInView:`: return `NSPoint` — unmapped type `NSPoint`
    // SKIPPED `previousLocationInView:`: return `NSPoint` — unmapped type `NSPoint`
    fn type_(this) -> TouchType {
        return touch_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("type\0"))));
    }

}

