// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSBitmapImageRep.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;

// `NSTIFFCompression` (NS_ENUM)
enum TIFFCompression {
    None,
    CCITTFAX3,
    CCITTFAX4,
    LZW,
    JPEG,
    NEXT,
    PackBits,
    OldJPEG,
}

fn t_i_f_f_compression_raw(v: TIFFCompression) -> i64 {
    return match v {
        TIFFCompression::None => { 1 as i64 },
        TIFFCompression::CCITTFAX3 => { 3 as i64 },
        TIFFCompression::CCITTFAX4 => { 4 as i64 },
        TIFFCompression::LZW => { 5 as i64 },
        TIFFCompression::JPEG => { 6 as i64 },
        TIFFCompression::NEXT => { 32766 as i64 },
        TIFFCompression::PackBits => { 32773 as i64 },
        TIFFCompression::OldJPEG => { 32865 as i64 },
    };
}

fn t_i_f_f_compression_from_raw(raw: i64) -> TIFFCompression {
    if raw == (3 as i64) { return TIFFCompression::CCITTFAX3; }
    if raw == (4 as i64) { return TIFFCompression::CCITTFAX4; }
    if raw == (5 as i64) { return TIFFCompression::LZW; }
    if raw == (6 as i64) { return TIFFCompression::JPEG; }
    if raw == (32766 as i64) { return TIFFCompression::NEXT; }
    if raw == (32773 as i64) { return TIFFCompression::PackBits; }
    if raw == (32865 as i64) { return TIFFCompression::OldJPEG; }
    return TIFFCompression::None;
}

// `NSColorRenderingIntent` (NS_ENUM)
enum ColorRenderingIntent {
    Default,
    AbsoluteColorimetric,
    RelativeColorimetric,
    Perceptual,
    Saturation,
}

fn color_rendering_intent_raw(v: ColorRenderingIntent) -> i64 {
    return match v {
        ColorRenderingIntent::Default => { 0 as i64 },
        ColorRenderingIntent::AbsoluteColorimetric => { 1 as i64 },
        ColorRenderingIntent::RelativeColorimetric => { 2 as i64 },
        ColorRenderingIntent::Perceptual => { 3 as i64 },
        ColorRenderingIntent::Saturation => { 4 as i64 },
    };
}

fn color_rendering_intent_from_raw(raw: i64) -> ColorRenderingIntent {
    if raw == (1 as i64) { return ColorRenderingIntent::AbsoluteColorimetric; }
    if raw == (2 as i64) { return ColorRenderingIntent::RelativeColorimetric; }
    if raw == (3 as i64) { return ColorRenderingIntent::Perceptual; }
    if raw == (4 as i64) { return ColorRenderingIntent::Saturation; }
    return ColorRenderingIntent::Default;
}

// `NSBitmapFormat` (NS_ENUM)
enum BitmapFormat {
    AlphaFirst,
    AlphaNonpremultiplied,
    FloatingPointSamples,
    SixteenBitLittleEndian,
    ThirtyTwoBitLittleEndian,
    SixteenBitBigEndian,
    ThirtyTwoBitBigEndian,
}

fn bitmap_format_raw(v: BitmapFormat) -> i64 {
    return match v {
        BitmapFormat::AlphaFirst => { 1 as i64 },
        BitmapFormat::AlphaNonpremultiplied => { 2 as i64 },
        BitmapFormat::FloatingPointSamples => { 4 as i64 },
        BitmapFormat::SixteenBitLittleEndian => { 256 as i64 },
        BitmapFormat::ThirtyTwoBitLittleEndian => { 512 as i64 },
        BitmapFormat::SixteenBitBigEndian => { 1024 as i64 },
        BitmapFormat::ThirtyTwoBitBigEndian => { 2048 as i64 },
    };
}

fn bitmap_format_from_raw(raw: i64) -> BitmapFormat {
    if raw == (2 as i64) { return BitmapFormat::AlphaNonpremultiplied; }
    if raw == (4 as i64) { return BitmapFormat::FloatingPointSamples; }
    if raw == (256 as i64) { return BitmapFormat::SixteenBitLittleEndian; }
    if raw == (512 as i64) { return BitmapFormat::ThirtyTwoBitLittleEndian; }
    if raw == (1024 as i64) { return BitmapFormat::SixteenBitBigEndian; }
    if raw == (2048 as i64) { return BitmapFormat::ThirtyTwoBitBigEndian; }
    return BitmapFormat::AlphaFirst;
}

// `NSBitmapImageFileType` (NS_ENUM)
enum BitmapImageFileType {
    TIFF,
    BMP,
    GIF,
    JPEG,
    PNG,
    JPEG2000,
}

fn bitmap_image_file_type_raw(v: BitmapImageFileType) -> i64 {
    return match v {
        BitmapImageFileType::TIFF => { 0 as i64 },
        BitmapImageFileType::BMP => { 1 as i64 },
        BitmapImageFileType::GIF => { 2 as i64 },
        BitmapImageFileType::JPEG => { 3 as i64 },
        BitmapImageFileType::PNG => { 4 as i64 },
        BitmapImageFileType::JPEG2000 => { 5 as i64 },
    };
}

fn bitmap_image_file_type_from_raw(raw: i64) -> BitmapImageFileType {
    if raw == (1 as i64) { return BitmapImageFileType::BMP; }
    if raw == (2 as i64) { return BitmapImageFileType::GIF; }
    if raw == (3 as i64) { return BitmapImageFileType::JPEG; }
    if raw == (4 as i64) { return BitmapImageFileType::PNG; }
    if raw == (5 as i64) { return BitmapImageFileType::JPEG2000; }
    return BitmapImageFileType::TIFF;
}

// `CIImage` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct CIImage {
    opaque _obj: *u8,
}

impl CIImage {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSColor` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Color {
    opaque _obj: *u8,
}

impl Color {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSColorSpace` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct ColorSpace {
    opaque _obj: *u8,
}

impl ColorSpace {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSBitmapImageRep` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct BitmapImageRep {
    _obj: *u8,
}

impl BitmapImageRep {
    fn raw(this) -> *u8 { return this._obj; }

    // SKIPPED `initWithFocusedViewRect:`: param `NSRect` — unmapped type `NSRect`
    // SKIPPED `initWithBitmapDataPlanes:pixelsWide:pixelsHigh:bitsPerSample:samplesPerPixel:hasAlpha:isPlanar:colorSpaceName:bytesPerRow:bitsPerPixel:`: extra init variant (one `new` per type)
    // SKIPPED `initWithBitmapDataPlanes:pixelsWide:pixelsHigh:bitsPerSample:samplesPerPixel:hasAlpha:isPlanar:colorSpaceName:bitmapFormat:bytesPerRow:bitsPerPixel:`: extra init variant (one `new` per type)
    // SKIPPED `initWithCGImage:`: extra init variant (one `new` per type)
    // SKIPPED `initWithCIImage:`: extra init variant (one `new` per type)
    // SKIPPED `imageRepsWithData:`: return `NSArray<NSImageRep *> * _Nonnull` — generic collection
    fn image_rep_with_data(data: *u8) -> option::Option[BitmapImageRep] {
        let obj: *u8 = rt::msg_id_id(rt::get_class(#str_ptr("NSBitmapImageRep\0")), rt::sel(#str_ptr("imageRepWithData:\0")), data);
        if obj == { 0 as *u8 } {
            return option::Option[BitmapImageRep]::None;
        }
        return option::some(BitmapImageRep { _obj: rt::retain(obj) });
    }

    // SKIPPED `initWithData:`: extra init variant (one `new` per type)
    fn get_bitmap_data_planes(this, data: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("getBitmapDataPlanes:\0")), data);
    }

    // SKIPPED `getCompression:factor:`: (return, arg) shape not yet modelled
    // SKIPPED `setCompression:factor:`: (return, arg) shape not yet modelled
    // SKIPPED `TIFFRepresentationUsingCompression:factor:`: (return, arg) shape not yet modelled
    // SKIPPED `TIFFRepresentationOfImageRepsInArray:`: param `NSArray<NSImageRep *> * _Nonnull` — generic collection
    // SKIPPED `TIFFRepresentationOfImageRepsInArray:usingCompression:factor:`: param `NSArray<NSImageRep *> * _Nonnull` — generic collection
    // SKIPPED `getTIFFCompressionTypes:count:`: (return, arg) shape not yet modelled
    fn localized_name_for_t_i_f_f_compression_type(compression: TIFFCompression) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id_i64(rt::get_class(#str_ptr("NSBitmapImageRep\0")), rt::sel(#str_ptr("localizedNameForTIFFCompressionType:\0")), t_i_f_f_compression_raw(compression));
        return bridge::to_text_option(ns);
    }

    // SKIPPED `canBeCompressedUsing:`: (return, arg) shape not yet modelled
    // SKIPPED `colorizeByMappingGray:toColor:blackMapping:whiteMapping:`: (return, arg) shape not yet modelled
    fn init_for_incremental_load(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("initForIncrementalLoad\0")));
    }

    // SKIPPED `incrementalLoadFromData:complete:`: (return, arg) shape not yet modelled
    // SKIPPED `setColor:atX:y:`: (return, arg) shape not yet modelled
    // SKIPPED `colorAtX:y:`: (return, arg) shape not yet modelled
    // SKIPPED `getPixel:atX:y:`: (return, arg) shape not yet modelled
    // SKIPPED `setPixel:atX:y:`: (return, arg) shape not yet modelled
    // SKIPPED `bitmapImageRepByConvertingToColorSpace:renderingIntent:`: (return, arg) shape not yet modelled
    fn bitmap_image_rep_by_retagging_with_color_space(this, new_space: *u8) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("bitmapImageRepByRetaggingWithColorSpace:\0")), new_space);
        return bridge::obj_option(obj);
    }

    fn bitmap_data(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("bitmapData\0")));
    }

    fn is_planar(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("isPlanar\0"))) != (0 as i8);
    }

    fn samples_per_pixel(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("samplesPerPixel\0")));
    }

    fn bits_per_pixel(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("bitsPerPixel\0")));
    }

    fn bytes_per_row(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("bytesPerRow\0")));
    }

    fn bytes_per_plane(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("bytesPerPlane\0")));
    }

    fn number_of_planes(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("numberOfPlanes\0")));
    }

    fn bitmap_format(this) -> BitmapFormat {
        return bitmap_format_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("bitmapFormat\0"))));
    }

    fn t_i_f_f_representation(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("TIFFRepresentation\0")));
        return bridge::obj_option(obj);
    }

    // SKIPPED `CGImage`: return `API_AVAILABLE CGImageRef` — unmapped type `API_AVAILABLE CGImageRef`
    fn color_space(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("colorSpace\0")));
    }

    // SKIPPED `representationOfImageRepsInArray:usingType:properties:`: param `NSArray<NSImageRep *> * _Nonnull` — generic collection
    // SKIPPED `representationUsingType:properties:`: param `NSDictionary<NSBitmapImageRepPropertyKey,id> * _Nonnull` — generic collection
    // SKIPPED `setProperty:withValue:`: (return, arg) shape not yet modelled
    fn value_for_property(this, property: str) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id_id(this._obj, rt::sel(#str_ptr("valueForProperty:\0")), bridge::nsstring(property));
        return bridge::obj_option(obj);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

