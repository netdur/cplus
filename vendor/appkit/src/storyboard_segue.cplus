// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSStoryboardSegue.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `NSStoryboardSegue` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct StoryboardSegue {
    _obj: *u8,
}

impl StoryboardSegue {
    fn raw(this) -> *u8 { return this._obj; }

    // SKIPPED `segueWithIdentifier:source:destination:performHandler:`: block method with non-void return
    // SKIPPED `initWithIdentifier:source:destination:`: init arg shape not yet modelled
    fn perform(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("perform\0")));
    }

    fn identifier(this) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("identifier\0")));
        return bridge::to_text_option(ns);
    }

    fn source_controller(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("sourceController\0")));
    }

    fn destination_controller(this) -> *u8 {
        return rt::msg_id(this._obj, rt::sel(#str_ptr("destinationController\0")));
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSSeguePerforming` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `performSegueWithIdentifier:sender:`: arg shape (only <=5 object args modelled)
// SKIPPED `shouldPerformSegueWithIdentifier:sender:`: arg shape (only <=5 object args modelled)
fn SeguePerforming_noop_v_2(user: *u8, a0: *u8, a1: *u8) { return; }

struct SeguePerforming {
    prepare_for_segue_sender: fn(*u8, *u8, *u8),
    opaque user: *u8,
}

fn SeguePerforming_new(user: *u8) -> SeguePerforming {
    return SeguePerforming { prepare_for_segue_sender: SeguePerforming_noop_v_2, user: user };
}

impl SeguePerforming {
    fn set_prepare_for_segue_sender(ref this, handler: fn(*u8, *u8, *u8)) {
        this.prepare_for_segue_sender = handler;
        return;
    }
}

fn SeguePerforming_key() -> *u8 { return { 28672 as *u8 }; }

fn SeguePerforming_prepare_for_segue_sender_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8) {
    let c: *SeguePerforming = { synth::get_associated(self_obj, SeguePerforming_key()) as *SeguePerforming };
    let f: fn(*u8, *u8, *u8) = { (*c).prepare_for_segue_sender };
    let u: *u8 = { (*c).user };
    f(u, a0, a1);
    return;
}

fn create_SeguePerforming(ctx: *SeguePerforming) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_SeguePerforming\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_prepare_for_segue_sender: i8 = synth::add_method_v_2id(cls, rt::sel(#str_ptr("prepareForSegue:sender:\0")), SeguePerforming_prepare_for_segue_sender_imp, #str_ptr("v@:@@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, SeguePerforming_key(), { ctx as *u8 });
    return d;
}

