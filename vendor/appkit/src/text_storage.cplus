// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSTextStorage.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `NSTextStorageEditActions` (NS_ENUM)
enum TextStorageEditActions {
    NSTextStorageEditedAttributes,
    NSTextStorageEditedCharacters,
}

fn text_storage_edit_actions_raw(v: TextStorageEditActions) -> i64 {
    return match v {
        TextStorageEditActions::NSTextStorageEditedAttributes => { 1 as i64 },
        TextStorageEditActions::NSTextStorageEditedCharacters => { 2 as i64 },
    };
}

fn text_storage_edit_actions_from_raw(raw: i64) -> TextStorageEditActions {
    if raw == (2 as i64) { return TextStorageEditActions::NSTextStorageEditedCharacters; }
    return TextStorageEditActions::NSTextStorageEditedAttributes;
}

// `NSArray` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Array {
    opaque _obj: *u8,
}

impl Array {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSLayoutManager` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct LayoutManager {
    opaque _obj: *u8,
}

impl LayoutManager {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSNotification` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct Notification {
    opaque _obj: *u8,
}

impl Notification {
    fn raw(this) -> *u8 { return this._obj; }

}

// `NSTextStorage` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct TextStorage {
    opaque _obj: *u8,
}

impl TextStorage {
    fn raw(this) -> *u8 { return this._obj; }

    fn add_layout_manager(this, a_layout_manager: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("addLayoutManager:\0")), a_layout_manager);
    }

    fn remove_layout_manager(this, a_layout_manager: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("removeLayoutManager:\0")), a_layout_manager);
    }

    // SKIPPED `edited:range:changeInLength:`: (return, arg) shape not yet modelled
    fn process_editing(this) {
        rt::msg_void(this._obj, rt::sel(#str_ptr("processEditing\0")));
    }

    // SKIPPED `invalidateAttributesInRange:`: (return, arg) shape not yet modelled
    // SKIPPED `ensureAttributesAreFixedInRange:`: (return, arg) shape not yet modelled
    // SKIPPED `layoutManagers`: return `NSArray<NSLayoutManager *> * _Nonnull` — generic collection
    fn edited_mask(this) -> TextStorageEditActions {
        return text_storage_edit_actions_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("editedMask\0"))));
    }

    fn edited_range(this) -> rt::Range {
        return rt::msg_range(this._obj, rt::sel(#str_ptr("editedRange\0")));
    }

    fn change_in_length(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("changeInLength\0")));
    }

    // SKIPPED `delegate`: return `id<NSTextStorageDelegate> _Nullable` — generic collection
    // SKIPPED `setDelegate:`: param `id<NSTextStorageDelegate> _Nullable` — generic collection
    fn fixes_attributes_lazily(this) -> bool {
        return rt::msg_i8(this._obj, rt::sel(#str_ptr("fixesAttributesLazily\0"))) != (0 as i8);
    }

    // SKIPPED `textStorageObserver`: return `API_UNAVAILABLE id<NSTextStorageObserving>` — generic collection
    // SKIPPED `setTextStorageObserver:`: param `API_UNAVAILABLE id<NSTextStorageObserving>` — generic collection
}

// `NSTextStorageObserving` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `processEditingForTextStorage:edited:range:changeInLength:invalidatedRange:`: arg shape (only <=5 object args modelled)
// SKIPPED `performEditingTransactionForTextStorage:usingBlock:`: arg shape (only <=5 object args modelled)
fn TextStorageObservingProtocol_noop_id_0(user: *u8) -> *u8 { return { 0 as *u8 }; }
fn TextStorageObservingProtocol_noop_v_1(user: *u8, a0: *u8) { return; }

struct TextStorageObservingProtocol {
    text_storage: fn(*u8) -> *u8,
    set_text_storage: fn(*u8, *u8),
    opaque user: *u8,
}

fn TextStorageObservingProtocol_new(user: *u8) -> TextStorageObservingProtocol {
    return TextStorageObservingProtocol { text_storage: TextStorageObservingProtocol_noop_id_0, set_text_storage: TextStorageObservingProtocol_noop_v_1, user: user };
}

impl TextStorageObservingProtocol {
    fn set_text_storage(ref this, handler: fn(*u8) -> *u8) {
        this.text_storage = handler;
        return;
    }
    fn set_set_text_storage(ref this, handler: fn(*u8, *u8)) {
        this.set_text_storage = handler;
        return;
    }
}

fn TextStorageObservingProtocol_key() -> *u8 { return { 28672 as *u8 }; }

fn TextStorageObservingProtocol_text_storage_imp(self_obj: *u8, cmd: *u8) -> *u8 {
    let c: *TextStorageObservingProtocol = { synth::get_associated(self_obj, TextStorageObservingProtocol_key()) as *TextStorageObservingProtocol };
    let f: fn(*u8) -> *u8 = { (*c).text_storage };
    let u: *u8 = { (*c).user };
    return f(u);
}

fn TextStorageObservingProtocol_set_text_storage_imp(self_obj: *u8, cmd: *u8, a0: *u8) {
    let c: *TextStorageObservingProtocol = { synth::get_associated(self_obj, TextStorageObservingProtocol_key()) as *TextStorageObservingProtocol };
    let f: fn(*u8, *u8) = { (*c).set_text_storage };
    let u: *u8 = { (*c).user };
    f(u, a0);
    return;
}

fn create_TextStorageObservingProtocol(ctx: *TextStorageObservingProtocol) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_TextStorageObservingProtocol\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_text_storage: i8 = synth::add_method_id_0id(cls, rt::sel(#str_ptr("textStorage\0")), TextStorageObservingProtocol_text_storage_imp, #str_ptr("@@:\0"));
        let add_set_text_storage: i8 = synth::add_method_v_1id(cls, rt::sel(#str_ptr("setTextStorage:\0")), TextStorageObservingProtocol_set_text_storage_imp, #str_ptr("v@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, TextStorageObservingProtocol_key(), { ctx as *u8 });
    return d;
}

