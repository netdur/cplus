// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSMenuItemBadge.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;

// `NSMenuItemBadgeType` (NS_ENUM)
enum MenuItemBadgeType {
    None,
    Updates,
    NewItems,
    Alerts,
}

fn menu_item_badge_type_raw(v: MenuItemBadgeType) -> i64 {
    return match v {
        MenuItemBadgeType::None => { 0 as i64 },
        MenuItemBadgeType::Updates => { 1 as i64 },
        MenuItemBadgeType::NewItems => { 2 as i64 },
        MenuItemBadgeType::Alerts => { 3 as i64 },
    };
}

fn menu_item_badge_type_from_raw(raw: i64) -> MenuItemBadgeType {
    if raw == (1 as i64) { return MenuItemBadgeType::Updates; }
    if raw == (2 as i64) { return MenuItemBadgeType::NewItems; }
    if raw == (3 as i64) { return MenuItemBadgeType::Alerts; }
    return MenuItemBadgeType::None;
}

// `NSMenuItemBadge` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct MenuItemBadge {
    _obj: *u8,
}

impl MenuItemBadge {
    fn raw(this) -> *u8 { return this._obj; }

    fn updates_with_count(item_count: i64) -> MenuItemBadge {
        return MenuItemBadge { _obj: rt::retain(rt::msg_id_i64(rt::get_class(#str_ptr("NSMenuItemBadge\0")), rt::sel(#str_ptr("updatesWithCount:\0")), item_count)) };
    }

    fn new_items_with_count(item_count: i64) -> MenuItemBadge {
        return MenuItemBadge { _obj: rt::retain(rt::msg_id_i64(rt::get_class(#str_ptr("NSMenuItemBadge\0")), rt::sel(#str_ptr("newItemsWithCount:\0")), item_count)) };
    }

    fn alerts_with_count(item_count: i64) -> MenuItemBadge {
        return MenuItemBadge { _obj: rt::retain(rt::msg_id_i64(rt::get_class(#str_ptr("NSMenuItemBadge\0")), rt::sel(#str_ptr("alertsWithCount:\0")), item_count)) };
    }

    // SKIPPED `initWithCount:type:`: init arg shape not yet modelled
    // SKIPPED `initWithCount:`: extra init variant (one `new` per type)
    // SKIPPED `initWithString:`: extra init variant (one `new` per type)
    // SKIPPED `init`: extra init variant (one `new` per type)
    fn item_count(this) -> i64 {
        return rt::msg_i64(this._obj, rt::sel(#str_ptr("itemCount\0")));
    }

    fn type_(this) -> MenuItemBadgeType {
        return menu_item_badge_type_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("type\0"))));
    }

    fn string_value(this) -> option::Option[text::Text] {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("stringValue\0")));
        return bridge::to_text_option(ns);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

