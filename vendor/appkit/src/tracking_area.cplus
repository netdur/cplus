// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSTrackingArea.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;

// `NSTrackingAreaOptions` (NS_ENUM)
enum TrackingAreaOptions {
    NSTrackingMouseEnteredAndExited,
    NSTrackingMouseMoved,
    NSTrackingCursorUpdate,
    NSTrackingActiveWhenFirstResponder,
    NSTrackingActiveInKeyWindow,
    NSTrackingActiveInActiveApp,
    NSTrackingActiveAlways,
    NSTrackingAssumeInside,
    NSTrackingInVisibleRect,
    NSTrackingEnabledDuringMouseDrag,
}

fn tracking_area_options_raw(v: TrackingAreaOptions) -> i64 {
    return match v {
        TrackingAreaOptions::NSTrackingMouseEnteredAndExited => { 1 as i64 },
        TrackingAreaOptions::NSTrackingMouseMoved => { 2 as i64 },
        TrackingAreaOptions::NSTrackingCursorUpdate => { 4 as i64 },
        TrackingAreaOptions::NSTrackingActiveWhenFirstResponder => { 16 as i64 },
        TrackingAreaOptions::NSTrackingActiveInKeyWindow => { 32 as i64 },
        TrackingAreaOptions::NSTrackingActiveInActiveApp => { 64 as i64 },
        TrackingAreaOptions::NSTrackingActiveAlways => { 128 as i64 },
        TrackingAreaOptions::NSTrackingAssumeInside => { 256 as i64 },
        TrackingAreaOptions::NSTrackingInVisibleRect => { 512 as i64 },
        TrackingAreaOptions::NSTrackingEnabledDuringMouseDrag => { 1024 as i64 },
    };
}

fn tracking_area_options_from_raw(raw: i64) -> TrackingAreaOptions {
    if raw == (2 as i64) { return TrackingAreaOptions::NSTrackingMouseMoved; }
    if raw == (4 as i64) { return TrackingAreaOptions::NSTrackingCursorUpdate; }
    if raw == (16 as i64) { return TrackingAreaOptions::NSTrackingActiveWhenFirstResponder; }
    if raw == (32 as i64) { return TrackingAreaOptions::NSTrackingActiveInKeyWindow; }
    if raw == (64 as i64) { return TrackingAreaOptions::NSTrackingActiveInActiveApp; }
    if raw == (128 as i64) { return TrackingAreaOptions::NSTrackingActiveAlways; }
    if raw == (256 as i64) { return TrackingAreaOptions::NSTrackingAssumeInside; }
    if raw == (512 as i64) { return TrackingAreaOptions::NSTrackingInVisibleRect; }
    if raw == (1024 as i64) { return TrackingAreaOptions::NSTrackingEnabledDuringMouseDrag; }
    return TrackingAreaOptions::NSTrackingMouseEnteredAndExited;
}

// `NSTrackingArea` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct TrackingArea {
    _obj: *u8,
}

impl TrackingArea {
    fn raw(this) -> *u8 { return this._obj; }

    // SKIPPED `initWithRect:options:owner:userInfo:`: param `NSRect` — unmapped type `NSRect`
    // SKIPPED `rect`: return `NSRect` — unmapped type `NSRect`
    fn options(this) -> TrackingAreaOptions {
        return tracking_area_options_from_raw(rt::msg_i64(this._obj, rt::sel(#str_ptr("options\0"))));
    }

    fn owner(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("owner\0")));
        return bridge::obj_option(obj);
    }

    // SKIPPED `userInfo`: return `NSDictionary<id,id> * _Nullable` — NSDictionary with non-string key `id`
    fn drop(ref this) {
        rt::release(this._obj);
    }
}

