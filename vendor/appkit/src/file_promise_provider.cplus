// Auto-generated by cpc-bindgen (--objc). DO NOT EDIT.
// Source header: /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/System/Library/Frameworks/AppKit.framework/Headers/NSFilePromiseProvider.h
//
import "objc/runtime" as rt;
import "objc/bridge" as bridge;
import "stdlib/text" as text;
import "stdlib/option" as option;
import "objc/synthesis" as synth;

// `NSOperationQueue` (Foundation/ObjC). Non-owning handle (factory/singleton); `opaque`, no drop.
struct OperationQueue {
    opaque _obj: *u8,
}

impl OperationQueue {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> OperationQueue { return OperationQueue { _obj: ptr }; }

}

// `NSFilePromiseProvider` (Foundation/ObjC). Owned via alloc/init; `drop` releases the +1.
struct FilePromiseProvider {
    _obj: *u8,
}

impl FilePromiseProvider {
    fn raw(this) -> *u8 { return this._obj; }

    fn from_raw(ptr: *u8) -> FilePromiseProvider { return FilePromiseProvider { _obj: ptr }; }

    fn new_with_file_type_delegate(file_type: str, delegate: *u8) -> FilePromiseProvider {
        let cls: *u8 = rt::get_class(#str_ptr("NSFilePromiseProvider\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        return FilePromiseProvider { _obj: rt::msg_id_id_id(alloced, rt::sel(#str_ptr("initWithFileType:delegate:\0")), bridge::nsstring(file_type), delegate) };
    }

    fn new() -> FilePromiseProvider {
        let cls: *u8 = rt::get_class(#str_ptr("NSFilePromiseProvider\0"));
        let alloced: *u8 = rt::msg_id(cls, rt::sel(#str_ptr("alloc\0")));
        return FilePromiseProvider { _obj: rt::msg_id(alloced, rt::sel(#str_ptr("init\0"))) };
    }

    fn file_type(this) -> text::Text {
        let ns: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("fileType\0")));
        return bridge::to_text(ns);
    }

    fn set_file_type(this, file_type: str) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setFileType:\0")), bridge::nsstring(file_type));
    }

    fn delegate(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("delegate\0")));
        return bridge::obj_option(obj);
    }

    fn set_delegate(this, delegate: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setDelegate:\0")), delegate);
    }

    fn user_info(this) -> option::Option[*u8] {
        let obj: *u8 = rt::msg_id(this._obj, rt::sel(#str_ptr("userInfo\0")));
        return bridge::obj_option(obj);
    }

    fn set_user_info(this, user_info: *u8) {
        rt::msg_void_id(this._obj, rt::sel(#str_ptr("setUserInfo:\0")), user_info);
    }

    fn drop(ref this) {
        rt::release(this._obj);
    }
}

// `NSFilePromiseProviderDelegate` delegate protocol -> runtime class-synthesis helper.
// SKIPPED `filePromiseProvider:writePromiseToURL:completionHandler:`: arg shape (only <=5 object args modelled)
fn FilePromiseProviderDelegate_noop_id_1(user: *u8, a0: *u8) -> *u8 { return { 0 as *u8 }; }
fn FilePromiseProviderDelegate_noop_id_2(user: *u8, a0: *u8, a1: *u8) -> *u8 { return { 0 as *u8 }; }

struct FilePromiseProviderDelegate {
    file_promise_provider_file_name_for_type: fn(*u8, *u8, *u8) -> *u8,
    operation_queue_for_file_promise_provider: fn(*u8, *u8) -> *u8,
    opaque user: *u8,
}

fn FilePromiseProviderDelegate_new(user: *u8) -> FilePromiseProviderDelegate {
    return FilePromiseProviderDelegate { file_promise_provider_file_name_for_type: FilePromiseProviderDelegate_noop_id_2, operation_queue_for_file_promise_provider: FilePromiseProviderDelegate_noop_id_1, user: user };
}

impl FilePromiseProviderDelegate {
    fn set_file_promise_provider_file_name_for_type(ref this, handler: fn(*u8, *u8, *u8) -> *u8) {
        this.file_promise_provider_file_name_for_type = handler;
        return;
    }
    fn set_operation_queue_for_file_promise_provider(ref this, handler: fn(*u8, *u8) -> *u8) {
        this.operation_queue_for_file_promise_provider = handler;
        return;
    }
}

fn FilePromiseProviderDelegate_key() -> *u8 { return { 28672 as *u8 }; }

fn FilePromiseProviderDelegate_file_promise_provider_file_name_for_type_imp(self_obj: *u8, cmd: *u8, a0: *u8, a1: *u8) -> *u8 {
    let c: *FilePromiseProviderDelegate = { synth::get_associated(self_obj, FilePromiseProviderDelegate_key()) as *FilePromiseProviderDelegate };
    let f: fn(*u8, *u8, *u8) -> *u8 = { (*c).file_promise_provider_file_name_for_type };
    let u: *u8 = { (*c).user };
    return f(u, a0, a1);
}

fn FilePromiseProviderDelegate_operation_queue_for_file_promise_provider_imp(self_obj: *u8, cmd: *u8, a0: *u8) -> *u8 {
    let c: *FilePromiseProviderDelegate = { synth::get_associated(self_obj, FilePromiseProviderDelegate_key()) as *FilePromiseProviderDelegate };
    let f: fn(*u8, *u8) -> *u8 = { (*c).operation_queue_for_file_promise_provider };
    let u: *u8 = { (*c).user };
    return f(u, a0);
}

fn create_FilePromiseProviderDelegate(ctx: *FilePromiseProviderDelegate) -> *u8 {
    let name: *u8 = #str_ptr("Cplus_FilePromiseProviderDelegate\0");
    var cls: *u8 = rt::get_class(name);
    if cls == { 0 as *u8 } {
        cls = synth::allocate_class_pair(rt::get_class(#str_ptr("NSObject\0")), name, 0 as usize);
        let add_file_promise_provider_file_name_for_type: i8 = synth::add_method_id_2id(cls, rt::sel(#str_ptr("filePromiseProvider:fileNameForType:\0")), FilePromiseProviderDelegate_file_promise_provider_file_name_for_type_imp, #str_ptr("@@:@@\0"));
        let add_operation_queue_for_file_promise_provider: i8 = synth::add_method_id_1id(cls, rt::sel(#str_ptr("operationQueueForFilePromiseProvider:\0")), FilePromiseProviderDelegate_operation_queue_for_file_promise_provider_imp, #str_ptr("@@:@\0"));
        synth::register_class_pair(cls);
    }
    let d: *u8 = synth::alloc_init_class(cls);
    synth::set_associated(d, FilePromiseProviderDelegate_key(), { ctx as *u8 });
    return d;
}

