[package]
name    = "gmodule"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/share/gir-1.0/GModule-2.0.gir (namespace GModule 2.0)
# Reproduce: cpc-bindgen --gobject GModule --out vendor/gmodule --use GLib=glib --use GObject=gobject_gir --use Gio=gio --use cairo=cairo --use Graphene=graphene --use HarfBuzz=harfbuzz --use Pango=pango --use PangoCairo=pangocairo --use GdkPixbuf=gdkpixbuf --use Gdk=gdk --use Gsk=gsk --use Gtk=gtk4
# Coverage:  9 items, 0 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"

[link]
libs = ["gmodule-2.0"]
