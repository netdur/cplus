[package]
name    = "adwaita"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/share/gir-1.0/Adw-1.gir (namespace Adw 1)
# Reproduce: cpc-bindgen --gobject Adw --out vendor/adwaita --use GLib=glib --use GObject=gobject_gir --use GModule=gmodule --use Gio=gio --use cairo=cairo --use Graphene=graphene --use HarfBuzz=harfbuzz --use Pango=pango --use PangoCairo=pangocairo --use GdkPixbuf=gdkpixbuf --use Gdk=gdk --use Gsk=gsk --use Gtk=gtk4
# Coverage:  1361 items, 52 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"
cairo       = "*"
gdk         = "*"
gdkpixbuf   = "*"
gio         = "*"
glib        = "*"
gobject_gir = "*"
graphene    = "*"
gsk         = "*"
gtk4        = "*"
harfbuzz    = "*"
pango       = "*"

[link]
libs = ["adwaita-1"]
