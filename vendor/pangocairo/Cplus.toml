[package]
name    = "pangocairo"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/share/gir-1.0/PangoCairo-1.0.gir (namespace PangoCairo 1.0)
# Reproduce: cpc-bindgen --gobject PangoCairo --out vendor/pangocairo --use GLib=glib --use GObject=gobject_gir --use GModule=gmodule --use Gio=gio --use cairo=cairo --use Graphene=graphene --use HarfBuzz=harfbuzz --use Pango=pango --use GdkPixbuf=gdkpixbuf --use Gdk=gdk --use Gsk=gsk --use Gtk=gtk4
# Coverage:  26 items, 2 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"
cairo       = "*"
gio         = "*"
glib        = "*"
gobject_gir = "*"
harfbuzz    = "*"
pango       = "*"

[link]
libs = ["pangocairo-1.0"]
