[package]
name    = "glib"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/lib/x86_64-linux-gnu/gir-1.0/GLib-2.0.gir (namespace GLib 2.0)
# Reproduce: cpc-bindgen --gobject GLib --out vendor/glib --use GObject=gobject_gir --use GModule=gmodule --use Gio=gio --use cairo=cairo --use Graphene=graphene --use HarfBuzz=harfbuzz --use Pango=pango --use PangoCairo=pangocairo --use GdkPixbuf=gdkpixbuf --use Gdk=gdk --use Gsk=gsk --use Gtk=gtk4
# Coverage:  1704 items, 412 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"

[link]
libs = ["gobject-2.0", "glib-2.0"]
