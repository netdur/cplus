[package]
name    = "espidf"
version = "0.0.1"
edition = "2026"

# espidf — ESP-IDF bindings (esp32-xtensa): GPIO, esp_timer, sleep, UART log.
# Heap-free wrappers for #[no_alloc] loops. No [link]: IDF/CMake links firmware.
# See docs/.

[dependencies]
stdlib = "*"
