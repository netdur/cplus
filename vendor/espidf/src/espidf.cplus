// Umbrella module: one import surfaces the binding set.

import "./gpio" as gpio;
import "./timer" as timer;
import "./task" as task;
import "./log" as log;

pub fn now_us() -> i64 {
    return timer::now_us();
}

pub fn delay_ms(ms: u32) {
    task::delay_ms(ms);
    return;
}
