[package]
name    = "cairo"
version = "0.0.1"
edition = "2026"

# Auto-generated by cpc-bindgen --gobject.
# GIR:       /usr/share/gir-1.0/cairo-1.0.gir (namespace cairo 1.0)
# Reproduce: cpc-bindgen --gobject cairo --out vendor/cairo --use GLib=glib --use GObject=gobject_gir --use GModule=gmodule --use Gio=gio --use Graphene=graphene --use HarfBuzz=harfbuzz --use Pango=pango --use PangoCairo=pangocairo --use GdkPixbuf=gdkpixbuf --use Gdk=gdk --use Gsk=gsk --use Gtk=gtk4
# Coverage:  175 items, 0 SKIPPED (see `// SKIPPED` in src).

[dependencies]
gobject = "*"
stdlib  = "*"

[link]
libs = ["cairo-gobject", "cairo"]
