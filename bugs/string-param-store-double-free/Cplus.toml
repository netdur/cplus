[package]
name    = "string_param_store_double_free"
version = "0.0.1"
edition = "2026"

[[bin]]
name = "repro"
path = "src/main.cplus"

# The `Vec[Text]` guard needs the stdlib (vec + text). Vendored via the
# `vendor/stdlib` symlink, like the docs/examples projects.
[dependencies]
stdlib = "*"
