[package]
name    = "match_if_arm_enum_ctor"
version = "0.0.1"
edition = "2026"

[[bin]]
name = "repro"
path = "src/main.cplus"

# No dependencies: the bug reproduces in pure C+ with two small enums.
[dependencies]
